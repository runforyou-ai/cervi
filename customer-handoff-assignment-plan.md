# 转人工承接与分配方案

日期：2026-09-19。状态：待评审。

本文是 [客服能力路线图](customer-service-roadmap.md) 3.1 节的实施方案：让 AI 转人工和新进线的客户会话都有真人及时承接并回复。方案涉及接单与接待量、自动分配、积压补分配、未响应回收、队列等待提醒、工作时间和转人工话术。转人工事务的现有契约见 [AI 客服角色行为方案](agent-role-behavior-plan.md) 第 6 节，本文只写在它之上的变化。

## 1. 已确定的决策

| 事项 | 决策 |
|---|---|
| 接单开关 | 复用成员现有的工作状态：只有「工作中」的真人客服参与自动分配，「休息中」「下班了」不分配新会话。登录不再把工作状态重置为「工作中」 |
| 最大接待量 | 成员级字段，企业内一份；创建成员表单默认 10，成员详情可修改 |
| 当前接待量 | 本人负责的开放客服周期数 |
| 超时阈值与工作时间 | 企业级设置，在系统设置新增「客服」页维护 |
| 提醒通道 | 复用现有新消息系统通知的投递链路和开关：全局消息通知开关、工作状态、应用可见性 |
| 在线与离线 | 本方案不建设，随路线图 3.7 的 Messenger 在线标识一起建设。超时按负责人是否回复判断，与在线状态无关 |
| 移动端提醒 | 复用本地通知，只在 App 进程存活且不在前台时弹出；远程推送（APNs、FCM）另立事项 |

## 2. 现状与缺口

- 渠道路由的目标只有公共队列、团队、成员三种，AI 员工按成员配置。入站按首选、备用、公共队列依次解析（`chatstate.ResolveNewSessionRoute`），转人工只解析备用路由（`chatstate.ResolveHandoffRoute`）。团队目标只要求团队存在，路由到团队或公共队列后不会分配给任何人。
- 工作状态（`organization_identities.work_status`）只在前端用于控制新消息通知，路由与分配完全不读取；登录时被强制改为 `working`（`auth/login.go`）。
- 收件箱「队列」视图就是当前周期无负责人（`load_inbox.go` 的 `filterCustomerInbox`），不区分团队，`team_id` 只存储、只出现在事件里。
- 真人停用或失去客服角色后，他负责的开放周期仍挂在他名下；只有 AI 员工有交接逻辑（`HandOffAgentServiceSessions`）。
- `service_sessions.assigned_at` 与 `first_response_at` 都只记第一次，无法判断当前负责人何时接手、客户从何时开始等待回复。
- 对客转人工通知由模型的 `handoff_to_human.message` 或固定文案给出，只说明已转交，不区分是否已有人承接、是否非工作时间。
- 可靠任务支持延迟执行（`AvailableAt`）和 Cron 调度，没有撤销接口，也没有企业维度；企业没有时区字段。
- 新消息系统通知由前端观察器根据 SSE 会话版本通知判断，系统事件不触发通知；没有定向发给某个成员的提醒事件。

## 3. 数据模型

新增迁移在实施时通过 `wails3 task make:migration` 生成。

### 3.1 成员接待量

`users` 增加：

| 字段 | 类型 | 含义 |
|---|---|---|
| `max_service_sessions` | `integer NOT NULL DEFAULT 10` | 最大接待量：自动分配时本人可负责的开放客服周期上限 |
| `last_service_assigned_at` | `timestamptz` | 最近一次被自动分配的时间，用于接待量相同时轮转 |

最大接待量只约束自动分配，由分配器在一处检查。真人手动领取、回复时隐式领取、接管、重开、他人转交和渠道指定成员的路由不受上限约束，这些操作由人明确发起或配置，满员成员仍可继续处理。

### 3.2 客服周期

`service_sessions` 增加：

| 字段 | 类型 | 含义 |
|---|---|---|
| `assignee_assigned_at` | `timestamptz` | 当前负责人获得本周期的时间；负责人变化时更新，进入队列时清空 |
| `awaiting_reply_since` | `timestamptz` | 客户开始等待人工或 AI 回复的时间；为空表示没有待回复的客户消息 |
| `reminded_at` | `timestamptz` | 本轮等待已发出提醒的时间；负责人、队列或等待起点变化时清空 |

`awaiting_reply_since` 的维护规则：

| 事件 | 变化 |
|---|---|
| 客户消息写入开放周期 | 为空时置为该消息时间，已有值保持 |
| 真人对客回复 | 清空 |
| AI 对客回复或追问（AI 负责期间） | 清空 |
| AI 转人工 | 置为交接时间：转人工对客通知不算人工回复，客户的问题仍待真人处理 |
| 未响应回收、停用交还队列 | 保持原值 |
| 周期关闭 | 清空 |

`assigned_at` 与 `first_response_at` 语义不变。

### 3.3 企业客服设置

`settings` 表新增两个键，读写沿用 `object_storage.s3` 的做法；键不存在时按默认值处理。

`customer_service.assignment`：

| 字段 | 默认值 | 含义 |
|---|---|---|
| `responseReminderMinutes` | 5 | 负责人超过该时长未回复时提醒负责人 |
| `responseReclaimMinutes` | 15 | 负责人超过该时长未回复时退回队列并重新分配；必须大于提醒时长 |
| `queueReminderMinutes` | 5 | 会话在队列中等待超过该时长时提醒对应成员 |

`customer_service.business_hours`：

| 字段 | 含义 |
|---|---|
| `enabled` | 是否启用工作时间；未启用时始终按工作时间处理 |
| `timeZone` | IANA 时区，默认 `Asia/Shanghai` |
| `weekly` | 周一至周日各自的时间段列表，每段为 `HH:mm` 起止，同一天可以有多段，空列表表示休息 |
| `overrides` | 按日期覆盖：`{ date, periods }`，`periods` 为空表示当天休息；用于节假日和调休上班日 |

`domain` 提供纯函数：给定设置和时刻，判断是否处于工作时间，并计算下一个工作时段的开始时间（向后最多查找 366 天，找不到时视为无下次处理时间）。

## 4. 分配

### 4.1 可分配成员

可分配成员同时满足：

- 真人身份，账号有效，角色为客服（沿用 `activeCustomerServiceIdentityQuery`）；
- 工作状态为「工作中」；
- 当前接待量小于最大接待量；
- 团队队列时是该团队成员，公共队列时是企业内任一客服。

候选按当前接待量升序、`last_service_assigned_at` 升序（空值优先）、身份编号排序，取第一位。AI 员工不参与队列分配，其工作状态也不参与路由。

### 4.2 路由解析

入站首选路由与转人工备用路由的解析增加工作状态判断，最大接待量不参与指定成员路由：

| 路由目标 | 入站新周期 | 转人工 |
|---|---|---|
| 公共队列 | 进入公共队列并尝试自动分配 | 同左 |
| 团队 | 进入团队队列并在团队内尝试自动分配 | 同左 |
| 真人成员，工作中 | 分配给该成员 | 分配给该成员 |
| 真人成员，不在工作中 | 改用备用路由；备用路由不可用时进入公共队列 | 进入公共队列 |
| AI 员工 | 按现有资格判定 | 进入公共队列（现有） |

团队队列中无人可分配时，周期保持 `team_id = 团队`、负责人为空；公共队列时二者都为空。自动分配成功后 `team_id` 保留为该周期所属的队列，用于回收时退回原队列。领取、接管、转交不改变 `team_id`（现有行为）。

### 4.3 锁序与并发

分配必须在进入会话锁之前锁定候选成员，与现有「目标身份先于会话」的锁序一致：

1. 在会话锁之外读出候选列表；
2. 依次对候选的 `organization_identities` 行取 `FOR NO KEY UPDATE`，在锁内复核工作状态、角色、账号状态并重新统计接待量，不满足时释放并尝试下一位；
3. 再进入渠道身份锁与会话锁，复核周期仍在该队列中后写入负责人。

工作状态修改同样锁定该身份行，因此「切换为休息中」与「分配给他」串行执行。同一成员的并发分配在身份行上排队，接待量不会超过上限。

### 4.4 分配入口

| 入口 | 执行方式 |
|---|---|
| 入站新周期路由到队列 | 在入站事务内按 4.3 分配 |
| AI 转人工、AI 失去资格的管理交接 | 在交接事务内按 4.3 分配，结果决定对客话术（第 6 节） |
| 真人停用、真人失去客服角色 | 在该管理操作事务内把他负责的开放周期退回原队列（负责人清空，`team_id` 保持），逐个投递分配任务 |
| 积压补分配 | 成员切换为「工作中」、关闭周期、转交出周期、最大接待量调高、被回收后，事务内投递该成员的补分配任务 |
| 定时扫描兜底 | 第 5 节的扫描发现队列中有等待会话且存在可分配成员时投递分配任务 |

补分配任务先锁定该成员身份行，再按 `awaiting_reply_since` 升序从其所在团队队列和公共队列中取会话逐个分配，直到接待量达到上限或没有待分配会话。成员所在多个团队时，各团队队列与公共队列合并后按等待时间排序。

分配任务与补分配任务都不设幂等键：执行时在锁内重新判断，重复执行只会得到相同结果。

### 4.5 系统事件

沿用 `service_session_*` 事件族与 `ServiceSessionOperatedEvent` 载荷，新增两种事件：

| 事件 | 写入时机 | 载荷要点 |
|---|---|---|
| `service_session_assigned` | 自动分配成功 | 目标成员、来源队列（团队或公共队列）；没有操作人 |
| `service_session_returned` | 未响应回收、停用或失去客服角色退回队列 | 原负责人、退回的队列、原因 `response_timeout` 或 `assignee_unavailable` |

AI 转人工时直接分配到人的情况只写现有的 `service_session_handed_off`，其 `target` 为实际承接成员，不再追加 `assigned` 事件。前端 `conversation-timeline.tsx` 为两种新事件增加渲染。

## 5. 超时与提醒

### 5.1 规则

| 规则 | 适用周期 | 起算 | 动作 |
|---|---|---|---|
| 未响应提醒 | 开放、负责人为真人、`awaiting_reply_since` 非空 | `max(awaiting_reply_since, assignee_assigned_at)` | 超过 `responseReminderMinutes` 提醒负责人一次 |
| 未响应回收 | 同上 | 同上 | 超过 `responseReclaimMinutes` 退回原队列，写 `service_session_returned`，并在排除原负责人的前提下重新分配 |
| 队列等待提醒 | 开放、负责人为空、`awaiting_reply_since` 非空 | `awaiting_reply_since` | 超过 `queueReminderMinutes` 提醒一次：团队队列提醒该团队中工作中的客服，公共队列提醒全部工作中的客服 |

AI 负责的周期不适用这些规则，其超时跟进由路线图 3.2 定义。计时按自然时间，不扣除非工作时间。重新分配无人可接时周期留在队列，由队列等待提醒接续。

### 5.2 执行

注册一个 `@every 30s` 的 Cron 调度 `customer-service-timeout-scan`（队列 `maintenance`，`MaxAttempts: 1`），跨企业扫描到期的开放周期，按周期投递单条处理任务，结构与 `customer-delivery-scan` 一致。单条任务在事务内锁定会话后复核条件：

- 条件仍满足且 `reminded_at` 为空：发提醒并写 `reminded_at`；
- 达到回收时长：先锁定候选成员、再锁会话，完成退回与重新分配；
- 条件已不满足（已回复、已转交、已关闭）：直接结束。

扫描粒度 30 秒，提醒和回收最多晚 30 秒左右。MVP 阶段不为扫描新增普通索引。

### 5.3 提醒投递

提醒复用新消息系统通知，服务端只负责把提醒定向送到成员客户端：

- `realtime` 新增定向到用户受众的通知种类 `service_attention`，载荷为会话编号、客服周期编号和原因：`assigned`、`response_overdue`、`queue_waiting`、`returned`（发给被回收的原负责人）。网关的成员帧白名单与协议同步增加。
- 自动分配（包括补分配和转人工直接分配到人）成功后向负责人发送 `assigned`；5.1 的提醒分别发送对应原因。通知在事务提交后发布。
- 前端新消息通知模块订阅该帧，读取会话名称后经 `notifyNewMessage` 同一套策略投递：全局消息通知开关、工作状态为「工作中」、应用不可见，按 `service_attention:<周期编号>:<原因>` 去重。文案按原因本地化，例如「新的客户会话已分配给你」「客户等待回复已超过 5 分钟」。
- 桌面端、Web 与移动端共用该模块；移动端走现有本地通知。

`service_attention` 是临时事件，客户端离线时丢失；会话的负责人和队列状态以收件箱为准。

## 6. 转人工话术

对客通知改为由承接结果决定，模型只提供转交原因：

| 承接结果 | i18n Key | 中文文案 |
|---|---|---|
| 已分配到人（不论是否工作时间） | `agent.customer_handoff_assigned` | 已为您转接人工客服{name}，请稍候。 |
| 无人可分配，工作时间内 | `agent.customer_handoff_queued` | 已为您转接人工客服，正在排队，请稍候。 |
| 无人可分配，非工作时间，有下次处理时间 | `agent.customer_handoff_after_hours` | 现在是非工作时间，我们已记录您的问题，将于{time}起为您处理。 |
| 无人可分配，非工作时间，无下次处理时间 | `agent.customer_handoff_after_hours_unscheduled` | 现在是非工作时间，我们已记录您的问题，工作时间内会尽快为您处理。 |

- 文案按渠道 `default_locale` 取词；`{name}` 为承接成员的显示名称；`{time}` 按企业工作时间时区格式化为月、日、时、分，并附 UTC 偏移（如 `9月20日 09:00（GMT+8）`）。
- 模型主动转人工、依据不足、预算耗尽、无效输出、运行失败、AI 失去资格等全部交接路径统一使用上表，删除 `agent.customer_handoff_fallback` 与 `agent.customer_failure_fallback`。
- `handoff_to_human` 工具删除 `message` 参数，只保留 `reason`；工具描述改为「系统会按承接结果通知客户」。场景规则中的转人工说明同步修改。
- 通知仍以原 AI 员工为发送者，幂等键不变。

入站新周期路由到队列时不发送对客话术；访客侧的等待说明由路线图 3.7 的回复时间文案承担。

## 7. 界面

| 位置 | 变化 |
|---|---|
| 创建成员表单 | 增加必填「最大接待量」，默认 10，正整数 |
| 成员详情 | 增加「最大接待量」编辑行 |
| 工作状态菜单（桌面端导航、移动端「我的」） | 保持现有三项；「工作中」的帮助文案说明会被分配新的客户会话 |
| 系统设置 | 新增「客服」页签（`/settings/customer-service`），两个分组：「分配与提醒」维护三个时长；「工作时间」维护启用开关、时区、每周时段和日期覆盖 |
| 收件箱队列视图 | 列表项显示所属团队；增加队列筛选：全部、公共队列、各团队 |
| 会话时间线 | 渲染 `service_session_assigned` 与 `service_session_returned` |

## 8. PR 拆分

每个 PR 内部闭环，按顺序合入。

| PR | 范围 |
|---|---|
| A 接待量与自动分配 | 3.1、3.2 的字段；登录不重置工作状态；4.1 至 4.5 全部分配入口与事件；真人停用和失去客服角色退回队列；`service_attention` 的 `assigned` 通知及各端投递；成员表单与详情；收件箱队列团队标识与筛选 |
| B 工作时间与转人工话术 | 3.3 的两个设置键与「客服」设置页（本 PR 只含工作时间分组）；工作时间计算；第 6 节话术与 `handoff_to_human` 参数调整；同步修订 AI 客服角色行为方案 |
| C 超时与提醒 | 「分配与提醒」设置分组；第 5 节扫描、未响应提醒与回收、队列等待提醒；`service_attention` 其余原因 |

## 9. 验收

- 团队队列、公共队列、指定成员三种去向：有可分配成员时分到接待量最少的成员；接待量相同时轮转；全部满员时留在对应队列。
- 指定成员不在工作中：入站改走备用路由，转人工进入公共队列并自动分配。
- 并发：多条入站同时分配给同一成员时不超过最大接待量；分配与工作状态切换交错时，切换为休息中之后不再收到新分配。
- 成员切换为工作中、关单、转交出后，按等待时间从所在团队队列和公共队列补分配。
- 真人停用或改为非客服角色后，其开放周期退回原队列并重新分配。
- 转人工话术四种结果按工作时间和分配结果正确选择，非工作时间显示正确的下次处理时间，时区与日期覆盖生效。
- 负责人超时未回复先提醒、再回收，回收后不会分回原负责人；已回复、已转交、已关闭的周期不提醒；每轮等待只提醒一次。
- 队列等待提醒只发给对应团队或全部工作中的客服。
- 提醒在桌面端、Web 与移动端经新消息通知策略投递：关闭消息通知、非工作中、应用可见时不弹出。

## 10. 不在本方案内

- 成员在线与离线状态、Messenger 在线标识：路线图 3.7。
- 移动端远程推送（APNs、FCM）。
- 现有新消息通知对全部客户会话生效的范围收窄。
- AI 负责周期的超时跟进与关单：路线图 3.2。
- 按技能或咨询分类分配：路线图 3.3 的意图路由。
