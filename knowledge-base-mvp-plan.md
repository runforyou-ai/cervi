# 知识库最小 MVP 开发顺序

**本文已被 [知识文档处理管线 Go 化与 Haystack 移除方案](knowledge-go-pipeline-plan.md) 取代。** 原件转换由 markitdown 独立容器承担，分段、向量化、分段存储与召回编排均在 Go 服务端进程内实现，Haystack 与 Hayhooks 已从仓库移除。以下内容保留作为选型实验与评测依据。

状态：PR 1、PR 2 已实现；PR 3 及之后的 Haystack 相关范围不再执行，实际实施顺序见上述 Go 化方案。

## 1. 目标与职责

实施顺序：**Cervi 文档增删改查 → 知识库配置 → 同步到 Haystack，完成解析、分段、索引和召回 → Cervi 召回测试 → Agent 联调**。

- Cervi 保存原始资料、问答、分组和知识库配置，管理文件、企业边界、来源修订、可靠任务与索引发布状态。
- Haystack 在 Python 管线内执行文件转换、清理、分段、embedding、索引和召回；Hayhooks 提供内部 HTTP 接口。Go 不再实现一套分段逻辑。
- Cervi 根据当前来源和索引版本校验召回结果，提供人工测试和 Agent 共用的检索业务入口。
- 问答 CRUD 已存在，继续沿用；新建文档使用独立业务模型。文件保存成功、解析成功和索引可用分别表示不同结果。

## 2. 开发运行方式

- 不修改 Docker Compose，不增加共享 Haystack 服务。
- 每个工作区沿用 `wails3 task run:haystack`，读取当前工作区的 PostgreSQL 配置和 `HAYSTACK_PORT`，连接本工作区数据库。
- Go 通过服务地址环境变量连接当前工作区的 Haystack；具体变量随接入 PR 补入 `.env.example` 和服务端配置。
- PostgreSQL、NATS 继续共享实例；各工作区独立数据库、NATS 命名空间和 Haystack 进程。HTTP 请求不携带跨工作区数据库路由。
- 沿用现有 PostgreSQL 扩展和 `haystack` schema。未配置或未启动 Haystack 时，文档、问答及配置管理仍可使用。
- 集成验证通过 Task 启动连接本工作区测试数据库的独立 Haystack 进程，结束后清理本次进程并确认端口释放。

## 3. PR 顺序

| 顺序 | 标题 | 交付结果 |
|---|---|---|
| PR 1 | 知识库文件上传、列表、预览与分组管理 | 用户能上传、预览、移动和删除文档 |
| PR 2 | 知识库模型、分段与召回配置 | 用户能保存后续处理资料所需的配置 |
| PR 3 | Haystack 文件解析、分段、索引与召回管线 | 固定测试资料通过 Go 客户端跑通处理和召回 |
| PR 4 | 文档与问答同步、索引状态和重建 | Cervi 中的真实资料及其变化可靠同步到 Haystack |
| PR 5 | 知识库召回测试 | 用户在 Cervi 中验证查询、命中内容和来源 |
| PR 6 | Agent 知识库联调 | Agent 使用绑定范围内的文档和问答回答 |

PR 3 和 PR 4 是“同步到 Haystack 并完成处理”阶段的两个独立提交范围；先验证执行接口，再接入真实业务生命周期。PR 1、PR 2 不依赖该阶段。

### PR 1：知识库文件上传、列表、预览与分组管理

范围：

- 标准知识库增加独立文档模型，关联本地文件记录和所属分组；文件名作为文档名称。暂不提供手工文档、重命名、替换原件或文档正文编辑。
- 以 Haystack 3.1.1 内置 `MultiFileConverter` 的九类格式作为首批范围：TXT、Markdown、HTML、PDF、DOCX、PPTX、XLSX、CSV、JSON。扩展名包括 `.txt`、`.md`、`.markdown`、`.html`、`.htm`、`.pdf`、`.docx`、`.pptx`、`.xlsx`、`.csv`、`.json`，扩展名大小写统一归一化。其他转换集成支持的格式另行扩展。
- 每次选择最多 10 个文件，每个文件不设置产品级大小上限；每个文件形成一条文档，允许同名，不自动覆盖。复用现有分片上传，前后端落实每批 10 个限制。
- 点击上传文档先打开模态框，展示支持格式、每批 10 个和单文件大小不限的说明；支持拖拽及一次多选，加入文件后立即在框内显示逐项上传进度，完成后保留结果，关闭后回到原列表。成功创建的文档状态统一为“初始”，不表示正在排队或已建立索引。
- 文档列表提供名称搜索与分页；列为名称、类型、大小、状态、创建时间、操作。按创建时间倒序，最新上传在前，不显示更新时间；同时间记录使用文档编号倒序保持稳定顺序。
- 最后一栏直接展示小尺寸描边“详情”按钮和三点菜单。菜单仅包含“移动分组”和“删除”，删除前确认。
- 移动分组在当前列表打开对话框，目标限定为同一知识库的分组。已有文档的分组不能删除；删除知识库时删除所属文档并释放文件引用。
- 点击“详情”直接进入预览页，复用 Git 历史中删除的 Dify 文档预览交互。左上角显示文件名；右上角按顺序显示 disabled 的“查看分段”和可用的“返回”。本期没有下载入口。
- 预览支持 PDF、Markdown、HTML、DOCX、XLSX、CSV、TXT，复用原有解析与渲染组件；JSON 作为文本展示。PPTX 等尚无预览器的格式显示“无法预览”。文件读取或解析失败显示失败及重试，不当作格式不支持。
- 预览、移动或删除后保留仍有意义的分组、搜索、页码和滚动位置。新增页面走现有 Web/桌面共用路由。
- 复用临时上传、事务激活和本地/对象存储读取。补充知识文档文件用途、归属校验和预览访问鉴权；失败或取消不遗留已激活的孤立文件，删除复用既有文件清理机制。

预览复用来源：提交 `332cc33` 的父版本中的 `knowledge-document-page.tsx`、`knowledge-document-preview.tsx`、`document-preview-parser.ts` 和 `document-pdf-preview.tsx`。按本地接口与允许上传格式恢复组件和必要依赖，删除 Dify 连接判断、外部文件缓存和本期不用的解析分支；不恢复外部知识库接口。

文件类型依据：已核对本机 Haystack 3.1.1 分发包中的 `multi_file_converter.py` 与官方文档。内置 PDF 转换不含 OCR；JSON 默认按 `content` 字段提取，其他结构需要在后续解析 PR 明确抽取规则；不得把任意 JSON 或扫描 PDF 都标记为解析成功。[MultiFileConverter](https://docs.haystack.deepset.ai/docs/multifileconverter)、[JSONConverter](https://docs.haystack.deepset.ai/docs/jsonconverter)

Haystack 的额外 LibreOffice 集成可以将 DOC、XLS、PPT、ODT 等转换后再交给文档 converter，但需要 `libreoffice-haystack` 和本机 LibreOffice。本次不把这些额外依赖纳入首批格式基线。[LibreOfficeFileConverter](https://docs.haystack.deepset.ai/docs/libreofficefileconverter)

验收：

- 九类格式的文件校验、原件保存与预览行为符合上表；不支持上传的格式明确拒绝。10 个文件可提交，11 个文件明确拒绝；上传大小不新增产品上限。
- 多文件部分失败时，成功文件保留，失败项可重试，重试不重复创建成功文档；上传完成留在列表，文档显示“初始”。
- 列表创建时间排序、分页和搜索稳定；操作列、菜单、预览页按钮及返回行为符合上述交互。
- 文件激活、取消、移动、删除、分组占用、整库删除、企业隔离和预览鉴权通过服务端验证；本地与外部临时 S3 均验证读取及清理。
- 复用的 PDF 预览器不提供下载工具栏，HTML 使用原有清理与沙箱，预览资源在离开页面时释放；Web/桌面实际验证支持和不支持的预览样本。

边界：不接入 Haystack 处理链路，不恢复下载、手工文档或内容编辑；本 PR 的“修改”仅包含移动分组。索引状态和后续任务在相应 PR 扩展，“初始”是尚未提交处理的业务状态。

### PR 2：知识库模型、分段与召回配置

范围（2026-09-09 确认）：仅扩展现有创建、编辑知识库表单，每个知识库独立保存配置，不新增设置页签。

| 字段 | 要求 |
|---|---|
| 向量模型 | 必填，复用供应商目录中的 embedding 模型 |
| 向量维度 | 必填，正整数 |
| 分段长度 | 文档库必填，默认 512，范围 256–2048 |
| 分段重叠 | 文档库必填，默认 50，范围 0–200 |
| 召回数量 | 必填，默认 3，范围 1–20 |
| 重排模型 | 不选择即关闭重排，选择后开启 |

- 问答库不展示、不保存分段长度和分段重叠。
- 编辑时切换向量模型，在保存前二次确认，明确提示需要重新索引全部资料；取消不提交，其他字段修改直接保存。
- 配置直接更新，不引入配置版本管理。本次只保存参数，不执行解析、索引或召回。
- 服务端校验数值范围、模型用途及同企业归属，供应商与模型删除、编辑保留引用校验。

验收：前端构建、55 项前端测试和完整服务端测试通过；浏览器验证文档库/问答库创建与回填、默认值、数值原生校验、模型切换确认及取消、重排选择与清空、普通参数直接保存。

后续 PR 中旧的配置版本和多代次发布方案不再作为实施要求；索引执行交互在对应功能开发前另行确认。

### PR 3：Haystack 文件解析、分段、索引与召回管线

范围：

- 扩展现有工作区服务，增加 Go HTTP 客户端、配置快照和明确的错误契约。
- 提供文件/文本输入、来源删除、向量召回和同一来源版本相邻分段读取接口；按配置执行 embedding 与可选重排。
- 使用 Haystack converter 和 splitter 组织处理管线，尽可能复用现成组件覆盖 PR 1 的常用文档格式，逐项记录格式、依赖、提取能力和测试结果。
- 支持范围以真实样本验证为准。暂不支持、解析为空、加密或损坏的文件明确失败，保留 Cervi 原件供下载或后续重试；不得静默跳过并标记可用。
- 分段携带企业、知识库、来源类型及编号、内容修订、索引代次与位置；页码只使用解析器提供的真实信息。原始文件读取与处理临时副本遵循文件生命周期。
- 用固定文档和问答样本验证接口，暂不接入业务保存时的自动同步。

验收：Go 客户端跑通真实资料的解析、分段、embedding、索引、召回、相邻读取和删除；中文长文无遗漏且不超过模型输入上限；重复写入不重复；不同模型维度隔离；错误可定位且不泄露凭据。多格式验收包含表格、代码、金额、跨页文本及不支持样本。

### PR 4：文档与问答同步、索引状态和重建

范围：

- 将文档和既有问答接入同一来源生命周期，增加内容修订、来源集合修订、索引记录及可靠任务。
- 提供已有资料的“建立索引”，以及后续新增、内容修改、文件替换和删除的自动同步。单纯移动分组保留已有 embedding。
- 展示排队、处理中、可用、失败及失败重试；状态依据实际远端完成结果与版本核对更新。
- 模型或分段配置变化时建立新代次，完成后原子发布；重建期间保留仍有效的当前代次，失败保留原可用索引。
- 覆盖来源变更与重建并发、重复任务、旧任务晚完成、服务重启和远端清理；删除来源或知识库后立即停止交付相应内容。
- 问答命中在业务层按条目折叠，读取当前版本的完整答案。

验收：从 Cervi 保存真实资料后可通过内部检索接口命中；修改与删除后不交付旧内容；重建期间的增删改不会被旧任务覆盖；失败和重试状态准确；仅变更重排配置不重建向量。

### PR 5：知识库召回测试

范围：

- 在当前知识库增加与 URL 同步的召回测试页签，提交查询并展示命中内容、来源、分段位置和诊断分数。
- 普通文档展示原文片段，可查看相邻分段和原件；问答展示主问题及完整答案，合并相似问题命中。
- 人工测试统一经过 Cervi 检索业务入口，使用该知识库的配置。区分无命中、未就绪和失败；不直接由前端调用 Haystack。

验收：中文同义问法、单字有效查询、无关问题、跨段资料、长答案和失败反馈通过；已删除或修订失效的结果与游标不可读取；页面交互和来源跳转通过验证。

### PR 6：Agent 知识库联调

范围：

- 将人工测试使用的检索实现注入既有 `search_knowledge`，沿用 Agent Revision 保存的知识库范围。
- 复用现有多查询、多库融合，补齐来源索引版本游标和交付前来源有效性校验。
- 联调普通文档引用、相邻上下文读取和问答完整答案；绑定为空时不注册工具，失效绑定或检索失败明确报告。

验收：同一资料在人工测试与 Agent 中可一致检索；单库、多库、多查询、局部失败、企业隔离、Run 配置快照及知识库删除通过。Agent 的资料回答可追溯来源，无支持资料时不声称已找到依据。

## 4. PR 1 验证记录

- 服务端集成测试覆盖批次幂等与并发重试、10/11 个限制、创建时间倒序、搜索分页、移动分组、分组占用、文档及知识库删除、企业隔离和本地预览鉴权。
- S3 分支通过 HTTP 契约测试验证直接签名读取、关闭对象存储后按原存储类型读取、删除后不再签发、文档及整库删除后的对象清理。开发企业尚未配置 S3 凭据，真实外部 S3 的浏览器 CORS、读写及清理仍待环境验证；已有预签名链接在过期或对象清理前仍可能有效。
- 前端解析回归测试覆盖允许预览的格式、HTML 清理、工作表切换、未知格式和损坏文件；Web 和桌面通过实际页面检查上传、初始状态、预览按钮和移动分组。模拟单文件网络失败后，批次成功项保留，重试未产生重复文档。
- 上传模态框补充验证：浏览器通过系统选择器一次选择两份文件，框内显示逐项进度并保留完成结果；桌面 MCP 触发拖放事件，验证 11 个文件拒绝、有效多文件批次立即上传及进度完成。此次 Pi 审核未发现功能缺陷，大小不限说明按已确认需求保留。
- 桌面 PDF 在自动化环境中受到系统隐藏窗口的动画帧暂停影响；验证时仅在测试页面临时使用计时器驱动帧回调，确认实际 PDF canvas 和正文渲染。产品代码未增加计时器或后台绘制分支，前台窗口恢复行为仍需真实交互复核。
- Pi CLI 首轮已审核并处理有效反馈；补充复核因审核服务额度不足返回 403，未取得第二轮结果。预览内容继续在离开页面后释放，避免大文件持续占用客户端内存；不缓存整份原件以换取再次进入时免传输。

## 5. 分段与解析的实施依据

Haystack 官方将 converter 用于将不同格式文件转换成统一 Document；首批上传范围按 PR 1 的内置转换组件基线限定；项目在 PR 3 对允许上传的格式逐项保存实际解析结果与依赖，不静默跳过无法处理的来源。[Converters](https://docs.haystack.deepset.ai/docs/converters)

官方 `DocumentSplitter` 支持按段落、句子等单位划分，提供长度、重叠与短片段合并参数，并传递来源元数据。项目据此在 Haystack 内组织转换、必要清理、分段和索引步骤。[DocumentSplitter](https://docs.haystack.deepset.ai/docs/documentsplitter)

`RecursiveDocumentSplitter` 按给定分隔符顺序递归拆分，支持字符、词或 token 单位，可作为中文长文处理的候选；最终组件和参数按固定版本与真实语料验证选择，不直接照搬英文默认值。[RecursiveDocumentSplitter](https://docs.haystack.deepset.ai/docs/recursivesplitter)

项目验收要求：核对实际 embedding tokenizer 的输入上限；保留中文、代码、金额、否定和表格上下文；原文与分段可关联，全文覆盖且相邻分段可读取。问答保留条目及各问题、答案编号，长内容在 Haystack 内处理，召回后由 Cervi 关联完整答案。

配置直接保存，不维护配置历史版本；后续重新索引使用当前知识库配置。MVP 不提供手工分段编辑器。OCR、复杂版式和多模态能力按解析组件及样本效果另行确定，不作为文档 CRUD 的前置条件。

## 6. 开发与交付要求

- 先交付 PR 1 文档管理，再依次开发其余 PR，不提前用检索设施阻塞内容管理。
- 按仓库约定生成迁移、appservice 适配层和 Wails 绑定；使用中文注释、提交及 PR 描述。
- 各 PR 运行适用的构建和测试；涉及页面结构或交互时验证界面。提交 PR 前执行 Pi CLI 审核并处理反馈。
- 每次验证清理本次启动的服务、客户端和测试进程；保留其他工作区与共享 PostgreSQL/NATS。
