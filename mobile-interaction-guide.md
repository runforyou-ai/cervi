# 移动端导航与交互约定

## 本轮交付

移动端以「消息／通讯录／我的」作为固定一级导航。消息接入真实分类与客户筛选，保留已有单聊详情；通讯录和个人设置先建立入口与明确的未开放状态。

`mobile-navigation-prototype.html` 是可直接打开的交互原型，使用演示数据，不连接企业服务端。实际移动端实现仍使用 `appservice` 数据契约与已有认证流程。

## 页面与路由

| 层级 | 页面 | 地址 | 当前能力 |
| --- | --- | --- | --- |
| 一级 | 消息 | `/inbox` | 全部、客户、内部真实列表 |
| 一级 | 通讯录 | `/contacts` | 四个分类入口 |
| 一级 | 我的 | `/me` | 当前用户、企业、个人设置入口、退出确认 |
| 详情 | 成员单聊 | `/inbox/direct/:conversationID` | 已有会话历史和文本发送 |
| 详情 | 企业成员 | `/contacts/employees` | 暂未开放 |
| 详情 | AI 员工 | `/contacts/ai-employees` | 暂未开放 |
| 详情 | 团队 | `/contacts/teams` | 暂未开放 |
| 详情 | 外部联系人 | `/contacts/external` | 暂未开放 |
| 详情 | 个人设置 | `/me/settings` | 暂未开放 |

一级页面显示底部导航；详情页面使用独立布局，隐藏底部导航。标题栏固定在顶部，列表独立滚动。顶部安全区由工作区提供，底部安全区由一级导航或详情布局提供。

连接、登录继续使用已有独立路由，不出现工作区导航。

## 消息分类与筛选

- 消息标题下方显示「全部／客户／内部」，与 Web、桌面端共用 `InboxScope`。
- 客户与内部是业务范围；单聊与群聊是会话形态。内部包括真人单聊、Agent 单聊和群聊，本轮不增加单聊／群聊子筛选。
- 各分类直接按参数请求 `loadInbox`，不在已经截断的全部列表上进行本地业务筛选。
- 客户范围通过底部面板选择「排队中／我负责的／同事／已关闭」；同事范围可进一步选择负责人，排除当前用户，保留真人与 Agent 候选。
- 面板编辑的是临时筛选；「应用筛选」后才更新地址与查询。取消、点击遮罩、Escape 或 Android 返回均放弃本次编辑。
- 分类与筛选写入 `scope`、`view`、`assignee` 查询参数，默认值省略。查询 key 包含完整的规范查询，与其他端同参数的查询共享缓存。
- 变更筛选替换当前地址，不把每次点击加入返回历史。
- 群聊显示名称、头像、消息摘要和时间，标注「群聊详情暂未开放」。客户会话保留渠道与处理状态，标注「仅查看摘要」。本轮只有单聊可以进入详情。

## 导航与返回

- 一级导航之间使用替换导航；返回键不依次遍历曾点击的一级模块。
- 站内打开详情使用压入导航，并标记存在来源。标题返回沿历史返回；直接打开详情时返回所属一级模块。
- 消息分类、客户筛选和各列表滚动位置在当前登录工作区内保留；退出登录后释放，不做跨账号、跨企业或跨应用重启持久化。
- 列表位置按分类和筛选分别记录，切换查询时使用独立滚动节点，数据就绪后恢复。返回已有单聊列表时恢复来源条件与位置。
- Android 返回首先遵循系统键盘收起行为；键盘关闭后，原生端向页面发送可取消的 `cervi:back` 事件。页面先通过捕获阶段发出 Escape 关闭当前菜单或弹层；没有浮层时，详情标题组件调用与标题返回相同的导航逻辑。一级页面未处理该事件时，继续 WebView 返回或系统默认退出。
- 不在本轮新增 iOS 自定义边缘滑动手势，也不建设通用页面缓存或离线导航栈。

## 菜单与页面载体

| 操作类型 | 交互位置 | 本轮状态 |
| --- | --- | --- |
| 消息筛选 | 分类下方条件入口 → 底部面板 | 已接入 |
| 刷新列表 | 标题右侧文字按钮 | 已接入 |
| 发起单聊、创建群聊 | 消息标题右侧「新建」菜单 | 随业务功能开放，本轮不显示空菜单 |
| 个人设置 | 我的页面列表入口 → 独立页面 | 页面占位 |
| 会话静音、群资料等对象操作 | 详情标题右侧三点菜单 | 随业务功能开放 |
| 退出账号、移除成员等危险操作 | 原入口 → 确认弹窗 | 已接入退出确认 |
| 选择负责人 | 筛选面板中的原生选择控件 | 已接入 |
| 聊天、资料、长表单 | 独立详情路由 | 单聊已接入，其余按后续功能接入 |

短操作优先使用底部面板，完整任务优先使用独立页面。面板关闭后恢复触发焦点和原页面，不堆叠多层业务面板。新增和编辑表单沿用项目约定，通过底部取消离开，不增加标题返回操作。

## 页面状态和触控

- 标题与分类固定，客户范围显示筛选入口，全部与内部分类下直接显示列表。首次加载、空列表和首轮错误在内容区展示。
- 后台刷新保留内容；失败时标题操作显示「刷新失败」，可点击重试，不挤压列表或关闭正在操作的筛选面板。
- 未开放功能与真实空数据使用不同文案；不填充模拟业务数据、不增加无响应按钮。
- 主要触控目标至少 44px，分类入口为整行可点击。焦点状态明确，图标按钮有可访问名称。
- 各页面共用全选快捷键规则：非编辑区域阻止 Ctrl+A 和 Command+A 的整页全选；输入框、文本域和可编辑内容保留全选，消息正文仍可正常选择复制。
- 消息输入沿用现有组件，点击发送、回车换行；本轮不增加键盘动画、输入模式和发送生命周期变化。
- 中英文文案同步维护。长姓名、企业名、英文文案应在窄屏下保持可读且不横向溢出。

## 后续 PR 接入顺序

1. 发起单聊：新建菜单、成员选择、首条消息与失败重试。
2. 通讯录只读目录与成员资料：替换分类占位，在资料页接入发消息入口。
3. 群聊详情和触屏交互：复用详情布局，接入历史、发送、群资料与成员。
4. 客户会话详情与处理操作、个人设置、未读和通知分别独立交付。

本轮不修改业务数据结构、API 契约或权限语义。

## 验收清单

- 消息 → 客户 → 同事筛选 → 切换通讯录 → 返回消息，筛选和列表位置仍在。
- 打开单聊后标题返回和 Android 系统返回都回到来源列表，一级导航恢复。
- 筛选面板取消与系统返回不改变查询；应用后请求携带对应分类、视图和负责人。
- 群聊进入全部和内部列表，客户视图只显示客户；空列表和失败状态对应当前查询。
- 后台刷新失败时内容、滚动和正在编辑的筛选面板保持稳定。
- 通讯录四个分类和个人设置进入明确的未开放页面，返回与底部导航行为一致。
- 中文和英文窄屏下，顶部、底部安全区与消息输入不重叠。
- iOS 和 Android 真机验证键盘弹出／收起、前后台恢复、返回键及面板触发焦点。

## 本轮验证记录

- 前端 TypeScript 检查与生产构建通过。
- iOS arm64 模拟器 `.app` 打包通过；Android arm64 发布 APK 在临时 Java 21 环境打包通过。本机默认 Java 25 的 Android Lint 检查失败，未修改系统 Java 配置或跳过发布检查。
- 使用 Node 模拟 DOM 加载实际移动端 React 页面，以模拟接口验证分类请求参数、负责人选择、排除自己、导航记忆、滚动节点隔离、返回恢复、弹层取消、后台刷新失败、直接进入子页后的返回及退出确认。该检查不代表已连接真实企业服务端进行联调。
- 可点击原型的分类、筛选、通讯录、聊天详情、我的页面和退出确认交互检查通过。
- Pi CLI 审核提出的滚动节点隔离、异常回跳保留查询问题已处理；详情页原生返回也已统一到标题返回逻辑。
- 未控制浏览器或真机。键盘、触控、系统返回键和安全区的实际视觉效果仍需按上面的真机验收清单确认。
