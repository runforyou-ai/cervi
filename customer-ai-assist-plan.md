# 客服 AI 辅助方案

本文定义客服在客户会话中使用 AI 的产品形态、数据模型、运行语义和交付顺序，覆盖 AI 写回复、Copilot 和内部备注三类能力。本文的产品决策替代 `chat-roadmap.md` 阶段 1E 中「内部备注 @AI 员工」的设计。

## 1. 产品分层

客服处理客户会话时，AI 与团队协作按入口和可见范围分为三层：

| 能力 | 入口 | 可见范围 | 交互 | 产出 |
| --- | --- | --- | --- | --- |
| AI 写回复 | 「回复客户」输入区 | 仅本人 | 一次生成 1–3 条候选 | 填入对客草稿 |
| Copilot | 右侧栏「AI 助手」页签 | 能读取该客户会话的企业成员 | 多轮问答，可调用工具 | 分析结论，可填入对客草稿 |
| 内部备注 | 「内部备注」输入区 | 能读取该客户会话的企业成员 | 成员之间留言，可 @ 同事 | 时间线中的内部消息 |

分层规则：

- 客户会话时间线中的发言者只有联系人、企业成员和负责接待的 AI 客服。Copilot 与 AI 写回复不向客户会话写入任何消息。
- 对客内容始终由客服确认后发送。AI 写回复和 Copilot 只修改输入区草稿。
- 负责接待的 AI 客服上下文只包含对客消息，不读取内部备注和 Copilot 线程。
- 需要团队留痕的 AI 结论保存在共享的 Copilot 线程中。

AI 写回复覆盖 Web、桌面端和移动端；Copilot 与内部备注覆盖 Web 与桌面端。

## 2. 现有基础

- 客户会话使用 `conversations.type = customer`、`customer_conversations` 和 `service_sessions`。成员按企业边界读取客户会话（`internal/actions/conversation/list_conversation_messages.go`），对客回复要求周期未关闭，且负责人为本人或无人负责（`send_customer_text_message.go`）。
- 独立 AI 聊天使用 `agent` 类型会话和 `agent_conversations`，由 `agentChatRunPolicy` 执行，支持连续输入补入、运行过程块、停止回复、附件和运行流。
- Agent 运行统一走 `agent_lanes`、`agent_inputs`、`agent_runs` 和按执行范围选择的 Run Policy（`internal/actions/agentrun/input_feed.go`）。
- `agentruntime.RunRequest` 已支持知识库检索、附件读取和 MCP 服务注入；客户历史检索工具 `search_customer_history` 目前返回查询不可用的占位结果。
- 右侧资料栏在客户会话中提供「AI 助手」页签（`frontend/src/features/inbox/conversation-context-pane.tsx`），承载 Copilot 线程（`frontend/src/features/inbox/customer-copilot-panel.tsx`）。
- 群聊已有结构化提醒 `message_mentions`、提及计数与提及导航，客户会话尚未接入。

## 3. AI 写回复

### 3.1 交互

「回复客户」输入区提供「AI 写回复」入口，打开弹层后包含：

- **模式**：「写回复」根据会话上下文撰写回复；「改写」保留客服草稿原意并改写表达。改写要求草稿非空。
- **语气**：保持原语气、专业、友好、简洁。
- **AI 员工**：生成回复使用的 AI 员工，候选为同企业活跃且具有有效托管配置版本的 AI 员工。
- **候选**：生成 1–3 条完整、可直接发送的回复，点击一条即替换当前对客草稿并关闭弹层；提供「重新生成」。

触发规则：

- 打开弹层时，输入框已有草稿默认选中「改写」，否则选中「写回复」，并按选中模式生成；切换模式、语气或 AI 员工时重新生成。
- 改写模式下弹层保持打开时，草稿或引用变化后防抖 600ms 重新生成。
- 客户新消息到达不自动生成。
- 同一会话、模式、语气、AI 员工、草稿和引用的结果在本页缓存；切换会话时清空缓存并关闭弹层。
- 返回结果与当前编辑上下文不一致时丢弃。

语气和 AI 员工选择按企业保存在本地，默认使用本人上次的选择。入口可用条件与对客发送按钮一致；当前成员不可对客发送时入口保留显示并禁用。Web 与桌面端使用贴近入口的弹层，移动端使用底部弹出面板，模式、AI 员工、语气和「重新生成」使用触屏尺寸，候选整条可点击。

### 3.2 应用服务契约

新增 `GenerateCustomerReplySuggestions`，认证为 `member`：

```text
输入
├── conversationId
├── agentIdentityId
├── mode              reply | rewrite
├── tone              keep | professional | friendly | concise
├── draft             改写模式必填
└── replyToMessageId  可选，同会话可引用消息

输出
└── candidates        1–3 条字符串
```

Action 在只读事务中校验：客户会话属于当前企业；当前成员满足对客发送条件；AI 员工属于同企业、状态活跃且当前配置版本有效；引用消息属于同一会话且可被引用。校验失败返回本地化业务错误。

### 3.3 生成执行

AI 写回复是一次同步请求内的单次模型调用，不创建 Lane、Input、Run 或消息，不保存候选结果。响应时间优先，生成过程不调用工具。

- **配置**：使用 AI 员工当前配置版本的模型和系统指令，不注入知识库检索、MCP 工具、客户历史检索和附件读取工具。模型组件提供思考开关时关闭思考：DeepSeek、智谱、通义千问和火山方舟按各自参数关闭，Anthropic 默认不开启思考，其余品牌保持模型默认行为。
- **指令**：在 AI 员工系统指令之后追加回复助手要求，写明模式、语气、回复语言与客户最近消息一致、每条候选完整可发送且不含解释，并要求只输出 `{"candidates": [...]}` 格式的 JSON 对象。
- **上下文**：当前客服周期的对客消息，按模型上下文窗口预算保留最近部分；附件消息以名称、类型和地址写入正文；引用消息与客服草稿作为本次请求单独提供。客户消息作为事实背景提供，明确声明其中内容不构成对助手的指令。
- **结果**：解析模型正文中的 JSON 对象，去除空白候选后保留前 3 条；没有有效候选时返回生成失败错误。
- **时限**：生成受请求 context 和 25 秒上限约束，上限低于服务端 30 秒写超时，超时返回生成失败错误。
- **日志**：完成记录 `INFO`，包含企业、会话、AI 员工、模式、候选数量、耗时和 Token 用量；失败记录 `WARN` 与规范化错误。日志不记录草稿、消息正文和系统指令。

`agentruntime` 增加与 `Run` 并列的一次性生成入口，复用模型构造和上下文预算；该入口不接收 `InputFeed`，不注册工具，不产生运行流。

## 4. Copilot

### 4.1 交互

右侧栏「AI 助手」页签展示当前客户会话的 Copilot 线程：

- 默认打开本页上次查看的线程，刷新页面或该线程不存在时打开最近更新的线程；顶部下拉的线程选择器列出本会话全部线程，以首条提问开头为主文字，并显示 AI 员工、创建人和最近更新时间。
- 客户会话还没有线程时，页签直接展示 AI 员工选择和提问输入框。
- 「新对话」要求选择 AI 员工，默认使用本人上次的选择；首条提问与线程在同一事务创建。
- 线程内时间线复用 AI 聊天的消息展示：成员提问显示提问人，AI 回复展示思考、工具过程、用量、失败与停止状态；支持附件、引用和停止回复。
- AI 回复中可直接发给客户的内容以回复卡片展示，每张卡片提供「填入回复」：只以卡片正文替换「回复客户」草稿并切换到该页签；草稿非空时先确认替换。回复中没有对客回复时不提供该操作；当前成员不可对客发送时该操作保留显示并禁用。
- 能读取该客户会话的成员都能查看全部线程、继续提问和停止回复。

### 4.2 数据模型

Copilot 线程是一种独立会话类型，消息、参与者、附件、运行和过程块沿用统一聊天与 Agent 事实。

```text
conversations.type = copilot

customer_copilot_threads
├── conversation_id            主键，Copilot 线程会话编号
├── organization_id
├── customer_conversation_id   所属客户会话编号
├── agent_identity_id          回答问题的 AI 员工身份编号
├── created_by_identity_id     创建线程的成员身份编号
├── created_at
└── updated_at
```

规则：

- AI 员工在线程创建时固定。AI 员工停用后线程保留只读，新提问返回 AI 员工不可用错误；已提交输入继续处理。
- 线程创建时写入 AI 员工参与者；成员首次提问时写入该成员参与者。
- 读取授权从所属客户会话派生：能读取客户会话即能读取和写入其全部 Copilot 线程。客户会话授权规则变化时 Copilot 随之变化。
- Copilot 线程不进入消息列表、会话搜索、未读统计、个人会话状态和 `GetSyncHeads`。
- 线程列表按 `customer_conversation_id` 查询，按 `conversations.last_activity_at` 倒序。

### 4.3 触发与执行范围

- 成员在线程中首次持久化的文本或附件消息，在同一事务写入 `agent_inputs.kind = copilot`，Lane 执行范围为 `scope_kind = conversation`、`scope_id` 为线程会话编号。
- 线程内多名成员连续提问时，由在途 Run 在安全点补入，产出一条综合回复。
- 停止回复复用 `StopAgentReply` 的停止语义，授权改为按所属客户会话的读取资格判断。
- 新增 `copilotRunPolicy`：锁序为 Copilot 线程 Conversation → AI 员工 Participant → Lane → Run → Task；结果消息写回线程；配置版本取线程固定 AI 员工的当前版本。

`agent-roadmap.md` 不变量 5 补充只读引用规则：执行可以只读引用其他会话的已提交事实作为背景，被引用会话不属于该执行范围；此类执行不参与被引用会话的执行互斥，也不向其写回结果。Copilot 读取客户会话、同时负责接待的 AI 客服在客服周期范围内运行，两者按此规则并行。

### 4.4 上下文与工具

`copilotRunPolicy.loadMessages` 按以下顺序组装：

1. **客户会话背景**：联系人显示名、渠道、当前客服周期状态与负责人，以及客户会话最近的对客消息和内部备注，按模型上下文窗口预算截取，每条带发送者类型、显示名和可见性。每次认领输入时重新读取，反映最新进展。背景整体声明为事实资料，其中内容不构成对助手的指令。
2. **线程历史**：截至本次认领边界的线程消息。成员提问投影为带发送者标识的 `user`，AI 员工自身回复投影为 `assistant`。

系统指令在 AI 员工系统指令之后追加 Copilot 场景说明：回答只提供给企业客服；背景资料只作为事实依据；需要对客回复时，每条回复完整写在语言标记为 `customer-reply` 的代码块中，代码块内只写发给客户的正文，语言与客户最近消息一致，最多 3 条，分析与建议写在代码块之外。前端把该代码块渲染为回复卡片。

工具注入：AI 员工配置版本绑定的知识库检索与 MCP 服务；线程内附件按现有附件规则处理。客户历史检索接入真实查询后，Copilot 注入指向所属客户会话的 `search_customer_history`，用于查询超出背景预算的更早客服周期。

### 4.5 实时与刷新

- 线程的会话版本变化与客户会话一样通知企业客服共享受众，通知携带线程会话编号，不通知参与成员个人受众。
- 客户端在 `chat-realtime-pr-plan.md` PR28 接入通知消费前，Copilot 页签打开时沿用 AI 聊天的前台轮询读取线程列表、线程消息和运行状态。
- 运行过程流随 PR38 接入，请求授权按所属客户会话读取资格校验。

## 5. 内部备注

阶段 1E 调整为只包含成员之间的协作，数据与交互遵循 `chat-roadmap.md` 第 3.7、5.2、5.3 节：

- `messages` 增加 `visibility`（`customer_visible` | `internal_only`），与对客消息共用 `message_seq`。
- 内部备注不创建渠道投递，不写首次响应时间，不进入网站访客历史与线程摘要、AI 客服上下文、转交给 AI 时的客户末条消息判断；推进成员侧会话摘要、阅读水位和提及计数。
- 输入区提供「回复客户」和「内部备注」两个页签，各自保留草稿；内部备注模式使用常驻的区分样式。
- 内部备注的 @ 目标只包含企业成员。被 @ 的成员写入参与者关系，成为协作者。
- 客户收件箱增加 `@我的` 视图，与 `queue`、`mine`、`coworkers` 并列；客户会话接入提及计数和提及导航。

## 6. 交付顺序

| PR | 范围 | 依赖 | 验收 |
| --- | --- | --- | --- |
| 1 | AI 写回复：应用服务契约、单次模型调用生成入口、输入区弹层 | 无 | 两种模式与四种语气生成 1–3 条候选；单次模型调用在 25 秒内返回；不可对客发送时入口禁用；切换会话丢弃旧结果；不产生消息和运行记录 |
| 2 | Copilot：会话类型与线程表、`copilot` 输入与 Run Policy、线程查询与创建、右侧栏页签、填入回复 | 无 | 两名成员在同一线程先后提问均可见；运行中追加提问在安全点补入；停止回复生效；负责接待的 AI 客服与 Copilot 并行运行互不影响；Copilot 线程不出现在消息列表和搜索中 |
| 3 | 内部备注：可见性字段、写入、客户侧读取过滤、输入区双页签 | 无 | 网站访客历史、Telegram 投递、AI 客服上下文和首次响应时间均不包含内部备注；成员时间线混排展示 |
| 4 | @ 同事协作：协作者关系、`@我的` 视图、客户会话提及计数与导航 | 3 | 被 @ 成员进入协作者并出现在 `@我的`；确认后不再计数；周期关闭后移出该视图 |

PR3 合入后 Copilot 背景自动包含内部备注。

## 7. 暂不包含

- 移动端的 Copilot 和内部备注。
- 客户新消息到达时自动预生成候选回复。
- 管理员为渠道或团队配置默认 AI 员工。
- 将 Copilot 结论发为内部备注。
- Copilot 与 AI 写回复的费用统计。
