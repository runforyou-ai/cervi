# 消息页静音提醒与工作状态通知后续方案

## 文档目的

本文档记录聊天注意力状态的剩余交付边界。服务端已建立 `@所有人`、会话静音和提醒未读事实；Web、桌面端也已接通静音交互、独立未读标记和提醒计数。后续继续接通 `@所有人` 输入与展示和本地系统通知。

完成本文档对应 PR 后删除本文档，并把最终交付边界更新到 `chat-roadmap.md`。

## 前置契约

服务端已经提供以下契约：

- 群消息 `mentionAll`，且 `@所有人` 不展开为成员级 `message_mentions`。
- Direct 和 Group 会话列表项的 `muted`、独立 `markedUnread` 与客观 `unreadCount`。
- Group 的 `mentionedUnreadCount` 同时统计显式 `@当前用户` 和 `@所有人`，同一消息只计一次。
- Inbox 顶层 `attentionUnreadCount`：未静音内部会话贡献客观未读与独立标记（1）的较大值；静音 Group 只贡献提醒未读；静音 Direct 不贡献。
- 更新当前用户会话静音状态的应用服务命令。

## 产品语义

客观未读、提醒未读和系统通知保持独立：

| 场景 | 会话行未读 | 工作台提醒总数 | 系统通知 |
| --- | --- | --- | --- |
| 未静音 Direct 或 Group | 正常增加 | 正常增加 | 工作中且全局通知开启时允许 |
| 静音 Direct | 灰色未读增加 | 不增加 | 不发送 |
| 静音 Group 普通消息 | 灰色未读增加 | 不增加 | 不发送 |
| 静音 Group `@自己` 或 `@所有人` | 灰色未读增加 | 增加 | 工作中且全局通知开启时允许 |
| 用户处于离开或下班 | 保持上述未读 | 保持上述提醒总数 | 不发送 |

切回工作中后不补发离开或下班期间错过的系统通知。工作状态只是当前系统通知规则，不修改已读水位、静音状态或提醒未读。

## 交付范围

### `@所有人` 输入和展示

- Group 输入框的 `@` 候选列表首项显示「所有人」。
- 只有选择候选项才提交 `mentionAll=true`；手写相同文字不产生提醒事实。
- 草稿中最多存在一个结构化 `@所有人` token，删除 token 后同步恢复为 `false`。
- 发送失败后的重试保留 `mentionAll`，切换会话时清理对应草稿状态。
- 消息气泡按结构化字段展示 `@所有人`，不解析本地化正文推断。
- `@所有人` 不隐式触发群内 Agent。

### 已交付：静音交互与工作台提醒

- Direct 和 Group 列表右键菜单支持静音与取消静音，没有未读时也可操作。
- 静音图标显示在时间下方；灰色客观未读和群聊提及标记已接通。
- 独立未读标记已接通，不回退已读或提及水位；手动标记本身不投递系统通知。
- 内部范围圆点与桌面应用未读计数使用 `attentionUnreadCount`；会话行保留客观 `unreadCount`。
- 保存后失效统一 Inbox 查询，保持列表排序、选择和滚动位置；静音和标为未读不改变普通已读或提及查看水位，标为已读推进普通水位并清除独立标记。
- 后续通知工作继续沿用这些交互，不增加静音时长、完全静音或独立的“仅提及”设置。

### 本地系统通知

新消息只有同时满足以下条件才调用平台通知：

```text
消息符合当前会话提醒策略
AND messageNotificationsEnabled
AND workStatus == working
AND 应用当前不在前台聚焦状态
```

- 静音 Direct 的任何消息都不通知。
- 静音 Group 仅在显式 `@当前用户` 或 `@所有人` 时通知。
- `away` 和 `off_duty` 只抑制系统通知，不清除提醒总数。
- 用户切换工作状态后立即更新当前通知策略。
- Web、桌面端和以后移动端共享服务端提醒语义，不各自重新解释 ChatSubject 关系。

## 明确不做

- 服务端推送、移动推送和统一 Realtime Gateway。
- 移动端群聊入口与交互。
- 静音截止时间、定时恢复、全局 DND 和通知日程。
- 群聊“完全不提醒”、按角色提醒和自定义提醒组。
- `@Agent`、`@所有人` 触发 Agent 或 Agent 响应策略。
- 为大群逐成员创建未读、提醒或通知记录。
- 为本地通知增加补发队列；恢复工作中后不补发旧通知。

## 后续验收与既有交互回归

- 从候选列表选择 `@所有人` 可以发送并在时间线正确展示。
- 手写 `@所有人` 不形成结构化提醒。
- 静音 Group 的普通消息只增加灰色会话行未读。
- 静音 Group 的 `@自己` 和 `@所有人` 同时增加灰色会话行未读与工作台提醒总数。
- 静音 Direct 的消息只增加灰色会话行未读。
- 取消静音后，尚未阅读的普通消息重新进入提醒总数。
- 工作中按会话策略发送系统通知；离开和下班均不发送。
- 切回工作中不补发旧通知，既有提醒总数保持。
- 全局消息通知关闭时不发送系统通知，但两类未读不受影响。
- 静音、切换工作状态和刷新 Inbox 不改变当前会话、列表滚动与已读水位。
- Web 生产构建通过，并使用 Wails MCP 验证桌面端右键菜单、未读样式、工作状态切换和通知门闩。
