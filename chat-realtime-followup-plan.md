# 聊天实时同步后续事项

日期：2026-09-19。状态：待排期。

本文汇总聊天实时同步在已交付能力之外、按需求或实测证据才开启的事项。已交付的变更版本、兜底校验、实时传输、列表、个人置顶与锁序规则见 [聊天路线图](chat-roadmap.md) 第 10.8–10.14 节，会话名称搜索见「聊天记录检索」一节。

第 1–4 节只记录事项、当前行为、启动条件和启动时须遵守的约束，具体设计在启动时单独成文；第 5 节是已规划的会话输入状态 PR 计划。

## 1. 待开启事项

| 事项 | 当前行为 | 启动条件 |
|---|---|---|
| 网站挂件网络与窗口状态拆分为 TS 模块 | 请求、身份、线程目录、消息窗口与发送状态都在 `internal/publicweb/chat.js` 内，事件流通知触发既有拉取逻辑 | 挂件需要与成员端共用传输代码，或修改 `chat.js` 状态逻辑时出现可复现的回归 |
| 实时链路指标与启动就绪顺序 | 认证失败、慢连接结束与 NATS 发布失败只记录 `WARN` 日志；服务端没有对外 ready 阶段 | 出现需要监控实时链路的真实部署，或已观察到启动就绪顺序导致的连接失败 |
| 会话列表虚拟化与窗口裁剪 | 列表按已加载窗口全部渲染，控制器保存双向边界与锚点 | 真实列表规模出现可复现的渲染或滚动问题，并记录实际节点数与滚动耗时 |
| 实时链路容量与故障验证 | 只有集成测试覆盖撤销、慢连接、最长存活与服务端下线；没有容量数据 | 出现真实部署规模，或已观察到连接、队列、锁争用的具体问题 |

## 2. 按证据恢复的原设计

以下设计在范围评审中删除或推迟，当前由更简单的做法承担，出现对应证据时再恢复。

| 原设计 | 当前做法 | 启动条件 |
|---|---|---|
| 持久用户／客服／访客水位与压缩唤醒行 | 客户端内存投影刷新即重建，`GetSyncHeads` 实时聚合可见会话的版本哈希和 | `GetSyncHeads` 聚合在真实数据量下实测过重，或确定建设持久离线库 |
| 完整同步索引、重建基线、保留窗口与恢复 epoch | 重连等价于重读已加载窗口 | 客户端需要跨会话持久游标，即离线库落地时 |
| 会话变更事件日志与 `sync_seq` | 以 `conversations.version` 表达会话有变化，客户端重读业务 Query | 按会话版本重读被实测证明过重，需要细粒度失效 |
| Protobuf 协议与代码生成工具链 | 手写 JSON 事件，Go 与 TypeScript 以共用夹具锁定格式 | 事件载荷显著变大变热，或协议需要提供给第三方 |
| 一次性连接票据 | 原生端由 Go 侧 `apiproxy` 持有事件流，Web 端请求头携带既有 Bearer | 原生端不再由 Go 侧持有连接，或出现新的凭据边界要求 |
| 事务内 `realtime_outbox` 与租约发布器 | 事务内登记通知，提交后异步发布 Core NATS，丢失由 30 秒兜底校验恢复 | 实测提交后发布丢失导致的感知延迟不可接受，或兜底校验间隔需要显著拉长 |
| 跨执行节点运行快照 | Gateway 与 Agent 执行在同一进程内定位 Run | Gateway 与 Worker 拆分部署 |
| 多节点 Drain、随机重连分散与多节点验收 | 单实例部署，收到退出信号即拒绝新事件流并有界结束现有事件流 | 服务端拆分为多实例部署 |
| 服务端定期复核授权与群失权控制通知 | 事件流最长存活 1 小时；运行过程流逐次校验阅读资格；群失权由 `conversation_removed` 通知客户端关闭运行过程流 | 实测 `conversation_removed` 丢失导致失权后仍收到 AI 流、最长存活时间限定的撤销窗口不可接受，或出现需要即时收回的其他受限事件 |
| 客户端实体版本比较与串行追赶 | 客户端不保存实体版本，收到通知即失效对应 Query 并按短窗口合并重读 | 通知触发的重复重读被实测证明过重 |
| WebSocket 双向实时连接 | SSE 只承载下行事件，上行全部走普通 HTTP | 出现事件流与 HTTP 无法满足的高频双向需求，届时下行事件一并迁移 |
| 挂件跨标签页身份发现、本地通知跨标签页前台协调、NATS 恢复时主动校验探针、前端事件处理队列上限 | 各标签页独立连接与拉取，NATS 恢复场景由 30 秒兜底校验覆盖 | 出现实际重复通知、身份发现遗漏或发送堆积反馈 |

## 3. 启动时须遵守的约束

- **挂件拆分：** 经现有 messenger 构建入口迁入 `frontend/src/publicweb`，按网络身份、线程目录、消息窗口与待发送状态拆分，DOM 事件转成显式操作；保留 Go 模板、主题与现有 DOM 外观，产物继续由 Go 嵌入链路加载，bundle 不包含成员登录模块或内部 Agent 详情。先验证与现有事件流及拉取逻辑等价，再删除迁出的原实现。
- **指标与就绪顺序：** 启动顺序为依赖就绪 → 路由与订阅可服务 → 对外 ready，未就绪时不接受事件流请求；指标按结构化原因码聚合认证失败、慢连接关闭、发布失败与探针不一致，不记录凭据或正文。多节点 Drain 与随机重连分散在拆分部署时另行处理。
- **列表虚拟化：** 先测量节点数与滚动耗时并记录必要性；只替换可见行渲染，数据分页、选择与同步契约沿用；行 key 固定为会话 ID，裁剪保留可恢复的前后边界与语义锚点，只保留一个滚动补偿所有者；不以固定截断条数代替分页或容量治理。
- **容量与故障验证：** 以可重复的 Task 场景覆盖同用户多会话、小群扇出、共享客服受众、资料批量失效、突发重连和持续热点，记录数据量、在线连接数、受众大小、持续时间与机器配置；故障注入点只用于测试，不成为生产接口；合成测试数字不写成产品承诺。锁优化、普通性能索引与大群 Shared Fanout 按结果另行开启。

## 4. 待补的验证记录

| 事项 | 当前状态 | 补记时机 |
|---|---|---|
| iOS 与 Android 真机上的 SSE 长响应、事件流重连与本地通知 | 只在桌面开发的 mobile-preview 验证过原生端 SSE 长响应 | 各自真机接入时 |
| Cloudflare Tunnel 与 HTTPS／HTTP/2 部署下的 SSE 下发 | WebSocket 探针阶段验证过隧道，改为 SSE 后未记录 | 首次在隧道或正式 HTTPS 部署下验证时 |
| 移动端置顶排序：从手柄开始的触摸不切换范围、排序中打开的菜单关闭后才结束排序 | 审核修正后没有界面复验记录 | 下次涉及移动端置顶的界面验证时 |
| 会话搜索「查看全部」：移动端返回恢复已加载窗口与滚动位置、移动端首次读取失败重试、修改或清空检索词回到分组结果 | 审核修正后没有界面复验记录 | 下次涉及消息页检索的界面验证时 |

## 5. 会话输入状态 PR 计划

聊天过程中实时看到对方正在输入，覆盖成员单聊与群聊、客服看访客输入、访客看客服输入。按收发闭环拆成两个 PR，每个 PR 内同时交付上报与展示，不出现只能上报或只能展示的中间版本。

### 共用契约

- **上报：** 输入状态随真实功能走普通 HTTP 并经过 `appservice.Service`，每次请求独立认证，服务端不做节流；不写数据库、不推进会话版本、不进入消息表、Task Outbox 或 JetStream。服务端完成授权后直接发布 Core NATS，不开启写事务，发布失败只记录日志。
- **状态：** 请求只携带会话编号与 `active`。客户端在输入内容变化时上报 `active=true`，持续输入期间每 3 秒至多再报一次；发送、清空输入、切换会话、页面隐藏或 5 秒无输入时上报 `active=false`。接收端 6 秒内未收到刷新即自动清除，`active=false` 与该发送者的新消息到达同样立即清除；服务端不保存输入状态，丢失的上报由到期清除兜底。
- **事件：** 成员事件流新增 `conversation_typing`（conversationId、发送者 ChatSubject 编号、active，成员与访客发送者统一表示），访客事件流新增公开事件 `visitor_typing`（conversationId、active，不携带内部身份编号）。两种事件不参与会话版本、探针与失效，只驱动界面临时状态；发送队列内同一会话同一发送者只保留最新一条，溢出规则沿用各自事件流。Go 定义、TypeScript 定义与共用夹具同一提交更新。
- **展示：** 接收端按发送者 ChatSubject 从已加载的会话参与者或摘要解析名称，不在事件中携带姓名；本人其他设备的输入状态不展示。成员端提示的具体位置与文案在启动时确认。

### PR A：成员单聊与群聊正在输入

- **依赖：** 无。
- **范围：** 单聊与群聊的发送方上报输入状态，会话内其他有效真人成员看到「某某正在输入」；群聊多人同时输入时合并展示。AI 员工不上报，AI 生成期间沿用现有运行状态展示。
- **落点：** `internal/appservice/backend.go` 新增 `ReportConversationTyping`（`POST /conversations/:conversationID/typing`）及生成的适配层与绑定；`internal/actions/conversation` 的授权 Query；`internal/realtime` 的通知种类与 Gateway 白名单；`internal/realtime/protocol` 与 `frontend/src/api/realtime`；Web／桌面与移动端会话详情与输入框。
- **实施：** 授权要求当前身份是会话的有效发送成员（群聊未解散、未退出，单聊对方账号状态不影响上报）；受众为除发送者以外当前有效真人成员的用户受众，大群沿用小群逐用户扇出。客户端输入框按共用契约上报与清除，会话详情订阅事件并按到期规则维护临时状态，同步协调器不处理该事件。
- **验收：** 对方输入时出现提示，停止输入、发送或切走后提示在到期窗口内消失；退出群聊或被移除后不再收到该群的输入状态；本人其他设备不显示本人输入；断网或事件丢失时提示最迟 6 秒后消失；上报失败不影响输入与发送。
- **验证步骤：** 用两个账号在 Web、桌面与移动预览中分别测试单聊与群聊的输入、停顿、发送和切换会话；群内三人同时输入时提示合并；移除一名成员后其端不再显示该群输入状态。集成测试覆盖授权拒绝（非成员、已退出、跨企业）与受众不含发送者。

### PR B：客户会话中客服与访客互相看到正在输入

- **依赖：** PR A 的共用事件与客户端到期逻辑。
- **范围：** 网站访客在挂件中输入时，客服在客户会话中看到访客正在输入；客服在对客回复框输入时，访客在挂件中看到正在输入。客服撰写内部备注不向访客上报；Telegram 等外部渠道不提供访客输入信号，也不向外部平台发送输入动作。
- **落点：** 成员端复用 PR A 的上报接口与 `conversation_typing`；访客端新增公开路由 `POST /public/website-channels/:channelID/conversations/:conversationID/typing`，与发送、目录、历史共用 `authorizeWebsiteVisitor`；`internal/publicweb/chat.js` 的输入上报与 `cv-typing` 提示；客户会话回复框与内部备注切换。
- **实施：** 访客上报按渠道身份可读线程授权，发布到企业客服共享受众 `customer_inbox`，成员端只在打开该客户会话时展示。客服上报要求当前身份可回复该会话（周期开放且符合现有回复规则），发布到该会话渠道身份的访客目录受众；只在对客回复框上报，切到内部备注即上报 `active=false`。访客端提示复用挂件现有的 `cv-typing` 节点，不展示客服姓名；AI 生成期间的提示与真人输入共用同一节点。
- **验收：** 访客输入时客服端出现提示、客服对客输入时访客端出现提示，停止后按到期规则消失；客服撰写内部备注时访客端不出现提示；其他渠道身份与其他企业收不到；渠道停用后访客事件流结束，不再收到输入状态。
- **验证步骤：** 在挂件与 Web／桌面客服端互相输入、停顿与发送；客服在回复框与内部备注之间切换，确认只有回复框触发访客端提示；两个访客浏览器同时打开同一渠道，只有对应线程的访客看到提示。集成测试覆盖访客与客服两个方向的授权拒绝和受众范围。

