# 聊天重构与实时功能已完成记录

本文保存 `chat-realtime-pr-plan.md` 中已合并 PR 的原始范围、实施记录、验证记录和审核记录，供追溯实现依据与验证结论。仍在推进的范围和共用契约见 `chat-realtime-pr-plan.md`。

已合并：PR01（`#216`）、PR02（`#218`）、PR03（`#222`）、PR04（`#224`）、PR05（`#232`）、PR06（`#233`）、PR07（`#234`）、PR08（`#238`）、PR09（`#245`）、PR10（`#248`）、PR11（`#249`）、PR12（`#253`）、PR13（`#256`）、PR14（`#267`）、PR14A（`#272`）、PR15（`#276`）、PR16（`#279`）、PR17（`#288`）、PR18（`#294`）、PR19（`#306`）、PR20（`#307`）、PR21（`#338`）、PR25（`#308`）、PR25A（`#315`）、PR26（`#341`）、PR27（`#322`）、PR28（`#327`）、PR29（`#329`）、PR30（`#335`）、PR32（`#346`）、PR33（`#339`）、PR34（`#348`）、PR36（`#354`）、PR37（`#305`）、PR38（`#340`）。

原清单中的 PR00（禁用 AI 员工后保留会话）改编为新清单的 PR16，由 `#279` 交付。

## 已完成 PR 原文

### PR01：对齐路线图和实施边界

- **依赖：** 无。
- **范围：** 更新聊天／Agent 路线图：本地消息顺序、内存投影断线追赶、首次访客连接时机、最小协议、列表刷新／选择规则和通知边界；纠正当前单聊唯一关系、Telegram 入站及群聊 Agent 状态的过时描述。列出真人、渠道、Agent、资料、权限和个人置顶写入口及锁序。
- **落点：** `chat-roadmap.md`、`agent-roadmap.md`，逐一追踪 conversation／agentrun／channel 的写 Action。
- **实施：** 产出“入口 → 守卫 → 锁定对象与顺序 → 事实写入 → 受众”表；分别列真人发送、Telegram／网站入站、AI 回调和系统资料任务，不能把没有当前用户的入口强行套入 LockActiveUser。把本清单的消息本地顺序、列表行为表及访客时机同步回路线图。
- **验收：** 路线图与本清单无冲突；置顶、消息顺序、冷启动等产品选择有明确结论；不包含业务代码或新表。
- **验证步骤：** 抽查群移除、客服接管、AI 写回三条链路能沿表找到实际函数；已确认置顶规则直接落文，其他产品分歧在实施前解决。

### PR02：提取群聊访问与锁定能力

- **依赖：** PR01。
- **范围：** 在 `actions/chatstate` 提取真实共用的会话授权／锁定能力，群文本、群管理、已读、提及与静音共用锁后资格检查；不引入通用 Repository、反射事件或模型 Hook。
- **落点：** `internal/actions/conversation/group_conversation.go`、`group_management.go`、`mark_conversation_read.go`；新增 `internal/actions/chatstate`。
- **实施：** 区分可读、可发、可管理三种资格；写路径在会话／成员关系锁定后判断，Query 复用语义但不额外加写锁。先迁移群路径，不在本条改消息顺序或通知投递；删除被替代的群访问辅助函数。
- **验收：** 群移除与个人设置并发时不会写入过期关系；重入群、群主转让、解散只读和阅读基线保持现有语义。
- **验证步骤：** A 先读取群资料，B 移除 A 并提交，A 再静音／发送应失败；把 B 换成解散操作时，历史查询仍成功、发送失败。

### PR03：统一真人单聊与独立 AI 聊天的锁序

- **依赖：** PR02。
- **范围：** `direct` 真人单聊与 `agent` 独立 AI 聊天的首发、后续发送，以及 AI 聊天 Agent 的开始、认领、成功、失败和最终失败收尾。已有会话统一 Conversation → Participant → 个人状态（发送者才需要）→ AgentState → Run → 任务执行记录；新建共享主体按身份 ID 升序处理。
- **落点：** `conversation/direct_conversation.go`、`agent_conversation.go`、`internal_text_message.go`、`chat_subject.go`；`chatstate/access.go`；`agentrun/schedule.go`、`agent_chat_execute.go`、`execute.go`、`input_feed.go`。群创建、增员同步调整共享主体创建顺序，保留原展示顺序。
- **真人首发：** 当前用户守卫 → 按规范身份对读取；已有会话锁定后复核资格，显式首发可恢复归档；不存在时按身份 ID 确保双方主体，创建 Conversation、唯一关系和 Participant，再进入统一发送路径。身份对唯一冲突整笔回滚，在新事务中重读并锁定胜出的会话；普通 Query 不加写锁。
- **AI 首发：** 按草稿稳定 conversationId 定位；已存在时锁后核对企业、用户、Agent、类型和活跃状态；不存在时按身份 ID 确保主体，原子创建会话、扩展、参与者及首条消息。相同草稿与消息编号幂等确认，不同草稿保持独立，不按身份对复用或取消旧 Run。
- **发送与执行：** 等待会话锁后复核当前发送资格，再检查消息幂等；新消息、摘要和个人已读在 Agent 输入调度前保存，幂等命中不重复副作用。Agent 先读 Run 定位信息，再通过运行策略锁会话和参与关系，随后锁 State、Run、Task 并校验执行尝试和租约。成功 `text`、失败 `agent_error` 及终态、消费水位同事务提交，每个 Run 最多一条结果消息。
- **已确认行为：** 停用目标拒绝之后资格校验的新发送，已通过事务内资格校验的发送允许完成，不新增目标账号锁。停用 Agent 或归档 AI 会话不取消已提交的排队、运行中或尚待消费输入；这些输入继续处理并写入结果。真人归档只在显式首发时恢复；不新增 AI 归档／恢复操作。幂等重放仍须通过当前发送授权。
- **边界：** 客服通过原策略保持业务边界，完整会话锁序归 PR04；共用 begin 也按策略上下文 → State → Run → Task 进入。消息统一追加与序号仍归 PR05–06，不新增迁移、前端或 appservice 契约。
- **验收：** 真人双向首发只建一份长期会话，AI 同草稿重试只确认一份结果、不同草稿各自独立；发送与 AI 成功／失败无反向锁序；不同 Conversation 的输入、Run 和结果不混合；旧租约不能写回，重复终态回调不重复消息。
- **验证步骤：** 用可控查询屏障和数据库阻塞关系组织首发及共享主体竞争；交错真人发送与 Agent 开始、认领、成功、失败、最终失败；覆盖等待锁期间失权／归档／目标停用、停用前已接受发送、AI 停用／归档前已提交输入、多个 AI 会话并行。使用实际任务执行上下文验证旧尝试、旧 Worker、过期租约及最终失败回调。2026-09-08 已通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（27 项）和 `wails3 task build:server`（含 TypeScript 检查及前端生产构建）。Pi CLI 只读审核未发现实质缺陷，已修正文档中的旧函数名、过时验证说明及一处主体帮助函数注释；可选的查询精简与错误码拆分未改变本轮行为。Web 已于同日通过 `http://127.0.0.1:8081` 完成回归：真人双账号收发（主账号浏览器操作、第二账号 HTTP API）、相同 clientMessageId 幂等确认、AI 首发及后续回复、同一 AI 的两个独立 Conversation、失败 agent_error 刷新后保留和会话间结果隔离。AI 链路使用仅监听本机的临时 OpenAI 兼容模型桩覆盖成功／失败，经实际 NATS Worker 执行，不代表外部模型验证；临时供应商、Agent、会话、消息和任务记录均已清理。本轮入口检查纠正：企业按 access_host 绑定，localhost:8081 显示初始化页不能证明 127.0.0.1:8081 对应企业未初始化。

### PR04：统一客户会话与客服 Agent 的锁序

- **依赖：** PR03。
- **范围：** 网站／Telegram 入站、成员回复、领取／转交／关闭／重开和 AI 取消／写回，统一 Conversation → CustomerConversation → ServiceSession → AgentState → Run。补齐渠道凭据、身份、文件和任务回调的锁序交叉检查。
- **落点：** `receive_customer_text_message.go`、`receive_website_customer_text_message.go`、`manage_service_session.go`（均在 conversation）；以及 `agentrun/customer_execute.go`。
- **实施：** 首次入站尚无 Conversation 时，先稳定定位渠道身份／客户关系，再创建或锁现有会话；已有会话统一锁序。最终写回必须在锁后重新检查 serviceSessionId、负责人、Run 尝试和租约，前置 HTTP 检查不作为写回凭证。
- **验收：** 首次入站、续开周期、人工接管与迟到 AI 回复并发收敛；不因阅读创建参与者或改变负责人。
- **验证步骤：** 阻塞 AI 写回，让人工领取／转交／关闭先提交，再释放 AI；旧结果不得成为当前客服回复。反向顺序则只允许已提交回复一次，且所有摘要对应同一周期。

- **PR04 实现记录：** 从已合并 PR03 的 `6dc3bdc` 开始，提交前快进同步 `8f96135`；`chatstate.LockCustomerServiceSession` 统一已有会话、客户扩展和当前周期锁，入站与成员回复按需要使用拆分的会话／周期入口。转交目标身份先于会话锁；Agent 开始、认领、成功、失败及最终失败经相同策略入口再锁 State、Run、Task。网站／Telegram 入站在渠道身份稳定定位后锁会话，联系人 Participant 在周期确定后创建，消息落库后调度 Agent；Agent 回复 Participant 在 Task 门禁之后创建，抑制结果不建参与关系。同一 Conversation 锁覆盖这两类参与者写入，不存在持有参与者锁再首次获取会话锁的入口。共享企业身份主体统一使用冲突后重读能力；Telegram 头像新旧文件按 ID 锁定。
- **交叉检查：** Telegram 真人回复包含 `customerdelivery.Prepare → Enqueue`，前置渠道共享锁、渠道身份锁，之后锁会话与周期并原子保存消息和投递；配置使用渠道／Bot 外层锁后按 Channel → Setting 更新。Webhook 先锁 Setting 再锁渠道身份与会话，不反向更新 Channel；投递认领与人工处理按渠道身份 → Delivery，网络调用在事务外。联系人恢复使用渠道身份 → Contact，不反向锁会话；目标停用及路由重置不进入会话业务集合。本条不改变 Telegram 的投递重试规则或网站 Agent 的渠道能力。
- **验证记录：** 已添加数据库屏障覆盖网站首次入站幂等竞争、客服管理与回复的会话前置锁、显式重开与客户续开竞争、Agent 各阶段与入站交错、人工接管／转交／关闭续开与迟到结果的双向提交顺序，以及客服旧尝试／Worker／过期租约。2026-09-08 已通过 `wails3 task test:server`（含客服各阶段、真实任务上下文和唯一索引未提交等待）、`wails3 task test:desktop`、`wails3 task test:frontend`（27 项）以及 `wails3 task build:server`（含绑定生成、TypeScript 检查和前端生产构建）；审核修正及同步 main 后再次通过服务端全量测试、前端测试和服务端生产构建。Web／公开 Messenger 在 `http://127.0.0.1:8081` 验证访客首发、成员首次回复自动领取、真人转交／接管、关闭、显式重开和客户消息续开第二周期；临时 AI 客服经真实 NATS Worker 和仅监听本机的 OpenAI 兼容模型桩完成回复，两端均能读取持久结果，不代表外部模型或 Telegram 真平台验证。同步 main 和审核修正后，已再次完成访客首发、客服回复、访客刷新恢复与同周期关闭／重开的冒烟验证。临时渠道、联系人、用户、Agent、供应商、消息、周期与运行任务已清理，本轮进程均已关闭，8081、9246、18404 端口已释放。Pi CLI 完整只读审核未发现生产锁序缺陷；已按反馈补强唯一冲突等待屏障、收窄会话类型条件、修正注释并合并重复锁定入口，修正复核已确认无阻塞问题。

- **Telegram 真平台补测（2026-09-08）：** 使用用户配置的公网渠道与本机 Telegram 客户端验证文本入站、中文／emoji 双向传输、3 条真人回复实际送达、领取、关闭后连续入站只新增第 2 个周期，以及显式重开保留第 2 个周期。3 条测试回复的 Delivery 均为 sent、attempt=1，平台消息 ID 按投递顺序递增；官方 getWebhookInfo 返回 pending_update_count=0，未返回错误。对真实入站消息的业务字段重建回调，在进行中与关闭后分别并发重放 4 次，均返回 204，消息／参与者／周期数量及最后消息不变，关闭状态不被复活；错误 Secret 返回 401。公网脚本请求被 Cloudflare 1010 拦截，重放和错误 Secret 检查通过同一服务的本地入口完成，不计为公网脚本验证。当前无可转交的客服同事，真平台未补测转交，沿用前述自动化与网站验证。测试消息以 PR04 TG 标记保留，渠道配置、原有记录和用户启动的服务保持可用；本轮未启动服务进程。
- **补测发现：** 关闭后时间线中的周期标题可能仍显示“进行中”，页头及数据库已为关闭，重新加载页面后标题正确。`use-conversation-timeline.ts` 的增量合并仅在有新消息或 Agent 状态变化时更新，周期状态变化未使现有窗口收敛；记录为前端查询收敛回归项，本 PR 未修改前端实现。

### PR05：集中消息追加与摘要维护

- **依赖：** PR04。
- **范围：** 各类文本、群系统消息、附件消息（含批量与说明）、AI 成功／失败及主动停止结果消息调用 chatstate 的事务内追加能力；集中维护会话／周期摘要及附件取消后的摘要重算。业务 Action 继续负责路由、领取、引用、提及、附件状态与文件激活、客服首响和 Agent Trigger。共享模块不反向依赖 Action 或实时传输。
- **落点：** conversation 各发送／入站文件、群系统消息、`attachment_message.go`、`attachment_batch.go`、`attachment_upload.go` 和 agentrun 最终回复／停止路径；新增 chatstate 的事务内追加与摘要重算函数。
- **实施：** 追加函数接收调用方事务、已锁会话、发送主体及规范化消息；返回已保存 Message 和本次是否新建。幂等命中先返回既有事实，不再次改摘要、触发 Agent 或入 Outbox；领域 Action 继续决定客服路由和群系统文案。此项是保行为提取，PR06 才切换序号契约。
- **验收：** 所有真实写入口覆盖；消息、摘要、Trigger 与 task_outbox 一起提交或回滚；幂等重放不产生额外副作用。
- **验证步骤：** 分别从真人、群系统、访客、Telegram、附件及 AI 成功／失败／停止入口写入消息，断言摘要指向正确消息；在写摘要／Trigger／task_outbox 后主动回滚，消息及任务记录均不存在，群分配器恢复原位置。覆盖批量附件重放、说明、取消末条／唯一附件后的摘要，以及 Telegram 旧来源时间不回退摘要。
- **范围确认（2026-09-08）：** 用户确认纳入 PR04 之后合并的附件和停止回复能力，保持现有业务行为；统一消息序号与排序切换仍归 PR06。
- **实现记录：** 基于 `5f63ef0`，`chatstate.AppendMessage` 接收调用方事务、已锁会话和规范化消息（含发送 Participant），返回已保存消息与新建标记；群序号在确认未重放后分配，消息与会话／周期摘要一起保存。Action 保留副作用前的完整幂等校验，Agent 以同事务 Run 终态门禁排除重复回调；客服首响由业务策略决定。`RecomputeConversationSummary` 负责附件取消后的末条摘要重算，删除被替代的消息 INSERT、摘要更新和群序号辅助函数。本条不新增迁移、业务接口或实时 Outbox。
- **验证记录（2026-09-08）：** 已通过 `wails3 task test:server`（含新增的共享追加重放／群序号回滚、Telegram 旧来源时间，群改名系统消息及取消末条附件摘要，以及加强后的 Agent／网站访客／Telegram 消息、摘要、Trigger 与真实 task_outbox 回滚断言）、`wails3 task test:desktop`、`wails3 task test:frontend`（39 项）和 `wails3 task build:server`（含绑定生成、TypeScript 检查与前端生产构建）。原有附件生命周期、引用、取消、AI 成功／失败／停止及客服锁序回归均通过。本条未改变界面结构或交互流程，未启动界面或外部平台手动验证；测试仅重建本 worktree 的测试库，未重建开发库或启动持久服务。审核修正后再次通过服务端全量测试与服务端生产构建。Pi CLI 完整只读审核未发现阻塞缺陷；已按建议删除附件重复重放分支、统一锁定会话赋值位置、调整注释并补齐上述回滚与摘要断言，针对性复核确认未引入实质问题。

### PR06：统一消息序号、分页与阅读水位

- **依赖：** PR05。
- **范围：** 真人单聊、AI 聊天、群聊和客户会话统一 `message_seq`／`last_message_seq`，会话锁内分配并设唯一约束；同步修改各端分页、引用定位、已读和提及比较，删除群专属排序分支。来源时间／顺序保留，晚到外部消息按本地接收位置展示；HTTP 序号用字符串、TS 用 bigint。
- **落点：** `models/message.go`、`models/conversation.go`、`chatstate/message.go`、消息／引用 Query 与 `types_conversation.go`。
- **实施：** 新增统一分配器和会话内序号唯一约束，把 before／after／上下文／阅读与提及的排序全部切换；展示时间仍来自来源字段。事务回滚允许未提交编号重用，已提交分配器不随摘要重算回退。迁移、全部读取方、绑定及旧排序删除同 PR 完成，避免双顺序并存。
- **验收：** 屏障测试覆盖先开始后提交、回滚、Telegram 旧时间消息及大整数精度；重入群基线、乱序提及确认正确。摘要可重算，分配器不回退；用空库／可丢弃测试库验收结构切换，已有开发库停服后按仓库流程重建，不提供保留原数据的回填或运行时回退。
- **验证步骤：** A 已取会话锁但未提交时 B 发送，断言 B 等待并排在 A 后；A 回滚后无可见空消息。造超过 JS 安全整数的序号，经 HTTP、Wails、前端比较后分页和已读仍精确。

- **范围确认（2026-09-08）：** 用户确认开始 PR06，并允许按需停服重建本 worktree 的开发库，不保留旧数据。附件、停止回复沿用 PR05 已纳入的业务行为，全部消息读取方同时切换；列表活动时间仍归 PR08。
- **实现记录：** 基于 `3821a12`，同步 `b58480e`。共用追加入口在会话锁内为所有消息分配 `message_seq`，以 `last_message_seq` 保存已提交分配位置；新增迁移将消息序号设为必填并建立会话内唯一约束，删除群专属排序及摘要中的来源顺序副本。个人状态持久化 `read_seq`，已读提交仍使用消息 ID，服务端按序号单调推进；重入群以本次加入系统消息初始化普通已读与提及基线。成员、访客、引用上下文、未读计数、提及和 Agent 历史边界均按统一序号读取；成员与访客共用绑定会话的序号游标，HTTP／Wails 返回字符串，前端比较使用 bigint。待发气泡保留本地队列顺序，不伪造持久序号。
- **验证记录（2026-09-08）：** 已通过服务端全量测试、桌面端测试、前端测试（同步 main 前 39 项，知识库收缩合入后 34 项）、服务端生产构建及 macOS arm64 原生构建，完成 appservice 与 Wails 绑定生成。新增真实数据库测试覆盖四种会话的提交屏障与回滚重用、大整数双向分页／引用上下文／访客读取／HTTP 传输、删除已读目标后的水位、摘要重算不回退分配器及唯一约束；已有附件、停止回复、重入群和乱序提及回归通过。`task1` 已停服重建，新增迁移 Down／Up 已实测，随后按同步后的完整迁移再次重建。
- **界面记录：** Web、macOS 桌面端及 `mobile-preview` 用 65 条超过 JS 安全整数的群消息验证历史窗口、跨页引用与返回最新，Web 验证提及导航。实际 Wails 调用返回末条及阅读水位 `9007199254741056`，50 条窗口首尾差精确为 49。公开 Messenger 验证首发、客服回复、刷新恢复、历史补页和跨页引用，来源时间改早的末条仍保持本地末尾位置。同步 main 后再次完成企业初始化、单聊展示、65 条大整数群消息跨页引用及返回最新的 Web 冒烟验证。未执行 Telegram 真平台、外部模型或 iOS／Android 真机验证。界面验证结束后已关闭本轮 Server、Vite、桌面端、移动预览及 MCP，8081、9246、9100 端口已释放。
- **审核记录：** Pi CLI 完整只读审核未发现阻塞问题；已按反馈修正序号及分页注释、移除测试中的来源三元排序，并明确列表仍使用来源时间，独立活动时间由 PR08 交付。同步 main 后的针对性复核确认无生产阻塞，测试已适配当前 Backend 构造签名。

### PR07：保存发送方客户端消息关联

- **依赖：** PR06。
- **范围：** 明确保存 clientMessageId 与发送者关系，成员／访客查询仅向本人返回关联；保留正文、引用、提及的幂等冲突检查，不解析或公开 idempotency_key 充当关联契约。
- **落点：** `models/message.go`、发送 Input／消息 DTO、成员及网站消息 Query。
- **实施：** 为 clientMessageId 定义明确的发送方作用域，与业务幂等键分开保存；首次发送、重放响应、历史查询均返回同一关联。返回关联的条件是请求身份等于发送身份，内部 Agent 或来源渠道键不能借此暴露。
- **验收：** 超时重试只保存一条消息；其他用户和访客读不到他人的发送关联或内部幂等键；各端绑定同步生成。
- **验证步骤：** 同 ID 同内容重放得到同一 messageId；同 ID 改正文／引用／提及返回既有冲突；另一个成员读取同一可见消息时关联字段为空；另一个访客读取该会话被拒绝，访客读取客服回复时关联字段为空。

- **范围确认（2026-09-08）：** 成员发送编号按企业内成员身份隔离，跨设备、跨会话属于同一作用域；网站发送编号按渠道访客身份隔离，跨线程属于同一作用域。不同发送者可使用相同编号，同一发送者不能复用编号改变会话或发送意图。覆盖真人单聊、独立 AI 聊天的成员输入、群聊、客服人工回复、网站访客文本及现有附件／批量附件入口；发送状态收敛仍归 PR15／PR37。
- **编辑与限时撤回边界：** clientMessageId 只标识首次发送，与 messageId、message_seq 一起保持稳定；编辑不创建新消息，也不重新计算撤回时限，撤回后旧发送重试不能恢复消息。后续开放编辑／限时撤回时，发送幂等校验须改用不可变的原始发送意图（可保存规范化指纹），不能继续与可编辑正文比较；编辑、撤回使用独立操作幂等标识，变化通过 sync_seq 同步。本条不新增编辑、撤回、意图指纹或历史版本功能；撤回时限、提示和 AI／外部渠道处理规则在对应 PR 确认。
- **实现记录：** 基于 `da7853a`，新增可空 `messages.client_message_id`，成员发送者复用 Participant → ChatSubject → 企业身份关系，访客同时核对消息所属 ServiceSession 的渠道身份；业务幂等键独立保存，不解析内部键生成公开关联。网站幂等键改为 `chmsg:<contact_channel_identity_id>:<client_message_id>`，发送编号统一规范化为小写 UUID。首次发送、重放及成员／访客历史返回同一关联，非本人返回 null；Agent、系统与外部渠道消息不填充关联。迁移不回填历史数据，不增加兼容分支。

- **验证记录（2026-09-08）：** 已运行 `go generate ./internal/appservice` 和 `wails3 generate bindings -clean=true -ts -i`；通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`、`wails3 task build:server`（含前端生产构建）及 `wails3 task darwin:build ARCH=arm64`。真实数据库覆盖成员并发重放、正文／引用／提及冲突、成员及访客编号作用域、跨线程冲突、首发和历史关联；附件及 AI 聊天既有集成测试补充关联断言，HTTP 验证同一成员两个登录会话可读本人关联、其他成员及访客关联不泄露。服务端全量回归在补充 AI 输出为空的断言后再次通过。仅重建本 worktree 测试库，未修改开发库、未启动业务服务或界面运行时；本条未改变布局或交互，未做手动界面、Telegram 真平台或移动真机验证。

- **审核记录：** Pi CLI 完成完整只读审核，未发现生产阻塞项；采纳建议精简 DTO 注释，并明确重复登录用例验证令牌无关的本人关联，移除未独立断言的测试注释。网站历史保留周期关联查询，符合现有客户消息关系；最终绑定差异仅为自动生成的 clientMessageId 字段，无 dock／color 绑定删除。

### PR08：统一列表活动时间和读取快照

- **依赖：** PR06。
- **范围：** 使用独立、同会话单调的 lastActivityAt 排序，数据库在会话锁内生成活动时间；收件箱行与总数在一致只读快照中读取。新消息／系统消息更新活动，token、已读、静音和客服状态变化不改排序；DTO 区分活动排序时间与预览来源时间，前端不再用 Date 精度重算业务顺序。
- **落点：** `models/conversation.go`、chatstate 追加函数、`inbox/load_inbox.go`、`types_inbox.go` 和前端列表比较函数。
- **实施：** 新增 lastActivityAt，消息追加时取数据库当前时间与该会话已有值的较大值；相同时间由 ID 打破并列。客服状态、用户偏好只改投影。Query 在一个只读一致快照内读取行及权威总数，前端按服务端顺序展示。
- **验收：** 多实例时钟差异、相同时间、空会话及混合类型排序稳定；客户“全部”范围沿用原规则；客服未读不自动并入桌面总提醒。
- **验证步骤：** 一条带昨天来源时间的 Telegram 消息今天入站：预览仍显示昨天，列表活动排到今天；在行查询与总数查询之间提交另一消息，当前响应不得混用两个快照。

- **范围确认（2026-09-08）：** 用户确认开始 PR08。统一活动写入、读取快照及前端顺序，保持新建会话可达；独立详情查询、选择状态和临时集合的完整清理仍归 PR09。
- **实现记录：** 基于 `7ced7eb`，新增可空 `conversations.last_activity_at`，不回填历史数据。共用追加入口在会话锁内取 `GREATEST(last_activity_at, clock_timestamp())`，幂等重放、摘要重算及纯状态变化不推进活动。统一收件箱各类型查询及混排使用数据库精度的活动时间与 ID，行数据和完整未读／提醒总数在同一只读 Repeatable Read 事务中读取；客户范围与提醒口径保持原规则。列表 DTO 和单聊首发／查找摘要携带 `lastActivityAt`，预览仍用 `lastMessageAt`。Web／桌面删除 Date 二次排序，临时会话保持可达，列表响应接管后按服务端顺序展示；移动端沿用服务端顺序。
- **验证记录（2026-09-08）：** 完成 appservice 与 Wails 绑定生成；通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（34 项）、`wails3 task build:server`（含前端生产构建）及 `wails3 task darwin:build ARCH=arm64`。真实数据库新增用例覆盖四种会话的数据库活动时钟、未来／过去来源时间、活动单调、重放、回滚与摘要重算；混合列表微秒精度、同时间 ID 顺序及空会话；appservice 读取列表后并发提交消息，当前响应与下次响应分别保持行和总数一致。Telegram 用真实接收 Action 验证昨日来源消息今日入站、重复投递、公共客户范围及关闭／重开不移动活动，个人已读、静音和纯资料／系统消息区别均有回归。补充用例初次因夹具未注入 Telegram 调度器失败，修正夹具后服务端全量再次通过。
- **界面记录：** Web 创建群聊、首发后刷新及空群创建验证通过，列表刷新后仍选中目标；将本轮测试消息来源时间设为昨天，Web、macOS 桌面端及 `mobile-preview` 均保持活动会话在前且预览显示昨天。实际 Wails 调用返回活动时间 `2026-09-08T14:35:23.593944Z`，完整保留微秒。开发库仅应用新增迁移并添加本轮验证数据，未重建或回填旧数据；未验证 Telegram 真平台、外部模型或移动真机。验证结束后关闭本轮服务端、桌面端、移动预览、Vite 与 MCP，确认 8081、9246、9100 端口释放。
- **审核记录：** Pi CLI 配置恢复后完成完整只读审核，未发现阻塞项。采纳建议统一 AI 会话查询错误上下文，并让静音／已读测试直接使用该轮设定的活动时间基准；处理后服务端全量测试再次通过。活动时间与通用更新时间保持各自语义，不增加性能索引；不回填历史数据的影响在 PR 描述中明确说明，沿用已确认的 MVP 边界。

### PR09：详情选择与列表结果解耦

- **依赖：** PR08。
- **范围：** 增加独立会话摘要／详情读取及按 ID 批量查询，逐项返回失权结果；Web／桌面移除“选中项消失就打开第一条”。客服本人操作默认保留详情，明确操作才切目的队列；筛选移除仍可读的详情保持，真实失权清除正文、引用、草稿和缓存，主动新建仍打开目标。
- **落点：** `direct_backend_inbox.go`、`types_inbox.go`、`inbox-page.tsx` 的 selectedConversation／startedConversations／leftGroupConversationIDs。
- **实施：** 新增按 ID 独立详情和批量摘要 Query，输入不依赖当前列表页；批量逐项区分可读、不匹配当前筛选、不可用且不泄露不可见实体。选择只由导航／明确用户操作改变，移除用本地集合长期覆盖服务端结果的做法。
- **验收：** 第 50 条外深链、转交、关闭、筛选变空不切换到另一会话或误标已读；解散但可读的群保留只读详情。
- **验证步骤：** 直接打开第 80 个会话，刷新只有前 50 条时详情仍在；把本人客服会话转给他人后保留可读详情；移出群后详情正文、引用及草稿被清理。

- **范围确认（2026-09-09）：** 用户确认按本轮建议实施。移动端真人单聊与 AI 聊天一并接入独立摘要，移动列表分页和返回锚点仍归 PR13。领取、转交、关闭、重开及回复隐式领取后保持当前筛选和详情，不新增目的队列跳转入口；点击已有队列导航才切换。主动退群清空选择并关闭窄屏详情，被动失权或不可用深链显示简短提示，不自动打开下一条。主动切换筛选恢复该查询已记录的选择，无记录则未选中；会话自身变化导致不匹配筛选时仍保留可读详情。
- **实现边界：** 独立摘要只按阅读资格授权，批量摘要在同一只读快照内逐项返回匹配、筛选外可读或不可用；不可用项不区分不存在、跨企业及失权。保留轮询，确认失权后清理正文、引用、草稿、附件队列与共享缓存，并忽略迟到结果；网络错误保留已展示详情。客服转交、关闭和群解散造成只读时保持输入组件与草稿，文本只读且可选择复制，禁用发送；真实失权才卸载清理。新建会话先读取权威摘要再由草稿切换，不依赖首页接管临时集合；关闭创建群聊表单或卸载页面后忽略迟到回调，已完成的创建只刷新列表。

- **验证记录（2026-09-09）：** 已通过服务端全量测试、桌面端测试、前端测试（37 项）、前端与服务端生产构建、macOS arm64 原生构建；已重新生成 appservice 与 Wails 绑定。新增回归覆盖首页外独立读取、四类摘要、批量结果顺序与重复 ID、筛选外可读、跨企业与失权、领取／转交期间快照一致性、解散只读、移出重入，以及共享缓存与附件准备请求迟到后的清理。Web 实测 80 个群的首页外深链与刷新、筛选选择恢复、被动失权、主动退群、客服领取与转交；桌面 MCP 实测新建群、转交／关闭／重开保持路由、引用及草稿；Web 实测解散群保留只读历史与草稿；mobile-preview 验证无路由摘要的真人单聊深链。移动 AI 摘要通过服务端回归和前端构建，未调用真实模型做界面验证。本轮创建的 82 个会话及访客关联数据已清理，本轮启动的 Web／桌面／Vite／MCP 已关闭，8081、9246、9100 端口已释放。

- **审核记录：** Pi CLI 已分别完成后端、前端和收尾局部审核，均无阻断缺陷。按建议补齐独立摘要失败日志的企业编号、筛选恢复说明，并将只读草稿保持为可选择复制的文本。复核附件遗留建议时确认既有两分钟上传活跃期限会向接收方返回失败，失权后不追加需要成员权限的取消请求；补充真实数据库回归验证超时失败且消息位置不变。不可用摘要继续轮询以支持重新获权自动恢复；按 ID 批量摘要和缓存 key 保留供后续列表控制器使用。

- **提交前同步：** 同步 `main` 的 `ea6bf26c`，保留 Telegram 引用回复和移动端头部更新，补齐失权时的引用状态缓存清理，并改用通用重试文案。同步后再次通过服务端全量测试、桌面端测试、前端测试（42 项）、服务端生产构建与 macOS arm64 原生构建；Pi CLI 对冲突整合完整复核通过，未发现实质问题。本轮未启动持久验证服务。

- **冲突更新：** 再次同步 `main` 的 `24fa04f0`（前端共享组件重构），创建群聊采用共享图片上传与成员选择组件，同时保留关闭、卸载和重开时的过期请求保护；会话列表采用共享时间与客服状态文案函数，独立详情逻辑不变。51 项前端测试、前端生产构建及 Pi CLI 冲突解决复核均通过。

### PR10：服务端真实收件箱分页

- **依赖：** PR09。
- **范围：** 全部／内部／客户统一 keyset 分页，替代每类型截取 50 条后混排；游标绑定完整查询、精确排序值、ID、排序版本，定义空时间边界。现有调用方同 PR 适配首屏，暂不增加空置顶区或搜索接口。
- **落点：** `inbox/load_inbox.go` 的各类型查询和混排、`LoadInboxInput`／`Inbox`、`frontend/src/api/inbox.ts` 及各端首屏调用。
- **实施：** 先组合真人单聊、AI 聊天、群聊和客户会话的可见候选再按共用顺序取 limit+1，返回 items、nextCursor、hasMore；游标绑定查询身份、版本和原始排序边界，删除游标行仍能按边界续读。总数不按当前页累加，空时间区单独表达 null 边界。
- **验收：** 不同类型与客户筛选、并列时间、游标行移除、重复页／空页均可继续；列表未返回不等于不存在；不使用 offset。
- **验证步骤：** 各类型至少造 60 条、夹入同时间和空时间，顺序翻页结果与一次完整 SQL 排序一致；读第一页后删除边界行，下一页可继续，换筛选使用旧游标得到明确无效结果。


- **范围确认（2026-09-09）：** 用户确认开始 PR10，默认每页 50 条，Web／桌面／移动端本轮只适配首屏；接受“全部／内部”从原每类最多 50 条混排改为统一排序的前 50 条，后续浏览仍由 PR12–13 接通。首页外已打开详情继续独立读取，完整未读与提醒总数不受分页影响。
- **实现记录：** 四类有权候选先在数据库中 `UNION ALL`，统一按 `last_activity_at DESC NULLS LAST, id DESC` 读取 `limit + 1`，再在同一只读 Repeatable Read 快照中按 ID 读取摘要及完整未读总数。删除各类型固定截断和 Go 混排，按 ID 资格判断共用候选筛选；返回现有 `conversations` 字段及 `nextCursor`／`hasMore`。不透明游标绑定企业、用户、规范化筛选与排序规则版本，保存微秒活动时间或明确空时间边界及 ID，不依赖边界行存活；无效游标返回本地化 `inbox_cursor_invalid`，终页返回空游标。页大小可在续页调整，不属于筛选身份。独立 `InboxQuery` 供按 ID 资格读取与前端筛选使用，分页字段只属于 `LoadInboxInput`；API Proxy 和 Wails 绑定按契约重新生成。
- **一致性边界：** 每次请求的列表和总数共享快照，跨请求不保留数据库事务，也不保证活动变化期间的一次遍历等同历史全量快照；新消息导致的上浮与筛选变化由后续列表刷新和锚点能力收敛。本轮不新增迁移、普通性能索引、置顶、搜索或滚动加载交互。

- **验证记录（2026-09-09）：** 已完成 appservice 与 Wails 绑定生成，通过服务端全量测试、桌面端全量测试、前端测试（53 项）、前端生产构建、`wails3 task build:server` 和 `wails3 task darwin:build ARCH=arm64`。真实数据库以四类各 60 条会话覆盖全量 SQL 对照、微秒并列、空时间区、客户各队列及负责人筛选、默认页大小、重复请求、页外未读总数；另覆盖时间／空时间边界行删除、失权空尾页、跨账号和跨企业游标，以及列表与总数的并发快照一致性。HTTP 与原生代理测试覆盖分页参数和响应元数据，游标单测覆盖损坏载荷、排序版本及查询身份。原生代理新增用例初次因夹具未初始化客户端凭据失败，修正后桌面端全量通过。本轮仅调整接口和首屏取数，没有新增布局或交互，未启动 Web、桌面端或移动预览做可视化验证；未调用业务对话模型或真实渠道。本轮测试服务在测试结束时关闭，未启动持久服务或调整开发库；构建有宿主 macOS 链接目标版本警告，构建成功。

- **审核处理：** Pi CLI 首轮完整审核未发现阻断问题。采纳候选查询精简建议：新增 `candidates.go` 统一阅读资格及有效关联，分页和按 ID 资格核对只投影 ID／活动时间，摘要按相同阅读基线追加；同步减少内部总数查询中不需要的 AI 运行摘要读取。临时只读 `EXPLAIN (ANALYZE, BUFFERS, VERBOSE)` 对照确认候选阶段不再执行未读、群成员数量或 AI 运行摘要计算，按 ID 条件可下推到各类型分支；诊断代码已移除，不把此小规模执行时间作为性能承诺。补充同快照下候选与摘要一一对应的直述注释。未采纳任意页大小上限，沿用 MVP 边界；保留 `math.MaxInt` 校验以防 `limit + 1` 溢出，该正整数本身能通过 HTTP 解析，并非不可达分支。跨页活动变化语义已在上文明确。
- **收尾复核：** Pi CLI 对查询抽取再次只读复核通过，确认阅读资格、四类摘要列、全局未读、AI 首发摘要与原有筛选均保留，无实质问题，也无需再删减查询层次。临时执行计划诊断代码移除后，服务端全量测试与服务端生产构建再次通过；保留任务开始时已有的五个 dock/color 生成文件删除。本轮未启动持久服务，既有 8081 服务保持运行，9246、9100 无本轮遗留进程。


### PR11：列表锚点上下文查询

- **依赖：** PR10。
- **范围：** 按 queryIdentity 和 conversationId／原排序边界读取邻近列表窗口，返回前后边界和资格；用于缓存淘汰、深处返回及定位，不从第一页逐页重建到目标。
- **落点：** 新增 inbox 锚点窗口 Query、appservice 方法和 DTO；复用 PR10 的资格谓词与排序。
- **实施：** 输入包含 queryIdentity、anchorId、原排序边界及前后窗口大小；返回锚点状态、邻域 items、双向游标和是否还有前后页。提供原排序边界时始终按原边界查邻域；未提供时才按仍匹配筛选的锚点当前位置定位，不设置远移阈值，不靠不断加载首屏找回，也不扩大当前筛选。
- **验收：** 锚点不存在、不再匹配、无消息、邻近结果为空及权限撤销有明确结果；上下文仍遵守当前筛选。
- **验证步骤：** 定位第 200 条后使其上浮、被移除或改为不匹配，分别断言返回当前资格与原邻域；前后都无结果时返回真正空窗口，并由客户端选择空态。

- **范围确认（2026-09-09）：** 用户确认开始 PR11。有原位置时恢复原邻域，无原位置时按会话当前位置定位；同轮补齐双向续页与完整已加载区间重读。仅交付查询、跨端契约和测试，列表控制器与界面交互仍归 PR12–13；不新增迁移、置顶、搜索或实时订阅。
- **接口契约：** 列表行新增 `positionCursor`，绑定企业、用户、规范化筛选及排序版本，保留数据库精度；独立摘要不携带查询位置。`GetInboxContext` 接收 `query`、`anchorId`、可选 `anchorCursor` 和前后条数（零值各取 25），原游标必须属于该锚点及当前查询。锚点结果复用 matching／outside_query／unavailable，匹配锚点携带当前位置游标；列表只渲染窗口行，独立锚点可能与窗口内同一 ID 重叠，不再额外插入列表；窗口始终按原位置构造，锚点是否仍在其中由当前排序及资格决定。不存在、跨企业和失权统一不可用，不返回摘要；无原游标且锚点不匹配时返回未定位的空窗口，不使用首页替代。
- **窗口与续页：** `LoadInbox` 增加互斥的 `beforeCursor`，返回 `startCursor`／`endCursor`／`hasBefore`，现有 `hasMore` 仍表示窗口之后还有会话，`nextCursor` 在无后页时为空。各方向最终均按活动降序返回。`ReadInboxWindow` 按同一查询的首尾游标重读包含边界的完整闭区间，不按页大小截断，边界行移动、删除或失权不改变区间；响应保留请求边界，区间为空也可重试或向两侧续读。客户端另用按 ID Query 核对已加载项的当前资格，不能把区间外上浮当成失权。窗口读取及前后存在性探测、锚点资格和摘要在各自一次只读 Repeatable Read 快照内完成；跨请求不保持历史快照。
- **查询实现：** 分页、邻域和闭区间共用候选资格与双向排序谓词，以独立存在性查询确定前后范围；因此普通分页不再使用 `limit + 1`，也不再需要对应的整数加一溢出分支。时间为空时仍以 ID 定位，前页查询反向取最近邻后恢复活动降序。邻域为空只表示该窗口无结果，不能单独据此断言整个筛选为空。

- **验证记录（2026-09-09）：** 已完成 appservice 与 Wails 绑定生成，通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（53 项）、`wails3 task build:server`（含前端生产构建）和 `wails3 task darwin:build ARCH=arm64`。真实数据库用 220 条会话验证第 200 条定位、微秒并列及空时间区、前向遍历、160 条闭区间重读、锚点上浮／失权／删除、首尾边界删除后继续双向读取；另覆盖四类会话、所有客户队列及指定负责人、领取后筛选外可读、缺失或跨企业锚点、跨查询／用户／企业游标、倒置区间，以及全空邻域和非空列表中的空区间。通过可控查询屏障验证锚点和完整区间的候选、摘要、资格来自同一快照。HTTP 与原生代理覆盖前向游标、嵌套请求、双向边界元数据及锚点／窗口头像归一化。新增接口与边界测试补齐后，服务端和桌面端全量再次通过。未新增界面交互，未启动 Web／桌面／移动预览做可视化验证；未调用真实渠道或业务模型，未调整开发库。生产构建有宿主 macOS 链接目标版本及重复系统库警告，构建成功。

- **审核处理：** Pi CLI 已完成完整只读审核，未发现阻塞性缺陷，确认排序、空时间、边界删除、资格、筛选、快照、双向续读及原生头像归一化符合契约。采纳建议：明确独立锚点可能与窗口行重叠、列表只渲染窗口；补充原位置未变时并入中心的直述注释；统一列表、独立摘要、批量摘要及窗口的错误转换，异常 WARN 携带企业与查询入口；增加大写 UUID 游标归一化测试；反馈处理后服务端全量测试与服务端生产构建再次通过。锚点与窗口内同一会话的摘要读取仍在同一快照中分别完成，沿用审核建议不为当前规模增加复用分支。五个 dock/color 生成文件删除为任务开始前已有变更，不纳入本次提交；本轮测试进程已结束，未启动持久服务，原有 8081 服务保持运行，9246、9100 无本轮遗留进程。

### PR12：Web／桌面列表分页与滚动锚点

- **依赖：** PR11。
- **范围：** 提取列表控制器，接通补页；保存首个稳定可见 ID、像素偏移、相邻候选和窗口边界。轮询刷新重读已加载边界窗口，深处只更新内容／资格、暂存活动序重排；顶部无交互或显式查看最新才跟随。刷新保留 ScrollArea，工具区／头像／摘要行尺寸稳定；锚点消失选原后继再前驱，窗口全空才重读邻近范围。原生锚定与手动补偿只保留一个所有者。
- **落点：** `frontend/src/features/inbox/inbox-page.tsx`；新增 feature 私有列表控制器，Query key 放 `resource-keys.ts`。
- **实施：** 状态至少区分 initial、ready、loadingMore、refreshing 与局部失败；维护已加载 ID／双向边界、pendingChanges 和锚点，不另存业务 DTO。刷新／补页按共用串行调度规则执行，刷新从已加载窗口边界与按 ID Query 重读；响应成功后一次布局测量恢复偏移，失败保留内容。
- **验收：** 上方插删、边界行变化、刷新／下一页失败不跳位；每视图只发一个补页请求；快照刷新可独立运行，不依赖 WebSocket。
- **验证步骤：** 已加载第 51–150 条、锚在 80，补页 151+ 未返回时点击刷新，待补页收尾后重读完整扩展范围，不丢已加载页；再使第 80 条上浮，刷新仍看原第 81 条附近。连续触底只发一次补页。注入补页／刷新失败，重试不重置顶部，也不把空响应当列表全空。

- **范围确认（2026-09-09）：** 用户确认开始 PR12。Web／桌面端按每页 50 条自动补页，固定高度工具区提供刷新、查看最新及待更新提示，失败原位重试；移动端返回恢复与完整页签历史分别留 PR13／14，不新增实时订阅、置顶、搜索或业务迁移。
- **实现记录：** 新增 feature 私有的列表控制器、Query 接入 hook 与视口适配器。控制器只保存 ID 顺序、服务端排序位置、双向边界、失权 ID 和浏览状态，摘要由批量资格 Query 缓存持有；查询 key 携带企业、用户和规范化筛选。首页、双向补页、轮询、刷新与查看最新共用串行队列，重复请求合并，返回的 Promise 等待本轮队列完成；查询切换和失权使旧结果失效，不取消 Wails 业务调用。刷新等待在途补页结束后重读完整扩展区间，并按 ID 核对已加载项；顶部新增跨过一页时重读连续扩展范围，深处只更新内容和资格并暂存重排，显式刷新恢复原邻域，查看最新重建首页。完整窗口为空时调用原位置邻域查询，空窗口不解释为整个筛选为空。
- **布局与恢复：** 列表工具区固定 40px、行高固定 68px，刷新和失败保留原 ScrollArea；保存首个可见 ID、像素偏移、原后继／前驱及排序游标，React 布局提交后补偿一次。禁用该视口的原生滚动锚定，滚动、指针和菜单交互抑制自动重排；隐藏容器保留恢复意图。独立详情先确认失权时立即移除对应行并作废在途旧窗口；其余行继续读取当前缓存，新的权威批次接替后清除含失权摘要的旧批次，避免整列闪空与误触发补页。保留全局未读快照与独立详情读取。
- **验证记录（2026-09-09）：** 本轮基于 PR11 合并提交开发，随后同步 `ad5f3cb3`。已通过前端测试（审核修复后 69 项）、服务端与桌面端全量测试、前端生产构建、服务端生产构建和 `wails3 task darwin:build ARCH=arm64`。新增可控响应顺序测试覆盖补页在途时刷新完整扩展范围、重复触底、刷新中补页排队、上浮、空邻域、资格读取失败不接纳半页、独立详情先失权、旧代次响应和顶部跨页扩展；React 布局测试覆盖上方插入、锚点移走、后继消失、单次像素补偿及隐藏容器恢复。
- **界面验证：** 使用隔离的临时数据库、企业访问地址与 NATS 命名空间构造 220 条测试会话。Web 实际滚动加载到 150 条，第 85 条活动时间上浮后深处轮询保留原位，显式刷新后第 86 条仍保持原来的 52px 偏移；查看最新回顶到最新 50 条并清除提示。macOS 通过 Wails MCP 验证重复触底只发一个补页请求，补页收尾后刷新读取完整 150 条窗口；注入刷新／补页失败时，100 条内容、滚动位置与原 ScrollArea 保持，重试继续原窗口。独立详情先失权的复测记录为 100 → 99 条，无中间空列表，详情不可用且旧敏感批次已清除。审核修复后另用 120 条临时会话复测轮询失败，点击工具区“重试”后保留原 100 条窗口、4000px 滚动位置及同一 ScrollArea；后续轮询恢复由可控响应测试覆盖。未发送真实业务消息、调用渠道或业务模型；移动端交互留 PR13。
- **审核处理：** Pi CLI 已完成完整只读审核。修复轮询失败后重试被同一错误门禁吞掉的问题，主动重试改为重读原窗口，并补充恢复后继续轮询的测试；排队／合并请求统一返回队列完成 Promise 并增加等待顺序测试。简化待更新状态的三元分支，补充初次加载 INFO、带筛选上下文的失败 WARN 和局部顶边的直述注释；初始失败不展示“还没有会话”，真正空查询只在主区展示空态。其余分页、资格、缓存归属和锚点语义经审核确认符合本轮范围。
- **收尾：** 审核修复后前端测试（69 项）、服务端生产构建和 macOS arm64 原生构建再次通过；构建有宿主 macOS 链接目标版本及重复系统库警告，构建成功。两轮界面验证启动的服务端、桌面、Vite 与 MCP 均已关闭，18081、9246、9100 端口已释放，临时数据库已删除，桌面连接地址恢复为原 8081，原有 8081 服务及共享 PostgreSQL／NATS 保持运行。五个 dock/color 生成文件删除为开始任务前已有变更，不纳入本次提交。

### PR13：移动端列表分页、返回恢复与三端自动排序

- **依赖：** PR12。
- **范围：** 移动聊天列表复用控制器，用 ID＋偏移替代仅保存 scrollTop；详情返回、旋转、字体／宽度改变后重测，缓存失效调用锚点查询。三端统一自动排序和自然向上浏览，移除 PR12 的刷新、查看最新与待更新提示；桌面窄屏 Sheet 保持底层列表。
- **落点：** 移动端 `mobile-inbox-page.tsx`、`mobile-inbox-navigation.tsx`、`mobile-navigation.tsx`，以及共用的 `features/inbox` 列表控制器、Query hook、视口与列表面板。
- **实施：** 离开列表保存 ID、偏移、原邻居、窗口和尺寸；可见后重测，缓存缺页用锚点 Query 恢复。三端自动应用活动排序，正在看的会话上浮后保留原后继、前驱位置；触摸、惯性、键盘及浮层期间暂缓重排，尾部补页即时追加。向上浏览主动补页和核对最新窗口，不设置刷新、查看最新和待更新入口。
- **验收：** 触屏惯性、弹性回弹和滚动条拖动不与补偿争夺控制；当前行上浮后原邻居位置稳定，向上滚动自然看到最新且无重复。详情返回、尺寸改变、缓存丢失及失败重试不丢失原浏览区域。
- **验证步骤：** 在第 100 条附近让该行收到新消息，验证原第 101 条保持像素偏移；再进详情、他端发消息、旋转返回，覆盖缓存存在与丢失两条路径。惯性滚动中读取更新和追加下一页，停止后无二次跳位；向上补至真正顶部，最新会话只出现一次，窄屏 Sheet 和错误重试保持原容器。

- **范围确认（2026-09-09）：** 用户确认开始 PR13，并将上述自动排序行为统一应用到移动、Web 和桌面端。深处的新消息自动上浮，当前行上浮时以原后继、前驱保位；按住、滚动和惯性期间先更新内容，停止后一次性重排。向上滚动自动取得最新列表，无需刷新、查看最新或待更新提示，仅保留加载和失败原位重试。默认每页 50 条，不新增实时订阅、置顶、搜索、业务契约或迁移，完整页签历史仍归 PR14。PR12 的确认与验收记录保留为历史交付证据，其按钮与深处暂存行为由本条替代。


- **实现记录：** 移动端与 Web／桌面端共用列表控制器和 Query 接入；身份、前台状态、未读计数及附件清理由各端传入。刷新和双向补页均读取连续区间并核对资格，原已覆盖顶部的窗口在深处也自动纳入顶部新增，不丢中间页；交互期间只提交摘要和资格，将会影响现有行位置的顺序及边界元数据留到停止时一次性应用；向下补页立即追加尾部，不另存业务 DTO。当前锚点活动位置改变时跳过它，依次使用原后继、前驱保留像素位置。
- **移动恢复与布局：** 登录会话内按企业、用户和规范化筛选保存窗口元数据、原邻居及视口尺寸；详情返回时缓存仍在则展示并重读完整原窗口，缓存缺失则直接读取原位置上下文。移动端保留原生滚动，桌面保留 ScrollArea；两者共用唯一滚动补偿，关闭原生锚定。旋转、字体和宽度变化由 ResizeObserver 重测，指针取消后的惯性继续保护，菜单、键盘和窄屏 Sheet 结束后再提交顺序。移除常驻刷新、查看最新、待更新文案及旧移动列表的 scrollTop 恢复，仅失败时提供不挤动列表的局部重试。
- **界面验证：** 在隔离临时数据库中建立企业、两个测试成员和 220 条群聊。mobile-preview 加载 150 条，在第 100 条为首个可见行（-10px）时由另一测试成员发消息，该行上浮后原第 101 条仍保持 59px；详情中发消息并切换横竖屏，返回后原邻居仍保持 36px。清空列表 Query 缓存后直接恢复原邻域 51 条，原位置 -33px／36px 保持，向上滚动自动补到最新顶部。桌面 MCP 模拟触摸取消后的连续滚动，滚动值按输入连续增加，停止后排序只应用一次，450ms 后位置无二次变化；窄屏 Sheet 开关保留同一底层容器和原邻居 40px。注入窗口失败后重试，150 条内容、6090px 位置与原 ScrollArea 保持。Web 生产构建实际加载 150 条，第 81 条上浮后原第 82 条保持 32px。触摸／惯性验证使用预览中的可控事件与滚动，未在 iOS／Android 真机验证弹性回弹；未调用渠道或业务模型。


- **验证记录（2026-09-09）：** 已同步主分支 `1ad34bba`（Wails beta.18），使用临时目录中按 go.mod 安装的匹配版本 CLI，未改动全局 CLI。通过 `wails3 task test`（前端 75 项、服务端和桌面端全量）、`wails3 task build:server`（含前端生产构建）和 `wails3 task darwin:build ARCH=arm64`。审核修订后再次通过前端 75 项、服务端生产构建和 macOS arm64 原生构建。可控测试覆盖在途补页/刷新合并、连续范围、上浮保邻居、触摸期间尾部追加、菜单/键盘/覆盖层结束恢复、接近顶部触发、失权、过期响应、缓存恢复与尺寸重测。beta.18 移动预览另验证按住时连续追加到 100／150 条，松手前后均为 6255px；底部错误条仅重试按钮接收点击，重试后同一容器、150 条和 6255px 保持。构建仍有宿主 macOS 链接目标版本及重复系统库警告，构建成功。
- **审核处理：** Pi CLI 已完成完整只读审核，未发现阻断缺陷。采纳错误条点击遮挡、重复 positions 赋值、注释和 PR13 任务书整理建议，并补充菜单、键盘、覆盖层与顶部过渡测试。局部空窗口继续由共用面板显示“此处暂无会话”和双向入口，全查询为空才显示整体空态，避免扩大筛选；不增加重复空态分支。完整窗口重读的读取量随已加载范围增加，本轮为排序与连续边界正确性保留并直述其成本，后续实时接入收敛；覆盖层期间保留现有轮询以继续更新资格，不另加降频策略。
- **收尾：** 两轮界面验证启动的服务端、桌面、Vite 与 MCP 均已关闭，18081、9246、9100 已释放，临时数据库已删除；桌面连接恢复到原 8081，原有 8081 进程及共享 PostgreSQL／NATS 保持运行。开始任务前已有的五个 dock/color 绑定文件删除保持原状，不纳入本次提交。

### PR14：选中会话定位与列表边界

- **依赖：** 无（PR12、PR13 已合并）。
- **已由 PR12／PR13 交付：** 旧响应不覆盖新查询（控制器按 generation 丢弃过期结果）、交互期间暂缓重排与失权立即清理、菜单和指针按会话 ID 绑定、隐藏容器不按零高度恢复。本条只交付余下部分。
- **范围：** 进入列表时按已选中会话定位，不持久化滚动位置；选择历史保存键按规范化查询生成。修正切到空筛选时的列表报错、末页底部空白和轮询期间的页脚占位。
- **落点：** `inbox-list-controller.ts`、`use-inbox-list.ts`、`inbox-list-panel.tsx`、`inbox-route.tsx`。
- **实施：** 进入时有选中会话则以它为初始锚点读 `GetInboxContext`，锚点无原位置，按邻居补偿对齐到窗口顶部；`ReadContext` 在原游标为空时按锚点自身位置推导邻域，因此不需要保存滚动状态。记录了原位置的锚点才判定为已移动。空区间兜底只接受属于本窗口的锚点。页脚在可向前翻页、分页出错和空列表加载时占位。
- **边界：** 不保存滚动锚点，不新增浏览状态存储；工作台页签模型不变，`/inbox` 仍只有一个页签。
- **验收：** 带选中会话进入列表时该会话停在窗口顶部，无选中时停在顶部；选中会话不属于当前筛选时读取首页并回到顶部；从已滚动的非空筛选切到空筛选不报错；末页最后一条会话贴到底部，有数据时的轮询不改变列表高度。
- **验证步骤：** 深处选中会话后切换筛选再切回，核对该会话距容器顶为零；带会话参数刷新地址，核对同样定位。滚到末页测量最后一行到容器底的距离。从已滚动的非空筛选切到空筛选，核对无报错且服务端无游标失效日志。

- **实现记录：** 选中会话不属于当前筛选时回落首页；带原位置的书签锚点读到空邻域时保留空窗口。视口空区间兜底原先会取上一个查询的锚点并被游标绑定校验拒绝，改为只接受本窗口锚点。页脚不再无条件渲染，末页空白由 50px 降为 6px。选择历史保存键改用 `normalizeInboxListQuery`。定位成功时不读首页，提醒未读总数等下一轮轮询更新，最多晚约 3 秒。
- **文档：** 按实际交付改写本条，删除「相同查询两个页签各滚到不同位置」验收项（`/inbox` 只有一个页签，场景不可构造）；新增 PR14A 条目，补充 Pi CLI 调用方式。
- **验证记录（2026-09-10）：** 桌面端经 Wails MCP 使用 70 个群聊与 50 个网站访客会话实测：深处选中后切「内部」再切回，选中行距容器顶 0px、窗口 51 行；无选中往返 scrollTop 为 0、首页 50 行；带 `?conversation=` 刷新地址定位距顶 0px；末页末行距容器底 6px；从已滚动的客户筛选切到空的已关闭筛选无报错、服务端无游标失效日志。轮询期间页脚占位因终端驱动时窗口不可见无法构造，依据代码判定。前端测试 79 项通过，新增定位读取邻域、定位失败回落首页、带原位置书签不回落三项；`wails3 task common:build:frontend` 与 `wails3 task build:server` 通过。
- **审核处理：** Pi CLI 审核后修正页脚轮询占位与空邻域回落范围，定位锚点构造下沉到控制器，并收紧注释。

### PR14A：收件箱导航重排与范围筛选

- **依赖：** PR14。
- **范围：** 会话范围纵栏延伸到中栏顶端，搜索、筛选和新建归入会话列表列头部；客户视图收敛为「排队中、我负责的、@我的、同事」，@我的按阶段 1E 交付前保持禁用占位。新增按当前范围提供条件的筛选浮层：客户范围为渠道加服务状态，全部与内部范围为会话类型多选。`CustomerInboxView` 去掉 `closed`，已关闭改由服务状态条件表达。新增收件箱渠道候选查询，包含已停用渠道。
- **落点：** `domain/inbox.go`、`actions/inbox/load_inbox.go` 的 `LoadInput` 与 `filterCustomerInbox`、`actions/inbox/candidates.go`、`actions/inbox/cursor.go`、appservice `types_inbox.go`、`backend.go` 与 `direct_backend_inbox.go`、`tools/appservicegen`、新增 `components/ui/popover.tsx` 与 `features/inbox/inbox-filter.tsx`、`inbox-page.tsx`、`inbox-pane-top.tsx`、`inbox-customer-queue-filter.tsx`、`inbox-route.tsx`、`inbox-list-controller.ts`、`resource-keys.ts`、`mobile-inbox-navigation.tsx`、`mobile-inbox-page.tsx`。
- **实施：** `filterCustomerInbox` 在既有 `cci` 关联上追加渠道条件并按输入的服务状态取值，不新增 JOIN；`listCandidates` 按选中的会话类型决定参与 UNION 的分支，勾满全部类型归一化为不限类型。客户队列条件只在客户范围解释，其他范围一律按空条件读取。`inboxCursor` 携带渠道、服务状态和类型并在解码时校验，排序版本不变。渠道候选复用消息渠道查询，返回本企业全部渠道并按类型和名称排序。生成器新增枚举切片查询参数支持，`kinds` 以重复查询参数传输。筛选触发按钮为固定宽度图标，已设置条件只用角标表示，不改变相邻搜索框宽度。
- **边界：** 本条不实现 @我的 视图本身、不新增渠道多选、不提供已读或未读筛选，也不改变各视图的资格判断规则。
- **验收：** 三个客户视图与两种服务状态的组合都按字面查询，无结果即空列表；停用渠道的历史会话仍可筛出且候选列表保留该渠道；切换任一筛选维度使旧游标失效且不串页；`ReadByIDs` 的资格判断随渠道和类型条件收敛；范围外条件被规范化清空，跨范围类型和非法渠道编号返回校验错误。
- **验证步骤：** 同一联系人在两个渠道各有会话，逐个视图切换渠道核对结果集；关闭一条本人负责的会话，在「我负责的 + 已关闭」中查到；内部范围只勾群聊时仅返回群聊；切到「全部」和「内部」时 URL 与请求都不带渠道和状态参数。覆盖 Web／桌面端及移动端，运行契约生成、前端与服务端测试和构建。

- **实现记录：** 前端范围规则集中到 `inbox-query.ts`，地址参数解析、写回、类型勾选和查询规范化由 Web、桌面端与移动端共用，移除列表控制器中重复的一份规范化。筛选浮层紧贴触发按钮向右展开，触发按钮固定 36px。
- **验证记录（2026-09-12）：** 通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（81 项）、`wails3 task common:build:frontend`、`wails3 task build:server` 与 `tsc --noEmit`，已运行契约生成与绑定生成。新增 `inbox_filter_integration_test.go` 覆盖渠道筛选在排队与已关闭视图收敛、资格随渠道和类型收敛、全选类型与不限类型结果及游标一致、范围外条件规范化、非法组合校验错误、渠道候选含停用渠道并排序；新增 `inbox-query.test.ts`，并在 `internal/api` 与 `internal/apiproxy` 测试中补充三个筛选维度与 `kinds` 重复查询参数。
- **界面验证：** 真实浏览器验证纵栏贯通布局、四个页签、两种筛选浮层、渠道与状态改变 URL 和结果集、`kinds=group` 只返回群聊、跨范围切换丢弃范围外类型、筛选按钮宽度恒为 36px，浮层在 1440px 与 430px 下紧贴触发按钮且不溢出。移动端筛选面板因本机无可用 MCP 客户端且缺少辅助功能与屏幕录制权限，未做可视验证，由类型检查、构建和共用查询层测试覆盖。
- **审核处理：** Pi CLI 发现「全部」范围勾选客户会话后切到「内部」仍携带 `kinds=customer`，后端返回校验错误导致列表报错，通过集中范围规则修复并补测试；同时处理进度说明式注释、反向表述、契约注释与领域定义不一致、移动端面板标题和测试缺口。

### PR15：成员发送气泡与查询结果收敛

- **依赖：** 无（PR07、PR09 已合并）。
- **范围：** 本地待发送项通过本人 clientMessageId／messageId 与查询消息对齐；成功响应、查询补拉和离开页签后的结果共用发送状态收尾。保持手动失败重试，不新增离线自动发送队列。
- **落点：** `use-outgoing-conversation-messages.ts`、`use-conversation-timeline.ts`、`frontend/src/api/inbox.ts`。
- **实施：** 本地发送项依次经历 pending、confirmed 或 failed；查询含本人关联时可确认发送，即使命令响应尚未到达。临时 ID 只用于展示，messageId 到达后统一去重；发送完成通知归属原会话，路由卸载不取消已经执行的命令。
- **验收：** 查询先到、响应先到、超时已提交、切换会话后响应迟到均只显示一个气泡；改正文／引用／提及生成新逻辑编号。
- **验证步骤：** 可控地交换发送响应与消息查询顺序，两种顺序都只剩一个气泡；服务端提交后客户端超时，再手动重试仍只出现一条，切到另一会话不得插入原会话回复。

- **实现记录：** 新增 `outgoing-message-store.ts` 按会话分组保存发送项，`succeed`／`fail`／`discard` 按发送逻辑编号在全部分组中定位，`reconcile` 删除窗口已收录的项；新增工作台级 `outgoing-message-context.tsx`，Web／桌面端与移动端各挂一份，`useOutgoingMessages` 同时读取正式会话与草稿分组，并在转正后移交草稿发送项。删除 `use-outgoing-conversation-messages.ts`。发送框在组件存活判断之前写入结果，时间线的气泡合并与发送收尾共用窗口覆盖判定，失权入口与移动端群聊离开路径清理发送状态。
- **附件：** 附件队列接入同一份发送状态，入队、入库成功、失败、取消和失权分别调用 `start`、`succeed`、`fail`、`discard`，队列只负责文件传输。取消成功后才丢弃发送项，取消请求失败时写回失败态并保留重试。群聊单文件上传仍为独立路径。
- **验证记录（2026-09-12）：** 通过 `wails3 task test:frontend`（96 项，新增发送状态存储 9 项，附件队列补充丢弃、取消失败、页面退出和失权断言）、`tsc --noEmit` 与 `wails3 task common:build:frontend`。可控顺序测试覆盖查询先到、响应先到、已提交后客户端超时、切换会话后迟到响应、草稿移交、失权清理、窗口覆盖判定、取消回滚和页面退出收尾。
- **界面验证：** 经 `wails3 task dev:mcp` 的桌面窗口与 mobile-preview 验证正常发送、服务端停止后失败与重试、切换会话与切回、草稿失败后转正、草稿首发、连续发送 4 个附件及入库响应返回前切换会话，各场景数据库均只有一行消息。未执行 iOS 与 Android 真机，移动端无附件入口。
- **合并与审核：** 与 `#275` 的 `mention-token.ts` 迁移冲突时保留删除旧 hook，引用改指 `@/lib/mention-token`。Pi CLI 三轮审核处理草稿移交时机、附件收尾与入库状态所有权、重复匹配条件、Provider 缺失兜底、移动端群聊失权清理、取消失败后发送项恢复、页面退出收尾与注释表述；保留按发送逻辑编号全局定位，作为离开页签和草稿移交后收尾的前提。

### PR16：禁用账号后保留单聊与 AI 会话

- **依赖：** 无实时基础设施依赖；以已合并的独立 AI 会话实现为基础。原清单编号为 PR00。
- **范围：** 禁用 AI 员工或真人成员后，仍在对方的成员消息列表保留已有 AI 会话和真人单聊；聊天页显示「该 AI 员工已禁用」或「该成员已禁用」并禁止发送新消息。账号禁用不撤销会话阅读资格，已提交输入继续执行，有在途 Run 时保留独立停止回复入口。
- **落点：** `internal/actions/inbox/candidates.go` 的 `agentConversationsQuery`、`directConversationsQuery`、`load_inbox.go` 的 AI 会话与单聊摘要、appservice `types_inbox.go`、`features/inbox/conversation-main.tsx`、`apps/mobile` 的 AI 会话页与单聊线程、`features/contacts/agents/agents-panel.tsx` 的禁用确认文案。
- **实施：** AI 会话摘要返回 `agentStatus`，单聊摘要返回 `peerStatus`；列表、按 ID 资格和未读汇总只按会话归属与会话状态查询；新消息发送、新建 AI 会话和发起单聊继续校验账号状态，通讯录入口保持不变。发送框复用既有 `disabledReason` 显示禁用提示，停止回复入口在运行过程中独立可达。账号状态只在列表三点菜单修改，沿用已有确认对话框，确认前不保存状态。文案明确禁止新消息、已提交任务继续执行、相关渠道分配重置为公共队列，团队关系保留；恢复后仍为下班，渠道配置不自动恢复。
- **边界：** 本条不新增会话归档／恢复、批量取消、群聊 Agent 或客服接管语义；停止回复由独立 Agent PR 实现，本条只保持入口可达。
- **验收：** 列表三点菜单取消确认不改变账号状态，确认只提交一次；禁用前后历史、未读汇总、当前会话选择和滚动位置保持，不能新发消息或发起新会话，但能查看在途结果并主动停止；恢复后允许发送，跨端状态一致，企业及成员隔离不变。
- **验证步骤：** 分别禁用 AI 员工和单聊对方，验证会话仍在列表与按 ID 核对中、未读汇总不变、发送与发起新会话被拒绝、恢复后可发送；禁用后停止在途回复与已接受输入继续执行沿用既有停止回复和锁序测试。从列表三点菜单禁用并取消／确认，检查渠道目标、下班状态和恢复后的行为。覆盖 Web／桌面端及移动端，运行契约生成、前端与服务端测试和构建。

- **实现记录：** 评审时发现真人单聊存在同样的隐藏问题，一并纳入并改写本条标题。前端新增 `useAccountDisabledReason`，Web／桌面端与移动端 AI 会话页、单聊页共用它设置发送框禁用提示；移动端账号禁用时同时禁用失败重试，与 Web 端一致。原条目中的 `agent-detail.tsx` 已不存在，范围、落点和实施按实际交付改写。
- **验证记录（2026-09-13）：** 运行绑定生成，通过 `wails3 task common:build:frontend`（含 tsc）与 `wails3 task test:server`。新增 `disabled_account_conversation_integration_test.go`，覆盖 AI 员工与真人成员禁用后会话仍在列表与按 ID 核对中、状态字段为禁用、未读汇总不变、发送与发起会话被拒绝、恢复后可发送；已有用例覆盖禁用后停止回复与已接受输入继续执行。未执行界面验收，禁用后发送框的实际展示待验证。
- **审核处理：** Pi CLI 审核后将禁用提示抽为共用 hook，对齐移动端失败重试，查询注释改为直述，补充未读汇总与发起单聊断言，并收口本条验收与验证步骤。

### PR17：会话变更版本、身份资料版本与同步探针

- **依赖：** 无（PR06、PR08 已合并）。合并原清单 PR16 与 PR21。
- **范围：** 新增 `conversations.version`，会话锁内随消息追加、群资料和附件上传完成或取消推进；新增本人身份资料版本，随登录态可见的成员资料、角色、工作状态和账户级偏好推进；`conversation_user_states` 新增本人会话状态版本，随已读、提及确认、静音和手动未读推进；新增 `GetSyncHeads` 探针。不建事件日志表，不建持久受众水位表。
- **落点：** `models/conversation.go`、`chatstate/message.go`、用户账号模型与资料、工作状态、偏好、登录和角色分配 Action、`models/conversation_user_state.go` 与已读／提及确认／静音／手动未读 Action、`actions/inbox/candidates.go`、appservice 新增 `GetSyncHeads`。
- **实施：** 版本与消息、摘要、活动时间在同一事务内推进；幂等重放和纯读取不推进。身份资料版本存在本人用户账号行上，企业身份字段经统一入口更新并按实际变化推进，写入保持用户账号先于企业身份的锁序；会话状态版本与个人状态同行更新，设备通知许可仍留本地；实际无变化的已读或设置不推进。探针复用已交付的候选资格查询，在一次只读一致快照内返回可见会话数量、可见会话 `(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希之和（缺个人状态行按 0 计入）与身份资料版本，只投影聚合值。
- **验收：** 四种会话的版本单调、事务回滚不留版本；幂等重放不推进；探针在会话新增、移除、失权、消息和资料变更后都发生变化；只改本人偏好时会话版本不变而身份版本变；只标已读、确认提及、静音或手动未读时会话版本不变而校验和变；跨企业与失权不扩大探针范围。
- **验证步骤：** 并发向同一会话追加消息，断言版本逐次推进且不丢；把成员移出群后探针的数量和校验和都变化；对一个低版本会话追加消息，断言校验和变化（而最高版本可能不变）；失去一个会话同时加入另一个同版本会话，数量不变而校验和变化；另一端标记已读或静音后本端校验和变化，重复标记不变；尚无个人状态行的会话追加消息，校验和变化；用户没有任何会话时改工作状态，探针仍变化。

- **实现记录：** 迁移新增 `conversations.version`、`conversation_user_states.version` 与 `users.profile_version`，默认 0。`chatstate.AppendMessage` 在分配 `message_seq` 的同一语句推进会话版本，覆盖成员消息、客户入站、AI 回复和群系统事件；群资料修改、附件上传完成或取消同样推进。个人会话状态版本在已读（`read_seq` 前进时）、设置或清除手动未读、静音、提及确认和入群阅读基线的写入中推进。身份资料版本放在用户账号行，企业身份字段经 `identity.UpdateUserIdentity` 用 PostgreSQL 18 `RETURNING old/new` 与数据修改 CTE 在显示名、头像、角色、工作状态实际变化时推进，邮箱和账户偏好在用户账号更新语句中按实际变化推进；登录与角色分配先锁用户账号，保持用户账号先于企业身份的锁序。`LoadInboxQuery.SyncHeads` 在一条语句内返回探针三项，appservice 新增 `GetSyncHeads`（`GET /sync/heads`）。
- **确认项：** 身份资料版本和后续 `pinOrderVersion` 存 `users` 表；附件上传完成和取消推进会话版本，上传失败不推进；身份资料版本覆盖登录态可见的全部字段，修改密码与团队不推进。
- **验证记录（2026-09-13）：** 运行 appservice 生成器与 Wails 绑定生成，绑定仅新增 `GetSyncHeads` 与 `SyncHeads`。macOS 本机 PostgreSQL 18.6 上通过 `wails3 task test:server`，新增 `TestConversationVersionAppend`、`TestSyncHeadsConversationChanges`、`TestSyncHeadsIdentityProfile`，并在 `TestAttachmentBatchLifecycle` 增加版本断言；`wails3 task common:build:frontend` 通过且构建后绑定无额外变化。本条无前端交互，未执行界面验证和原生端、移动端构建。
- **审核处理：** Pi CLI 审核后精简注释，身份资料测试改在已有会话上断言校验和不变并覆盖邮箱修改，附件生命周期测试补版本断言。未采纳：同一事务内版本可能推进两次（如群改名同时写系统消息），版本只用于判断是否变化，契约只要求单调；探针包含企业全部客服会话，与当前全部成员可查看客服会话的阅读资格一致。

### PR18：提交后发布与受众通知契约

- **依赖：** PR17。合并本清单原 PR23 的发布职责。
- **范围：** 事务内登记待发通知，包含受众种类、受众 ID、通知种类和最小载荷；提交成功后按 namespace、企业、受众种类和受众 ID 异步发布 Core NATS，请求响应不等待发布，回滚则丢弃。接入内部会话新消息（含附件消息、群系统事件与 AI 结果消息）、本人已读／提及确认／静音／手动未读，以及身份资料与账户偏好变化。附件改为传完才入库：客户端先上传临时文件，传完后逐个发送附件消息，服务端不保存上传中的占位消息、上传状态和活跃期限，附件通知随消息追加登记。受众种类显式区分 `user`、`customer_inbox`、`visitor_directory`，本条只登记 `user`。不建通知表和后台发布器，业务事务内不发 NATS，不在网络中持事务。
- **落点：** 新增 `internal/realtime` 的 `RunInTx`／`Notify` 与 `Publisher`，服务端装配接入发布器生命周期；`chatstate.AppendMessage` 与 `NotifyConversationMembers`、附件发送 Action 与前端附件队列、个人会话状态 Action、`identity.UpdateUserIdentity` 与 `UpdateUserAccount`，以及到达这些登记点的写事务入口。不改 `task_outbox`，不建立每用户 JetStream Consumer。
- **实施：** 写事务经 `realtime.RunInTx` 执行，`Notify` 把通知记在该事务的上下文中，在事务外调用视为编程错误并 panic；提交成功后整批交给进程级发布器。发布器使用独立 NATS 连接，启动时 NATS 不可用也后台重连，断连期间不缓存；有界队列和单个协程按已提交批次的入队顺序发布，并发事务之间的通知可能乱序；队列已满或发布失败记录 `WARN` 并丢弃，不重试、不影响业务结果，由兜底探针恢复。载荷只含通知种类、会话 ID 和字符串版本，不含正文、姓名或未读数。内部会话通知当前真人成员，客户会话不登记成员受众；个人会话状态与身份资料只通知本人，管理员修改他人资料时通知资料所属用户；版本为 0 的个人状态行与缺行等价，不登记。同一事务内同一受众、同一通知种类、同一会话的多次变化合并为一条并保留最高版本，撤销控制不参与合并。附件每个文件独立发送并沿用各自的发送编号，同批按选择顺序提交：前面的文件未结束时后续文件等待，上传或发送失败、取消的文件不阻塞后续文件，失败文件在发送方本地保留重试并在重试成功后排在会话末尾；说明随最后一个文件发送；AI 聊天附件与文本一样逐条进入 Agent 输入流，连续输入由同一次运行认领；删除 `message_attachments` 的上传状态与活跃期限及对应的状态查询、心跳和取消撤回接口。
- **边界：** 群资料修改、成员变化和不追加消息的 Run 状态转换由 PR19 接入；群系统事件经消息追加已发送会话变更通知。客户会话受众由 PR20 接入。
- **验收：** 回滚不发布；实际无变化不登记；个人设置与身份资料不广播给其他人；载荷不含业务内容；NATS 不可用时业务写入照常成功且请求不被阻塞。
- **验证步骤：** 登记通知后主动回滚，版本不变且订阅端收不到通知；同一事务写两个会话收到两条通知、同一会话两次追加只收到最高版本；本人已读、静音、手动未读、提及确认只发往本人受众，重复操作不发布；工作状态、账户偏好与管理员修改邮箱只通知资料所属用户；附件发送通知单聊双方；附件按选择顺序发送，前面的文件失败不阻塞后续文件，未完成上传或已过期的临时文件不能发送；NATS 不可达时发送消息成功，探针值仍变化；发布阻塞时事务提交照常返回，队列已满与发布失败记录 `WARN`。

- **实现记录：** `realtime.RunInTx` 为写事务创建通知批次，`Notify` 按「企业、受众种类、受众 ID、通知种类、会话」合并并保留最高版本；提交成功后整批交给进程级 `Publisher`，Subject 为 `cervi.<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，载荷为 `kind`、`conversationId` 与字符串 `version`。`chatstate.AppendMessage` 追加后调用 `NotifyConversationMembers` 通知内部会话当前真人成员；已读水位推进、手动未读、静音与提及确认在写入生效时通知本人；`identity.UpdateUserIdentity` 返回推进了版本的用户账号，新增 `identity.UpdateUserAccount` 用 `RETURNING old/new` 判断邮箱与账户偏好变化，管理员修改他人资料或角色时通知资料所属用户。到达登记点的写事务改用 `realtime.RunInTx`，覆盖单聊、AI 聊天、群聊发送与群管理、附件、个人会话状态、Agent 运行完成／失败／停止回复、登录、工作状态、账号状态、资料、偏好、管理员修改用户和角色分配。附件删除 `SendAttachmentBatch`、`UpdateAttachmentUploads`、`ListAttachmentStates`、`CompleteAttachmentUpload` 接口，迁移删除 `message_attachments.upload_status` 与 `upload_expires_at`，`SendAttachmentMessage` 只接受已上传未过期的文件；前端附件队列最多三个并发上传，同批按选择顺序串行发送。
- **确认项：** 身份资料通知原清单无 PR 负责，并入本条；附件改为传完才入库在本条完成，发送方刷新或关闭页面时上传中的附件不保留；群资料修改、成员变化和不追加消息的 Run 状态转换留给 PR19，客户会话受众留给 PR20。
- **验证记录（2026-09-14）：** 运行 appservice 生成器与 Wails 绑定生成；macOS 本机 PostgreSQL 与 NATS 上通过 `wails3 task test:server`，新增 `realtime_notification_integration_test.go` 以真实 NATS 订阅覆盖群消息通知全部真人成员、回滚不发布、同一事务两会话两条且同会话合并、个人会话状态只通知本人且重复操作不发布、身份资料只通知资料所属用户、附件通知单聊双方、NATS 不可达时发送成功且探针变化；`TestPublisherKeepsCommitsNonBlocking` 注入阻塞的发送函数，覆盖发布阻塞时提交不等待、队列已满与发布失败记录 `WARN`。合入最新 `main` 后 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（97 项）、`wails3 task common:build:frontend` 与 `wails3 task build:server` 均通过。Codex CLI 以桌面端 Wails MCP 与 Web 双账号完成附件界面验证：多文件按选择顺序送达且说明只在最后一个文件、接收方上传期间无占位、取消与中断上传不阻塞同批文件、失败重试排在末尾、AI 聊天以附件首发、群聊附件与引用正常。未执行原生端与移动端构建（移动端无附件入口）。
- **审核处理：** Pi CLI 审核后其余直接调用 `chatstate.AppendMessage` 的测试事务改用 `realtime.RunInTx`，注释改为直述，发布失败 `WARN` 补充受众与版本字段；附件改造按审核把重试附件移到本批末尾、页面退出时未发送附件置为失败、取消／失权／页面退出统一释放临时文件。Codex CLI 只读审核后拆出发布器发送函数并补发布阻塞测试，发布顺序表述改为按已提交批次入队顺序。未采纳：`TestMessageSequenceCommitOrder` 中先取锁的事务需由测试控制提交或回滚，保留 `BeginTx`；AI 聊天多文件首发标题取首个文件名；附件参数校验沿用会话不存在错误；同一草稿连续两批的跨批次交错由服务端建会话与幂等保证结果正确。

### PR19：成员会话变化接入

- **依赖：** PR18。合并削减前编号的原 PR18 与原 PR23。
- **范围：** 群创建、资料、增员、移除、退出、转让、解散，以及成员会话中 AI 运行的排队、运行中、成功、失败、停止回复和失效收敛接入版本推进与通知。变更前后的真人受众都写通知，失权者只收仅含会话 ID 的 `conversation_removed` 通知；Agent 不消费成员受众。客户会话受众与人工接管、转交、关闭等客服周期变化由 PR20 接入。
- **落点：** `internal/realtime` 通知种类、`chatstate` 会话版本推进、`conversation/group_conversation.go`、`group_management.go`、`agentrun/execute.go`。
- **实施：** 建群在 `realtime.RunInTx` 内以初始版本 1 插入会话并通知全部真人成员；群资料实际变化时推进版本并通知，改名另追加系统事件，同一事务的通知合并为最高版本。增员、转让、解散经系统事件的消息追加通知当前成员；移除与退出在更新成员关系后向失权的真人登记 `conversation_removed`，该种类不与会话变更合并、载荷不含版本，重入是新的有效关系；退出者作为系统事件操作人另收本人阅读水位通知。解散保留成员关系与只读历史，只通知会话变更。`chatstate.TouchConversation` 在已持有会话锁的事务内推进版本并通知真人成员：Agent 运行由排队转为运行中时推进，崩溃恢复重入不推进；认领输入、完成或失败事务中运行策略判定失效、收敛为取消且不追加消息时推进。成功、失败与停止回复由结果消息推进版本；入队与群成员变化触发的取消和触发消息或系统事件同事务提交。仅内存进度不推进版本，重复终态回调按当前运行状态判定无变化。
- **验收：** 移除后重入恢复当前成员关系；新成员阅读基线正确；解散只读与失权区分；失败结果消息与 Run 终态、无结果消息的取消都能通过 HTTP 恢复；事务失败不留半条系统消息，也不发布通知。
- **验证步骤：** A 被移除后立刻重入，延迟消费通知仍恢复最新成员关系；A 仅被移除时批量摘要不可读，通知载荷无群名或正文。让模型失败并生成 agent_error，另构造不产生消息的取消，断开 Socket 后仅靠 HTTP 恢复两种终态；旧租约成功回调晚于取消到达时不得复活 Run。

- **实现记录：** `internal/realtime` 新增 `conversation_removed` 通知种类与 `UserConversationRemoved`，载荷版本为 0 时省略，与会话变更分属不同合并键。新增 `chatstate.TouchConversation`，在调用方持有会话锁的事务内推进会话版本并通知内部会话真人成员，客户会话只推进版本。建群改用 `realtime.RunInTx`，以初始版本 1 插入会话并通知全部真人成员；群资料实际变化时推进版本，改名另追加系统事件，同一事务合并为最高版本；移除成员向被移出的真人、退出向退出者本人登记 `conversation_removed`；增员、转让、解散经系统事件的消息追加通知当前成员，解散不发送失权通知。Agent 运行 `begin` 改用 `realtime.RunInTx`，由排队转为运行中时推进版本，崩溃恢复重入不推进；认领输入、完成与失败事务中运行策略判定失效、收敛为取消且不追加消息时推进版本，认领事务同步改用 `realtime.RunInTx`。
- **确认项：** 人工接管只出现在客户会话领取流程中，随客户会话受众由 PR20 接入，共用执行路径对客户会话同样推进版本但不登记成员受众；新成员入群阅读基线推进的本人会话状态版本不单独通知，入群成员随系统事件收到会话变更通知；退出者作为系统事件操作人，阅读水位推进后仍收到一条本人会话状态通知，客户端重读时按失权结果处理。
- **验证记录（2026-09-15）：** macOS 本机 PostgreSQL 与 NATS 上 `wails3 task test:server` 47 个包通过，`wails3 task test:desktop` 22 个包通过。`TestRealtimeGroupMembershipNotifications` 以真实 NATS 订阅依次覆盖建群、改简介、改名合并、移除与重入、主动退出、转让、失去管理资格的移除失败不留系统消息也不发布、解散只发送会话变更，每步按期望集合精确比较并以一次本人静音收尾；「独立 AI 聊天/运行状态实时通知」覆盖模型失败时开始与 agent_error 结果各推进一次、运行中重入不推进；「Agent 群内点名/失效运行收敛推进会话版本」覆盖移出执行中的 AI 员工后失败回调收敛为 `agent_removed` 取消且只推进一次。重建测试库后以 `-v` 单独运行，确认子测试实际执行。本条无契约、绑定与前端改动，未运行绑定生成、前端构建与界面验证。
- **审核处理：** Pi CLI 审核后认领输入路径改用 `realtime.RunInTx` 并在收敛为取消时推进版本；群成员通知用例以本人静音收尾并补充改名合并用例；`begin` 注释改为直述并删除重复的行内注释，`Notification` 注释补充版本为 0 时省略；退出者本人阅读水位通知保留既有行为，测试注释与清单如实描述。未采纳：延迟消费旧失权通知后重入的乱序场景属于客户端重读行为，服务端不增加用例；停止回复经结果消息推进版本，沿用已有用例。

### PR20：客服共享收件箱通知

- **依赖：** PR18。
- **范围：** 网站与 Telegram 客户入站、成员回复与 AI 最终消息、隐式领取、领取与人工接管、转交、关闭、显式重开及新周期接入会话版本与企业客服受众通知；Telegram 投递状态变化（发送认领、发送结果与人工处理）、渠道启停和更换机器人同样推进受影响客户会话的版本。共享受众不逐客服扇出；本人客服已读只写本人受众。
- **落点：** `internal/realtime` 客服受众通知、`chatstate` 会话变更通知、`conversation/manage_service_session.go` 与客服收发 Action、`customerdelivery`、`channel` 的 Telegram 回调、连接与启停、`agentrun/cancellation.go`。
- **实施：** 所有有权客服共享一条企业受众（`customer_inbox`，受众 ID 为企业 ID），通知只表示该客户会话需要重读；公共／本人／同事／关闭资格仍由现有 Query 判断。`chatstate.NotifyConversationChanged` 对客户会话登记共享受众通知，消息追加与 `TouchConversation` 由此覆盖入站、回复、AI 运行状态与新周期；领取在负责人实际变化时推进版本，转交、关闭、重开推进版本且不改活动时间。投递写入按渠道身份、会话、投递的顺序加锁，状态实际写入时推进版本，渠道停用期间认领不写入投递；渠道启停在状态实际变化时按会话 ID 顺序推进有待发送投递的会话版本；更换机器人改变引用可用性，推进该渠道全部客户会话版本。`realtime.RunInTx` 接受 `bun.DB` 或 `bun.Conn`，客服写入路径统一在其中执行。负责人变化不为所有客服创建 Participant，个人已读继续走本人受众。
- **验收：** 公共、本人、同事和关闭视图同时收敛；筛选迁移不等于授权移除；客服参与者不被误当全部有权阅读者。
- **验证步骤：** A 领取公共会话后，B 的公共列表移除、A 的本人列表出现，双方有权详情仍可读；关闭／重开改变队列但不改活动时间；AI 最终回复也能触发共享受众通知。

### PR21：访客授权与线程目录通知

- **依赖：** PR20。
- **范围：** 提取网站 HTTP 共同授权，建立渠道身份级受众通知与目录／会话公开投影查询，补齐未知新线程发现。只返回访客公开投影。
- **落点：** `website_visitor_direct_backend.go`、`api/website_visitor.go`、`conversation/list_website_conversations.go`、`list_website_messages.go`。
- **实施：** 把 Cookie／Header 恢复出的渠道身份作为唯一受众，目录按该身份列出线程；成员 Query 的内部字段不复用到访客 DTO。首次消息以外的读操作不建立联系人。
- **验收：** 同一访客两标签页新建不同线程可互相发现；跨渠道／身份请求被拒绝；初始化和草稿不创建联系人，客服内部资料不外泄。
- **验证步骤：** 同渠道同访客在两个标签页分别首发新线程，互相能发现；换另一个渠道或伪造线程 ID 读不到消息；空白页反复初始化后数据库业务记录数不变。

- **范围确认（2026-09-15）：** 本条只交付服务端能力，挂件建立事件流并改用通知触发拉取由 PR32 完成；访客事件流本身由 PR26 交付，本条只负责发布到访客目录受众。
- **实现记录：** `realtime.VisitorDirectoryConversationChanged` 以渠道身份记录 ID 为受众 ID 构造 `visitor_directory` 受众通知。`chatstate.NotifyConversationChanged` 在客户会话通知企业客服共享受众之后，按 `customer_conversations` 关联的渠道身份再登记一条访客目录通知，并用 `channels.type = website` 限定：只有网站渠道存在访客事件流，Telegram 客户会话与客服 Copilot 线程不产生该通知。新增公开路由 `GET /public/website-channels/:channelID/conversations` 返回 `{conversations}` 目录，供访客在初始化之后重新发现线程；`WebsiteVisitorService.InitializeMessenger` 改为复用同一个 `ListConversations`，目录与初始化返回同一份公开投影。HTTP 共同授权提取为 `authorizeWebsiteVisitor` 中间件：统一写 `Cache-Control: no-store`、按 Header 与 Cookie 恢复访客 Token、失败按公开错误体拒绝并把渠道外部编号写入请求上下文，发送、目录与历史三条路由共用，PR26 的访客事件流可直接复用。Action 层把两处重复的渠道身份读取合并为 `loadWebsiteVisitorIdentity`，尚未建立身份时返回未找到而不创建联系人，目录查询据此返回空集合、历史查询返回会话不存在。
- **验证记录（2026-09-15）：** `wails3 task test:server` 全量通过；`wails3 task build:server` 通过，`go generate ./internal/appservice` 无差异（访客服务不在 `Backend` 契约内，无需重新生成绑定），构建改写的 `frontend/bindings` 已还原。新增 `TestRealtimeVisitorDirectoryNotifications`：同一访客新建第二线程时共享受众与本人访客目录受众各收到一条同版本通知，目录查询列出两个线程，另一访客身份的线程只通知其自身受众且不进入前者目录。新增 `TestWebsiteVisitorDirectoryHTTP`：缺少或格式非法的访客 Token 被共同授权拒绝（400）；空白页连续初始化两次并读取目录后企业联系人数量不变；同一 Token 的两个标签页各新建线程后目录同时列出两条；另一访客身份读目录为空、读线程历史返回 404；同一 Token 在另一个网站渠道下是不同身份，目录为空且读不到原渠道线程；目录路由只接受 GET。目录行字段被逐一核对为 `id`、`title`、`preview`、`previewSenderIdentityType`、`lastMessageSeq`、`lastMessageAt`、`serviceSession`，`serviceSession` 只含 `id` 与 `status`，客服内部资料不外泄。既有客户会话通知测试同步补齐访客目录受众期望；Telegram 投递通知测试保持只有共享受众一条，确认网站渠道限定生效。
- **未验证：** 访客侧界面未改动，挂件仍按现有三秒轮询工作，新目录接口在浏览器中的实际调用由 PR32 验收。

### PR25：成员 Gateway、帧契约与连接认证

- **依赖：** PR18。合并原清单 PR28、PR29、PR30，以及本清单 PR24 与 PR35 的原生端子路径、心跳和关闭部分；PR20 已合并，客服共享受众订阅随本条交付。
- **范围：** 按 `chat-roadmap.md` 第 10.11 节定义首版 WebSocket JSON 帧：Authenticate、ClientHello／ServerHello、Ping／Pong、ConversationChanged、ConversationRemoved、ConversationStateChanged、IdentityProfileChanged、SessionRevoked、ServerGoingAway、RealtimeError；访客帧由 PR26 增加，AI 流订阅与流帧由 PR38 增加，PinOrderChanged 由 PR40 增加，均遵守同一版本与未知帧规则，64 位整数一律用字符串。Server 内嵌 Gateway，首帧限时 Authenticate；Web 使用现有 Bearer，原生端由 Go 侧 Proxy 持有连接并把帧经 Wails 事件交给 TS。按本节点在线受众订阅，禁止 Queue Group 与企业通配订阅。登出按 token session、停用按用户发送控制通知，连接设最长存活时间。原生端支持子路径部署，配置心跳，停止时发送下线提示后有界关闭。不建票据表。
- **落点：** 新增 `internal/realtime/protocol`（Go 帧定义、编解码与共用夹具 `testdata/frames.json`）、`internal/realtime/gateway`（连接、发送队列与受众订阅）与 `frontend/src/api/realtime/protocol.ts`；`internal/realtime` 通知新增 `session_logged_out`、`user_disabled` 撤销种类与 tokenSessionId；`actions/auth/logout.go` 与 `actions/user/update_status.go` 登记撤销；`appservice.DirectBackend` 提供 `AuthenticateMember`／`MemberSyncHeads`；Service 新增原生端 `ConnectRealtime`／`DisconnectRealtime`，由 `internal/apiproxy/realtime.go` 实现；服务端 `Assets.Middleware` 装配与 `internal/config/server` 的 `realtime.maxFrameBytes`。
- **实施：**
  - **帧契约：** Go 结构体与 TS 类型各自手写，线上 `type` 使用 snake_case 并与 NATS `kind` 一致；以共用夹具锁定线上格式、协议主版本和字符串整数边界，Go 覆盖两个方向的编解码，TS 覆盖客户端帧编码与服务端帧解码。
  - **挂载与地址：** 连接路径为 `/api/realtime`；Gateway 在服务端 `Assets.Middleware` 中位于租户上下文中间件之内处理该路径的升级请求，升级前沿 `Unwrap` 解包 Wails 写入器，允许来源保持默认同源。原生端按服务器地址路径拼接，与 API Proxy 一致。
  - **下线：** 服务端退出时 Gateway 先拒绝新的升级请求（503），再向现有连接发送 `server_going_away` 并以同名原因关闭，默认 5 秒内未完成的连接强制断开，之后停止通知发布器。
  - **认证与寿命：** 连接依次处于 awaiting_auth、authenticated、closing；认证前只允许 Authenticate，五秒内未认证即以 `authentication_timeout` 关闭；Authenticate 复用业务调用的身份解析，令牌须存在、未过期且账号活跃，身份解析同时返回令牌编号（即 tokenSessionId）与到期时间。连接最长存活 1 小时且不晚于登录会话到期，到期以 `session_expired` 关闭后客户端重连并重新认证。认证后收到 `client_hello` 时登记 tokenSessionId、userId 和企业，并安装本人用户受众与本企业客服共享受众（`customer_inbox`，受众 ID 为企业 ID）订阅，当前阶段所有成员均可阅读客户会话；Flush 之后重新校验登录会话，再读取 Hello 探针值；变更通知可能先于 `server_hello` 送达。
  - **撤销：** 登出在删除令牌的事务内登记携带 tokenSessionId 的 `session_logged_out`，停用在账号状态事务内按用户受众登记 `user_disabled`；Gateway 收到后清除该连接未发送的帧，发送 `session_revoked` 并以 `session_revoked` 原因关闭。
  - **发送队列与心跳：** 每条连接一个写协程与一条有界发送队列，单帧上限由 `realtime.maxFrameBytes`（环境变量 `REALTIME_MAX_FRAME_BYTES`，默认 65536，范围 64–256KB）配置，同时约束客户端帧读取；变更通知按同一会话同一种类保留最高版本，`conversation_removed` 与撤销控制不合并；队列溢出时按 `slow_consumer` 关闭连接，单帧写入超过 10 秒说明对端停止读取，直接断开；客户端重连后经探针恢复。客户端每 25 秒发送 Ping，服务端 60 秒内未收到任何帧即以 `idle_timeout` 关闭；心跳间隔与代理空闲时间一起记录。协议错误发送 `realtime_error` 后以错误码作为关闭原因，认证读取失败使用 `unavailable`。
  - **原生端：** Service 的 `ConnectRealtime`／`DisconnectRealtime` 由 PR27 的传输内核驱动启停与退避重连；Go 侧 `apiproxy` 用当前登录凭据拨号 `<服务器地址路径>/api/realtime`，拨号期间登录会话或企业服务器变化时丢弃新连接，发送 Authenticate、ClientHello 并每 25 秒发送 Ping，把服务端帧原文经 `cervi:realtime:frame` 事件、关闭码与原因经 `cervi:realtime:closed` 事件交给前端，事件携带本地连接编号区分新旧连接；登录、登出与切换企业服务器时关闭当前连接。
  - **日志与凭据：** 认证失败、慢连接关闭和发布失败记录 `WARN`，不记凭据或正文；原生端不把长期 Token 交给 TS。
- **验收：** 共用夹具下两端编解码结果一致，未知可忽略帧与不支持的协议主版本分别有明确结果，超过 JS 安全整数的值正确；一用户多设备均收到通知；先注册订阅再给探针值；慢消费者断开后可补拉；已连接后登出或停用不再收到通知；撤销事务回滚不产生错误断连；连接到达最长存活时间后关闭，凭据有效时重连成功，登出或停用后重连认证失败；原生端经剥离子路径前缀的反向代理可连，空闲超过代理默认时间仍正常。
- **验证步骤：** 客户端帧由 TS 编码、Go 解码，服务端帧由 Go 编码、TS 解码，均与共用夹具一致；在安装订阅与读取探针之间提交消息，客户端通过 Hello 或后续通知至少发现一次；同账号建立两条连接两端均收到；堵塞一端读流触发有界关闭，另一端继续正常工作。连接后登出本次 token 只关闭对应登录会话，停用用户关闭其全部会话；分别丢弃登出与停用的控制通知后，连接在最长存活时间到期时关闭，重连认证失败。SIGTERM 后连接收到下线提示，重启后客户端经探针补齐停机期间的变化，退出不关闭共享 NATS。
- **平台探针结论（原 PR24，2026-09-14）：** macOS、Go 1.27.1、Wails v3.0.0-beta.21 服务端模式、`coder/websocket` v1.8.15；临时端点用首版帧应答认证、Hello 与 Ping，每个场景保持 75 秒，验证后删除。
  - **挂载与写入器：** Wails AssetServer 在匹配服务 Route 前对 WebSocket 升级请求直接返回 501，Gin `/api` 路由与 Wails 服务 Route 都无法升级；中间件收到的 `assetserver.contentTypeSniffer` 延迟下发响应头，直接 `websocket.Accept` 时 101 响应随连接劫持丢失、客户端握手超时，沿 `Unwrap` 解包到 `*http.response` 后握手成功。
  - **截止时间：** `net/http` 劫持连接时清除读写截止时间，Wails 默认 30 秒读写超时不影响升级后的连接。
  - **Origin 与 Host：** `coder/websocket` 默认只接受与 Host 相同的 Origin；本地复刻 `ingress` 重写规则的反向代理与 Cloudflare Tunnel 都保留原始 Host（隧道带 `X-Forwarded-Proto: https`），跨源连接被拒绝；原生端 Go 连接不带 Origin。
  - **Web（Chrome 153）：** 同源 `ws://` 与 Cloudflare Tunnel `wss://` 均保持连接，双向 ping／pong 正常；Chrome 请求 `permessage-deflate`，服务端默认不协商。Wails 运行时以 `window.location.origin + "/wails/runtime"` 调用绑定，Web 端不支持子路径部署。
  - **macOS 原生端：** 探针程序使用与 `apiproxy` 相同的 Go 网络栈，未在桌面应用进程内运行；带 30 秒 `Timeout` 的 `http.Client` 可直接拨号，本地、Cloudflare Tunnel 与剥离 `/cervi` 前缀的反向代理均通过。
  - **未验证：** iOS、Android（PR33 记录）与 `ingress` 自动证书模式。

### PR25A：实时传输改为 SSE

- **依赖：** PR25。按 PR 清单「第四轮调整」原地改造 PR25 已交付的成员 Gateway 与原生端连接。
- **范围：** 成员实时连接从 WebSocket 改为 SSE 事件流 `GET /api/realtime`，请求头 Bearer 认证，事件只由服务端下行；删除 Authenticate、ClientHello、客户端 Ping／Pong、Authenticated、SessionRevoked、ServerGoingAway、RealtimeError、关闭原因码、Hello 能力协商与 `realtime.maxFrameBytes` 配置；原生端改为流式 GET。受众订阅、按版本合并的发送队列、登出与停用撤销、最长存活时间以及先订阅后读探针的顺序保持 PR25 语义。同步改写 `chat-roadmap.md`、`agent-roadmap.md` 与 PR 清单中以 WebSocket 为前提的设计。
- **落点：** `internal/realtime/gateway`、`internal/realtime/protocol` 与共用夹具、`frontend/src/api/realtime/protocol.ts` 与前端协议测试、`internal/apiproxy/realtime.go` 与 `backend.go` 共用的错误响应解码、`internal/appservice/types_realtime.go` 与 Service 的 `ConnectRealtime`、服务端装配与 `run_server.go`、`internal/config/server`；删除 `coder/websocket` 直接依赖。
- **实施：**
  - **认证与建立：** 请求头 `Authorization: Bearer` 与 `Accept-Language` 复用业务调用的身份解析，失败返回与业务 HTTP 接口相同的错误体和状态码，Gateway 下线中、NATS 未就绪、订阅失败或无法清除读超时时返回 503 与 `kind=unavailable` 的业务错误体。认证后登记连接、订阅本人用户受众与本企业客服共享受众并等待 NATS Flush，重新校验登录会话并读取同步探针，再写 200 响应头（`text/event-stream`、`Cache-Control: no-cache`、`X-Accel-Buffering: no`）与首个 `server_hello`。
  - **写出与超时：** 每个事件写成一行 `data: <JSON>` 加空行后 Flush；通过 `http.ResponseController` 清除 Wails 服务器默认读超时，每次写入前设置 10 秒写截止时间；服务端每 25 秒发送 `ping`。
  - **队列、撤销与寿命：** 沿用 PR25 的单写协程、有界发送队列与按会话种类合并；队列溢出、登出或停用撤销、到达最长存活时间时停止发送并结束响应，撤销时清除未发送的事件，不发送原因事件。
  - **下线：** 收到 SIGINT／SIGTERM 时拒绝新的事件流请求并结束全部事件流，默认 5 秒内未结束的强制断开，之后停止通知发布器，Wails 执行 `http.Server.Shutdown` 时不再等待事件流。
  - **协议：** 事件集合为 `server_hello`、`ping`、`conversation_changed`、`conversation_removed`、`conversation_state_changed` 与 `identity_profile_changed`；Go 保留服务端事件的编码与解码，TypeScript 只保留解码，共用夹具删除客户端方向样例。
  - **原生端：** `ConnectRealtime(meta)` 不再接收应用版本与能力参数，用 `clientsession` 中的 Bearer 发起不设整体超时的流式 GET（等待响应头最长 30 秒）；非 200 响应沿用 API Proxy 的错误解码，401 时清除本地凭据并返回登录会话错误；逐个解析 `data:` 事件，把 JSON 原文经 `cervi:realtime:frame` 投递，60 秒未读到任何事件时主动断开，流结束经只含连接编号的 `cervi:realtime:closed` 投递。登录、登出与切换企业服务器时关闭当前流。
- **验收：** 缺失、无效或已登出的令牌得到 401 且错误体与业务接口一致；同一用户两条事件流均收到用户受众与客服共享受众通知；订阅与探针读取之间提交的变化在 `server_hello` 或后续通知中至少出现一次；登出只结束对应登录会话的事件流，停用结束该用户全部事件流且重连得到 401；撤销事务回滚不结束事件流；到达最长存活时间后结束且凭据有效时可重连；停止读取的事件流被有界结束，其他事件流照常收到通知；事件流在超过 Wails 默认读写超时后仍持续下发；服务端下线时事件流在时限内结束；原生端按服务器地址路径拼接并投递事件与流结束。
- **验证记录：** 2026-09-15 通过 `wails3 task test:server`（含 SSE 网关集成测试：同一用户两条事件流的用户受众与客服共享受众投递、撤销事务回滚、登出与停用结束事件流并重连得到 401、认证与订阅之间登出、服务器 1 秒读写超时下持续心跳与通知、最长存活时间、服务端下线与慢连接）、`wails3 task test:desktop`（含原生端事件投递、流结束与 401 清除本地凭据）、`wails3 task test:frontend`（100 项）与 `wails3 task build:server`。以本机 `bin/cervi-server`（Wails v3.0.0-beta.21 服务端模式，默认 30 秒读写超时）实测：未携带令牌返回 401 与业务错误体；`server_hello` 在 0.04 秒内到达，第 25 秒收到 `ping`，事件流持续 40 秒未被服务器超时中断，响应头为 `text/event-stream`、`Cache-Control: no-cache` 与 `X-Accel-Buffering: no`；事件流打开时发送 SIGTERM，事件流 0.02 秒内结束、服务端进程 0.04 秒内退出。未验证 Cloudflare Tunnel、自动 HTTPS `ingress` 与 iOS／Android。验证结束后本轮服务端进程已退出，8080 端口已释放。

### PR26：访客事件流受众

- **依赖：** 无（PR21 已交付，PR25、PR25A 已合并）。
- **范围：** 访客在同源 iframe 中请求访客事件流：Cookie 模式使用 `EventSource` 携带渠道 Cookie，Header 恢复模式用 fetch 流式读取并携带同一 Header；服务端恢复渠道身份后订阅独立访客 Subject，只发送公开事件白名单；渠道停用撤销事件流。
- **落点：** `api/website_visitor.go`、`website_visitor_direct_backend.go`、渠道停用入口与 Gateway 访客认证 adapter。
- **实施：** 受众使用数据库渠道身份 ID，不使用 Cookie 原值或可逆外部标识构造 Subject。先验证渠道与访客归属，再允许其目录与会话公开投影；认证失败沿用访客 HTTP 接口的错误体与状态码。尚无业务身份时不建立事件流，也不为请求创建联系人。
- **验收：** 请求中指定 conversationId 不能扩大接收范围；独立链接、嵌入、Header 恢复与预览互相隔离；成员 Bearer 不能用于访客事件流；停用渠道后已有事件流结束，重新请求被拒绝。
- **验证步骤：** 抓取访客事件与 Subject，不能出现原始 Cookie 值；跨身份指定线程失败；停用渠道后重新请求被拒。

- **实现记录：** 公开路由新增 `GET /public/website-channels/:channelID/realtime`，与发送、目录、历史共用 PR21 的 `authorizeWebsiteVisitor` 中间件读取访客 Token 并写入渠道外部编号，Gin 处理器只把响应交给 Gateway 的 `ServeVisitor`。`AuthorizeWebsiteVisitorQuery` 按启用的网站渠道和外部编号解析渠道身份：渠道停用或不存在返回渠道未找到，尚未建立业务身份返回会话未找到，读操作不创建联系人；`WebsiteVisitorDirectBackend.AuthenticateVisitor` 把它映射为与访客 HTTP 接口一致的本地化错误体与状态码。Gateway 的成员与访客事件流收敛到同一条 `stream` 实现，差异由 `streamRoute` 表达：受众 Subject、可下发事件白名单、撤销标识、最长存活时间与订阅生效后的问候构造。访客受众为 `visitor_directory.<渠道身份记录 ID>` 与 `website_channel.<渠道 ID>`，Subject 只使用数据库内部 ID，访客 Token 与 Cookie 原值不进入 Subject。连接按白名单过滤发送队列：访客只下发 `visitor_hello` 与 `conversation_changed`，成员专用的个人状态、失权与身份资料事件直接丢弃。协议新增 `visitor_hello` 事件（只含连接编号，访客没有同步探针），Go、TypeScript 与共用夹具同步。渠道停用入口 `UpdateMessageChannelStatusAction` 改用 `realtime.RunInTx`，停用网站渠道时登记 `channel_disabled` 撤销控制发往渠道受众，Gateway 收到后结束该渠道全部访客事件流；受众 ID 取更新结果中的规范渠道 ID，请求参数中大小写不同的 UUID 同样发往订阅所用的 Subject。事件流的授权到期时间以绝对时间保存在 `streamRoute` 中，存活时长在计时器启动时结算，握手耗时计入授权到期时间之内。
- **验证记录（2026-09-16）：** `wails3 task test:server` 全量通过，`wails3 task test:desktop`、`wails3 task test:frontend`（155 项）、`wails3 task build:server`、`wails3 task common:build:frontend` 与 `wails3 task darwin:build ARCH=arm64` 通过；`go generate ./internal/appservice` 无差异（访客服务不在 `Backend` 契约内），构建改写的 `frontend/bindings` 已还原。新增 `TestVisitorRealtimeStream`（经真实 Gin 路由与 HTTP 服务端，服务器读写超时设为 1 秒以确认长响应不受其约束）：尚未建立身份的访客请求被拒（404）；只携带成员 Bearer 而无访客 Token 的请求被拒（400）；客服回复后本访客事件流收到该线程的 `conversation_changed`，另一访客身份即使在请求中指定他人线程编号也收不到任何事件；访客事件流持续超过服务器 1 秒读写超时后仍存活并按 300 毫秒间隔收到心跳，其后的客服回复照常送达；本轮发布的全部 Subject 经 NATS 通配订阅抓取，确认使用渠道身份记录 ID 且不含访客 Token 原值；用大小写不同的渠道 ID 停用渠道后两条访客事件流均结束，重新请求被拒（404）。新增 `TestVisitorConnectionDropsInternalFrames` 验证访客白名单丢弃成员专用事件。
- **未验证：** 挂件尚未建立事件流，`internal/publicweb/chat.js` 仍按现有三秒轮询工作，浏览器中的 `EventSource`／fetch 恢复模式、预览隔离与重连退避由 PR32 验收。

### PR27：共享客户端事件流与会话代次

- **依赖：** 无（PR25、PR25A 已合并）。
- **范围：** `src/api/realtime` 实现无 feature 依赖的 TS 传输内核；一个应用实例一条成员事件流、业务页签共用，浏览器各标签页独立连接。原生端驱动 Go 侧 `ConnectRealtime`／`DisconnectRealtime` 并消费 `cervi:realtime:frame`／`cervi:realtime:closed`，Web 端用 fetch 携带 Bearer 流式读取 `data:` 行。流结束和 60 秒无数据按网络错误抖动退避重连，会话错误进入对应入口，协议主版本不支持时停止。同时建立登录会话代次，覆盖登录、初始化、登出、切换企业、会话恢复与多标签页令牌变化。移动端外壳由 PR33 接入；本条不消费通知、不删除轮询。
- **落点：** `frontend/src/api/session-scope.ts`、`client.ts`、`auth.ts`、`conversation-read-queue.ts`；`frontend/src/api/realtime` 的 `realtime-client.ts`、`web-transport.ts`、`native-transport.ts` 与 `index.ts`；`lib/resource-client.ts`、`lib/session-navigation.ts`、`features/session/use-realtime-connection.ts`、`hooks/use-session-generation.ts`、`apps/shared-app-routes.tsx`、`features/workspace/workspace-layout.tsx`；`internal/apiproxy/realtime.go` 的建立日志记录 HTTP 协议版本。
- **实施：**
  - **会话边界：** `beginSessionBoundary()` 提升代次并按订阅顺序同步通知订阅方（实时客户端关闭事件流与计时器，未读标记队列丢弃旧排队链），再清空查询缓存。登录与初始化保存令牌后进入新代次；登出先取原令牌、清除本地令牌并进入新代次，再用原令牌调用 `Logout`，登出返回后不再写入令牌或缓存；切换企业服务器在调用 `ConnectServer` 前进入新代次；`recoverSession` 对 login／connect／setup 状态都进入新代次，login 额外清除令牌。原生端 `Login` 与 `Logout` 在 Go 侧持有同一把会话锁，旧登出不会清除新凭据。
  - **调用结果：** `call()` 记录发起时代次，成功与失败结果只在代次未变时交付，过期结果既不 resolve 也不 reject，调用方不再执行 Toast、导航和后续操作，旧会话的登录失效错误不会触发新会话的入口恢复。登录、初始化、登出、切换企业与原生连接使用不受代次约束的 `invoke()`。
  - **多标签页：** Web 端记录本代次归属的令牌；`storage` 事件，或 `call()` 与事件流读取凭据时发现令牌已被其他标签页改动，即采用新令牌并进入新代次，本次调用不再发起。Web／桌面工作台以代次为 key 重新挂载，重新读取身份、缓存并建立事件流。
  - **状态机：** 状态为 disconnected、connecting、ready、backoff、stopped，`server_hello` 进入 ready 并清零失败次数。每次连接尝试有独立编号，`stop()`、代次变化和重连都使旧尝试的事件与结束回调失效。第 n 次连续失败的退避上限为 1s×2ⁿ⁻¹（最高 30s），等待时间取上限的一半到上限之间；`resume()` 只在 backoff 时立即重连；会话错误与协议主版本不支持进入 stopped，网络恢复与前台恢复不会重启；无法解析的事件记录 `WARN` 后忽略。
  - **原生端：** 传输在模块级串行执行连接与断开：上一次连接请求结束且完成清理后才发起下一次连接，已关闭的在途请求调用 `CancellablePromise.cancel()` 缩短等待，调用方已关闭而请求成功时先 `DisconnectRealtime`。连接编号返回前到达的事件先缓存，拿到编号后只回放本连接的事件；流结束、连接失败和关闭都注销 Wails 事件监听。
  - **Web 端：** 在 `open` 返回后读取凭据，携带 `Accept: text/event-stream`、`Accept-Language` 与 Bearer 请求 `/api/realtime`；非 2xx 响应按业务错误体转换为 `ApiError`；按行解析并保留跨数据块的半行；从发起请求起 60 秒未收到任何数据即中止并按网络错误处理。
  - **外壳挂载：** Web 与桌面工作台在身份就绪后 `start()`，卸载时 `stop()`；`online` 与回到前台时调用 `resume()`；订阅方目前只处理会话错误并调用 `recoverSession`。
- **验收：** 企业／账号切换先提升代次再清缓存，旧 Promise、事件流和回调失效；多标签页换账号后身份、缓存、普通请求和事件流归属一致；旧连接请求晚到不替换新事件流；重连与旧流结束回调交错时只保留一条事件流；401 进入登录，协议主版本不支持时不无限重试。
- **验证记录：** 2026-09-15 通过 `wails3 task common:build:frontend`（含 tsc）、`wails3 task test:frontend`（125 项）与 `wails3 task test:desktop`。新增前端测试覆盖：慢响应在换代后返回被关闭且不交付；旧流结束回调与重连交错；会话错误与协议不支持不重试；online 与前台同时恢复只重连一次；start／stop／start；连接编号返回前到达的事件与结束事件回放；旧连接请求晚到时先断开再发起新连接；断开与新连接串行；监听清理；跨数据块行解析、非 2xx 错误体、空闲超时与关闭中止；过期代次的成功与失败结果不交付；新会话的未读标记不等待旧排队链。
  - **Web 端：** 以本机 `wails3 task run:server`（127.0.0.1:8081，HTTP，无 TLS）与无头 Chrome（CDP 驱动）实测。登录后 `/api/realtime` 返回 200、`text/event-stream`、协议 `http/1.1` 并进入 ready；停止服务端后按 0.76s、1.7s、2.7s、5.9s 退避，服务端恢复后重新 ready。A、B、C 三个标签页以 ai.shellphy 登录，C 经登录接口取得 jack 令牌写入本地存储后，A、B 经 storage 事件、C 在下一次请求时发现令牌归属变化，工作台重新挂载，用户菜单由「艾翔飞」变为「任杰」，三页新的事件流均携带 jack 令牌且没有旧令牌请求。A 从用户菜单退出登录后三页均回到登录页，本地令牌已清除，8 秒内没有新的事件流请求。
  - **桌面端：** 以 `wails3 task dev:mcp` 经 Wails MCP 实测。启动恢复登录后 Go 日志为 `实时事件流已建立 … protocol=HTTP/1.1`，前端 ready；停止服务端后 Go 侧事件流结束，前端收到 `unavailable` 错误并逐次退避，服务端恢复后重新 ready；从用户菜单退出登录后前端为 disconnected，Go 侧事件流以 `context canceled` 结束且 8 秒内没有重连；重新登录后建立新的事件流并 ready。
  - **未验证：** Cloudflare Tunnel（由维护者手动验证）、HTTPS／HTTP/2 部署、两个真实企业服务器之间的切换（由会话代次与传输单元测试覆盖）以及 iOS／Android（PR33）。验证结束后本轮启动的服务端、桌面端、Vite 与无头 Chrome 均已退出，8081、9246、9100 端口已释放。
- **审核记录：** 设计阶段经 codex 审核后修订：原生端连接改为串行，并在丢弃过期结果前完成连接清理；会话边界覆盖登出、切换企业、登录、初始化与会话恢复，登出改为先清除令牌并进入新代次；接入 `storage` 监听并在请求时核对令牌归属；过期代次结果改为不交付，旧会话错误不触发 Toast、导航与入口恢复。

### PR28：登录会话级同步协调器与兜底校验

- **依赖：** 无（PR17、PR27 已交付）。
- **范围：** 登录后启动，收到变更通知即集中失效对应 resourceKeys，同一 key 按短窗口防抖合并；通知不影响阅读水位。实现每 30 秒的 `GetSyncHeads` 兜底校验及前台、网络恢复、重连触发。移动端外壳由 PR33 接入；本条不删除轮询。
- **落点：** 新增 `frontend/src/features/session/sync-coordinator.ts` 与 `resource-refresher.ts`；`features/session/use-realtime-connection.ts` 装配协调器；`api/session.ts` 与 `api/index.ts` 导出 `getSyncHeads`；`hooks/resource-keys.ts` 中按会话划分的 key 允许省略会话编号作为失效前缀；新增 `frontend/tests/sync-coordinator.test.ts` 与 `resource-refresher.test.ts`。
- **实施：**
  - **通知映射：** `conversation_changed` 失效收件箱列表、列表行、提醒总数、最近会话，以及该会话的摘要、消息首页与增量页、提及进度与队列、群资料、投递与引用状态和已有单聊查找；`conversation_state_changed` 失效收件箱类查询与该会话的摘要、提及进度与队列；`conversation_removed` 失效收件箱类查询、检索结果与该会话摘要，由摘要重读确认失权并沿用既有资源清理；`identity_profile_changed` 失效身份。收件箱失效经列表 Hook 已有的缓存订阅进入列表控制器的串行重读。
  - **合并窗口：** 待失效 key 按内容去重，首个 key 登记后 300ms 统一失效，窗口不因后续通知顺延，持续热点按窗口分批收敛；同批已被较短前缀覆盖的 key 不重复失效。`ResourceRefresher` 先标记匹配查询失效，再只重读挂载中的查询，未挂载的查询打开时读取最新数据。同一查询串行读取：在途读取（含其他入口发起的读取）不取消，结果照常写入缓存，读取期间的失效合并为结束后的一次补读；补读等待查询自身读取结束，断网暂停的读取在网络恢复并完成后才补读，持续热点下每次读取结果都能生效；读取失败的查询保持错误状态并登记，在下一次合并窗口结束时重试，查询卸载或移出缓存后不再补读和重试。
  - **兜底校验：** 协调器启动时立即、之后每 30 秒无条件调用 `GetSyncHeads`，回到前台与网络恢复时额外调用；`server_hello` 携带的探针值与探针共用上次返回值，重连由问候完成校验。会话数量或校验和变化时按前缀失效全部会话类查询与检索结果，身份资料版本变化时失效身份，首个探针值按不一致处理；探针或问候成功后即使探针值一致也开启合并窗口，用于重试失败的同步读取。探针串行执行，在途时的再次请求于结束后补读一次，早于已应用结果发起的读取直接丢弃；读取失败交给会话恢复入口，其他错误记录 `WARN` 并等待下一周期。
  - **生命周期：** 协调器随工作台外壳在身份就绪后创建，与事件流同时启停；工作台按登录会话代次重新挂载，账号切换时销毁旧协调器，待合并失效、计时器与在途探针结果一并丢弃。协调器不保存实体版本、不持久化游标，也不修改查询缓存中的业务 DTO。
- **验收：** 离开消息页仍同步；空页、持续热点、读取失败和乱序通知正确收敛；本人写入回显的通知至多再触发一次重读；不持久化游标冒充离线库，不逐条通知手工修补 Query DTO；断开事件流后仅靠兜底校验也能收敛。
- **验证记录：** 2026-09-15 通过 `wails3 task test:frontend`（141 项，其中新增 16 项）与 `wails3 task common:build:frontend`（含 tsc）。新增前端测试覆盖：合并窗口内三条通知只失效一次；持续通知按固定窗口分批失效；各通知种类的 key 映射；同批前缀覆盖；首个探针值重读、相同值不重读、校验和与身份资料版本分别失效对应资源；问候与探针共用上次返回值；探针在途时只补读一次且旧读取不覆盖新值；探针失败交给错误入口并在下一周期继续；销毁后丢弃待合并失效、在途探针结果与后续通知；会话探针不一致时失效检索结果而会话变更通知不失效；探针与问候成功后重试失败读取；真实 QueryClient 下读取在途时的持续失效不取消读取、每次结果写入缓存并只补读一次，其他入口的在途读取结束后补读，断网暂停的读取不反复补读且网络恢复并完成后只补读一次，失败查询在重试时重读，未挂载查询只标记失效，卸载后不补读也不重试。
  - **Web 端：** 以本机 `wails3 task run:server`（127.0.0.1:8081，HTTP/1.1）与无头 Chrome（CDP 驱动，按绑定方法编号记录调用）实测，页面以 ai.shellphy 登录。启动时首个探针与 `server_hello` 合并为一轮重读（`LoadInbox`、`LoadIdentity`、`ReadInboxWindow`、`ReadInboxConversations`）。停留在个人资料页时，同账号另一登录会话经 `PATCH /api/profile` 改名，308ms 后界面用户菜单显示新名称，其间只有一次 `LoadIdentity`。回到消息页后 jack 在 20ms 内连续发送三条单聊消息，发送完成后 463ms 列表出现最后一条，该轮不在 3 秒轮询节拍上且只有一次 `LoadInbox`。屏蔽 `/api/realtime` 并重新加载页面后，事件流持续退避重连失败；此时改名，27.9 秒后由周期 `GetSyncHeads` 发现身份资料版本变化，约 300ms 后 `LoadIdentity` 更新界面。审核修订后按同一脚本重跑三个场景，结果一致（310ms、462ms、27.9 秒）。验证结束后恢复原显示名。
  - **未验证：** 桌面端与移动端界面实测（桌面端协调器与 Web 端共用前端实现，探针经既有 API Proxy 调用；移动端由 PR33 接入）、他端移出群后的失权清理、读取失败后的重试恢复、多标签页账号切换下的协调器重建（由会话代次与协调器销毁测试覆盖）。验证结束后本轮启动的服务端与无头 Chrome 均已退出，8081、9333 端口已释放。
- **审核处理：** codex 与 Pi CLI 审核后修订：通知失效不再取消在途读取，改由 `ResourceRefresher` 按查询串行补读，修复持续通知下慢读取结果始终被丢弃；同步读取失败的查询在后续合并窗口重试，修复身份资料重读失败后相同探针值不再失效导致无法恢复；会话探针不一致时补充失效检索结果，覆盖失权通知丢失后仍在展示的检索内容。类型导入改用 `@/` 别名，运行时依赖保留 node 测试可直接加载的相对路径。第二轮审核后补读改为等待查询自身的读取结束，修复 `refetchQueries` 对断网暂停的查询立即返回、补读形成无限微任务循环而卡死页面。

### PR29：消息窗口接入实时同步

- **依赖：** 无（PR15、PR28 已交付）。
- **范围：** 用版本驱动权威窗口重读，消除初始 Query 更新而 page 副本不变的问题；最新窗口补连续页，锚点窗口不拼远端尾部但处理资料、删除与失权。收到推送不自动已读；移除对应的成员消息和提及高频轮询。
- **落点：** `use-conversation-timeline.ts`、`conversation-window.ts`、`use-conversation-viewport.ts`、提及与阅读 hooks。
- **实施：** 以窗口模式 latest／anchor 和 queryGeneration 组织读取，权威 Query 更新必须能替换对应窗口内容；合并消息页不等于保留已删除实体。最新模式连续补到目标，锚点模式只重读当前窗口与引用依赖，远端尾部更新留提示。阅读仍由可见消息判定。
- **验收：** 上翻、引用跳转、窗口外原文删除、并发发送、过期页响应和连续提及查看稳定；Wails 调用仅忽略过期结果，不取消业务执行。
- **验证步骤：** 停在引用跳转窗口，同时发生新消息和原文删除：不跳尾部、引用失效、最新提示出现；连续提及导航后返回最新不丢消息；旧窗口响应晚到不能覆盖新会话。

- **范围确认（2026-09-15）：** 已加载窗口的重读由服务端新增闭区间读取承担，前端不按页回放拼接窗口。移动端同样使用 `ConversationTimeline`，在 PR33 接入前保留三秒轮询。会话摘要、群资料与列表轮询不在本条范围，留给 PR30。
- **实现记录：** 服务端新增 `ReadConversationMessageWindow`（`GET /conversations/:conversationID/message-window`），`ConversationMessageHistoryInput` 增加 `Start`／`End`，按 `message_seq` 读取包含两端的连续范围且不设条数上限；范围内消息全部删除时保留请求的两端，`HasEarlier`／`HasLater` 仍按边界判断，范围读取不与 before／after／around 组合。前端新增 `ConversationWindowController`（`conversation-window.ts`）串行调度：重读与补页排队执行，打开新窗口丢弃早于它的结果，定位期间完成的重读等定位结束后按窗口是否被替换决定合入；最新模式重读后连续补页到尾端，锚点模式只重读当前范围并在到达尾端时回到最新模式；重读结果与当前窗口一致时保留原窗口引用。`use-conversation-timeline.ts` 改为把窗口重读登记为挂载中的查询，由 PR28 的失效、重试与前台轮询共用同一入口，删除按 `after` 游标的增量轮询和本地 page 副本维护。视口新增 `keepReadingPosition`，重读合入前保存首个可见消息偏移，跟随最新时继续贴底。引用原文失效改为重读窗口，不再在前端手工标记。新增 `contexts/realtime-sync-context.ts`：Web／桌面工作台外壳声明实时同步可用，据此关闭成员消息、提及进度与队列、Telegram 投递与引用状态的轮询，移动端无此上下文继续轮询。`resourceKeys.conversationMessages` 改为按会话加页面实例参数，群资料操作的精确失效改为前缀失效。
- **验证记录（2026-09-15）：** `wails3 task test:server` 通过，新增 `TestConversationMessageWindowRange` 覆盖按首尾读取完整范围、删除首尾后边界收缩、范围内全部删除保留原游标、整段范围无更早更晚消息，以及首尾颠倒、只给一端、与其他游标组合和无权会话四类拒绝。前端 `wails3 task test:frontend` 149 项通过，新增 8 项窗口调度用例覆盖首次读取最新页、按首尾重读替换内容、连续补页到尾端、锚点范围重读与回到最新、补页在途时多次重读合并且按扩展后边界读取、打开新窗口丢弃旧结果、取消定位后仍合入重读结果、内容不变时保留原窗口、读取失败后下次重读恢复。`wails3 task common:build:frontend`（含 tsc）通过，绑定由 `wails3 generate bindings` 重新生成。界面验证由 Codex CLI 在 Web 与桌面端执行：历史分页、引用定位、双账号实时收消息、阅读位置保持与返回最新均正常。
- **未验证：** iOS 与 Android 真机；窗口内原消息删除只有服务端集成测试用 SQL 构造数据覆盖，产品当前没有删除消息入口。
- **审核处理：** Codex 审核发现 `useConversationMentionNavigation` 把 `useRealtimeSyncActive()` 写在 `&&` 短路右侧，页面失焦时该 Hook 不执行并在桌面端切换前后台时触发 React Hook 顺序错误，改为无条件调用后再参与轮询判断。

### PR30：列表实时更新与可变排序补全

- **依赖：** 无（PR14、PR28 已交付）。
- **范围：** 延续已交付的三端自动排序：顶部跟随最新，深处应用新项与重排并保留原邻域，仅交互期间延后布局。向上浏览自动取得最新会话，无刷新、查看最新或待更新提示。keyset 继续原边界，通知发现未加载却已上浮的会话，按会话 ID 去重；移除列表与群资料高频轮询。
- **落点：** 已交付的列表控制器、PR28 同步协调器、按 ID 批量摘要 Query；替换 inbox 页面轮询消费者。
- **实施：** 通知提供「哪些会话变化」，按 ID 取得当前资格和排序键，失效并重读权威连续窗口；普通 keyset 仍按原边界继续补历史，不改成最新边界。交互结束自动应用窗口顺序，当前锚点上浮时恢复原后继、前驱，回到顶部无需额外点击。
- **验收：** C90 在读取旧页期间上浮仍可发现；资格移除立即修复、真实失权立即清除；旧 generation 页面不接新窗口；`hasMore=false` 后仍可发现新项；刷新失败不换整棵 DOM。
- **验证步骤：** 初始显示 C1–C50，未加载的 C90 收到消息并排到首位；读取 C51 之后的页不会再返回 C90，但通知必须发现它。深处保持原邻域，向上滚动到顶部后 C90 只出现一次；`hasMore=false` 后新增会话也可发现。

- **范围确认（2026-09-15）：** 本条只改前端，服务端通知、批量资格 Query 与窗口读取均由 PR14、PR20、PR28 交付。客户会话 Copilot 线程列表轮询属于其自身功能，不在本条范围。移动端继续轮询，由 PR33 接入。
- **实现记录：** `InboxListController.request` 修正变更重读的合并语义：原实现在任何读取在途或排队时直接丢弃 `poll`，轮询时代由下一拍补上，改为通知驱动后会把补页期间到达的变更一直丢到兜底探针周期。现在 `poll` 只与尚未开始的 `poll` 或 `refresh` 合并，在途读取结束后必定补读一次，其余操作的合并规则不变。`use-inbox-list.ts` 按 PR29 的消息窗口做法，把列表窗口重读登记为挂载中的查询（key 为带页面实例标识的收件箱 key），首次读取、同步失效、失败重试与前台轮询共用该入口，删除原先监听 Query 缓存失效事件的订阅与独立定时器；定时重读只在未接入实时同步的外壳保留。`use-conversation-summary.ts` 与 `conversation-main.tsx` 的摘要、群资料轮询按 `useRealtimeSyncActive()` 关闭，失权检测改由 `conversation_removed` 失效摘要后重读为 `null` 触发。控制器读取失败时该查询同样失败，由 `ResourceRefresher` 在下一次合并窗口重试；`drain` 删除错误状态下跳过 `poll` 的规则，失败后到达的变更通知继续重读，`retry` 统一重试上次失败的操作。未接入实时同步的外壳回到前台时立即重读一次当前窗口。
- **验证记录（2026-09-15）：** `wails3 task test:frontend` 155 项通过。控制器新增 4 项：补页读取期间上浮的未加载会话只能由随后的变更通知发现、尚未开始的重读合并重复通知而在途读取不吞掉新通知、列表加载到尾端后新增会话仍可经通知发现、读取失败后到达的变更通知继续重读并恢复窗口；这些用例在回退对应的控制器修改后确实失败。新增 `inbox-list-sync.test.ts` 用真实 `QueryClient`、`ResourceRefresher` 与控制器装配同步查询，读取结果取发起时刻的顺序，断言在途读取期间到达的失效不被缓存层吞掉，以及读取失败登记为查询失败后由失败重试恢复；回退失败传递或错误态跳过规则，对应用例即失败。`wails3 task common:build:frontend`（含 tsc）通过，`frontend/bindings` 未被改写。界面验证由 Codex CLI 在 Web 端用双账号执行两轮：第一轮针对缓存失效事件接线，第二轮针对最终的挂载查询接线，六项全部通过——首屏加载约 0.2 秒无空白；单聊上浮并加未读约 0.32 秒；连发三条约 0.82 秒收敛到第三条且三条均可见；群改名约 0.80 秒、移出群约 0.83 秒；标签页确认进入 `hidden` 停留 35 秒后切回约 0.07 秒已是最新；切换客户、内部、全部筛选约 0.14～0.22 秒重新加载。列表停在中段、会话停在历史位置时收到新消息，两处可见位置均保持并显示新消息提示。耗时含界面操作与采样开销，非网络延迟。
- **已确认的预期现象：** 首次加载后有一次同参数的串行重读，来自同步协调器启动时把首个探针值按不一致处理。被移出群的一方收到「会话不可用」的 422，是摘要重读确认失权的既有路径；登录前的 422 是未登录的身份探测。全程无未捕获的 JavaScript 异常。
- **未验证：** 移动端（仍走轮询，由 PR33 接入）；桌面端 WebView 未单独复验，与 Web 共用同一套列表实现。

### PR32：网站挂件实时与多线程恢复

- **依赖：** 无（PR26 已交付）。
- **范围：** 在现有 `chat.js` 内建立 PR26 访客事件流，收到目录或会话通知、重连成功时调用现有目录与消息拉取逻辑；已有身份时初始化即连接，无身份不连接，首条有效消息建立身份后再连接；替换业务消息轮询。
- **落点：** `internal/publicweb/chat.js`、网站目录与窗口 HTTP 接口。
- **实施：** 状态分为无身份、已连接、退避重连；事件流出错时关闭并按抖动退避重建，不依赖 `EventSource` 的自动重连；重连成功与页面回到前台时重新拉取目录与当前线程窗口。访客同步使用挂件自身状态，不依赖成员登录协调器；其他标签页建立的身份在本页刷新后生效。
- **验收：** 新线程发现、离线补拉、发送响应乱序、引用窗口和渠道停用均收敛；初始化不创建业务记录，预览不连真实服务。
- **验证步骤：** 同一访客打开 A、B 两个已有身份的挂件，断线期间 A 新建第二线程，B 恢复后列出两条且不制造新联系人；空白页反复初始化不建立连接，也不创建业务记录。

- **实现记录：** 事件流用携带 `X-Cervi-Visitor-Token` 请求头的 fetch 流式读取 `GET /api/public/website-channels/:channelID/realtime`，不使用 `EventSource`：挂件在第三方站点的 iframe 内运行，Cookie 可能被浏览器拦截，而挂件初始化后本就持有访客 Token，与 `requestWebsiteJSON` 共用同一条凭据。读取按行切分并保留跨数据块的半行，只处理 `data: ` 行，25 秒心跳刷新空闲计时，60 秒无数据按网络错误结束。连接状态为 idle、connecting、ready、backoff、stopped，每次连接尝试带序号，只接收本次尝试的回调；流结束按上限 1s×2ⁿ（最高 30s）的一半到上限之间抖动重连，响应状态码小于 500 的拒绝（渠道停用、身份缺失、Token 非法）直接进入 stopped 不再重连，协议主版本不符同样停止。身份以「目录中存在线程」判断：初始化后有线程即连接，空白页只读取不连接，首条消息写入成功后再连接。通知处理复用既有拉取逻辑——`conversation_changed` 触发线程目录重新拉取，命中当前打开且已加载的线程时按 `after` 游标补拉增量消息；`visitor_hello` 同时触发两者，覆盖初始化读取与订阅生效之间提交的变更。目录重新拉取改用 PR26 的 `GET /conversations` 公开路由（原先只有初始化读取该投影），在途时结束后补拉一次；会话增量补拉同样在途合并，历史加载期间到达的变更在加载结束后补拉。拉取失败按同一抖动退避重试直到成功，事件流已停止时不再重试，断网期间到达的通知据此收敛。页面回到前台、挂件重新显示与网络恢复跳过剩余退避并重新拉取目录与当前窗口，页面卸载结束事件流。删除三秒消息轮询及其定时器、轮询目标判定与会话上的 `polling`／`pollSeq` 字段，会话字段改为 `refreshing`／`refreshSeq`／`refreshPending`。
- **验证记录（2026-09-16）：** `wails3 task test:server` 全量通过，`wails3 task build:server` 通过。界面验证用无头 Chrome 经 DevTools 协议驱动真实挂件页面 `/chat/<channelID>` 与真实服务端执行，连续三轮结果一致，13 项全部通过：空白页连续三次初始化不发起任何 `/realtime` 请求且不创建渠道身份；首条消息后恰好建立一条事件流并新增一个渠道身份；客服经成员接口回复后访客页面实时显示，全程消息类请求 3 次（旧实现按三秒轮询在同样时长内约 15 次以上）；本人发送与自身变更通知竞争后消息节点无重复；客服带引用回复后点击引用可定位并高亮原消息；同一访客的第二个页面在 CDP 断网期间收不到 A 新建的第二线程，恢复网络后线程目录补拉出两条且未新增联系人；渠道停用后事件流结束并只重连一次即停止，停用后不再重试拉取；管理端预览页不请求任何真实访客接口。验证期间发现并修复两处收敛缺口：通知触发的拉取在断网时失败后无人重试，以及事件流已停止后拉取重试仍在继续。审核修正后补充四项并全部通过（17 项）：通知先于发送响应返回时消息照常显示、输入框清空且线程数不变；线程切走期间积压 60 条后切回，一次补拉沿游标读到尾端并渲染 63 条（修复前停在 53 条）；历史加载期间回到前台产生一次加载结束后的增量补拉（修复前为 0 次）；后台标签页的前台事件不产生任何请求。三条问题均先在修复前的实现上复现失败，再在修复后通过。
- **审核修正（2026-09-16）：** 三处收敛问题按审核意见修复，每处都先在修复前实现上复现。其一，目录刷新先于发送响应返回时，`upsertRealConversation` 返回目录建立的占位对象而丢弃发送中的会话，页面表现为发送成功但消息不显示、输入框不清空、`activeConversation.id` 仍为空，继续发送会另建线程；现在占位对象不是当前打开会话时由发送中的会话接管该线程，保留其消息节点与草稿。其二，服务端按 50 条一页返回且向后方向不带 `hasMore`，单次补拉在积压超过一页时停在第 50 条且没有后续触发；现在本页返回消息即继续沿游标补拉，直到一次返回空结果。其三，历史加载期间到达的问候或前台事件被直接丢弃，历史查询之后、订阅生效之前提交的消息因此既不在历史响应中也没有后续通知；现在历史加载中的补拉同样登记为待补读，在加载结束后执行，打开会话时的首次加载不因此多发请求。另外按审核意见在问候、回到前台与结束事件流时取消待重试的拉取，并把退避计数改为只在没有待重试时归零，避免目录成功掩盖消息拉取仍在失败。
- **已知边界：** 其他标签页新建立的访客身份在本页刷新后才生效（跨标签页身份发现按第三轮调整推迟）；挂件收起时事件流仍在，通知照常刷新目录，只有前台事件驱动的补拉按可见性跳过；访客线程超过 20 条时最旧的线程被移出页面状态，该上限是挂件原有行为。
- **未验证：** iOS 与 Android 移动浏览器中的挂件事件流；访客侧 AI 逐字输出由 PR39 决定，当前沿用挂件既有的正在输入提示。

### PR33：移动端实时接入

- **依赖：** 无（PR29、PR30 已交付）。移动端接入后由 `RealtimeSyncProvider` 声明实时同步可用，消息窗口、提及、列表与会话摘要不再轮询。
- **范围：** 移动登录外壳接同一连接与同步内核，列表、单聊及已有群聊使用统一实时输入；恢复前台先做兜底校验，再按独立阅读规则工作。保留当前移动功能边界，不顺带开启建群或已读交互。
- **落点：** `frontend/src/apps/mobile` 登录外壳、mobile-inbox-page、mobile-individual-conversation-page、mobile-group-conversation-page。
- **实施：** 应用前台事件只触发同一协调器的兜底校验，不由每个页面各建连接；系统挂起后旧连接一律按可能失活处理。移动页面订阅共用版本输入，保持自身详情导航、阅读能力和草稿行为。桌面端开发模式的 mobile-preview 小窗与主窗口共用同一个 Go 后端，而 Go 侧只保留一条当前事件流，接入时先明确两个窗口的连接归属。
- **验收：** iOS／Android 前后台、旋转、详情返回、连接重建与旧响应隔离；完成对应回归后移除移动三秒轮询。
- **验证步骤：** 系统挂起期间另一端发送、已读、移除群成员，恢复后列表与详情收敛；快速返回再进另一会话，无重复连接、旧内容闪入或错误已读。iOS 与 Android 分别记录实际服务端地址拼接、TLS 结果与长响应事件的实时下发情况。

- **范围确认（2026-09-15）：** 本条只接入移动端外壳与移动端自有轮询，不改服务端通知、业务接口与迁移，不顺带开启移动端建群外的新交互。桌面开发模式的主窗口与移动端预览窗共用同一个 Go 后端，接入前先确定两个窗口的事件流归属。
- **实现记录：** `internal/apiproxy/realtime.go` 的 `realtimeClient` 由持有唯一成员事件流改为按前端窗口持有实时通道，`windowStreams` 保存该窗口的成员事件流、运行过程流与通道代次：`start` 登记新成员事件流并结束该窗口原有的通道，`disconnect(connectionID)` 按连接编号结束它所属窗口的通道，新增 `disconnectAll()` 供登录、登出与切换企业服务器使用，`startRun` 与 `disconnectRun` 在所属窗口的通道内登记和注销，接收协程结束时只删除自身登记。窗口标识由 `application_services_native.go` 从 Wails 绑定调用上下文的 `application.WindowKey` 解析，注入为 `NewBackend` 的 `caller` 参数；窗口刷新销毁 JavaScript 上下文、前端清理来不及执行时，下一次连接据此结束原有通道，无法识别窗口的调用归入同一条通道，沿用 PR38 的单条成员事件流语义。`RealtimeConnector.DisconnectRealtime` 与 `Service.DisconnectRealtime` 增加连接编号参数，绑定重新生成；`frontend/src/api/realtime/native-transport.ts` 的 `disconnect` 按编号断开。`RealtimeClient` 增加 `restart()`，只替换处于 ready 的连接，连接中、退避中与已停止的状态直接返回；`useRealtimeConnection` 增加 `restartOnResume` 选项，回到前台时先重建事件流再跳过退避并执行兜底校验，网络恢复只跳过退避并校验，回到前台与网络恢复成对触发时因此只建立一条新连接。`MobileWorkspaceLayout` 在身份就绪后调用 `useRealtimeConnection(..., { restartOnResume: true })` 并包裹 `RealtimeSyncProvider`，移动端消息窗口、列表、会话摘要、提及与群资料因此共用已交付的实时输入；移动端自有的页签提醒读取与群资料读取按 `useRealtimeSyncActive()` 关闭定时重读，群聊页恢复前台的一次性群状态校验同样只在未接入实时同步时执行。`SyncCoordinator` 处理 `conversation_removed` 与 `conversation_state_changed` 时补充失效该会话的群资料：移动端群聊页以群资料读取为失权判据，群资料同时携带本人免打扰状态，只失效摘要无法让群聊页及时发现失权，也无法让另一端修改的免打扰状态及时生效。
- **验证记录（2026-09-15）：** `wails3 task test:frontend` 155 项通过（新增 `conversation_removed` 失效群资料的断言）；`wails3 task test:desktop`、`wails3 task test:server` 全绿；`internal/apiproxy` 新增三项：两个窗口的事件流各自投递本连接事件且断开一个窗口不影响另一个、同一窗口重新连接关闭原有事件流、无法识别窗口时归入同一条通道；`wails3 task common:build:frontend`（含 tsc）通过，`frontend/bindings` 只有 `DisconnectRealtime` 一处预期变更。界面验证在 `wails3 task dev:mcp` 的移动端预览窗经 Wails MCP 执行，另一账号用 HTTP 接口发起变更：主窗口与预览窗同时建立两条事件流且互不取消；单聊新消息使列表上浮并出现头像未读角标、「内部」红点与消息页签角标；会话详情内新消息约 0.55 秒出现；窗口隐藏使 `visibilityState` 变为 hidden，隐藏期间发送的消息在窗口恢复后约 0.30 秒出现，同一时刻日志显示预览窗旧事件流结束、新事件流建立各一次，主窗口事件流不受影响；新建群聊实时出现在列表；移出群后约 0.32 秒退出群详情、提示「该群聊不存在或你已不在群内。」并从列表移除。轮询移除经页面内 fetch 计数确认：停留在列表页静置 12 秒无任何业务调用，随后一次界面操作立即产生 3 次 Wails 绑定调用，证明计数有效。验证期间服务端曾中断，客户端按退避重连并在服务端恢复后自动接上。耗时含界面采样开销，非网络延迟。
- **审核修正后的复验（2026-09-15）：** 两条事件流分别记录 `owner=1`（主窗口）与 `owner=2`（移动端预览窗）；刷新预览窗后日志显示关闭 `owner=2` 的原有事件流、建立新的 `owner=2` 事件流，主窗口事件流不受影响，窗口刷新不再留下并存的旧事件流。同一账号的另一登录会话开启与关闭群免打扰，移动端群聊页标题的静音图标分别在约 0.58 秒与 0.56 秒内跟随变化。再次执行窗口隐藏与恢复，事件流建立与结束各增加一次，挂起期间发送的消息在返回列表后已排到首位并带未读角标与页签角标。`wails3 task test:frontend` 158 项通过，新增的 `restart()` 用例在回退实现后确实失败；`wails3 task test:desktop`、`wails3 task test:server` 与 `wails3 task common:build:frontend` 全部通过。
- **已确认的预期现象：** 桌面开发模式下主窗口与移动端预览窗各建立一条事件流，等价于同一账号的两台设备，两窗口互不影响。
- **已知边界：** 窗口关闭后前端清理未执行时，该窗口的通道保留到应用退出或服务端一小时上限；窗口重新打开并连接时按窗口标识结束它。
- **与 PR38 的关系：** 本条变基到 PR38 之后，成员事件流的窗口归属与 PR38 的运行过程流、通道代次合并为按窗口的实时通道，`ConnectAgentRunStream` 登记到发起窗口的通道，`DisconnectRealtime` 结束该窗口的成员事件流与运行过程流，整体语义与 PR38 在单窗口下一致。变基后复验：两个窗口分别建立 `window=1`、`window=2` 的通道，刷新预览窗关闭该窗口原有事件流并重建、主窗口不受影响；移动端向 AI 员工发消息后运行过程流以 `window=2` 建立，思考过程与用量正常展示；另一账号发送的消息约 0.82 秒到达列表。`wails3 task test:frontend` 167 项、`test:desktop`、`test:server` 与 `common:build:frontend` 在新基线上全部通过。
- **未验证：** iOS 与 Android 真机的服务端地址拼接、TLS 结果与长响应事件下发；移动端搜索页、客户会话与独立 AI 聊天未单独复验，它们与已验证页面共用同一套消息窗口与摘要实现。

### PR37：模型增量消费与流基线

- **依赖：** 无。运行时增量不依赖 PR36 的过程详情拆分，先于 PR36 交付。
- **范围：** Runtime 真正消费 token 与工具进度，使用 runId、attempt、streamId、sequence 与稳定块编号，短周期合并；暴露当前内存快照和临时发布接口，不直接依赖传输实现。
- **落点：** `internal/integration/agentruntime/eino.go`、`progress.go`、`stream.go`、`attachment.go`、`types.go`，`internal/actions/agentrun/stream.go`、`execute.go`、`cancellation.go`。
- **实施：** 将整段输出收集点替换为运行时增量消费；每个 attempt 创建独立 streamId，块有稳定 blockId，同流 sequence 单调。内存快照与最高 sequence 同步捕获，发布端按短周期合并可重建增量，模型执行不等待浏览器消费。运行流中的工具调用只含名称、状态和起止时间，完整参数与结果经过程详情读取。增量携带起止序号，首尾相接的增量可合并，供 PR38 发送队列合并同一 Run 的待发增量；订阅在同一把锁内返回快照并按序回调增量，背压由连接发送队列承担，执行尝试退出时回调结束。
- **验收：** 同流重复／乱序、gap、尝试切换、工具并行与输入抢占正确；token 不写 Message、不推进会话版本、不发布变更通知；限速发送不阻塞模型执行。
- **验证步骤：** 分片产生文本、工具参数、工具结果并模拟并行工具，块顺序稳定；故意漏一片后快照可恢复全文；重试产生新 streamId，旧尝试尾部不能拼入新正文。

- **实现记录：** TurnLoop 开启 `EnableStreaming`，`processRecorder` 实现 `WrapModel`，按 `StreamingMeta.Index` 累积思考、正文和工具调用并分配稳定块编号；`AfterModelRewriteState` 按完整输出定稿并沿用编号，块顺序与流式阶段不一致时整体替换并记录 `WARN`，`agent_run_blocks` 结构不变。`StreamDelta` 携带 `runId`、`attempt`、`streamId` 与起止序号 `baseSequence`／`sequence`，操作包括写入块、追加块文本、移除块、追加或清空候选正文与重置；`StreamSnapshot.Apply` 处理重复、缺口和换流，`MergeStreamDeltas` 合并首尾相接的增量。发布协程约 50ms 合并一次，模型执行只写内存队列。`ExecuteAction.SubscribeRunStream(runID, onDelta, onEnd)` 在 `runningMu` 内取当前尝试、返回深拷贝快照并登记回调，之后在锁内按序推送增量，执行尝试退出或增量无法应用时回调结束，替换原 `Progress(runID)`。携带直传附件的模型调用在读取流式分片时失败，同样去掉多模态内容重试一次。
- **确认项：** 运行时增量先于 PR36 交付；WebSocket 帧与订阅授权由 PR38 实现。合入第三轮调整后订阅不自带缓冲与溢出关闭，积压由 PR38 的连接发送队列承担。
- **验证记录（2026-09-15）：** `wails3 task test:server` 通过；`agentruntime`、`agentrun` 单测在 `-race` 下重复运行通过，覆盖真实分片流式发布与定稿编号稳定、多段正文加工具的编号对应、安全点丢弃未完成调用及其运行流操作、增量重复／缺口／换流与合并等价、漏收增量后重订阅恢复全文、首尾相接增量合并与逐条应用等价、重试换流后旧尝试尾部不进入新流、附件建流失败与读分片失败两种回退。
- **审核处理：** Pi CLI 审核后订阅在 `runningMu` 内取当前尝试，快照与增量深拷贝，候选正文按分片序号成块，定稿操作一次登记，增量无法应用时结束运行流，并补充 `WARN` 日志与测试。

### PR38：成员 AI 运行过程流与终态收敛

- **依赖：** 无（PR28、PR37 已交付）。按 runId 使用独立的运行过程流，与 PR25A 成员事件流共用请求头认证与事件协议版本；取代原清单 PR44、PR45 的焦点注册与跨节点快照方案。
- **范围：** 在实时事件契约中增加 `run_stream_snapshot`、`run_stream_delta`、`run_stream_ended`；展开运行过程时发起 `GET /api/realtime/runs/{runId}`，收起即关闭请求。服务端先按会话授权再挂接该 Run 的内存流，先发当前快照，之后发增量；仅数据库提交后发送 `run_stream_ended` 并结束响应，持久终态覆盖临时候选。
- **落点：** `internal/realtime` Gateway 的运行过程流处理与发送队列、`agentrun` 运行流订阅、实时事件契约与共用夹具、原生端 `apiproxy` 的运行过程流转发、`frontend/src/api/realtime`、成员 `agent-process.tsx` 与运行展示控制器。
- **实施：** 请求时校验登录会话有效、账号活跃与 Run 所属会话的阅读资格，失败返回与业务接口相同的错误体与状态码；登出与停用的撤销控制同样结束对应登录会话或用户的运行过程流。每条运行过程流一个写协程与一条有界发送队列，同一 Run 的待发增量合并，溢出时结束该流并记录 `WARN`。快照按块拆分为多个事件，工具完整参数与结果经 PR36 过程详情 Query 读取。原生端 Go 侧按 runId 持有运行过程流，事件经 Wails 事件交给 TS 并携带本地流编号区分新旧流。客户端按 streamId 与增量的起止序号判断重复与缺口，发送队列合并后的增量可跨越多个序号；gap 或流结束后按退避重新请求取新快照，不做单独的快照修复协议。客户端收到 `conversation_removed` 或经资格读取发现失去会话阅读资格时，关闭该会话的运行过程流并丢弃临时候选；失权通知丢失且客户端未关闭时，服务端最多推送到当前 Run 结束。终态由持久 Query 确认，`run_stream_ended` 只是加速提示；运行过程未展开时不发起请求。
- **边界：** 服务端当前为单进程内嵌 Worker Pool，Gateway 与执行在同一进程内定位 Run。拆分部署时再评估跨节点快照，本条不预建。
- **验收：** 长输出期间成员事件流的变更通知不受运行过程流积压影响，慢客户端溢出只结束运行过程流并经重新请求收敛；断线重连、失败、取消和人工接管不复活旧候选；`run_stream_ended` 丢失可由会话版本恢复；未展开的运行不产生请求；无权请求被拒绝，登出或停用结束对应运行过程流，移出群的失权通知送达后客户端关闭该流，通知丢失时客户端经兜底探针发现失权后关闭该流。
- **验证步骤：** 展开过程中持续输出，同时向该会话发送新消息，客户端经成员事件流收到变更通知；堵塞运行过程流读取触发队列溢出后该流结束、成员事件流继续工作，重新请求后不重复拼接已有 token；丢弃 `run_stream_ended` 后仅靠会话版本仍显示最终消息；取消后晚到的 delta 不能重新打开运行中状态；无权用户与已登出令牌请求该 runId 均得到错误响应，运行中登出后运行过程流结束；运行中移出群后客户端关闭该流；丢弃失权通知时，客户端经兜底探针发现失权后关闭该流，服务端最多推送到当前 Run 结束。

- **实现记录：** `internal/realtime/protocol` 增加 `run_stream_snapshot`、`run_stream_delta` 与 `run_stream_ended`，与成员事件流共用协议主版本、编解码和 Go／TypeScript 夹具，工具调用只携带名称、状态与起止时间。服务端新增 `GET /api/realtime/runs/{runId}`：先按运行所属会话的阅读资格授权，运行不存在与不可读对外是同一结果，受众订阅生效后再次校验登录会话与阅读资格。每条运行过程流一个写协程，快照按文本预算拆成多个分片事件并先于增量写出，待发增量按序号相接合并为一条，不相接或合并后超出文本上限时按慢消费者结束该流并记 `WARN`。运行过程流只加入本人用户受众，登出、账号停用与所属会话失权都结束该流；运行不在本进程执行时直接写出结束事件，由客户端按持久事实收敛。受众成员抽象为 `audienceStream`，成员事件流与运行过程流共用受众订阅，变更通知只发给成员事件流，运行过程流只响应撤销控制。原生端按运行编号并发持有多条过程流，事件携带运行编号供订阅方在取得本地流编号前区分；成员事件流断开时一并关闭该登录会话下的全部过程流，事件流登记按会话代次比较并丢弃建立期间已被整体断开的旧事件流。前端按 `streamId` 与增量起止序号判重判缺口，缺口或断开后退避重新取快照；`run_stream_ended` 只作加速提示，送达过展示状态才触发时间线重读，运行尚未在服务端开始时只重连不刷新，运行不可读取时停止请求。运行中的过程默认展开，限高滚动并保持贴底，停止入口始终可见，首个快照到达前不占位；Web、桌面端与移动端共用同一条链路。
- **验证记录（2026-09-15）：** 服务端、桌面端与前端 164 项测试全部通过。新增 `TestRealtimeGatewayRunStream`（真实 NATS 与数据库）覆盖无权与不存在返回 404、本进程无运行直接结束、快照分片与增量写出、所属会话失权结束；`AuthorizeAgentRunStreamQuery` 集成断言覆盖本人可读、未知运行、无资格成员与失败运行仍可订阅；`TestRealtimeDisconnectClosesRunStreams` 覆盖断开时清理全部运行过程流。浏览器实测：思考文本、工具调用状态与回复正文逐字显示；运行中停止入口始终可见可点；结束后切换为最终消息无空窗，其后 30 秒无反复刷新，控制台无 error。
- **与 PR33 的关系：** PR33 变基到本条之后，成员事件流的窗口归属与本条的运行过程流、通道代次合并为按窗口的实时通道，`ConnectAgentRunStream` 登记到发起窗口的通道，`DisconnectRealtime` 结束该窗口的成员事件流与运行过程流；变基后复验结论见 PR33 条目。
- **不在本次范围：** PR39 网站访客公开 AI 流按计划推迟。长回复在模型单次最大输出处截断且界面无提示，由 `agentMaxOutputTokens` 常量导致，属既有行为。

### PR34：实时未读与本地通知闭环

- **依赖：** 无（PR29、PR30 已交付）。
- **范围：** 同步确认的新 Message 接现有通知策略；静音、@、工作状态、全局／设备开关及可见上下文独立判断。通知去重；总数绑定权威版本，客户端不做 +1/-1 推算。
- **落点：** `features/notifications/new-message-notifications.ts`、`platform/notifications.ts`、当前通知偏好与 PR28 同步入口。
- **实施：** 以 messageId 去重，并区分 live 与 catchup／bootstrap 来源；一次通知覆盖多条消息时读取实际新增范围后逐条执行策略，不用最后预览代表全部。通知内容来自已授权消息 Query，旧账号 generation 立即失效。
- **验收：** 一次通知合并多条消息时不只看最后预览；冷启动与重连历史不逐条补弹，他端已读只更新状态；旧总数不覆盖新版本；客户未读不进入桌面总提醒。不承诺跨设备恰好一次或退出后推送。
- **验证步骤：** 一次通知包含普通消息和 @ 消息，静音场景仍按既有 @ 策略判断；重连追赶十条历史只更新未读，不连弹十次。

- **实现记录：** 新增 `frontend/src/features/notifications/new-message-watcher.ts`，按登录会话持有 `NewMessageWatcher`：`server_hello` 到达时读取一次收件箱首页作为各会话末条基线，基线建立前的变更只登记位置不投递，冷启动与重连的历史因此不逐条补弹；`conversation_changed` 进入 300 毫秒合并窗口，窗口内同一会话只处理一次，基线读取与会话处理在同一条串行队列上执行。处理一个会话时先读该会话的权威行，失权（读取返回不存在）即丢弃基线；末条未变或未读为 0 只更新状态；静音的单聊、AI 聊天与客户会话直接跳过，静音群聊改按 `ListPendingConversationMentions` 的待查看提及过滤，只提醒 @ 本人的消息。实际新增范围取权威消息页中上次已知末条之后的部分并按未读条数截断，基线缺失或已被翻页移出时只按最新一条判断；逐条排除系统事件、本人在另一端发送的消息、空正文的运行状态消息与已投递过的 messageId（上限 500 条的先进先出集合）。`use-new-message-notifications.ts` 把权威读取、通知文案与投递接到观察器：标题取会话显示名，群聊正文为「发送者：预览」，附件取文件名，投递仍复用既有的工作状态、全局与本机开关、权限和可见上下文判断。通知投递函数是 `useEffectEvent`，不进入依赖数组，否则每次渲染都会重建观察器并丢失基线与去重集合。提醒总数改由 `resourceKeys.inboxAttention` 的权威查询提供：Web、桌面端工作台与移动端页签都直接读取 `loadInbox({ limit: 1 }).attentionUnreadCount`，同步协调器已在每次会话变化时失效该 key；删除收件箱列表控制器的 `unread` 端口、工作台的未读快照修订号与移动端「列表上报与页签读取取较新者」的合并逻辑，未读总数只剩一个所有者。
- **验证记录（2026-09-16）：** `wails3 task test:frontend` 173 项通过，其中 `frontend/tests/new-message-watcher.test.ts` 六项覆盖一次窗口内多条新消息逐条判断、重复不再投递、静音群聊只提醒提及、静音单聊不提醒、重连追赶只更新基线、他端已读与本人消息不提醒、基线缺失只按最新一条、失权会话丢弃基线，以及订阅时事件流已建立而没有问候事件的首次处理；`wails3 task test:desktop`、`wails3 task common:build:frontend` 与 `wails3 task build:server` 通过，本 PR 不含 Go 改动。界面验证用无头 Chrome 经 DevTools 协议驱动真实 Web 构建与真实服务端，九项全部通过：工作台载入；群聊连续到达的普通消息与 @ 消息各产生一条通知且正文分别正确；稳定后不再重复投递；断线期间积压十条历史并重启服务端强制事件流重建后，重连不产生任何通知；重连之后的新消息照常提醒一条；停留在设置页时新消息仍然提醒；群聊静音后普通消息不提醒；静音群聊的 @ 消息仍然提醒；随后恢复未静音。桌面端经 `wails3 task dev:mcp` 的 Wails MCP 登录验证账号后隐藏窗口，另一账号发送单聊消息，macOS 通知横幅显示标题「任杰」与该条正文。移动端预览窗在消息页停留时底部页签角标由权威查询提供，另一账号发送消息后提醒总数由 115 变为 116。
- **已知边界：** 事件流建立后到首个基线读取返回之间提交的消息只进入未读，不产生通知；本次基线之外（收件箱首页 50 条之外）的会话首次变化只按最新一条判断策略。移动端与桌面端的系统通知能力不在本条范围，移动端仍只更新应用内角标。
- **审核修正（2026-09-17）：** PR34 合并后的两份审核共指出五处问题，逐处先在修复前实现上用回归用例复现再修复。其一，新增范围先按 `unreadCount` 截断再过滤发送者，而服务端未读只统计他人发送的文本与运行失败消息：同批中夹有本人在另一端发送的消息或不计未读的附件消息时，靠前的他人消息被截掉且永久不再提醒；现在范围从已知末条与 `lastReadMessageId` 中靠后的一端开始，不再按未读条数截断。其二，重连只重置基线标记，在途的基线读取与会话处理仍按旧快照写入并投递，慢请求下重连会把断线期间的历史弹出来；现在 `server_hello` 推进连接代次并清空待处理会话，基线读取与会话处理都携带代次，过期结果一律丢弃，代次变化后重新排队基线读取。其三，会话行读取成功即推进基线，消息页或提及读取随后失败时该范围永久漏提醒；现在只有无需读消息页即可判定的路径（基线未建立、末条未变、无未读、静音的非群会话）立即推进基线，需要读消息页的路径在投递完成后才推进，失败时保留原位置由下一次事件重试，重复投递由 messageId 去重挡住。其四，桌面端同步提醒状态时把「未读确实清零」与「查询尚未重读仍是 0」混同，投递成功置位的待处理提醒会被过期的 0 立即清掉（Windows 托盘闪烁因此丢失，macOS 只用未读数显示 Dock 角标，本机观测不到）；现在只有提醒总数的快照不早于最近一次投递时才用 0 清除待处理提醒。其五，订阅时事件流已经建立且不会再有问候事件时，第一条变更被用于建立基线而不产生提醒；现在订阅时若事件流已就绪立即取一次基线。另外串行队列改为单个任务失败后继续接受后续任务。
- **审核修正后的验证（2026-09-17）：** `wails3 task test:frontend` 178 项通过，新增五项回归用例分别覆盖本人消息不挤掉同批他人消息、提醒范围按已读水位划定且不计未读的附件消息同样提醒、基线读取在途时重连按新代次重建、会话处理在途时重连不再投递、消息页读取失败后下次事件重新提醒该范围；逐项回退到修复前实现后都会失败。`wails3 task common:build:frontend` 通过。Web 端界面回归用同一套无头 Chrome 场景重跑，九项全部通过：群聊两条消息各一条通知、不重复投递、重启服务端强制重连后不补弹十条历史、重连后新消息照常提醒、设置页仍提醒、静音群普通消息不提醒而提及仍提醒。桌面端待处理提醒的修复在 macOS 上不可观测，只做了代码层修正。

### PR36：拆分运行摘要与过程详情

- **依赖：** 无（PR19 已合并，PR29 已交付）。
- **范围：** Run 摘要按 runId／agentIdentityId 返回集合，当前 UI 可选最相关项；消息只留过程引用，展开时取详情，移除每次消息查询携带完整 blocks 的实现。
- **落点：** `direct_backend_agent_process.go`、`types_agent_process.go`、`conversation/agent_process.go`、`features/inbox/agent-process.tsx`。
- **实施：** 列表与消息窗口只带轻量 Run 摘要和过程引用；独立过程 Query 按 runId 获取持久 blocks、工具参数结果和用量。当前 Run 集合与历史消息关联分开，失败通过结果消息定位，取消无消息也可选中查看；展开状态 key 用 runId。
- **验收：** 多 Run、无消息失败和历史过程均可读取；窗口更新不丢展开状态；运行详情按会话授权，保留工具完整参数、结果及用量。
- **验证步骤：** 同会话两个 Agent 各有 Run，刷新不会只剩「最近一个」；历史过程首次展开才请求完整 blocks，关闭再展开可用缓存；移出群后该 Query 不再返回详情。

- **实现记录：** 消息窗口原先只返回会话内最近一条运行（`created_at DESC LIMIT 1`），而 `AgentRunState` 对已由结果消息表达的终态运行不渲染，两条合起来使不写结果消息的取消提示丢失：`assignee_changed`（客户会话改派负责人）、`session_closed`（客户会话关闭）、`bot_changed`（Telegram 换机器人）、`agent_removed`（AI 员工被移出群聊）四种取消只能靠消息窗口底部那一行显示，AI 正在回复客户时改派负责人，新负责人的运行一起来就把取代的取消提示挤掉。`ConversationMessageList.latestAgentRun` 因此换成 `agentRuns`：仍在执行的运行，以及结束后会话再无新消息的取消运行，按创建顺序返回并各带 `agentIdentityId`，消息窗口底部按顺序渲染每一条；取代规则用最后一条消息的 `created_at` 判断，不用 `conversations.last_message_at`，后者存的是消息的 `originated_at`（应用或外部渠道时钟），与 `agent_runs.completed_at` 的数据库时钟不可比。过程内容此前只在成功事务中落库（`execute.go` 的 `complete`），失败和取消读不到中断前的思考与工具调用：`agentruntime` 的 `Run` 在返回错误时一并给出已积累的 `Usage` 和内容块；`recorder.blocks()` 只含中间块、回复正文保存在候选正文里，中断路径改用新增的 `partialBlocks()` 把尚未定稿的正文补成末尾的内容块；`ExecuteAction.persistPartialProcess` 在运行进入终态后写入内容块与用量并推进会话版本，已有内容或运行仍可继续时不写入，普通失败、运行被取消后返回的迟到结果、吸收后续输入时被抑制三条路径都经过它。`GetAgentRunProcessQuery` 放宽到已开始且已完成的运行，失败与主动停止从各自的结果消息展开，无结果消息的取消从底部状态行展开；只有确实产生过内容的运行才给出过程引用。新增迁移把 `agent_run_blocks` 的表注释从「成功 Agent 运行」改为「已完成 Agent 运行」。客户会话中 AI 位于外发侧，`incoming` 此前同时决定对齐方向和文字配色，而失败与停止消息走灰底通知气泡、底部运行状态行没有气泡，白字在浅色主题下不可读：配色改由新增的 `onPrimary` 决定，`incoming` 只决定对齐，运行中的实时过程一并收敛为页面前景色。顺带补上 `ConversationMessageList` 中 AI 头像在原生端的绝对地址补全，覆盖 `agentRuns` 与 `pendingAgents` 两处。
- **验证记录（2026-09-17）：** `wails3 task test:server`、`test:desktop`、`test:frontend`、`build:server`、`common:build:frontend` 全部通过，绑定由 `wails3 generate bindings -clean=true -ts -i` 重新生成。新增 `TestAgentExecutionScopeReturnsUnrepresentedRuns` 覆盖取消运行与新运行并存、以及新消息到达后取消提示被取代；新增 `TestAgentExecutionScopeKeepsSuppressedProcess` 用真实的 `CancelForServiceSession` 取消运行且不取消运行 context，覆盖吸收后续输入被抑制时的过程保留，把该分支改回原样跑过一次，用例如期失败。`TestRunCancellationDiscardsProcess` 改名为 `TestRunCancellationKeepsPartialProcess` 并反转断言，`assertStoppedAgentReply` 原先断言「无半成品过程」，改为按预期条数核对，停止用例改为产生真实过程内容并读取确认可展开；失败运行的集成断言改为验证错误消息携带过程引用、过程详情可按运行编号读取，没有产生内容的失败运行不给引用；前端窗口用例改用 `agentRuns`。界面验证用无头 Chrome 驱动真实 Web 构建与本地服务端：AI 会话注入一条「AI 员工已退出群聊」的无消息取消运行（含思考、工具调用、中断前正文）与一条排队运行，底部两条同时显示、取消那条可展开、工具的完整参数与结果都在，改动前该场景只显示排队那条；客户会话注入一条 `assignee_changed` 的取消运行，确认过程标题、用量、思考与中断前正文都是页面前景色且可读，缩进边框正常。验证数据已删除，本次启动的服务端与浏览器已关闭并确认端口释放，共享的 PostgreSQL 与 NATS 未受影响。
