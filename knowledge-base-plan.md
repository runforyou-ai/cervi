# Cervi 本地知识库接入方案

日期：2026-09-07。状态：待实施的开发方案。本文确定本地知识库的实现顺序、数据库部署边界和验收要求；长期记忆仅作为后续产品设计参考。

## 1. 方向与当前基础

本地知识库提供基础的资料管理与检索，高级文档处理和专业 RAG 能力继续通过 Dify 等外部连接器提供。本地执行层直接采用 Haystack + Hayhooks。

实施顺序为：**PostgreSQL + pgvector + pg_trgm 基础环境 → Haystack + Hayhooks 接入 → 后台模型配置与索引版本 → 已有本地问答检索 → 本地文档导入 → 常用文件解析**。长期记忆和聊天记录搜索另行排期。

知识库和长期记忆统一使用 Haystack + Hayhooks。所有 embedding、重排及未来解析/记忆使用的模型都由 Cervi 管理后台配置。Haystack 路线已完成第 8.6 节的小型动态配置验证，但仍需专用适配代码；长期记忆的技术方向已确定，产品交互与实现验收仍待后续完成。

当前代码已经具备：

- `knowledge_bases`、分组、本地问答条目，以及稳定编号的主问题、相似问题和答案，本次继续沿用。
- Dify 知识库详情、文档、分段和检索；`SearchService` 当前按 Dify 连接加载范围，本地检索待接入。
- Agent Revision 中的知识库绑定、`search_knowledge`、多查询 RRF 和游标上下文读取。
- 临时文件上传、事务激活、本地或对象存储读取，以及 PostgreSQL/NATS 可靠任务。

本文负责本地内容、索引生命周期和部署；[知识库 Agent Tool 方案](knowledge-base-agent-tool-plan.md) 继续负责既有工具契约、多查询融合和 Revision 范围；[Agent 路线图](agent-roadmap.md) 负责 Agent 执行。

## 2. 数据库部署决策

### 2.1 同一实例、同一数据库、同一账号

**Cervi 和 Hayhooks 共用同一个 PostgreSQL 实例、同一个数据库、同一套用户名和密码。** 向量与分段表放入独立 `haystack` schema；Cervi 沿用当前业务 schema。部署统一维护这一套数据库凭据。

| 部署对象 | 数据归属 | 管理者 |
|---|---|---|
| 当前数据库的 Cervi 业务表 | 身份、聊天、知识库、原文、模型配置、索引版本与任务状态 | Cervi 迁移与 Action |
| 同库 `haystack` schema | Haystack Document、分段向量与组件所需索引 | 专用适配代码调用 PgvectorDocumentStore |
| 本地目录或 S3 兼容对象存储 | 原始上传文件；后续需要保留的解析附件 | Cervi 文件模块 |

schema 划分表名与代码归属，同一账号可访问双方数据，双方共享资源和故障域。一个知识库的一个索引代次使用一张固定维度的分段表，各企业共用当前数据库与 `haystack` schema。表名由服务端内部编号生成，业务来源编号保存为 Document 元数据；物理表与授权知识库范围由服务端解析。这个布局优先保证不同维度与版本语义清晰，大量知识库/索引表的容量列入后续验收。

### 2.2 PostgreSQL 扩展安装基线

“基础库换到 pgvector”指将默认 PostgreSQL 镜像换成包含 pgvector 的 PostgreSQL 镜像，并在目标数据库启用 SQL 扩展 `vector` 和 `pg_trgm`；关系数据库、Bun、现有业务表和事务模型仍使用 PostgreSQL。

Compose 已通过独立的 `build/docker/Dockerfile.postgres` 构建 `cervi-postgres:18-vector0.8.6`，基于官方 `pgvector/pgvector` 的 PostgreSQL 18 镜像，锁定扩展版本、发行版标签及镜像摘要；受测的 `0.8.6-pg18-bookworm` 已提供两者，直接复用镜像中的扩展包。Dockerfile 将 `00-init-schemas.sql` 放入 `/docker-entrypoint-initdb.d/`，由 PostgreSQL 镜像在首次初始化时执行。产品保持 PostgreSQL 18 主版本。[pgvector 官方部署说明](https://github.com/pgvector/pgvector#docker)

- 镜像初始化默认库和 `template1`；后续新建的工作区业务库与测试库从 `template1` 继承扩展和 schema。
- **`vector` 和 `pg_trgm` 都是 Cervi 的数据库基线，在目标数据库的 `public` schema 启用。** 分段向量统一保存在 `haystack` schema。
- `pg_trgm` 随产品基线安装；Haystack 向量组件的数据库依赖是 `vector`。关键词检索接入与中文效果另行验收。
- 两个服务直接复用当前数据库账号和密码。

### 2.3 PostgreSQL 镜像初始化

`00-init-schemas.sql` 在默认库和 `template1` 中执行以下 SQL：

```sql
CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA public;
CREATE EXTENSION IF NOT EXISTS pg_trgm WITH SCHEMA public;
CREATE SCHEMA IF NOT EXISTS haystack;
```

初始化脚本只在空数据目录首次启动时执行；已有库需要手动执行同一脚本。Cervi 沿用原有业务迁移。

分段表仍由专用适配代码调用固定版本 PgvectorDocumentStore 创建，设置 `create_extension=False`；扩展由 PostgreSQL 镜像准备，业务迁移由 Cervi 执行。框架升级涉及表结构时单独验证并重建。

Compose 向共享 Hayhooks 提供 PostgreSQL 主机、端口、用户名与密码；目标数据库由每次请求提供。Cervi 配置共享检索服务地址与服务凭据；每次调用按第 2.4 节传入目标数据库并构造 DocumentStore，在请求内指定 `haystack` schema，保持账号全局 search_path。模型配置随请求快照传递。

共享 Hayhooks 在 `setup` 中加载管线，收到请求后访问目标数据库。Cervi 等待 PostgreSQL 可用后执行迁移，成功后再开放 HTTP 与 Worker。Hayhooks 独立启动；尚未就绪时本地检索显示待就绪，普通业务继续运行。

Cervi 经专用 HTTP 契约操作 Hayhooks 分段表，Bun 负责业务表。HTTP 写入独立于业务事务，按业务事务投递、远端完成核对、版本发布的顺序执行。

### 2.4 单实例服务、多工作区数据库

**开发环境的 PostgreSQL、NATS 和 Haystack + Hayhooks 都按单实例共享。** 各工作区继续用各自的 `POSTGRES_DB` 与 NATS 命名空间；共享 API 按每次请求的可信工作区上下文选择数据库。

Cervi 服务端从本工作区 `.env` 构造内部调用上下文，携带目标数据库名（业务库或测试库）；Hayhooks 使用共享 PostgreSQL 的主机、端口、用户名与密码，为该请求构造连接。数据库目标由经服务认证的 Cervi 内部请求提供；数据库主机和账号来自共享服务配置。工作区路由负责选库，企业身份与知识库范围由对应 Cervi 校验。

所有 DocumentStore、模型组件、临时处理文件与状态都归属本次请求，连接和模型配置采用请求局部状态。将来增加连接池或组件缓存时，缓存键必须包含完整数据库目标及配置版本。测试样本故意让两库使用相同知识库、模型配置和分段编号来发现串库。

共享服务由主工作区启动，其他 worktree 复用地址与服务凭据；每个数据库里的配置表、来源和 `haystack` schema 独立。生产部署同样可用这个入口，但通常只连接一个 Cervi 数据库，直接使用该库的配置。

PR 2 增加显式的 Hayhooks 集成测试选项：停止本工作区的测试任务与请求 → 只重建本工作区 `<POSTGRES_DB>_test` → 从 `template1` 继承扩展与 schema 后执行业务迁移 → 经共享 API 执行测试。重置期间保持共享 API 与其他数据库连接运行。Hayhooks 调用由该测试选项启用；仍遵循同一 worktree 同时只运行一轮数据库集成测试的约定。

共享服务运行固定构建的管线版本。修改管线或服务协议时用临时独立实例验收，完成后再协调升级共享版本；版本不匹配明确报错，共享服务升级由主工作区统一执行。测试结束只清理本次启动的独立实例和本工作区进程，保留既有共享服务。

## 3. Haystack + Hayhooks 与后台模型配置

### 3.1 直接接入的职责

首个接入基线为 Haystack 3.1.1、Hayhooks 1.24.0、pgvector-haystack 6.6.0，实施 PR 锁定依赖和镜像摘要。Haystack 提供解析、分段、embedding、PgvectorDocumentStore 与检索组件；重排根据供应商协议使用组件或专用适配，本轮实际使用自定义远程重排组件。Hayhooks 用固定的 PipelineWrapper 暴露服务端接口。Cervi 提供业务管理、来源生命周期、索引发布和现有 Agent Tool 编排。动态配置、请求快照、向量语义版本与代次指针由 Cervi 和专用适配代码实现。

Hayhooks 的 `setup()` 在部署时执行，动态行为要在每次 `run_api` / `run_api_async` 中由适配代码实现。Cervi 解析权限与配置后，传递本次执行需要的配置快照；模型/配置业务表由 Cervi 管理。适配代码为每次请求创建独立组件，在同一服务实例内应用新的模型快照。[PipelineWrapper 官方接口](https://deepset-ai.github.io/hayhooks/concepts/pipeline-wrapper/)

索引、检索、读取与删除通过内部契约调用，前端和 Agent 使用 Cervi 业务入口。原始供应商凭据用于服务间调用；业务响应、工具参数、Hayhooks 参数日志和 tracing 均使用脱敏内容。实验用管理 API 只是配置模拟器，产品复用 Cervi 既有管理后台与 appservice。

### 3.2 模型配置与生效规则

复用已有模型供应商目录的 `embedding`、`rerank` 类型。每个本地知识库选择 embedding 模型与是否使用重排，重排启用时选择 rerank 模型。未来需要的 LLM、VLM、ASR 也遵循后台统一管理要求，在引入对应能力时逐项验收；本轮只验证 embedding 与重排。

| 修改内容 | 生效方式 | 是否重建向量 |
|---|---|---|
| 重排模型、服务地址或开关 | 新请求读取新配置；在途请求使用已捕获的快照 | 否 |
| 同一向量模型的密钥 | 后续请求使用新凭据 | 否 |
| 同一向量模型的 Endpoint | 只有底层模型版本、预处理和输出语义相同才复用索引 | 满足前述条件时否 |
| 向量模型、模型修订、维度、归一化或 query/passage 前缀 | 建立新配置版本与索引代次，重建后发布 | 是，即使维度相同 |
| 分段配置 | 按新规则重建全库全部有效来源，完成后切换当前代次 | 是 |

供应商连接凭据与不可变的向量语义版本分开管理。已被索引引用的模型定义保持固定，修改语义字段时生成新版本。模型语义通过模型修订与预处理规则确认；供应商替换模型时，即使别名、Endpoint 与维度相同，也须重新确认语义。旧代次还有请求或重建依赖时，保留其模型版本与可用连接；禁用/删除供应商前检查引用，通过引用校验和索引切换流程完成变更。

重排换模型会改变分数分布，阈值须按新模型重新校准；统一相关性阈值另行建设。密钥立即撤销可能使在途请求失败，本轮验证了后续请求使用新凭据成功，在途请求仍可能因旧密钥撤销失败。

### 3.3 索引代次与请求快照

版本对象及职责如下：

| 对象 | 含义 |
|---|---|
| 向量语义版本 | 不可变的模型修订、维度、归一化与前缀 |
| 索引配置版本 | 向量语义版本与分段规则的组合；连接凭据与重排配置独立管理 |
| 索引代次 | 一次全库构建的编号，固定引用一个索引配置版本与一张分段表 |
| 内容修订号 | 一条来源的内容版本 |
| 来源集合修订号 | 知识库来源新增、内容修改或删除时，在同一业务事务内递增；PR 4 引入 |

知识库记录目标配置、当前可检索代次与重建中代次。查询在 Cervi 一次一致性读取中捕获数据库、当前表、向量语义版本、连接和重排配置，同一次请求始终使用该快照。来源有效性仍在交付前校验，已删除来源在交付前被过滤。

1. 仅模型或分段规则变化时，当前指针保持原代次。重建任务捕获目标配置与来源集合修订号，把全部有效来源写入新代次；重建期间仍可查询当前代次。
2. 新代次完成后核对分段数量、实际维度与各来源内容修订，在 Cervi 事务内比较目标配置、重建代次和来源集合修订号。全部匹配才发布新指针与对应来源状态，否则丢弃过时结果并重新调度当前目标。
3. 来源在重建期间增删改，先递增来源集合修订号；旧内容立即不可检索。普通来源索引任务按执行时捕获的当前代次及其模型写入，完成时校验该代次仍是当前代次、内容修订仍有效；代次已切换则丢弃该状态更新，重新投递当前代次。新建来源尚无当前代次时，纳入首次全库构建。
4. 来源变化会使已捕获旧集合的重建结果失效，新代次须包含当前集合的全部有效来源。查询当前代次时，按来源存在性与内容修订过滤旧内容，接受短暂少返回条目。
5. 新代次发布后，新请求使用新表，已捕获配置的请求可继续读旧表中仍有效的来源。失败保留当前代次并显示目标版本错误；晚完成的旧任务通过目标版本校验被丢弃。
6. 重复任务使用稳定操作编号与分段 ID，收敛到同一份可见数据。旧表须等在途读取结束再清理，失败表和过时代次也要回收；删除来源或知识库时按第 4 节先更新来源可见状态，再清理相关代次。

实验已验证模型配置变更与保留旧表时的切换；来源变化的完整产品链路、生产清理、服务重启恢复与失败状态列入实施验收。PR 3 验收配置与代次，PR 4 用真实问答来源补齐集合修订与并发发布验收。

## 4. 内容模型与索引生命周期

### 4.1 来源与版本

沿用已有问答表。一条主问题、多个相似问题和完整答案合成一个逻辑索引来源，保留各内容编号，相似问题归属同一问答条目。检索命中任何相关分段后，返回该条目当前版本的完整答案，答案按条目关联读取。

普通文档新增独立文档模型，保存知识库、分组、标题、文件引用或手工文本。文件内容由文档模型保存。索引记录按来源与索引代次保存：来源类型及编号、内容修订号、索引代次、远端 Document 来源标识、幂等操作编号、处理状态、错误及时间；索引配置版本从代次读取。各表在对应 PR 实现其业务用途时创建。

同一来源可以同时有当前代次与重建代次的状态，界面根据当前代次显示来源状态，知识库另行显示目标重建状态。远端分段 ID 包含来源编号、内容修订号和分段位置，存入所属代次表。业务变更、内容/来源集合修订、索引状态与任务投递在同一 Cervi 事务提交，持久化异步任务统一由 Cervi 管理。

### 4.2 写入、更新和删除

1. 新建来源后显示排队，Worker 在数据库事务之外调用 Hayhooks。
2. 由 Cervi Worker 等待适配接口的实际写入结果，并核对来源分段数量和版本，实际写入完成并通过核对后标记索引可用。
3. 普通来源任务只有内容修订仍有效、目标代次仍为当前代次，才能更新可检索状态；全库重建按第 3.3 节原子发布。失败显示错误，复用现有任务机制重试。
4. 修改正文、问题、相似问题或答案时增加内容修订号，立即停止返回旧版本；界面显示处理中，待新版本索引就绪后恢复检索。单纯移动分组保留已有 embedding。
5. 新内容版本写入成功后清理对应代次的旧分段；删除来源时清理各代次中的该来源。晚到的旧任务经过版本校验被丢弃；同一版本重复执行收敛为一份可见内容。
6. 删除来源时先在 Cervi 标记不可检索，在同一事务投递删除任务。索引记录保留到远端清理完成，供后台任务追踪来源。
7. 删除知识库时清理其全部索引代次；与该库有关的在途任务必须检查删除状态，已删除时结束任务。沿用现有 Agent 失效绑定语义。

召回结果和游标读取都要校验来源仍存在、版本有效、属于本次知识库范围。更新/删除与召回并发时，在向调用方交付结果前再次校验；已失效候选被过滤后允许少于目标条数，沿用既定阈值与范围。后台删除控制后续检索的可见性；已经送入在途模型上下文的资料仍保留在该次上下文中。

阶段状态复用已有知识文档状态语义：排队、处理中、可用、错误。暂停、启停、归档等操作只有实现对应生命周期后才开放，本地页面按已实现能力展示操作。

### 4.3 任务与可观测性

复用 `task_runs + task_outbox` 和至少一次执行语义，为索引任务分配独立队列，与 Agent 和文件清理 Worker 分别调度。Cervi 任务记录负责执行与重试，Hayhooks 负责本次管线调用。

记录 `INFO`：知识库、来源、修订号、操作编号、开始/完成状态与耗时；记录 `WARN`：远端失败、无可提取内容、旧版本结果被丢弃。日志字段限定为上述业务编号、修订号、状态、错误摘要与耗时；摘要中的服务凭据和文档原文统一替换为脱敏标记。HTTP 超时后按固定操作编号核对已写入分段并幂等恢复。

## 5. 检索、引用与分数

### 5.1 首版检索产品

首版提供“检索测试”和 Agent 知识检索，本地使用向量召回与按知识库配置启用的重排。关键词、语义与混合模式的独立切换在对应执行路径完成效果与性能验收后提供。

本地知识库按第 3 节从管理后台选择模型；Dify 继续使用它自身保存的配置。首次接入采用 Haystack 向量检索管线，人工检索和 Agent 共用实现与配置。

PR 4 调整 `SearchService` 的范围加载，先读取同企业知识库，再按本地/Dify 来源分别构造现有 `knowledgeretrieval.Source{Retrieve, Read}`。范围查询直接包含本地库，`integration_connection` 与 `external_resource_id` 用于外部来源。人工单库入口同步分派，本地问答页使用本地检索配置提供测试入口。

### 5.2 结果映射与上下文

继续使用现有知识库编号、文档编号、分段编号、位置、正文、答案和 cursor。Haystack Document ID 表示一个实际索引分段，通过元数据关联业务来源、内容修订、索引代次与位置；Cervi 业务文档使用业务来源编号。

- 本地普通文档的 `documentId` 使用 Cervi 文档编号，`segmentId` 由来源索引版本与分段位置构造；实际位置与远端 chunk 的对应关系通过 API 核对。返回框架保存的原文片段，文档名和文件权限由 Cervi 来源记录解析。
- 本地问答 `Retrieve` 在进入多查询融合前先按条目折叠，保留最佳候选名次，返回主问题与完整答案；`documentId`、逻辑 `segmentId` 都使用 Cervi 问答条目编号，位置为 1。这样现有 `knowledge_base_id + segment_id` 去重将相似问题命中合并为一份答案。问答 cursor 的读取范围限定为同一完整条目。
- cursor 增加 `sourceVersion`：本地来源必填，只表示内容修订号与索引代次共同确定的来源索引版本；缺失或不匹配即失效。重建、替换或删除后，旧版本游标失效。
- Dify 使用远端 document/segment ID，每次读取验证当前文档、分段及位置关系。`sourceVersion` 适用于本地来源，Dify 响应省略该字段，其游标表示外部当前内容。Tool 按知识库来源校验字段适用性，统一使用当前契约。
- 本地前后分段只读取同一来源索引版本。页级引用使用解析器提供的真实页码，其余结果提供文档/分段引用。

### 5.3 融合和阈值

保留现有多查询、多知识库 RRF 编排，在现有产品检索入口整理结果。每路先按自身规则召回，Cervi 再去重与按名次融合。Dify 的源端阈值仍只对其本路生效。

BM25、`ts_rank_cd`、pg_trgm similarity、向量 cosine、RRF 融合分数和重排 logit 各有自己的刻度。RRF 按名次融合，无答案判定另行处理。首版保留各路诊断分数，统一相关性阈值在完成校准后提供。

未来确需跨本地与 Dify 的统一过滤，可以在合并候选后用同一重排模型评分，再用独立中文正负例校准阈值。重排作用于已召回候选，源端过滤决定候选覆盖；即使使用 sigmoid 或 min-max 归一化，正确概率仍需独立校准。统一过滤另行交付。

无内容或纯标点输入在调用模型前明确拒绝；有效单字“税”“药”按正常查询纳入测试。无关问题返回候选时，Agent 仍需根据原文判断资料是否支持答案，依据原文支持程度决定是否作答。

## 6. 文件与多模态

原始文件沿用 Cervi 文件生命周期：临时上传、业务事务激活、按记录中的本地或对象存储类型读取。开启对象存储时，客户端通过预签名请求直传；索引 Worker 后台读取已激活文件作为索引输入。

第一批普通文档支持手工文本、UTF-8 TXT 和 Markdown；之后增加 DOCX、可提取文字的 PDF。上传原件保存成功、解析成功、索引可用是不同状态。解析为空明确失败，保留原件用于重试。

索引 Worker 向专用 Hayhooks 解析管线提交必要文件内容，使用明确的 Haystack converter 和 splitter；成功、失败或取消后清理处理临时副本。原件由 Cervi 文件模块持续保存。

分段验收使用中文长文与所选 embedding tokenizer，按实际 token 数量控制输入上限，核对全文覆盖后标记索引就绪。连续中文采用与模型 tokenizer 匹配的分段组件。文件格式按 PR 5/6 的样本验收结果开放；遇到输入覆盖问题时，补充有限的解析适配或缩小支持格式范围。

后续多模态按独立能力交付：

- 图片：OCR 文本可以用于检索；颜色、布局等问题可能需要重新读取原图送入 VLM。
- 扫描 PDF：单独验证转页图/OCR 管线，保留页码映射后再承诺页级引用。
- 表格：表头必须随行组保留，并验证型号、数值、列名与列语义。
- 音频：先确定本地或外部 ASR，测试姓名、型号、数字和否定，必要时保留时间戳与原音频。
- 视频、原生跨模态 embedding、视觉索引和 S3 网络链路列入后续验收。

原始图片、音视频与可重建的文本/向量索引分开存储。S3 保存原文件，PostgreSQL 保存向量与全文索引。

## 7. PR 拆分与验收

知识库按以下 PR 依次实施，长期记忆按独立产品计划交付。PR 1 已实现数据库镜像和初始化 SQL，后续 PR 沿用该基线。

### PR 1：更换数据库镜像并初始化扩展与 schema

范围：将 PostgreSQL 换为固定版本的 pgvector 镜像，通过镜像初始化脚本启用 `vector`、`pg_trgm`，创建 `haystack` schema；更新开发 Compose 的镜像引用。

验收：空卷启动后默认库已具备两个扩展和 schema，新建工作区库与测试库自动继承；已有卷重启保留数据，服务端测试通过。

### PR 2：接入 Haystack + Hayhooks 与动态执行契约

范围：固定版本共享服务镜像、工作区数据库路由、Go HTTP 客户端和专用管线，支持指定配置快照的写入、检索、读取和删除；复用数据库凭据，`haystack` schema 保存分段。未配置服务时既有问答 CRUD 与 Dify 仍可用。

验收：按产品基线启用 `vector` 与 `pg_trgm`，Hayhooks 使用与 Cervi 相同的数据库账号访问 `haystack`；给定两套真实 embedding/重排配置快照，写入、检索、读取与删除契约通过。验证同一 API 实例执行不同快照、不同维度表隔离、实际模型调用审计与错误契约。模型业务表和管理 API 由 Cervi 提供，适配服务使用传入快照；服务间调用使用真实凭据，业务响应与日志将凭据脱敏。验证单实例并发访问不同工作区库、相同编号按库隔离、单库重置时其他库持续可用，以及固定协议版本探测。分别验证 Hayhooks 先启动、数据库镜像先完成初始化、已有卷缺少扩展时的路径；分别报告 API 全局健康与工作区检索就绪状态。

### PR 3：知识库后台模型配置与索引代次

范围：复用已有供应商目录，新增本地知识库的模型选择、重排开关、目标/当前配置版本与重建状态；Cervi 负责持久化、请求快照、任务投递、版本发布和旧表清理。前端通过 appservice 契约生成。

验收：管理后台更换模型在同一 API 实例内生效；重排变化沿用已有向量，同维不同模型与不同维度都触发重建；已引用的向量语义按版本保存。以空库或测试直接写入的固定夹具验证新旧请求快照、晚到任务、失败保留旧代次、重复执行、重启恢复和安全清理。模型/供应商删除必须检查当前与在途引用。问答来源写入、来源集合修订与更新导致重建失效的规则在 PR 4 一起实现。

### PR 4：本地问答支持索引、人工检索和 Agent 使用

范围：已有主问题、相似问题、完整答案的来源修订与任务；处理状态、错误和重试；人工检索；单库/多库 SearchService 本地/Dify 分派；按条目折叠，接通 Agent 范围和版本游标。已有问答通过“建立索引”业务操作提交。

验收：主问题、相似问题、同义问法、长答案；更新后交付当前有效内容；重复任务、删除并发、模型重建期间来源修改与新增；同企业混合检索、跨企业隔离及 Run Revision。业务结果按条目合并相似问题命中并返回完整答案。固定小型回归语料随业务测试保存。

### PR 5：本地标准知识库支持文本与 Markdown 文档

范围：独立文档模型、手工文本、TXT/Markdown、文件激活与分组、解析索引、只读分段、原件与上下文引用；复用来源生命周期。

验收：创建、替换、删除与重试；中文长文 embedding 输入完整；符号、代码与金额原文保留；旧 cursor 在新版本发布后失效；本地文件与临时 S3 服务各实际跑通上传、索引读取和原件下载。S3 测试使用外部临时服务。

### PR 6：支持 DOCX 与文字 PDF 的基础解析

范围：两种格式、无文本错误与重试、临时副本清理；扫描 PDF 自动 OCR、复杂表格编辑、音视频和图像向量检索另行排期。

验收：中文 DOCX、文字 PDF、无文本扫描 PDF、跨段答案、表格/代码样本，核对正文覆盖、来源定位与 token 上限。表格列语义无法保留时必须明确支持边界。各产品页面使用既有 Web/桌面共享入口，移动端专属知识管理另行排期。

### 后续独立议题

中文词法与混合检索、重排质量与阈值校准、扫描件/图片、复杂表格、音视频和容量专项分别安排。长期记忆在交互确定后独立实施。

实现遵循仓库迁移、生成绑定、中文注释与 Pi CLI 审核约定。Cervi 业务表只增加主键及业务/幂等唯一索引；PgvectorDocumentStore 为检索创建的组件索引按固定版本验证，按组件原有实现管理。测试结束后关闭本次服务，保留共享 PostgreSQL/NATS。

## 8. 历史评测摘要与实施要点

### 8.1 证据边界

以下是 2026-09-05 至 2026-09-07 独立实验的历史摘要，对应此前完成的评测（第 8.6 节另列本次新增的动态模型验证）。原实验目录包含源码副本、模型缓存、小说、日志和结果，已按要求从当前工作区删除；本文保存结论、关键参数和已知失败。后续实施 PR 必须用固定版本和小型业务样本重新验收，并保存可复现的原始证据包。

基础质量集为 40 篇人工业务文档、26 个正例、3 个有内容的负例，以及空白/纯标点两个无效输入。小说来自 Project Gutenberg《三國志演義》《水滸傳》，保留繁体，生成 2,233 个约 500 字片段；API 基准通常只取其中 200 篇。百万行通过重复种子并添加高频词构造，用于检验重复数据与高频词的负载。

数据库通常限 2 CPU/4 GiB；API、模型服务的 CPU/内存组织、数据库小版本、切分数量和缓存状态各轮不同。文本模型使用本地 Qwen3-4B-Instruct-2507-4bit；embedding 为 multilingual-e5-small，384 维，带 query/passage 前缀和 512 token 上限；图像模型为 Qwen2.5-VL-3B-Instruct-4bit。下表用于记录各轮条件下的表现；严格跨产品性能比较、百万向量和百万图谱 API 另行评测。

`Hit@5` 表示前五个结果中包含至少一个标注来源；回答准确率与无答案识别能力分别验收。

### 8.2 全文检索实验

| 路径 | 观察 | 实施含义 |
|---|---|---|
| Go gse 1.0.2 + PG 18.6 原生 tsvector | 停用词 + OR 命中前五 25/26；百万行“啊”匹配约 50 万行，完整排名 P50 3.52 秒，四并发 40/40 超过 5 秒 | Go 分词改善部分中文词项召回，大候选集仍需承担排序成本 |
| 同一路径，低命中查询 | “PostgreSQL 备份”约 5,015 行匹配，P50 3.1 ms | tsvector 的性能与评分效果随查询及候选分布变化 |
| 提前无序取 5,000 候选 | 高频查询显著提速，但刻意构造的尾部高相关项被完全漏掉 | 候选截断以召回损失换取性能 |
| pg_trgm GIN 包含后全量评分 | 高频“啊”仍超时 | pg_trgm 高频结果排序仍需控制计算成本 |
| pg_trgm GiST 局部距离前十 | 百万行同样包含“啊”的集合，P50 1.9 ms，四并发 P95 13.5 ms | 距离索引与提前停止带来本轮收益，自然问句质量及其他查询需分别验证 |
| 无阈值 pg_trgm 局部 KNN | 小集 Hit@5 24/26，但负例仅 1/5 正确；该一个是空输入提前拒绝 | 最近邻召回后仍须判断内容相关性 |
| 旧框架词法布局 native / Go native / PGroonga | 2,273 条词法集 Hit@5 分别 11/26、23/26、20/26；Go + PGroonga 23/26 | PGroonga 中文自然问句仍需专门评测 |

几个实现细节必须保留：

- `LIMIT` 放在全量排名之后，前面的候选读取和评分仍处理完整候选集。排序时重复 `to_tsvector` 还会增加解析成本，预存向量可改善，但高频大结果排序仍可能超时。
- AND 对自然问题太严格，OR 会放大泛词噪声；停用词与有效范围限定需一起使用。“税”“药”等有意义单字按有效查询处理。
- 技术符号可能在分词后再次被 PostgreSQL parser 丢失，`C++` 与 `C#` 可能均变成 `c`。简繁归一化需要独立处理；搜索分词扩展词的位置需显式映射回原文位置。
- pg_trgm 局部相似度依赖词边界：“退款”在连续中文中的分数可能很低甚至为零；一两个中文字符的包含模式可能没有可用 trigram。GiST KNN 与字面包含按各自语义分别评估质量。
- 参数化语句的通用计划与手写字面量 SQL 计划可能不同；短词延迟曾因计划缓存跳变。后续性能验证必须执行真实应用查询。
- PGroonga 的高频排序在受测布局下比 native 更快，但仍有约秒级并发成本；主表/TOAST、缓存和查询语义会改变对比结果。
- 超时期间数据库探活成功，进程持续运行；本轮复现了昂贵请求，旧系统“整库卡死”的完整原因仍待调查。

旧框架的 Go 分词补丁依赖应用维护的搜索列，文本与搜索列须同步更新。该补丁记录为历史实验；当前采用第 3 节的 Haystack 专用管线。

### 8.3 框架历史比较

| 框架与版本 | 本轮能力 | 关键缺口 |
|---|---|---|
| 已淘汰的记忆框架（旧实验版本 0.9.2） | 240 篇 chunks 混合检索 Hit@5 26/26；有明确记忆、归纳和摘要能力；基础 bank/来源生命周期可用 | embedding/重排模型选择使用部署级配置；原生中文词法弱；分段、多模态与派生摘要删除仍有风险 |
| LightRAG 1.5.7，提交 `28ff1b05f2ac3f3e6fa14dd2cd33656579bd0c9c` | naive 428 段、40 篇图谱查询均 Hit@5 24/26；文档图谱问答可用 | 两个有效单字被 HTTP 400 拒绝；工作区请求头未切换实际实例；更新需删后再插；删除后答案缓存残留 |
| Haystack 3.1.1 + Hayhooks 1.24.0 + pgvector-haystack 6.6.0 | 440 段原生向量 Hit@5 26/26，管线可组合，图像/扫描 PDF 适配可运行 | 仍需自建来源生命周期、租户授权、记忆策略与文件保存；连续中文需调整默认空格切分方式 |

LightRAG 的 local/global/hybrid 是实体和关系检索，mix 再结合分段向量，其混合对象是图谱与分段。全 PostgreSQL 后端需要 pgvector + AGE；“只用 PostgreSQL core”的配置把向量存到本地 NanoVectorDB、图谱放到 NetworkX 文件，由 PostgreSQL 与本地文件分别存储。

Haystack 的 PgvectorDocumentStore 在只有 core 的库里初始化失败；预装 vector 后普通账号可用。其默认关键词组件是 `plainto_tsquery` AND，使用 PostgreSQL 原生词法查询。OVERWRITE 仅覆盖相同分段 ID；同一来源改文本后可能保留旧段，来源级替换需要显式实现。Hayhooks 文件上传返回 processed 时，本轮默认入口实际未持久化，随后下载为 404，原件须由 Cervi 持续保存。

当前只接入 Haystack + Hayhooks。上述其他框架结果作为历史参考保存；需要自建的业务生命周期已列入第 3～7 节。

### 8.4 已淘汰方案的记忆实验教训

以下保留此前记忆框架实验的关键结果，Haystack 记忆需按第 9 节独立验收。原实验细节可从文档历史追溯。

- 60 条合成对话（15 条关键事件、45 条干扰）在修正模型输出配置后，8 个核心问答正确，但仍出现把“预算”扩写为“贷款金额”。事实提取需同时验收忠实性与问答命中。
- 三轮来源删除后原始事实与归纳未检出旧预算，但更早的依赖缺失未被解释；未刷新摘要时 2/3 仍回答旧预算，刷新后 3/3 正确。自动刷新也有异步窗口，删除完成必须覆盖派生事实、摘要和缓存。
- 严格范围过滤可隔离主体；省略过滤的实验召回了其他用户资料。认证与授权负责确定有效范围，存储标签用于执行过滤。
- 小样本召回共 332 次无 HTTP 错误，有固定发起节奏，作为旧方案该负载下的测量记录。旧方案重启后在途任务曾恢复，Hayhooks 性能、SLA 和各故障路径仍需独立验收。
- 本地模型默认 512 输出 tokens 曾让归纳 JSON 截断：60 条消息到失败检查点约 743 秒、168 次 LLM 调用、63 次截断。调大输出预算后能恢复；另 12 条事件仍需 25 次调用、105,099 逻辑 tokens。记忆成本统计覆盖聊天、写入和后台提取/归纳；输出预算需按模型验证。

### 8.5 多模态与排名教训

旧记忆框架的 PNG 在配置本地 OCR 后能识别型号、故障码和维修事项；扫描 PDF 同一入口失败，PDFium 转页图后交给 VLM 才通过文字问答。三套框架的图片描述都曾遗漏蓝色矩形，后续文字记忆无法回答颜色；取回原图直接问 VLM 才答对。保留原件是能力组成部分。

旧记忆框架的原生 WAV 因缺少 `speech_recognition` 失败；该安装版 MarkItDown 的转录代码依赖 `recognize_google`，其转录配置独立于本地聊天模型；Google 调用保留为未执行的实验路径。额外接入 mlx-whisper 0.4.3 / whisper-small-mlx 后，两段约 12 秒合成普通话能处理预算和否定，但“陈小雨”“林青”、E-731 出错；后续模型还把错误姓名归为技术负责人。可靠语音记忆仍需解决上述转录与事实错误。

旧记忆框架设置保留原件后，三个文件可通过下载接口读回且 SHA-256 一致，包括解析失败文件；本轮验证范围是 BYTEA 存储。真实 S3 网络、视频、嘈杂语音及原生跨模态向量列入后续评测。

旧记忆框架无关候选的 RRF final 可以达到 1，记忆模式加权后还可能大于 1；答案可靠性需要另行判断。指定中文 CrossEncoder 在 240 篇语料的受限 CPU 上将 Hit@5 从 26 降至 24，有效查询中位延迟约 8.27 秒；阈值越高越容易漏掉正例。Haystack 的 logit 还可为负或大于 1。重排模型与阈值须通过独立样本选择和校准。

### 8.6 本次 Haystack 动态模型验证（2026-09-07）

本轮验证实验管理 API 保存配置后，固定运行的适配管线使用新配置的行为。Cervi 后台与 appservice 接入、中文质量、多模态和长期记忆分别列入后续验收。Go 标准库客户端调用实验管理 API；自定义 PipelineWrapper 把配置保存到测试业务表，每次请求读取配置快照。真实 Haystack embedder、DocumentWriter、PgvectorEmbeddingRetriever 与自定义远程重排组件执行模型调用，向量与排名均来自实际模型输出。

环境：Haystack 3.1.1 / Hayhooks 1.24.0 / pgvector-haystack 6.6.0，PostgreSQL 18 + vector 0.8.6；数据库 2 CPU/2 GiB、API 4 CPU/3 GiB、模型进程 4 CPU/8 GiB，模型已预热。两个模拟供应商 Endpoint 位于同一模型进程，验证兼容协议与路由；真实云供应商接入另行验收。测试业务表与 `haystack` 分段表均由同库的一个普通应用账号拥有并访问，账号没有 SUPERUSER、CREATEDB、CREATEROLE；初始化管理员仅负责创建角色和安装扩展。

| 用途 | 实际模型 | 配置 |
|---|---|---|
| 向量 A | intfloat/multilingual-e5-small | 384 维，query/passage 前缀 |
| 向量 B | BAAI/bge-small-zh-v1.5 | 512 维，中文 query 前缀 |
| 重排 A | cross-encoder/mmarco-mMiniLMv2-L12-H384-v1 | 真实 CrossEncoder logits |
| 重排 B | cross-encoder/ms-marco-TinyBERT-L2-v2 | 英文模型，用于验证模型切换；中文质量另行评估 |

两个知识库各 6 条合成中文资料，涵盖预算、型号、请假、退款、密码、交付，来源 ID 按库区分。以下 12 项断言全部通过：

| 检查 | 结果 |
|---|---|
| 向量语义按版本管理 | 修改同一配置 ID 的模型被拒绝，必须创建新版本 |
| 不同知识库使用不同模型与维度 | 20 次交错查询，最多 4 并发，无跨库来源或模型串用 |
| 重排切换与在途快照 | 新请求使用 B，已捕获配置的旧请求仍使用 A |
| 关闭重排 | 向量代次不变，底层无重排调用 |
| 在途读取跨索引切换 | 新请求用 512 维新表，旧请求仍用 384 维旧表 |
| 向量重建与发布 | 重建期间 20 次交错查询可使用旧代次，完成后发布新代次 |
| 旧重建任务晚完成 | 第 4 代先发布，第 3 代晚完成未覆盖它 |
| 维度错误的重建 | 实际 512 维写入 384 维表失败，第 4 代仍可查询 |
| 密钥轮换 | 旧密钥被模型端拒绝，更新后请求成功，索引未重建 |
| 同模型 Endpoint 切换 | 底层审计确认请求改发另一 Endpoint，代次不变 |
| 切换后的交错并发 | 16 次查询无来源或模型串用 |
| 无重启或管线重新部署 | 完整测试中同一 API 实例，只记录一次 setup，容器 RestartCount 为 0 |

独立核对了 64 次成功检索的实际 embedding/重排请求、来源 ID、模型与维度；共 70 次 embedding 调用、63 次重排调用。三个预期错误为语义配置覆盖、维度不匹配和旧密钥失效，无非预期失败。数据库核对六张代次表：五张各 6 条，失败表 0 条；最终 A 目标为失败的第 5 代、可用为第 4 代，B 仍为第 1 代。

62 次未被测试屏障暂停的成功检索，Go 客户端测得 P50 172.5 ms、P95 352.9 ms（经验分位数向下取整，含 HTTP、embedding 和按配置启用的重排）。这是模型进程串行推理条件下的混合用例小样本；吞吐上限与跨框架性能需另行测量。测试屏障在请求捕获旧配置后暂停执行，再验证配置切换后的行为。

验收范围：实验使用独立 API，错误通过 HTTP 200 内的 `ok:false` 表达；Cervi 管理页面、企业授权与业务错误契约由正式接入实现。过时代次与失败表保留用于核对，回收、故障恢复与失败状态机待实施验收。外部模型语义须持续确认，同维模型切换须重建向量，重排阈值须按模型校准，长期记忆按第 9 节验收。

脚本、原始响应、模型审计与配置保存在仓库外临时目录。本节保存关键用例与验收范围，实施 PR 随业务代码保存小型可复现契约测试及其固定输入。

### 8.7 本次共享实例、多工作区验证（2026-09-07）

在同一套依赖上另跑一轮：单个 Hayhooks 实例通过请求局部数据库上下文访问 `dynamic_w1`、`dynamic_w2` 两库，共用同一普通应用账号。测试业务配置与向量表在各库内同库同账号；两库故意使用相同模型配置 ID、知识库 ID、分段 ID 与物理表名，分别配置 384/512 维模型，正文带不同工作区标记。

Go 客户端的五项检查全部通过：40 次最多四并发的交错查询；重建一库时另一库的在途与新请求成功；重建后原工作区无需 API 重启即可重新索引检索；未知工作区被拒绝；全程无 API 重启或重新部署。含重置后的请求共 43 次成功检索，逐条核对实际返回正文归属与模型请求，两个数据库中的表归属和实际向量维度独立核对。

重置在本轮独立实例的工作区一执行 `DROP DATABASE ... WITH (FORCE)` 并重建，实验全程使用专用数据库。这验证了请求局部连接的方案；实验路由采用两个固定工作区映射；内部服务认证、动态数据库目标与大量连接池列入产品接入验收。产品入口按第 2.4 节实现。

### 8.8 本次扩展与启动边界验证（2026-09-07）

使用 pgvector/pgvector `0.8.6-pg18-bookworm` 与前述固定 Haystack/Hayhooks 版本，另建独立 PostgreSQL、Hayhooks 实例，执行数据库计数操作。八项检查通过：

- 镜像中的 `vector` 与 `pg_trgm` 均可安装，空数据库初始只启用 plpgsql。
- Hayhooks 在两个扩展均未启用时能启动；`setup` 构造了指向不存在数据库的 DocumentStore，也未立即连接。
- `create_extension=False` 首次执行 `count_documents()` 因缺少 vector 类型失败；仅安装 pg_trgm 后仍失败。
- 对目标库安装两个扩展后，同一 API 实例的 `count_documents()` 成功，无需重启。
- 另一个只启用 vector 的数据库也能调用成功，验证了组件的 vector 依赖；产品基线仍统一安装 pg_trgm。
- 默认自动安装路径创建的扩展为 vector；所有检查期间 API 实例标识不变。

本轮验证上游组件与专用管线的启动边界；PR 1 通过镜像初始化脚本提供两个扩展。此前第 8.6/8.7 节只启用 vector 的实验结果保留为历史事实，双扩展部署按新的基线独立验收。

## 9. 长期记忆：统一使用 Haystack，交互确定后开发

技术方向已确定为复用 Haystack + Hayhooks、同一 PostgreSQL 数据库和账号。以下记录后续开发建议；记忆开关、页面、数据表和自动写入在交互确定后独立实施。知识索引与记忆索引分别管理来源和范围，聊天内容按记忆提取与授权规则处理。

### 9.1 职责与推荐起点

Haystack 是记忆管线的执行框架；事实语义、纠正和遗忘由 Cervi 实现。

| 环节 | Haystack + Hayhooks | Cervi |
|---|---|---|
| 提取 | 按后台指定 LLM、提示与结构化格式，从新增交流提取候选事实 | 确定可写入来源、主体、时间与确认状态，校验候选并记录版本 |
| 持久化 | 为当前有效事实计算 embedding 并写入独立记忆索引 | 在业务表保存规范事实、原消息关联、有效/被替代/删除状态；作为重建依据 |
| 召回 | 按当前记忆模型配置和范围检索，按配置重排 | 注入企业、Agent、成员范围，过滤失效事实，交付带来源的上下文 |
| 纠正与遗忘 | 更新或删除指定版本的派生索引 | 确定替代关系、来源依赖与清空边界，阻止旧任务重新写回；控制完成状态 |

模型配置沿用第 3 节；提取 LLM 与提示版本在任务开始时固定，修改影响后续任务；已有事实按原提取版本保留，历史重提取通过明确的重处理操作执行。记忆 embedding 变更重建索引，重排变更影响新请求。未来 VLM/ASR 的模型选择同样由后台管理并随请求传递。

建议先限定内部成员与单个 Agent 的文本交流，按企业、Agent、成员隔离，保存有明确来源的事实，自动归纳摘要作为后续能力单独评估。跨 Agent 共享、客服联系人、群聊与 Agent 自身经验需分别确定授权和交互。

自动写入可从成功 Run 实际消费的新增交流开始评估，保留说话人、时间、原消息和确定性；失败 Run、人工发言和显式“记住这件事”另定规则。回答仍由既有 Agent 生成，若提供 `search_memory`，范围由服务端注入。自动召回或按需调用由产品交互决定。

接入前用 Haystack 重新验证多轮纠正、否定与不确定性、同名主体隔离、来源删除、彻底遗忘、在途任务和重启恢复，统计前台与后台模型用量。第 8.4 节的失败样本作为本方案独立记忆验收的输入。

### 9.2 必须先决定的交互

| 待决定事项 | 需要明确的语义 |
|---|---|
| 启用方式 | 企业级、Agent 级还是成员级；默认状态；是否对用户提示自动记忆 |
| 写入方式 | 自动提取、显式指令、人工确认，哪些发言可以成为事实 |
| 使用范围 | 同一会话、同一成员跨会话、跨 Agent、企业共享和客服周期边界 |
| 查看与纠正 | 展示原始来源还是派生事实，如何反馈错误、修改、删除或彻底遗忘 |
| 关闭与清空 | 停止读写是否保留已有数据；清空是否停止从旧聊天再次学习 |
| 原聊天删除 | 原消息、提取事实、归纳、摘要和缓存分别如何处理 |
| 在途任务 | 清空时正在写入或生成回答如何终止/隔离，何时向用户显示完成 |

### 9.3 后续实施要求

1. 记忆作为带来源的事实上下文使用，系统指令与工具授权由各自配置决定；助手猜测、他人观点、假设和用户确认分别标记。
2. “记住了”的反馈依据实际写入结果；事实落库、索引就绪及未来可能引入的摘要刷新分别有完成条件。
3. 删除原消息或事实时追踪索引、派生事实、摘要和缓存依赖；如果未来引入摘要，必须刷新并等待完成，或在刷新期间排除旧摘要。
4. 清空记录明确的数据边界，旧 Run、历史回放和重试在写入前检查该边界。已经送入在途模型上下文的资料需有单独处理语义。
5. 统计模型输出预算、前台对话 tokens、后台归纳成本和缓存用量。
6. 中文人名、代码、否定和时间变化都纳入回归；多模态分别验收上传、转录/解析忠实性与记忆正确性。
7. 主体、来源、索引表和操作编号由服务端解析并验证；同名主体隔离必须测真实身份映射。

## 10. 固定版本与参考资料

- [PgvectorDocumentStore](https://docs.haystack.deepset.ai/docs/pgvectordocumentstore)、[OpenAIDocumentEmbedder](https://docs.haystack.deepset.ai/docs/openaidocumentembedder)
- [LightRAG 固定版本 PostgreSQL 实现](https://github.com/HKUDS/LightRAG/blob/28ff1b05f2ac3f3e6fa14dd2cd33656579bd0c9c/lightrag/kg/postgres_impl.py)
- [Haystack 3.1.1](https://github.com/deepset-ai/haystack/releases/tag/v3.1.1)、[Hayhooks 1.24.0](https://github.com/deepset-ai/hayhooks/releases/tag/v1.24.0)
- [PostgreSQL pg_trgm](https://www.postgresql.org/docs/18/pgtrgm.html)、[预备语句与执行计划](https://www.postgresql.org/docs/18/sql-prepare.html)

外部在线文档会变化。具体实施以 PR 锁定版本的源码、安装流程和契约测试为准。
