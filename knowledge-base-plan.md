# Cervi 本地知识库接入方案

日期：2026-09-07。状态：开发方案，尚未接入业务。本文确定本地知识库的实现顺序、数据库部署边界和验收要求；长期记忆仅作为后续产品设计参考。

## 1. 方向与当前基础

本地知识库提供基础的资料管理与检索，高级文档处理和专业 RAG 能力继续通过 Dify 等外部连接器提供。本地执行层直接采用 Hindsight，不建设支持多种搜索引擎互换的通用框架。

实施顺序为：**PostgreSQL + pgvector 基础环境 → Hindsight 服务接入 → 已有本地问答检索 → 本地文档导入 → 常用文件解析**。长期记忆、聊天记录搜索不在本轮开发范围。

当前代码已经具备：

- `knowledge_bases`、分组、本地问答条目，以及稳定编号的主问题、相似问题和答案；不重建第二套问答模型。
- Dify 知识库详情、文档、分段和检索；`SearchService` 当前按 Dify 连接加载范围，本地知识库还不能真正参与检索。
- Agent Revision 中的知识库绑定、`search_knowledge`、多查询 RRF 和游标上下文读取。
- 临时文件上传、事务激活、本地或对象存储读取，以及 PostgreSQL/NATS 可靠任务。

本文负责本地内容、索引生命周期和部署；[知识库 Agent Tool 方案](knowledge-base-agent-tool-plan.md) 继续负责既有工具契约、多查询融合和 Revision 范围；[Agent 路线图](agent-roadmap.md) 负责 Agent 执行。

## 2. 数据库部署决策

### 2.1 同一实例、同一数据库、同一账号

**Cervi 和 Hindsight 共用同一个 PostgreSQL 实例、同一个数据库、同一套用户名和密码。** Hindsight 的内部表放入独立 `hindsight` schema；Cervi 沿用当前业务 schema。部署不增加第二个数据库、第二套账号或第二份数据库凭据配置。

| 部署对象 | 数据归属 | 管理者 |
|---|---|---|
| 当前数据库的 Cervi 业务表 | 企业、身份、聊天、知识库、原文记录、文件引用、索引来源与任务状态 | Cervi 迁移 |
| 同库 `hindsight` schema | bank、分段、向量、全文索引、异步操作等框架内部数据 | Hindsight 迁移 |
| 本地目录或 S3 兼容对象存储 | 原始上传文件；后续需要保留的解析附件 | Cervi 文件模块 |

schema 用于避免表名、迁移记录和代码归属混淆，**不提供账号权限隔离**。同一账号可以访问双方数据；CPU、内存、磁盘、连接数和故障域也共享。接受这一部署取舍，先保持简单，不增加跨库同步、权限隔离系统或自动迁移能力。

同库 Hindsight schema 可容纳本部署内多个企业的 bank。一个本地知识库对应一个 bank，使用包含企业和知识库标识的服务端生成编号；不为每个企业新建数据库或 schema。所有 HTTP 调用都由 Cervi 根据已认证身份或 Run Revision 解析范围，客户端和模型不能指定任意 bank。

### 2.2 PostgreSQL + pgvector 的含义

“基础库换到 pgvector”指将默认 PostgreSQL 镜像换成包含 pgvector 的 PostgreSQL 镜像，并在目标数据库启用 SQL 扩展 `vector`；关系数据库、Bun、现有业务表和事务模型仍使用 PostgreSQL。

当前 Compose 使用 `postgres:18-alpine`。实施时保持 PostgreSQL 18 主版本，采用官方 `pgvector/pgvector` 的 PostgreSQL 18 镜像，锁定扩展版本、发行版标签及镜像摘要；例如已发布的 `0.8.6-pg18-bookworm` 可作为验证起点。评测使用的 PostgreSQL 17 不意味着产品要降级。[pgvector 官方部署说明](https://github.com/pgvector/pgvector#docker)

- 镜像包含扩展安装文件，不代表数据库自动启用了扩展。每个 worktree 的业务库及测试库分别初始化一次。
- 当前库启用 `vector` 作为统一部署基线；Hindsight 的向量表只保存在 `hindsight` schema，不复制到 Cervi 业务表。
- 接入 Hindsight 的常规安装还准备 `pg_trgm`，扩展统一安装在双方可见的 `public` schema。`pg_trgm` 用于上游已有迁移/实体路径，不表示采用它建设中文全文检索。
- 不要求 zhparser、PGroonga、AGE、pg_textsearch 或厂商专属向量索引扩展。
- 两个服务直接复用当前数据库账号和密码，不要求额外长期维护迁移账号。云数据库如要求管理员安装扩展，由部署初始化执行一次，不能要求常态服务具备超级用户权限。

AWS RDS、阿里云 RDS 和腾讯云 PostgreSQL 均有 pgvector 支持文档，但扩展版本、数据库版本、实例规格及安装权限需要按目标实例验证，不能把“云厂商支持”写成所有实例已通过。当前未完成真实云实例验收。[AWS 扩展清单](https://docs.aws.amazon.com/AmazonRDS/latest/PostgreSQLReleaseNotes/postgresql-extensions.html)、[阿里云 pgvector 指南](https://www.alibabacloud.com/help/zh/rds/apsaradb-rds-for-postgresql/pgvector-use-guide)、[腾讯云扩展安装说明](https://www.tencentcloud.com/document/product/409/72650)

### 2.3 安装、运行与开发命名

安装或升级通过部署 Task 完成：准备当前数据库的扩展和 `hindsight` schema，执行各自迁移，再启动服务。迁移与运行都复用同一配置账号；若目标云实例限制 CREATE EXTENSION 或 CREATE SCHEMA，则把扩展/schema 准备和迁移所需权限列为一次性部署前置步骤，不增加第二套长期账号。注意上游迁移即使使用 `CREATE SCHEMA IF NOT EXISTS` 也可能检查数据库级 CREATE，不能只预建 schema 就假定迁移可执行。Hindsight 正常运行关闭启动迁移；启动探测版本和必需能力，缺失时报告明确配置错误。Cervi 的现有迁移入口不与 Hindsight Alembic 合并。

Compose 和部署 Task 从现有 `POSTGRES_*` 配置构造 Hindsight 数据库 URL，仅增加 `HINDSIGHT_API_DATABASE_SCHEMA=hindsight`，不要求用户再填写一份数据库名或密码。容器网络地址按现有部署方式解析，数据库、账号和密码保持一致。Hindsight 连接的 schema/search_path 必须使其表和 Alembic 记录落入 `hindsight`，同时可见 `public` 中的扩展类型与算符；不能修改账号的全局 search_path，影响 Cervi 连接。

Cervi 通过 Hindsight HTTP API 操作框架，不用 Bun 读写其内部表。虽然数据同库，跨服务 HTTP 操作仍不属于 Cervi 当前事务：业务状态与索引任务保持事务内投递，远端完成后再发布可检索状态。

每个 worktree 继续只使用自己的 `POSTGRES_DB`，例如 `cervi_task3`；Cervi 和该工作树的 Hindsight 都连接这个库。每个启用本地检索的 worktree 单独启动一份 Hindsight，Compose project/覆盖配置、端口和数据库 URL 跟随其 `.env`；不能像 PostgreSQL/NATS 那样共享一份固定指向其他工作树的 API 服务。测试同样共用现有 `cervi_task3_test`，其中保留独立 `hindsight` schema，不新增第二套测试数据库命名规则。

PR 2 为 `test:server` 增加显式的 Hindsight 集成测试选项：停止本轮测试 Hindsight → 现有 `db:test:reset` → 初始化扩展/schema 并执行 Hindsight 迁移 → 启动指向测试库的 API → 执行业务测试 → 无论成功失败都停止本轮 API。未启用该选项的既有测试不启动 Hindsight；实施 PR 必须启用它完成真实接入验收。测试期间只允许一轮数据库集成测试，沿用现有约定。NATS 命名空间和服务端口按 worktree 隔离，不能清理其他库或共享服务。

### 2.4 镜像切换、备份与恢复

Alpine 切换 Debian 镜像涉及运行库、排序规则和数据目录约定，不能直接替换镜像后把原数据卷挂上就宣称完成。PR 1 先在独立新卷验收新镜像；共享开发实例的切换单独协调停机，备份各 worktree 数据库，在新卷恢复并校验，保留旧卷直到确认。禁止以 `down -v` 或业务 `migrate:reset` 代替共享实例迁移。

Cervi 和 Hindsight 随同一个数据库备份，必须包含业务 schema、`hindsight` schema 和扩展定义，不能只备份 `public`。同时备份文件存储，记录镜像、扩展、embedding 模型及索引配置版本。同库 dump 可以提供数据库一致快照，但不能让正在进行的 HTTP 索引任务和外部文件写入也成为同一事务；恢复演练需暂停内容变更与索引任务、待已提交任务收敛，并建立对应文件备份点。

本地知识库的业务原文和文件是重建依据，Hindsight 索引可重建，但重建需要时间和 embedding 调用成本。不能因此把未来 Agent 派生记忆也视为可无损重算，记忆备份策略届时另定。

## 3. Hindsight 接入边界

### 3.1 知识库配置

使用评测过的 Hindsight 0.9.2 作为首个接入基线，正式 PR 锁定镜像摘要及 API 契约。知识库 bank 显式设置：

```yaml
retain_extraction_mode: chunks
enable_observations: false
enable_graph_retrieval: false
enable_temporal_retrieval: false
```

该模式保留分段文本，不把企业资料改写成记忆事实；普通文本索引不调用事实抽取或归纳 LLM，仍需要 embedding。默认使用 native 全文路径和向量召回、RRF 排序，不创建 mental models、不调用 reflect。接口参数应按固定版本验证，不能直接复制随后变化的在线文档。[Hindsight 配置说明](https://hindsight.vectorize.io/developer/configuration)

首版部署级配置固定一套经过中文样本验证的 embedding 模型、维度、输入上限和 query/passage 前缀；模型由 Hindsight 调用，Cervi 不重复计算向量。实验的 multilingual-e5-small 是验证基线，不是生产唯一选项；PR 2 必须锁定实际验收模型及服务镜像/模型修订，PR 3 沿用。记录索引配置版本；换模型、维度、前缀或切分配置需要重建，不能让新旧向量直接混用。

部署配置保存 Hindsight 地址和服务凭据。前端只展示本地检索服务是否可用，不新增一个与 Dify 并列、要求用户手工填写 bank 的连接器，也不把 Hindsight 密钥交给浏览器或 Agent。

bank 按本地知识库实际首次建立索引时幂等创建，新建和已有知识库使用同一路径；不为已有库执行回填迁移。知识库删除任务在 bank 不存在时视为已清理，不能把正常的未索引状态报告为远端故障。创建任务仍需验证知识库存在，避免删除后被晚到任务重建。

### 3.2 Cervi 负责的内容

- 知识库、分组、问答和文档的业务 CRUD，以及当前企业范围。
- 原始文件、原文版本、索引来源映射、处理状态与删除请求。
- 向 Hindsight 投递任务、检查真实完成条件、验证结果仍属于当前有效来源。
- 将 Hindsight 和 Dify 的结果接入现有 `search_knowledge` 与人工检索测试。

Hindsight 负责分段、embedding、索引、候选检索和内部排序。仅当指定文件格式需要时增加明确的解析适配，不在 Go 中再建设分词器、分段引擎、BM25 或索引调度框架。

## 4. 内容模型与索引生命周期

### 4.1 来源与版本

沿用已有问答表。一条主问题、多个相似问题和完整答案合成一个逻辑索引来源，保留各内容编号，不把相似问题当成独立问答。检索命中任何相关分段后，返回该条目当前版本的完整答案，不能把答案恰好落在另一段当成无答案。

普通文档新增独立文档模型，保存知识库、分组、标题、文件引用或手工文本。文件内容不借用问答答案字段。最小索引记录保存：来源类型及编号、内容修订号、索引配置版本、远端文档编号、远端操作编号、处理状态、错误及时间。只在相应 PR 出现真实用途时建表，不提前建立 memory 表或通用引擎注册表。

建议一条逻辑来源对应一条当前索引状态，远端文档编号包含来源编号和修订号。Cervi 的修订号与 Hindsight 操作编号承担不同语义：前者防止旧版本发布，后者用于请求重放和操作追踪。业务记录、索引状态和任务投递在 Cervi 的同一事务提交。

### 4.2 写入、更新和删除

1. 新建来源后显示排队，Worker 在数据库事务之外调用 Hindsight。
2. 按操作编号检查异步终态，并核对来源分段可读。文件解析完成后还可能有 retain 子任务，不能只看第一层 operation completed。
3. 只有当前来源修订号、配置版本均匹配，才能标记为可检索；失败明确显示错误，并支持复用现有任务机制重试。
4. 修改正文、问题、相似问题或答案时增加内容修订号，立即停止返回旧版本；界面显示处理中，接受短暂不可检索，不混用旧向量和新答案。单纯移动分组不重算 embedding。
5. 新版本写入成功后清理旧远端文档。晚到的旧任务不能覆盖新状态；同一版本重复执行不能产生多份可见内容。
6. 删除来源时先在 Cervi 标记不可检索，在同一事务投递删除任务。索引记录保留到远端清理完成；不能先删掉所有追踪信息再期待后台任务自行找回来源。
7. 删除知识库时清理其 bank；与该库有关的在途任务必须检查删除状态，不能重新创建被删除的库。沿用现有 Agent 失效绑定语义。

召回结果和游标读取都要校验来源仍存在、版本有效、属于本次知识库范围。更新/删除与召回并发时，在向调用方交付结果前再次校验；已失效候选被过滤后允许少于目标条数，不通过降低阈值或扩大范围补齐。已进入模型上下文的旧资料不能靠后台删除自动撤回，这与新检索的可见性是两件事。

阶段状态复用已有知识文档状态语义：排队、处理中、可用、错误。暂停、启停、归档等操作只有实现对应生命周期后才开放，不能直接展示 Dify 的全部状态为本地可操作功能。

### 4.3 任务与可观测性

复用 `task_runs + task_outbox` 和至少一次执行语义，为索引任务分配独立队列，避免长解析耗尽 Agent 或文件清理 Worker。Cervi 任务记录负责唤醒，Hindsight operation 负责远端处理；不增加第三套队列和无限多层重试。

记录 `INFO`：知识库、来源、修订号、操作编号、开始/完成状态与耗时；记录 `WARN`：远端失败、无可提取内容、旧版本结果被丢弃。日志不输出服务密钥和整份私有文档。普通 HTTP 超时不能视为远端未写入，应使用固定操作编号查询和恢复。

## 5. 检索、引用与分数

### 5.1 首版检索产品

首版提供“检索测试”和 Agent 知识检索，内部使用已验证的混合路径。**不先展示“关键词 / 语义 / 混合”三个切换项**：原生 PostgreSQL 的中文关键词召回仍有短板，Hindsight 的现成 API 也必须核对是否提供三种独立执行语义。后续有明确需求时再按真实能力提供，不用改权重或查看 trace 冒充独立检索方式。

本地知识库使用固定基础检索配置；Dify 继续使用它自身保存的配置。首次接入不加入 Go 分词补丁、PGroonga 或自写全文 SQL。人工检索和 Agent 共用同一条本地检索实现，避免测试页效果与实际对话不同。

PR 3 调整 `SearchService` 的范围加载，先读取同企业知识库，再按本地/Dify 来源分别构造现有 `knowledgeretrieval.Source{Retrieve, Read}`；不再让 Dify 连接的 INNER JOIN 排除本地库。人工单库入口同步分派，本地库不伪造 `integration_connection` 或 `external_resource_id`。本地问答页增加检索测试入口，不沿用外部文档页的 Dify 检索方式元数据。

### 5.2 结果映射与上下文

继续使用现有知识库编号、文档编号、分段编号、位置、正文、答案和 cursor。Hindsight memory ID、document ID 和 source chunk ID 先按固定版本核对，只在本地接入内部映射，不能把不同编号类型混用。

- 本地普通文档的 `documentId` 使用 Cervi 文档编号，`segmentId` 由来源索引版本与分段位置构造；实际位置与远端 chunk 的对应关系通过 API 核对。返回框架保存的原文片段，文档名和文件权限由 Cervi 来源记录解析。
- 本地问答 `Retrieve` 在进入多查询融合前先按条目折叠，保留最佳候选名次，返回主问题与完整答案；`documentId`、逻辑 `segmentId` 都使用 Cervi 问答条目编号，位置为 1。这样现有 `knowledge_base_id + segment_id` 去重能合并相似问题命中，不输出多份答案。问答 cursor 读取同一完整条目，不把相邻问答当成上下文分段。
- cursor 增加 `sourceVersion`：本地来源必填，表示内容修订号与索引配置版本共同确定的索引版本；缺失或不匹配即失效。重建、替换或删除后，不能拿旧位置读取新内容。
- Dify 没有 Cervi 管理的来源版本，`sourceVersion` 不提供；保留其远端 document/segment ID 语义，每次读取验证远端文档、分段存在及位置关系。它表示外部当前内容，不承诺历史快照或完整版本失效检测；不填固定版本占位，也不强造本地版本。按知识库来源校验字段适用性，不保留旧版 Tool 契约分支。
- 本地前后分段只读取同一来源索引版本。没有页码的解析结果只能提供文档/分段引用，不能伪造 PDF 页码。

### 5.3 融合和阈值

保留现有多查询、多知识库 RRF 编排；这是已有产品检索入口的结果整理，不扩展成可插拔搜索平台。每路先按自身规则召回，Cervi 再去重与按名次融合。Dify 的源端阈值仍只对其本路生效。

BM25、`ts_rank_cd`、pg_trgm similarity、向量 cosine、Hindsight final 和重排 logit 没有共同刻度。RRF 只利用名次，不能解决无答案判定。首版不增加统一 0～1 阈值，不把 final 显示成相关性百分比；保留源分数时明确它只用于本路诊断。

未来确需跨本地与 Dify 的统一过滤，可以在合并候选后用同一重排模型评分，再用独立中文正负例校准阈值。重排只能处理已召回候选，补不回源端已过滤的内容；sigmoid 或 min-max 归一化也不会自动把分数变成正确概率。这个能力不阻塞基础知识库。

无内容或纯标点输入在调用模型前明确拒绝；有效单字“税”“药”必须纳入测试，不能一律禁止单字。无关问题返回候选时，Agent 仍需根据原文判断资料是否支持答案，不得把“有候选”当作“有依据”。

## 6. 文件与多模态

原始文件沿用 Cervi 文件生命周期：临时上传、业务事务激活、按记录中的本地或对象存储类型读取。开启对象存储时仍由客户端预签名直传；索引 Worker 后台读取已激活文件属于索引输入，不改变客户端上传架构。

第一批普通文档支持手工文本、UTF-8 TXT 和 Markdown；之后增加 DOCX、可提取文字的 PDF。上传原件保存成功、解析成功、索引可用是不同状态。解析为空明确失败，保留原件用于重试。

索引 Worker 向 Hindsight 文件入口提交必要内容，解析器由 Hindsight 管理。优先采用处理后清理框架原件的配置；解析失败或取消时也要明确清理临时副本。Cervi 原件保留，Hindsight 中即使存在 BYTEA 文件表，也不作为产品主文件存储。

原生分段需用中文长文和所选 embedding tokenizer 实测：字符数量不能当作 token 数量，不能允许模型静默截断正文却把整份文档显示为已索引。当前 Hindsight 中文句界、重叠片段和表头保留不足，PR 4/5 的样本验收是文件支持边界，不直接开放所有解析器接受的格式。若官方配置仍不能满足基本输入，先补有限的解析适配或收窄格式支持；不默默改成自研分段框架。

后续多模态按独立能力交付：

- 图片：OCR 文本可以用于检索；颜色、布局等问题可能需要重新读取原图送入 VLM。
- 扫描 PDF：本轮 Hindsight 原生入口未通过，需要显式转页图/OCR，保留页码映射后再承诺页级引用。
- 表格：表头必须随行组保留，并验证型号、数值和列名，不能只验收文字总量。
- 音频：先确定本地或外部 ASR，测试姓名、型号、数字和否定，必要时保留时间戳与原音频。
- 视频、原生跨模态 embedding、视觉索引和 S3 网络链路尚未验收，另行安排。

原始图片、音视频与可重建的文本/向量索引分开存储。使用 S3 保存原文件不意味着 PostgreSQL 的向量或全文索引也存放在 S3。

## 7. PR 拆分与验收

本次文档 PR 仅提交方案、关联文档调整和忽略规则，不实际更换镜像、运行迁移或接入服务。

### PR 1：统一 PostgreSQL + pgvector 基础环境

范围：修改默认镜像和数据库准备 Task，确定同实例、同库、同账号的配置及扩展初始化；同步现有测试库和 CI 环境，补充新卷验证与共享实例切换说明。不增加知识业务表或 Hindsight 运行服务。

验收：空实例能创建当前 worktree 的业务库和测试库，并使用 vector；账号和连接配置只维护一份；缺少扩展或安装权限时错误清晰；现有服务端测试、构建通过。在独立卷完成备份恢复演练后，才能执行共享实例切换。数据源名称 `postgres` 可保持，避免把扩展名误当数据库协议。

### PR 2：接入 Hindsight 服务与知识库 bank 生命周期

范围：每个 worktree 的可选 Compose 服务、复用业务数据库凭据、固定版本的 Go HTTP 客户端、`hindsight` schema 与迁移/运行分离；首次索引幂等创建 bank 和知识库删除时清理 bank 的能力；基础能力探测、处理状态与测试 Task 启停顺序。未配置服务时已有本地问答 CRUD 与外部 Dify 仍可用，本地索引明确显示未配置。

验收：PostgreSQL 18 + 指定扩展组合下，Cervi 与 Hindsight 确认连接同库、同账号；双方表及迁移记录各归所属 schema，扩展算符可见，Cervi search_path 不受影响。本地默认 Compose 账号本身有超级用户权限，不为此增加第二套应用账号；另在临时验收实例中以同一个非超级用户账号运行双方，证明初始化后没有常态超级权限要求。

通过 Hindsight 侧的 chunks 入库、召回、来源 document 和 bank 删除契约测试，不在本 PR 建 Cervi 文档 CRUD；问答业务接入在 PR 3，文档模型在 PR 4。来源 chunk 与上下文映射、公开 API 的异步完成条件、重放幂等、服务重启及无 bank 删除通过；停止测试 API、重建共用测试库、迁移再启动的完整 Task 流程通过。保留上游需要的 pg_trgm，不把移除扩展的最小运行实验直接当成安装脚本。

### PR 3：本地问答支持索引、人工检索和 Agent 使用

范围：复用已有主问题/相似问题/答案，增加来源修订和索引任务；列表展示处理状态、错误和重试，问答页增加检索测试；调整单库/多库 SearchService 的本地/Dify 分派；本地先按条目折叠再进入已有融合，返回完整答案；接通 Agent 范围和版本游标，实现更新、删除及在途任务一致性。前端契约仍通过 appservice 生成。

已有本地问答由明确的“建立索引”业务操作提交当前内容；这是首次启用能力的正常索引操作，不添加一次性数据回填迁移或旧接口兼容逻辑。

验收：主问题、相似问题、同义问法能返回正确条目；长答案分段后仍返回当前完整答案；修改相似问题/答案后旧内容不再交付；重复任务、删除并发与晚到旧任务不复活来源；同企业本地与 Dify 混合检索、不同企业隔离、Run Revision 范围通过。固定基线语料应重建并保留小型业务测试，不能依赖已删除的实验目录。

### PR 4：本地标准知识库支持文本与 Markdown 文档

范围：独立文档模型，手工文本、TXT/Markdown 上传，沿用文件激活和分组；解析、索引、详情、只读分段、原件读取与上下文引用；复用 PR 3 的来源生命周期。

验收：新建、替换、删除和重试贯通；中文长文无 embedding 静默截断；符号代码与金额原文保留；旧 cursor 不读到新版本；本地文件与临时 S3 服务各完成一次实际上传、索引读取和原件下载。对象存储测试不在本地启动 S3 模拟服务；未完成真实链路前不得标记 S3 索引验收通过。

### PR 5：支持 DOCX 与文字 PDF 的基础解析

范围：增加两种格式并明确无文本文件错误、原件重试和框架临时副本清理。不包括扫描 PDF 自动 OCR、复杂表格编辑、音视频或图像向量检索。

验收：中文 DOCX、文字 PDF、无文本扫描 PDF、跨段答案、带表格/代码的代表样本；核对正文覆盖、字符完整性、来源定位和 token 上限。基础表格如无法保留列语义，必须在支持说明中明确，不能将读取成功等同于准确问答。所有产品界面按已有 Web/桌面共享入口实现，移动端专属知识管理页面另行排期。

### 后续独立议题

增强中文词法检索、重排与阈值、扫描 PDF/图片、复杂表格、音视频、持续容量与安全专项分别在明确需求下安排。长期记忆交互未定，不加入上述 PR 的依赖链。

所有实现 PR 遵循仓库迁移、生成绑定、中文注释和 Pi CLI 审核约定。Cervi 新业务表仅添加主键及业务/幂等唯一索引；Hindsight 自身索引由其迁移管理，不为遵守业务表约定而删改上游必需索引。运行测试后关闭本次服务，保留共享 PostgreSQL/NATS。

## 8. 历史评测摘要与不能丢失的结论

### 8.1 证据边界

以下是 2026-09-05 至 2026-09-07 独立实验的历史摘要，**不是本 PR 新跑的性能结果**。原实验目录包含源码副本、模型缓存、小说、日志和结果，已按要求从当前工作区删除，不提交 Git；本文保留结论、关键参数和已知失败，不再链接已删除的文件。后续实施 PR 必须用固定版本和小型业务样本重新验收，本文不能替代可复现的原始证据包。

基础质量集为 40 篇人工业务文档、26 个正例、3 个有内容的负例，以及空白/纯标点两个无效输入。小说来自 Project Gutenberg《三國志演義》《水滸傳》，保留繁体，生成 2,233 个约 500 字片段；API 基准通常只取其中 200 篇。百万行通过重复种子并添加高频词构造，不是百万篇独立标注文档。

数据库通常限 2 CPU/4 GiB；API、模型服务的 CPU/内存组织、数据库小版本、切分数量和缓存状态各轮不同。文本模型使用本地 Qwen3-4B-Instruct-2507-4bit；embedding 为 multilingual-e5-small，384 维，带 query/passage 前缀和 512 token 上限；图像模型为 Qwen2.5-VL-3B-Instruct-4bit。不能据下表做严格跨产品速度排名，也没有测百万向量或百万图谱 API。

`Hit@5` 表示前五个结果中包含至少一个标注来源；正例命中率不等于每句回答准确，也不包含无答案识别能力。

### 8.2 全文检索实验

| 路径 | 观察 | 实施含义 |
|---|---|---|
| Go gse 1.0.2 + PG 18.6 原生 tsvector | 停用词 + OR 命中前五 25/26；百万行“啊”匹配约 50 万行，完整排名 P50 3.52 秒，四并发 40/40 超过 5 秒 | Go 分词解决部分中文词项问题，不消除大候选集排序成本 |
| 同一路径，低命中查询 | “PostgreSQL 备份”约 5,015 行匹配，P50 3.1 ms | 不能概括为 tsvector 一律慢或评分一律更好 |
| 提前无序取 5,000 候选 | 高频查询显著提速，但刻意构造的尾部高相关项被完全漏掉 | 候选截断不是无损优化 |
| pg_trgm GIN 包含后全量评分 | 高频“啊”仍超时 | pg_trgm 不能自动避免高频排序问题 |
| pg_trgm GiST 局部距离前十 | 百万行同样包含“啊”的集合，P50 1.9 ms，四并发 P95 13.5 ms | 是具体距离索引与提前停止的收益，不代表自然问句准确或所有查询都快 |
| 无阈值 pg_trgm 局部 KNN | 小集 Hit@5 24/26，但负例仅 1/5 正确；该一个是空输入提前拒绝 | 总能找最近邻不等于找到了相关内容 |
| Hindsight 形态 native / Go native / PGroonga | 2,273 条词法集 Hit@5 分别 11/26、23/26、20/26；Go + PGroonga 23/26 | PGroonga 原文查询也不自动理解任意中文问句 |

几个实现细节必须保留：

- `LIMIT` 放在全量排名之后，不能限制前面的候选读取和评分工作。排序时重复 `to_tsvector` 还会增加解析成本，预存向量可改善，但高频大结果排序仍可能超时。
- AND 对自然问题太严格，OR 会放大泛词噪声；停用词不能代替有效范围限定。“税”“药”等有意义单字不应统一过滤。
- 技术符号可能在分词后再次被 PostgreSQL parser 丢失，`C++` 与 `C#` 可能均变成 `c`。简繁词典不等于简繁归一化；搜索分词扩展词的线性位置不等于原文位置。
- pg_trgm 局部相似度依赖词边界：“退款”在连续中文中的分数可能很低甚至为零；一两个中文字符的包含模式可能没有可用 trigram。GiST KNN 与字面包含是不同语义，不能混用质量结论。
- 参数化语句的通用计划与手写字面量 SQL 计划可能不同；短词延迟曾因计划缓存跳变。后续性能验证必须执行真实应用查询。
- PGroonga 的高频排序在受测布局下比 native 更快，但仍有约秒级并发成本；主表/TOAST、缓存和查询语义会改变对比结果。
- 超时期间数据库探活仍成功，未发生进程崩溃；实验复现的是昂贵请求，不是已解释用户旧系统“整库卡死”的全部原因。

Go 对 Hindsight 的试验性分词补丁依赖内部字段：`search_vector` 是应用维护列，不能只更新 `text_signals`。该补丁未形成正式扩展接口，不带入本方案。

### 8.3 三个框架的取舍

| 框架与版本 | 本轮能力 | 关键缺口 |
|---|---|---|
| Hindsight 0.9.2，提交 `ebad478240d3171bb88201ececda5e8d9883d22d` | 240 篇 chunks 混合检索 Hit@5 26/26；有明确记忆、归纳和摘要能力；基础 bank/来源生命周期可用 | 原生中文词法弱；中文分段、表头、扫描 PDF 需验证；派生事实和摘要删除有风险 |
| LightRAG 1.5.7，提交 `28ff1b05f2ac3f3e6fa14dd2cd33656579bd0c9c` | naive 428 段、40 篇图谱查询均 Hit@5 24/26；文档图谱问答可用 | 两个有效单字被 HTTP 400 拒绝；工作区请求头未切换实际实例；更新需删后再插；删除后答案缓存残留 |
| Haystack 3.1.1 + Hayhooks 1.24.0 + pgvector-haystack 6.6.0 | 440 段原生向量 Hit@5 26/26，管线可组合，图像/扫描 PDF 适配可运行 | 仍需自建来源生命周期、租户授权、记忆策略与文件保存；默认中文按空格切分不适用 |

LightRAG 的 local/global/hybrid 是实体和关系检索，mix 再结合分段向量，不等于 BM25 + 向量。全 PostgreSQL 后端需要 pgvector + AGE；“只用 PostgreSQL core”的配置把向量存到本地 NanoVectorDB、图谱放到 NetworkX 文件，并非全部索引在 PostgreSQL。

Haystack 的 PgvectorDocumentStore 在只有 core 的库里初始化失败；预装 vector 后普通账号可用。其默认关键词组件是 `plainto_tsquery` AND，不是 BM25。OVERWRITE 仅覆盖相同分段 ID；同一来源改文本后可能保留旧段，来源级替换需要显式实现。Hayhooks 文件上传返回 processed 时，本轮默认入口实际未持久化，随后下载为 404，不能据上传响应删除原件。

选择 Hindsight 是基于“基础知识库足够、未来记忆必需、减少框架组合”的取舍，不是认定它的分段或多模态最强。若只看复杂文档管线，Haystack 有价值；本轮不同时引入第二套框架。

### 8.4 Hindsight 深入评测

本节的记忆结果用于保留选型依据与已知风险，其中 observation、摘要和 reflect 不进入第 7 节知识库 PR。

深入评测的数据库为 PG 17.10，仅启用 plpgsql + pgvector 0.8.6，普通角色无数据库级 CREATE，Hindsight 关闭启动迁移。镜像仍含未启用的 PGroonga 二进制，未做完全剥离镜像测试。创建 schema 权限不足和 pg_trgm 算符不可见都曾导致初始化/运行失败；安装完成、移除 pg_trgm 后，已测运行路径通过。**这不证明原生安装只需 vector，也不证明 PG 18 或 AWS RDS 已验收。**

| 能力 | 深入评测结果与限制 |
|---|---|
| 中文连续记忆 | 60 条合成对话，15 条关键事件 + 45 条干扰；修正配置后 60 条 world、15 条 observation，8 个核心问答全部正确 |
| 纠正与确定性 | 新预算、短信偏好、延期、过敏未确认、邮件未发送、型号代码和未授权扣款均判断正确；仍有“预算”被扩写成“贷款金额” |
| 来源删除 | 三轮原始事实和 observation 均无已删预算；此前来源依赖缺失的问题本轮未复现，未修复，不能宣称消失 |
| 摘要失效 | 未刷新摘要时 2/3 仍回答旧预算；显式刷新后 3/3 正确；排除 mental models 也能避免该案例旧答案 |
| 自动摘要刷新 | 两次明确删除场景通过，包括删除最后来源后回答无信息；仍存在异步窗口，未实现产品可见性控制 |
| 范围隔离 | bank 与严格标签过滤通过；不带过滤同一 bank 会返回不同用户资料，标签不是权限系统 |
| 中断恢复 | 确认 retain 子任务 processing 后强制终止 API，重启约 12.5 秒恢复健康，随后 12 个来源完成；不是所有故障场景覆盖 |
| 基础知识库 | 独立 chunks bank，240 篇资料，26/26 正例进前五；替换、删除、引用通过；三个有内容的负例仍返回候选 |

记忆召回性能以 75 条记忆起步，包含 HTTP 和 embedding，不含回答生成：单并发 40 次 P50/P95 为 21.0/61.2 ms；四并发 40 次为 93.0/131.3 ms；同时写入 12 条事件、每秒一组四并发召回，252 次为 133.3/183.7 ms。总计 332 次无 HTTP 错误。这是小样本、有固定发起节奏的测量，不是最大 QPS 或生产 SLA。

初始本地 MLX 默认输出 512 tokens，归纳 JSON 被截断：60 条消息到失败检查点约 743 秒，168 次 LLM 调用、63 次 length 截断、6 条归纳失败，约 90.8 万逻辑 tokens。此为错误配置成本。设置 `HINDSIGHT_API_CONSOLIDATION_MAX_COMPLETION_TOKENS=8192` 后，通过官方 recover/consolidate 恢复六条；重启至完成约 60.4 秒、7 次调用、23,509 tokens，不是重跑全新 60 条入库。

正确配置后另外 12 条新事件的写入和归纳用了 25 次 LLM 调用、105,099 tokens，其中 59,483 输入 tokens 报告缓存命中，无截断。后台归纳成本不能只看 retain 返回用量；逻辑 tokens 和计费金额不同，缓存计价取决于供应商。8192 是本地验证值，不是所有模型的通用最优配置。

### 8.5 多模态与排名教训

Hindsight 的 PNG 在配置本地 OCR 后能识别型号、故障码和维修事项；扫描 PDF 同一入口失败，PDFium 转页图后交给 VLM 才通过文字问答。三套框架的图片描述都曾遗漏蓝色矩形，后续文字记忆无法回答颜色；取回原图直接问 VLM 才答对。保留原件是能力组成部分。

Hindsight 原生 WAV 因缺少 `speech_recognition` 失败；该安装版 MarkItDown 的转录代码依赖 `recognize_google`，不自动复用本地聊天模型，本轮未实际执行 Google 调用。额外接入 mlx-whisper 0.4.3 / whisper-small-mlx 后，两段约 12 秒合成普通话能处理预算和否定，但“陈小雨”“林青”、E-731 出错；后续模型还把错误姓名归为技术负责人。此链路不达可靠语音记忆要求。

Hindsight 设置保留原件后，三个文件可通过下载接口读回且 SHA-256 一致，包括解析失败文件；这验证了 BYTEA 存储，不是 S3 生命周期。旧实验未测真实 S3 网络、视频、嘈杂语音或原生跨模态向量。

无关候选的 Hindsight RRF final 可以达到 1，记忆模式加权后还可能大于 1；不能用 0.8 判断答案可靠。指定中文 CrossEncoder 在 240 篇语料的受限 CPU 上将 Hit@5 从 26 降至 24，有效查询中位延迟约 8.27 秒；阈值越高越容易漏掉正例。Haystack 的 logit 还可为负或大于 1。重排需要独立样本校准，不能照搬模型名和阈值。

## 9. 长期记忆参考：产品交互确定后再开发

本节不构成已批准的产品行为，不增加开关、管理页面、记忆表或自动写入任务，也不把记忆设为本地知识库上线前置条件。

### 9.1 推荐的技术起点

以下建议不是本轮需要实现的接口、表结构或产品交互。

- 优先复用 Hindsight 服务与同库 `hindsight` schema，知识 bank 和记忆 bank 分开配置，不能在文档库上直接打开自动归纳。
- 第一个真实场景可以限定内部成员与单个 Agent 的文本交流；建议按企业、Agent 和成员隔离。跨 Agent 共享、客服联系人、群聊及“Agent 自身经验”是不同范围，需单独确定身份和授权。
- Cervi 保留原始聊天、来源关联、操作状态与生命周期；派生事实由 Hindsight 管理，不要求复制到 Eino Store 或第二套 Cervi 事实表。
- `retain/recall` 可作为起点，回答由既有 Agent 生成；若采用 `search_memory`，范围由服务端注入。自动召回、模型按需调用或两者结合须由产品使用场景决定。
- 自动写入可从成功 Run 实际消费的新增交流开始评估，保留说话人、时间、原消息和确定性；须另定失败 Run、人工发言和显式“记住这件事”的处理规则。
- 开始时可只使用有明确来源的事实，不开启 observation 和 mental model；这是推荐的分阶段方式，不代表已在该精简配置下完成全部连续记忆验收。

### 9.2 必须先决定的交互

| 待决定事项 | 需要明确的语义 |
|---|---|
| 启用方式 | 企业级、Agent 级还是成员级；默认状态；是否对用户提示自动记忆 |
| 写入方式 | 自动提取、显式指令、人工确认，哪些发言可以成为事实 |
| 使用范围 | 同一会话、同一成员跨会话、跨 Agent、企业共享和客服周期边界 |
| 查看与纠正 | 展示原始来源还是派生事实，如何反馈错误、修改、删除或彻底遗忘 |
| 关闭与清空 | 停止读写是否保留已有数据；清空是否停止从旧聊天再次学习 |
| 原聊天删除 | 原消息、提取事实、归纳、摘要、缓存和备份分别如何处理 |
| 在途任务 | 清空时正在写入或生成回答如何终止/隔离，何时向用户显示完成 |

### 9.3 后续实现不能遗漏的事项

1. 记忆不是系统指令或工具授权；助手猜测、引用他人观点、假设与用户确认不能混为一谈。
2. 不能把模型回答“记住了”当成持久化成功。写入、归纳、摘要刷新分别有完成条件。
3. 删除原始事实不必然清除所有派生内容。旧来源依赖缺失案例需要重新回归；启用摘要时配置刷新并等待完成，或在刷新期间排除旧摘要。
4. 清空必须记录明确的数据边界，避免旧 Run、历史回放、失败重试重新写回。已经送入在途模型上下文的资料需有单独处理语义。
5. 明确模型输出预算、后台归纳成本和缓存用量；不能只统计前台对话 tokens。
6. 中文人名、代码、否定和时间变化都纳入回归；多模态先保证转录/解析忠实，上传成功不代表记忆正确。
7. bank、标签、source_id 和 operation_id 不能直接采用未经服务端验证的客户端输入；同名主体隔离必须测真实身份映射。

## 10. 固定版本与参考资料

- [Hindsight 0.9.2 发布](https://github.com/vectorize-io/hindsight/releases/tag/v0.9.2)
- [Hindsight 固定版本 HTTP/API 实现](https://github.com/vectorize-io/hindsight/blob/ebad478240d3171bb88201ececda5e8d9883d22d/hindsight-api-slim/hindsight_api/api/http.py)
- [Hindsight 固定版本检索实现](https://github.com/vectorize-io/hindsight/blob/ebad478240d3171bb88201ececda5e8d9883d22d/hindsight-api-slim/hindsight_api/engine/search/retrieval.py)
- [Hindsight 固定版本文件解析器](https://github.com/vectorize-io/hindsight/blob/ebad478240d3171bb88201ececda5e8d9883d22d/hindsight-api-slim/hindsight_api/engine/parsers/markitdown.py)
- [LightRAG 固定版本 PostgreSQL 实现](https://github.com/HKUDS/LightRAG/blob/28ff1b05f2ac3f3e6fa14dd2cd33656579bd0c9c/lightrag/kg/postgres_impl.py)
- [Haystack 3.1.1](https://github.com/deepset-ai/haystack/releases/tag/v3.1.1)、[Hayhooks 1.24.0](https://github.com/deepset-ai/hayhooks/releases/tag/v1.24.0)
- [PostgreSQL pg_trgm](https://www.postgresql.org/docs/18/pgtrgm.html)、[预备语句与执行计划](https://www.postgresql.org/docs/18/sql-prepare.html)

外部在线文档会变化。具体实施以 PR 锁定版本的源码、安装流程和契约测试为准。
