# 客服收件箱后续 PR 方案

本方案把当前工作区的角色统一与客服会话改造拆成两个可独立审查、按顺序合入的 PR。
HelmDesk 只作为交互参考；Cervi 继续以真人和 AI 共用的企业身份为核心模型。

## PR 1：企业身份统一角色归属

### 目标

- 把 `role_id` 从 `users` 移到 `organization_identities`，作为真人和 AI 员工唯一的角色来源。
- 保留 `users.status` 与 `agents.status` 各自的账号生命周期，不把角色继续放在子类型表中。
- AI 员工不能成为管理员，但可以配置为客服、普通成员或自定义角色。

### 改动

- 新增数据库迁移，将既有真人角色和 AI 默认角色写入企业身份，并移除 `users.role_id`。
- 登录身份、成员与 AI 的创建、详情、列表、编辑统一读取和写入企业身份角色。
- 批量角色调整接口改为接收 `organization_identity_id`，同一接口同时支持真人与 AI。
- 角色成员配置展示真人和 AI；AI 员工新建、详情和列表展示角色。
- 管理员保底规则只统计账号有效的真人管理员；AI 不允许配置为管理员。

### 不包含

- 角色权限拦截与权限矩阵。
- AI 客服执行、自动回复或运行状态机。

## PR 2：开放/关闭客服周期与四视图收件箱

### 目标

- `ServiceSession` 持久状态只保留 `open` 和 `closed`。
- 排队与负责关系由 `assignee_identity_id` 表达，不再保存 `waiting`、`active` 或 `pending`。
- 客户收件箱固定为「排队中 / 我负责的 / 同事 / 已关闭」。

### 改动

- 新增状态迁移，并把未关闭唯一约束改为 `status = 'open'`。
- 收件箱查询增加范围、客户视图和同事负责人参数；筛选状态同步到 URL。
- 「同事」使用下拉框，包含「全部」和所有账号有效、角色为客服的真人与 AI 员工。
- 增加领取、接管、转交、关闭和重新打开命令；重新打开已关闭周期时分配给操作人并进入「我负责的」。
- 转交接受角色为客服的真人和 AI 员工；转给 AI 只改变负责人，不伪装成已经完成自动回复。
- 会话头移除「交给 AI」，接入领取、接管、转给同事、关闭与重新打开操作；危险的关闭操作二次确认。
- 处理命令只精确失效源、目标和「全部」查询，非目标查询不立即重取；切换筛选只加载会话列表，不卸载整个消息页。
- 公开 Messenger 和移动端统一显示开放/关闭状态。

### 不包含

- `pending` 或挂起/恢复状态。
- AI 自动接待、自动分配、队列策略、SLA、满意度、未读和实时同步。
- 公开访客端点的限速与其他安全加固；按当前阶段约束继续后置。

## 合入顺序

1. 先合入 PR 1，确保所有企业身份都具有统一角色来源。
2. 再合入 PR 2，依赖统一角色查询客服候选，并完成收件箱和处理命令。

两个 PR 都不保留旧接口或双写兼容层；绑定与 appservice 适配代码随各自契约重新生成。
