# 群聊引用跳转与提及导航实施方案

本文记录本次交付的目标结构、业务语义和实施方案。业务代码与迁移已实现；验证结果见第 11 节。

## 1. 交付目标与范围

Web 与桌面端群聊支持点击引用定位原消息，并通过消息区域右下角的入口逐条查看 `@我` 和 `@所有人`。打开会话仍显示最新消息；定位后对目标消息整行短暂高亮；用户可随时返回最新消息。

本次同时补齐支撑导航的群消息顺序、连续确认水位、双向历史窗口和删除引用读取契约。通用消息上下文查询复用已有会话授权，供引用与提及共同调用。

移动端群聊、消息删除操作入口、编辑后新增提及、外部平台历史导入、实时推送和普通性能索引不在本轮交付范围。方案中的删除测试通过测试夹具覆盖现有软删除字段；不因此新增删除功能。

## 2. 产品行为

### 2.1 三个独立概念

| 概念 | 含义 | 推进或更新条件 |
| --- | --- | --- |
| 普通已读 | 用户的会话阅读进度 | 最新窗口激活且可见时推进，或主动标记已读 |
| 未读提及 | 普通已读水位之后的提及数量 | 随普通已读更新，用于会话列表和提醒 |
| 待查看提及 | 尚未通过有序导航确认的提及数量 | 仅通过提及导航连续确认，用于会话内部入口 |

打开群聊或点击“标为已读”只推进普通已读。即使用户已在最新页看见某条提及，只要没有通过导航确认，它仍是待查看项。这是明确的产品语义：导航表达待处理的提醒，不是对所有视觉阅读行为的自动追踪。

会话静音、全局通知开关和工作状态不修改提及查看水位，也不隐藏会话内部入口。引用跳转、手动滚动和发送消息不确认提及。

### 2.2 导航交互

1. 进入群聊，定位最新消息；存在待查看项时显示 `@ 3`。
2. 点击入口，重新获取当前待查看消息 ID 列表，冻结为本轮序列，跳转最早一条。
3. 进入本轮后显示 `@ 1/3`、“上一条”、“下一条”和“回到最新消息”。当前位置和分母来自本轮序列，不来自实时待查看数量。
4. 首次向前查看时，目标可见且服务端确认成功后才启用下一条。返回已确认的项目只定位，不重复写入。
5. 到达最后一条后保留本轮导航，允许回看；用户返回最新时结束本轮。导航期间到达的新提及进入下一轮。
6. 请求失败保留当前位置与原时间线，提供重试；不把网络失败当作目标失效。
7. 明确已删除的目标保留本轮序号，标记为失效并自动寻找下一条有效目标。总数不因确认或删除而变化；必要时显示简短“原消息已删除”状态。全部失效时结束本轮。

失效项之后没有有效目标、但本轮仍有可回看的项目时，保留最后一个有效项目并禁用下一条。点击其他引用时保留本轮当前位置，按第 8 节的暂停与恢复规则处理。

从锚点窗口发送消息时，先读取最新页，成功后切换到最新窗口、结束本轮导航并提交发送；期间禁用重复提交。读取失败则保留草稿、引用、本轮和原窗口，尚不发送。发送后的失败重试继续使用原客户端幂等键和最新窗口。这样用户发送后有清晰的阅读位置；本轮未确认的提及仍留在服务端。

## 3. 群消息顺序：使用会话内递增序号

### 3.1 必须解决的问题

当前 `SendGroupTextMessageAction` 在事务前生成 `originated_at`，分页和已读按 `(originated_at, source_order, id)` 比较。较早产生的消息可能较晚提交，进入已经越过的水位区间。仅让水位单调递增，无法防止漏提及。

本方案给群消息增加会话内序号，并统一群聊的时间线、分页、普通已读、提及确认和最新消息指针。不能只给提及查询引入一种与时间线不同的顺序。

选择序号的原因是它直接表达会话写入先后，既不依赖时间精度或时钟单调，也不需要人为修改消息时间。代价是必须同步改造全部群聊顺序消费者；本轮接受这项一致性改造，不扩展到尚未涉及的外部消息消费机制。

### 3.2 目标字段

| 表 | 字段 | 规则 |
| --- | --- | --- |
| `conversations` | `message_sequence bigint NOT NULL DEFAULT 0` | 群聊已分配的最大消息序号；空群为 0 |
| `messages` | `conversation_sequence bigint NULL` | 群聊文本与系统消息必须有值，从 1 开始；当前其他会话类型为空 |
| `conversation_user_states` | `last_reviewed_mention_message_id uuid NULL` | 连续确认水位指向的消息；初始空群为空，也允许指向本轮入群系统消息 |

为非空 `conversation_sequence` 建立 `(organization_id, conversation_id, conversation_sequence)` 唯一索引，作为业务顺序约束。保留现有幂等唯一索引，不增加普通查询索引。

序号在数据库和 Action 中使用 `int64`，在 appservice DTO 中以可空十进制字符串传输。前端使用生成字段，比较时按整数处理，禁止转为可能丢失精度的 JavaScript `number`。

`originated_at` 继续记录消息业务时间，`source_order` 保留现有来源顺序语义。群聊按序号排列；单聊、客户会话和公开 Messenger 仍按原三元组排列。群聊跨会话列表继续按最后消息时间排序，会话内顺序由序号确定。

### 3.3 分配与事务

群文本发送和所有群系统消息遵守同一写入过程：

1. 事务开始先调用 `identity.LockActiveUser`。
2. 使用企业和当前成员条件锁定会话行，避免未证明成员资格的请求占用会话锁；获得锁后再读取并确认成员资格和操作所需会话状态，不能只依赖等待会话锁之前的检查。
3. 检查消息幂等键。已存在时返回原消息，不分配序号。
4. 校验引用、提及目标和成员变更，递增会话序号并写入消息。
5. 在同一事务写入提及关系、更新群聊最新消息指针及相关用户状态，提交后释放会话锁。

群聊最新消息指针按序号推进，不能继续调用仅按三元组择大的摘要更新逻辑。消息时间在获得会话锁后生成，但时间不承担顺序正确性。删除最新消息不回退序号及指针；预览读取最近一条未删除消息。

发送和成员管理要求有效成员及 `active` 群；历史、上下文、待查看查询与确认允许有效成员访问 `active` 或 `archived` 群。现有 `lockGroupConversation` 只允许 active，实施时拆清会话锁与操作状态校验，确认不能直接沿用该限制。

成员增减、退出、解散和提及确认使用相同会话锁顺序。群聊写路径统一为“当前用户 → 会话 → 参与者/用户会话状态”，批量操作按稳定 ID 顺序处理，避免引入相反锁序。事务回滚同时回滚序号分配；客户端重试沿用幂等键。

这保证后提交的群消息不会出现在已确认序号之前，也让入群事件与发送消息有确定先后。

### 3.4 必须同步调整的读取路径

- 群消息历史、上下文查询、游标编解码和前端消息合并。
- 群聊 `MarkConversationRead` 及发送、系统消息产生的普通已读推进。
- Inbox 群聊未读数、未读提及数、全局未读汇总和提醒候选筛选。
- 群聊最新消息摘要、删除后的预览和引用目标定位。

重点调整共用的 `updateConversationSummary`、`advanceConversationUserReadState` 及 `MarkConversationRead`：显式按会话类型选取群聊序号规则或原三元组规则，不能将共用函数直接全部改成群聊行为。Inbox 群聊 LATERAL 和全局未读汇总中的群聊条件也必须同步调整，避免页面内外出现不同水位。只读身份仍信任 DirectBackend，不在 Query 重复校验账号状态。

appservice 游标显式区分群聊序号与现有三元组，并绑定会话。输入游标必须匹配实际会话类型。客户端只传递不透明游标，不自行构造或比较游标字符串。调整 `chat-roadmap.md` 中相应的目标约定，不保留群聊旧排序协议兼容分支。

## 4. 提及资格与入群基线

一条待查看提及必须同时满足：

- 当前身份仍是该群的有效成员；已归档但仍可访问的群允许阅读和确认。
- 消息属于当前企业、当前群聊，未删除，且不是当前用户自己发送。
- `mention_all = true`，或 `message_mentions` 存在当前用户 ChatSubject 的关系。
- 消息序号严格大于当前提及查看水位。

从 `messages` 查询并通过 `EXISTS` 匹配显式提醒，同一条消息同时 `@我` 和 `@所有人` 只返回一次。实时计数、导航列表和确认 Action 共用同一资格定义。

创建新群时没有历史消息，创始成员尚无个人会话状态行或提及水位为空都正常表示起点 0，不为初始化导航额外产生系统消息。首次确认以 upsert 建立个人会话状态，冲突时只操作提及水位。后续首次增员或重新加入时，在增员事务中把新增成员的普通已读和提及查看水位均设为本轮增员消息；保留静音等无关设置。现有成员的提及查看水位不变。

系统事件创建函数直接返回本次消息 ID/序号，初始化使用这个返回值，不间接猜测 `cv.last_message_id`。参与者仍复用原行、保留首次 `joined_at`。

离开前尚未确认的提及也随重新加入基线一起结束；加入前和离开期间的消息不会再次进入待查看队列。当前成员仍可依照既有历史权限通过引用查看加入前的消息。

水位指针读取保留软删除消息的序号；不能因为水位消息已删除而把进度当作 0。消息记录不物理删除，软删除也保留原提及关系，供确认命令识别原有资格。后续删除命令必须遵守同一会话锁规则。提及事实在本轮能力中写入后不再追加或修改，后续若支持编辑旧消息新增提及，必须另外定义提醒事件语义。

## 5. 服务端契约

所有业务契约定义于 `appservice.Backend`，带 `cervi:route`；Query 输入显式标记 `query`。Service、API 和 Proxy 使用统一生成器，绑定使用项目规定的 Wails 命令生成。DirectBackend 处理认证和错误本地化，Action/Query 返回语言无关结果。

### 5.1 查询与命令

| 方法 | 用途 | 主要返回值 |
| --- | --- | --- |
| `GetConversationNavigationState(conversationID)` | 群聊前台轮询与确认后刷新 | `pendingMentionCount`、可空 `reviewedThroughMessageID`、`reviewedThroughSequence`、可空 `latestMessageID`、`latestSequence` |
| `ListPendingConversationMentions(conversationID)` | 开始一轮导航时读取完整轻量队列 | 按序号升序排列的 `messageIDs`、可空 `lastTargetSequence` |
| `GetConversationMessageContext(conversationID, messageID)` | 定位窗口外的消息 | 目标消息所在的通用消息窗口 |
| `MarkConversationMentionReviewed(conversationID, messageID)` | 连续确认一条提及 | 当前提及水位 ID/序号及 `reviewed`、`alreadyReviewed` 或 `unavailable` 结果 |

状态 Query 中未建立的水位、没有可见消息时的最新序号以字符串 `"0"` 表达；非空消息的序号字段来自消息本身。`latestMessageID` / `latestSequence` 指最新未删除的可见消息，不直接使用可能指向已删除消息的会话摘要或分配计数器，避免一直显示不存在的新消息。空队列的 `lastTargetSequence` 为空。客户端用服务端返回的序号比较跨端水位和本轮终点，不拿 UUID 推断大小，也不额外加载最后目标正文。状态的数量与水位通过同一查询快照取得，列表和末项序号也来自同一快照。除通用上下文 Query 外，上述方法仅用于群聊。

### 5.2 选择一次读取轻量 ID 列表

MVP 在开始导航时一次返回当前全部待查看 ID，不返回正文、发送者和消息关系，也不分页。前端固定这份序列，分母就是数组长度。计数轮询使用独立状态 Query，不反复加载整份 ID 队列。

这样本轮不会因为查看水位变化、新消息或另一端推进而改变，也不需要服务端导航会话表、分页快照令牌或已查看关系表。列表通过单条有序 SQL 取得一致的消息集合；点击时列表已经为空则刷新数量，不进入空导航。

代价是开始导航时的数据量和待查看数量成正比。这是当前 MVP 的明确取舍；若实际积压规模要求分页，再设计带固定上下界的协议。不要在现阶段同时保留两种实现，或静默截断列表导致用户漏看。

### 5.3 连续确认事务

确认的是“截至此位置的有效提及均已处理”，不是任意目标的最大值。

Action 锁定当前用户、会话及用户会话状态后，按以下顺序处理：

1. 校验当前成员仍可访问该群，读取目标所属企业、会话、序号和原有提及关系。此处 SELECT 必须包含软删除行，不能沿用普通消息列表的删除过滤。
2. 目标不存在、不是本人的提及或是自己发送的消息时返回对应业务错误，不更新水位。
3. 目标序号不大于当前水位时幂等返回 `alreadyReviewed`。水位可能已由另一端推进，也可能已被重新入群基线推进。
4. 目标已软删除时返回 `unavailable`，不将其写成新的提及水位。
5. 按统一资格查询当前最早有效待查看消息。目标必须就是这一条，否则返回“提及进度已变化”的冲突结果并由客户端刷新。
6. 更新提及查看水位，返回 `reviewed`；普通已读状态不变。

已删除的较早消息被资格查询排除，因此不会阻塞之后的有效目标。删除全部剩余提及时，实时数量自然为 0，无需为清零强行推进水位。重复确认和超时后重试不重复写入。

前端只有收到明确成功结果才允许确认下一条；确认失败保留当前目标供重试。若目标显示后、确认前被删除，按 `unavailable` 处理。不能因为失败就把本地水位当作成功推进。

`reviewed`、`alreadyReviewed`、`unavailable` 均是正常返回体，HTTP 适配返回 2xx 和当前水位；前两者允许继续向前，后者按失效项规则处理。`progressChanged` 使用冲突错误，目标不属于本群、不是本人提及、本人发送或不存在使用相应业务错误；无权访问和身份失效走独立错误恢复。错误码在 appservice/i18n 中显式定义，不以字符串消息或模糊的“加载失败”推断是否可以跳过。

### 5.4 多端与权限变化

各端共享服务端水位，本轮列表保存在各自页面。另一端已确认的目标在本端可继续回看，服务端返回 `alreadyReviewed`；不会回退进度或改变本轮分母。

状态刷新发现水位超过本轮最后目标时，结束过期导航并刷新待查看数量，但保留当前历史窗口和滚动位置，不强制跳到最新。这个规则也覆盖离开后重新入群导致的基线前移。水位仅到达本轮最后目标时可保留本轮回看。

会话访问权丢失、用户被移出或身份失效，终止整个导航并走会话/会话入口恢复；不能将其当作若干消息失效逐条跳过。相同会话内的旧跳转响应必须被请求代次隔离。

## 6. 通用消息窗口与删除引用

### 6.1 窗口响应

将成员消息页统一为以下语义，普通分页和上下文查询使用同一 DTO：

| 字段 | 语义 |
| --- | --- |
| `messages` | 服务端权威顺序的连续可见消息数组 |
| `before` / `after` | 保留原响应字段名，统一表达首尾消息位置；非空窗口始终返回，用于翻页和增量读取 |
| `hasEarlier` / `hasLater` | 本次查询快照下，该方向是否还有未返回的可见消息 |

空窗口的两端游标为空。空的增量页不会覆盖已持有的尾游标或反向边界，只把本次请求方向标记为已到边界。方向是否还有历史记录必须读布尔字段，不能由 `after` 是否存在推断；到达最新也需要尾游标等待新消息。

`ListConversationMessages` 保留 `before` / `after` 查询方向，每页 50 条，统一正序返回。初始最新页的 `hasLater = false`；上下文或分页查询对两端分别给出真实边界。群聊按序号，其他现有会话类型按三元组。目标检测、邻域和边界使用单条 SQL 或同一短只读快照，避免前后查询读到互相矛盾的结果。

上下文 Query 先授权并校验目标属于该会话，返回目标及前后各最多 25 条可见消息；边缘不足时不强行补足另一侧。共用基础消息查询、DTO 组装、引用、提及和系统事件加载，不复制整套关系 JOIN。

`ConversationMessage` 增加可空 `conversationSequence` 字符串字段，群聊消息必须返回。历史页、上下文、发送回包和系统消息都复用同一契约及转换，前端不另外声明顺序模型。

目标已删除或不存在返回消息不可用；无权访问会话返回会话不可访问。普通时间线继续排除已删除主消息，序号不连续不代表丢页。

### 6.2 引用摘要契约

扩展生成的 `ConversationMessageReference`，增加明确的可用状态：

- 可用：返回目标 ID、正文和发送者。
- 已删除：保留目标 ID 和删除状态，不返回已删除正文；发送者按契约允许为空。

前端显示“原消息已删除”，禁用跳转；点击尚未刷新但已经删除的引用时，显示同样结果并失效相关消息资源。

正常引用关系指向软删除目标属于业务状态，不能再触发 `ErrDataInvariant` 导致整页失败。真正不存在的关联记录仍视为数据不变量错误，不新增历史数据修复或任意缺失兜底。

## 7. 前端状态与窗口管理

### 7.1 职责划分

| 模块 | 职责 |
| --- | --- |
| `use-conversation-timeline.ts` | 当前连续窗口、前后分页、消息顺序、最新模式与锚点模式 |
| `use-conversation-message-navigation.ts` | 统一定位流程、请求代次、目标可见确认与高亮 |
| `use-conversation-mention-navigation.ts` | 本轮 ID 序列、当前位置、已确认/失效状态及命令推进 |
| `conversation-mention-navigator.tsx` | 入口、位置、上一条、下一条、返回最新的展示 |
| `conversation-timeline.tsx` | 消息渲染、视口和行节点，组合上述能力 |

职责随实际代码体量拆分，不为几行逻辑额外抽函数。群聊使用生成的序号比较服务端消息；其他会话保留三元组顺序，并避免 `Date.parse` 截断小数秒造成同毫秒消息顺序错误。前后分页以服务端顺序为准，按 ID 去重；本地未落库消息通过客户端发送状态展示，不能参与服务端水位比较。

### 7.2 明确状态来源

- 页面只有一个正在渲染的连续窗口，模式为 `latest` 或 `anchor`；连续窗口的多页组合属于 hook 的 UI 状态，不回写 Query 缓存。
- 页面保存的本轮 ID 序列、滚动位置和发送草稿属于 UI 状态。
- 服务端查询结果交给 `useResource`；全部 key 在 `resourceKeys` 工厂定义，包含会话 ID、目标 ID、方向及游标等所有请求参数。
- 新增与重构的时间线读取、分页和轮询统一走 `useResource`，不另写 `useEffect + fetch` 取数样板，不手工修改 Query 缓存。
- 提及确认成功后失效导航状态资源；普通已读成功后仍按现有规则失效 Inbox。确认操作不自动重取并替换本轮队列。
- 绑定调用不取消；用请求代次及所属会话、窗口验证结果是否仍可应用。

每次开始本轮通过刷新得到新队列，再保存为本轮 UI 快照；不直接采用可能过期的缓存结果。开始后关闭队列查询的自动刷新，实时计数由状态 Query 独立负责。

导航状态复用 `useMemberChatPollingActive` 的前台门禁和现有成员消息轮询间隔，通过 `useResource` 的 `refetchInterval` 调度，不额外创建一套轮询计时器。

### 7.3 最新模式

无游标加载最新页；向上加载历史，尾端通过 `after` 增量读取。大量新增超过一页时仍逐页取得连续结果，不能跳过中间页直接拼入最新页。

接近底部时延续当前跟随行为；上翻时保持当前消息和像素偏移。只有确定窗口已覆盖尾端、页面激活且用户在底部时，才执行“最新消息已读”。分页尾端不能误当作会话最新。

发送状态和服务端保存结果只在最新窗口整合。替换本地发送项目时按 ID/客户端幂等标识去重，服务端消息按序号排序，不能让发送回包跨越尚未补齐的消息间隙：若保存结果超过已知连续尾端，先补齐 `after` 页；本地发送展示与服务端连续窗口分别管理。

### 7.4 锚点模式

任何主动引用或提及定位都进入锚点浏览意图，包括目标已存在于最新窗口的情况。已有窗口直接复用，只改变模式并滚动，不重新取上下文；这样近距离跳转同样不会被最新页自动贴底打断。

当前目标不在窗口时先后台读取上下文；请求成功后一次性切换窗口并定位，失败保持原内容。定位请求进行时暂停自动跟随，失败恢复原浏览意图。后续可向更早或更晚加载，每次只合并相邻页。

锚点模式不执行最新页合并、自动贴底或“标最新已读”。本轮先采取清晰的普通已读策略：浏览锚点不主动推进普通已读；回到最新再恢复既有已读流程。后台普通已读轮询结果也不能改变滚动位置。

“回到最新消息”按实际视口和会话尾端显示：上方浏览、当前窗口仍有后续页，或导航状态报告窗口之后有新消息时显示；手动滚到已覆盖真实尾端的窗口底部后隐藏。提及轮次进行中仍保留结束本轮并回到最新的入口。锚点期间通过导航状态 Query 获取真实最新消息 ID，发现尾端之后有更新只显示“回到最新消息”提示。历史补页不能记入新消息数量。若没有独立准确的新消息计数，只显示文字，不用序号差冒充未读条数。

向后补页读到查询时尾端后，仍保留锚点浏览意图，不自动切成跟随模式；由用户点击“回到最新消息”或发送消息切换。

### 7.5 返回最新

点击后重新读取最新页，成功再切换为最新模式并滚到底部，结束本轮导航；失败保留锚点和重试入口。直接使用独立最新页替换窗口，不能把它与遥远锚点合并。

轮询期间的响应、旧历史分页和先前上下文响应都必须验证窗口代次。切换会话、返回最新或新定位操作开始时递增代次，清理旧的定位目标和高亮。

底部导航与新消息提示共享固定布局区域；不会互相覆盖，也不通过插入正文行挤动消息。

## 8. 定位、可见性与动画

引用与提及调用同一个 `locateMessage(messageID)` 流程。当前窗口已包含目标时直接定位；否则加载上下文。每次请求只允许最新一次完成滚动及高亮，旧结果即使后返回也不生效。

定位使用稳定 `data-message-id`，仅滚动时间线视口，不滚动整个工作台。目标过高时对齐正文起始处；普通目标居中。等待 DOM 布局完成后以实际目标节点和当前视口交集判断可见，不把请求完成、DOM 存在或调用滚动 API 当作已查看。

提及确认同时满足：当前会话仍选中、页面可见、按平台规则窗口有焦点、当前定位代次有效、目标是当前导航项，并有足够正文进入视口。可见阈值沿用现有语义：至少 `min(目标高度的一半, 32px)`，使超长消息也能正常确认。

跳转期间禁用重复向前确认。用户在本轮内点击任意引用时，取消尚未发生的可见确认，高亮和焦点跟随引用目标，本轮序号仍指向原提及项；引用本身不确认提及，即使引用目标恰好是原提及项。原项未确认时禁用下一条并显示“继续查看”，点击后重新定位原项并恢复可见确认；原项已确认则可以直接进入下一条。回到最新或发送按第 2 节规则结束本轮。

切换会话或窗口同样取消尚未发生的可见确认。已经发往服务端的命令无法取消，服务端幂等处理；其迟到响应不能改写另一会话的 UI。若重新定位后原项实际上已确认，按 `alreadyReviewed` 正常继续。

高亮作用在消息行，不包含日期分隔或服务会话标题。优先使用 CSS 背景动画和动画结束事件，持续约 2 秒，不改变尺寸、不重挂载整个消息行；同一目标再次定位也重新触发。减少动态效果设置下改为短暂静态高亮，用单个可清理计时器结束。切换目标、会话及卸载时清理计时器和监听。

引用摘要使用原生按钮语义，支持 Enter/Space 和明确可访问名称；定位后为目标消息行提供程序化焦点，焦点移动不再触发额外滚动。导航状态使用简短状态播报，避免每条普通消息更新都被朗读。

## 9. 异常、日志与数据变更

| 场景 | 处理 |
| --- | --- |
| 上下文加载失败 | 原窗口不动，Toast 提示，可重试 |
| 目标已删除 | 引用展示删除状态；本轮导航标记失效并继续 |
| 确认失败/超时 | 当前项保持未确认，提供重试，禁止确认下一条 |
| 另一端已确认 | 幂等成功，本轮允许回看 |
| 提及连续性冲突 | 不推进；结束过期本轮并刷新入口，保留时间线位置 |
| 会话不可访问 | 停止整轮，恢复到可访问的会话列表 |
| 身份失效 | 使用统一会话入口恢复 |
| 旧请求返回 | 忽略 UI 结果，不取消 Wails 调用 |

遵循现有结构化日志入口，对数据库/服务错误记录操作、企业和会话 ID。正常跳转、重复确认、失效目标无需逐次写 `INFO`；连续性冲突可按现有业务错误方式返回，不制造告警噪声。不记录消息正文。新增具名函数和关键顺序约束用简洁中文直述注释。

已合入 main 的迁移不修改。实施时通过 `wails3 task make:migration NAME=add_group_message_sequence_and_mention_review` 生成增量迁移，提供 Down 和中文字段注释，加入本方案的唯一约束。不写旧群消息序号回填、旧游标兼容或空序号兜底；旧群消息没有序号的数据库不能直接作为本方案运行环境。开发环境先停止服务端，确认受影响数据后执行 `wails3 task migrate:reset` 并重新初始化；测试环境使用 Task 重建的测试库。不能将一次性修复写入产品迁移。

## 10. 实施顺序

1. **群消息顺序基础**：迁移与模型、文本/系统消息共用序号分配、会话锁顺序、幂等、普通已读/Inbox/通知及群游标改为序号，补充并发集成测试；同步架构文档。
2. **提及服务端**：入群基线、实时状态 Query、轻量完整列表、连续确认 Action 及权限/多端/删除测试。
3. **通用消息窗口**：共用关系加载、双向边界、上下文 Query 和已删除引用 DTO；调整已有成员消息页契约及消费者。
4. **时间线重构**：单一连续窗口、两种模式、统一资源读取、发送结果与轮询隔离、定位和高亮。先验证原有最新页与普通已读行为。
5. **接入交互**：引用按钮、提及导航、失效项/确认重试、返回最新、键盘和可见性门禁。
6. **整体验证与评审**：运行规定检查，使用 Wails MCP 验证桌面交互；Web 界面由用户操作反馈，或获得本任务明确授权后再控制浏览器；提交实现 PR 前调用 Pi CLI 并处理反馈。

每一步都保持明确的最终目标，不为同一未合并实现 PR 的中间模型追加过渡迁移。若拆分代码 PR，已合入的迁移按仓库规则使用后续增量修改。

## 11. 验证与验收

### 11.1 自动验证结果

- 服务端完整测试通过。新增 PostgreSQL 集成测试覆盖提及去重、普通已读独立、禁止跳跃确认、重复及并发确认、软删除水位、重新入群和静音保留、归档查看、企业隔离、上下文双向分页及删除引用。
- 事务屏障测试在 INSERT 后、提交前暂停写入，等待另一事务确实阻塞于 PostgreSQL 群锁，再验证序号与提交顺序。移除成员的屏障测试验证等待锁后的发送重新校验资格。
- 并发发送验证幂等重试只产生一条消息，群序号连续；改变来源时间不会改变群历史顺序。
- 前端测试覆盖超过 JavaScript 安全整数的序号、微秒来源时间、相邻页合并、空页和重复页；通过 `wails3 task test:frontend` 运行。
- 增量迁移在独立空库完成 Up → Down → Up 验证。

### 11.2 Wails MCP 页面验证

在独立验收企业和桌面存储中创建 85 条消息，通过真实 API Proxy、绑定和 React 页面验证：

- 三条提及按 `1/3 → 2/3 → 3/3` 确认，已确认项可以回看，普通已读不清除本轮。
- 当前窗口内定位复用消息；窗口外定位替换连续窗口；故意延迟 A 响应后定位 B，A 的晚到结果不会恢复旧窗口或高亮。
- 控制页面前台状态验证失焦不确认；引用打断后保留原提及位置，“继续查看”后才确认。
- 锚点期间新增消息不会改变消息行数或滚动位置；回到最新后读取到新增消息。
- 引用定位后手动滚回真实尾端时，“回到最新消息”隐藏；历史页末尾还有后续消息，或窗口外收到新消息时仍显示。最后一页追加且不触发滚动事件时，按钮按更新后的视口位置显示，手动滚到底后隐藏。
- 注入最新页读取失败后，发送保留草稿与锚点；恢复读取后才发送，并回到尾端。
- 高亮挂在目标消息整行；重复定位重新触发。前台状态与动画时钟由 MCP 测试注入控制。
- 末尾提及删除后自动跳过并保留有效项回看；另一设备推进越过本轮上界时结束旧导航。
- 引用失效后立即禁用摘要并清除旧正文；延迟历史分页期间刷新收件箱，原可见消息仍保持相同像素偏移。

当前环境的 Wails 原生 `Application.Show` 调用触发 `SIGTRAP`，窗口保持隐藏。上述验证使用实际 WebView 页面，但前台状态与动画时钟是测试控制值；操作系统焦点、真实窗口显示和减少动态效果的视觉表现仍需在正常桌面会话人工验收。未控制 Web 浏览器。

### 11.3 构建与审查

前端生产构建、服务端构建、桌面 Go 测试和 macOS arm64 原生构建通过。Pi CLI 完整审查指出的后向分页文案、引用失效状态及架构文档问题均已修正；已移除无关格式差异并重新生成绑定。Pi CLI 第二轮复核确认修正生效，未再发现可稳定复现的主流程问题。

实现阶段从仓库根目录执行：

```bash
go generate ./internal/appservice
wails3 generate bindings -clean=true -ts -i
wails3 task common:build:frontend
wails3 task test:frontend
wails3 task test:server
wails3 task test:desktop
wails3 task build:server
wails3 task darwin:build ARCH=arm64
```

生成器命令遵循 Backend 契约的专用约定；其他构建与验证使用平台 Task。`test:server` 会重建本 worktree 的测试数据库，同一时刻只运行一次。当前平台变化时使用对应原生构建 Task。桌面交互验证通过 `wails3 task dev:mcp` 启动，检查最新页、远距离引用、多条提及和并发到达消息的完整流程。
