# 聊天重构与实时功能 PR 清单

## 执行约定

PR01–PR19 与 PR37 已合并（最新 `#306`），原始范围与验证记录见 `chat-realtime-completed.md`。本文只保留共用契约和尚未交付的范围。2026-09-10 对原路线图做过一次范围削减评审，结论见「本轮范围削减」；原清单的 PR00 已改编为 PR16 并交付。2026-09-13 对通知发布、兜底探针和 AI 流传输做了第二轮调整，结论见「第二轮调整」，与第一轮结论冲突处以第二轮为准。2026-09-15 对授权复核、客户端版本追赶、帧优先级和非主线范围做了第三轮调整，结论见「第三轮调整」，与前两轮冲突处以第三轮为准。

- 一个 PR 交付一个明确行为；后端能力可用真实数据库与应用服务测试独立验收。正确性耦合的迁移、写入、读取及绑定一起提交，不拆成无法运行的中间版本。
- 保留 Conversation、ChatSubject、Participant、ServiceSession、Agent Run 和 appservice 边界。持久命令走 HTTP／Wails；WebSocket 推送版本通知、撤销控制和 AI 流等临时流帧，客户端通过业务 Query 读权威数据。
- 已确认的置顶语义不再重复确认：**个人置顶、本人跨端同步、手动排序、新消息不改变顺序**。
- 每个 PR 使用相关 Task 测试和构建，契约变更同 PR 运行 appservice 生成器及 Wails 绑定生成；不手改生成文件。提交前完成 Pi CLI 审核并处理反馈。界面验收遵守仓库授权约定，每轮清理本次启动的进程和端口。
- 已合入 main 的迁移不改；新增迁移由 Task 取实际时间生成并提供 Down。无历史回填、双协议或临时兼容分支。
- 有当前用户的写 Action 先执行 LockActiveUser；渠道／Agent 任务保留各自守卫。随后按共用契约锁会话和业务状态。
- 各端尚未切换实时前保留原轮询；只在对应端通过补拉、失权和阅读回归后移除。每个 PR 删除本次替代的代码，不保留两个状态所有者。

## 本轮范围削减

原清单把「实时」建成了一套可离线重放的持久同步引擎：三类受众各有持久水位表、压缩唤醒行、完整重建基线、保留窗口和恢复 epoch，外加 Protobuf 工具链、一次性票据表和跨节点快照。而清单自己把持久离线库列在范围外，客户端是刷新即重建的内存投影，服务端当前是单进程内嵌 Worker Pool。以下按 CLAUDE.md「MVP 阶段不为尚未出现的生产规模问题预先建设」削减。

| 原范围 | 处理 | 依据 |
| --- | --- | --- |
| `user_sync_states`、`user_conversation_wakeups`、客服／访客持久水位与压缩索引（原 PR17／19／20） | 删除；保留的 `realtime_outbox` 已由第二轮删除 | 持久行的唯一价值是离线重放，而离线库在范围外；客户端重连等价于重读已加载窗口。持久行也不减少扇出 |
| 完整同步索引与重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 推迟 | 服务对象是持久游标；内存投影刷新即重建，旧游标随页面消失 |
| `conversation_sync_events` 与 `sync_seq`（原 PR16） | 降级为 `conversations.version` | 事件载荷没有消费者，全部靠 Query 重读；细粒度失效属于优化 |
| Protobuf、Buf、Edition 2024 与 breaking 检查（原 PR27） | 降级为手写 JSON 帧契约，保留平台探针 | 帧载荷是几个整数且允许丢失；仓库已有 appservice 生成器和 Wails 绑定两条强制生成链路 |
| 一次性连接票据表（原 PR28 及 PR31 票据部分） | 删除 | 原生端由 Go 侧持有连接，凭据边界不变；Web 首帧沿用既有 Bearer |
| 跨执行节点运行快照（原 PR44） | 推迟 | 服务端当前单进程内嵌 Worker Pool，没有消费者 |
| AI 流走 Gateway 焦点注册与帧优先级（原 PR45／PR46 传输部分） | 改为独立 SSE；已由第二轮改为同一 WebSocket 按 runId 订阅 | AI 流是高频、可丢、临时的，与变更通知语义不同；省掉焦点注册表、帧饿死处理和快照修复 |
| 实时链路容量与故障验证（原 PR41） | 推迟为证据触发 | 尚无生产部署，合成压测数字不构成产品承诺 |

第一轮后剩余 33 个可交付 PR（原为 40 个，含未做的 PR00），当前数量见「第三轮调整」。条目数减少有限，真正的削减在内容：删掉了四个子系统，事件日志和协议工具链各降一级。

## 第二轮调整

第一轮削减后，兜底探针已经是「丢通知最终一定恢复」的唯一保证，但清单仍保留了为「通知不丢」服务的 Outbox 与租约发布器；探针的版本合计值存在碰撞；AI 流为避开帧优先级另开 SSE，使原生端要维护两条长连接。本轮按同一原则继续收敛。

| 调整对象 | 处理 | 依据 |
| --- | --- | --- |
| `realtime_outbox` 表与独立租约发布器（PR18、PR23） | 删除；事务内登记通知，提交成功后直接发布 Core NATS，PR23 并入 PR18 | Outbox 只关闭「提交后、发布前进程崩溃」这一窗口，该窗口丢失的通知由 30 秒探针恢复，符合通知可丢的契约；少一张表、一个后台组件和整套租约测试 |
| 探针的版本合计值（PR17） | 改为对可见会话 `(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希求和 | 数量加 `version` 求和在「失去一个会话、同时获得另一个同版本会话」时数量与合计值都不变，探针漏检 |
| 探针覆盖范围（PR17、PR40） | `conversation_user_states` 新增本人会话状态版本并纳入校验和；PR40 起探针同时返回 `pinOrderVersion` | 已读、提及确认、静音、手动未读和置顶顺序不推进会话版本；通知丢失后只能靠探针发现，探针必须覆盖所有写本人受众通知的状态 |
| AI 流独立 SSE（PR38、PR39） | 改走同一条 WebSocket，按 runId 显式订阅与取消订阅 | 原生端连接由 Go 侧 Proxy 持有，SSE 需要另一套认证、重连与事件转发；按 runId 订阅、首帧快照、流帧低于控制帧和变更通知、溢出时丢弃并要求重订阅，即可覆盖焦点注册与帧饿死问题；流帧优先级与溢出重订阅已由第三轮删除 |
| 多节点验收与 Drain、随机重连下线（PR25、PR35） | 降级为单节点正确；订阅继续按受众 Subject 定向，保持多节点可扩展的边界 | 服务端当前是单进程内嵌 Worker Pool，多节点行为没有真实部署可验收 |
| 资料变更逐会话推进版本（PR22） | 保留 | 探针因此自然覆盖资料变化，客户端只有一种失效路径；改名等低频操作的写放大在 MVP 规模可接受 |

编号保持不变以维持与 `chat-roadmap.md`、`agent-roadmap.md` 的引用一致；编号 PR23 空缺，发布职责由 PR18 交付。PR18 起剩余 28 个可交付 PR。

## 第三轮调整

第二轮之后，通知只携带版本，兜底探针保证最终恢复，但连接层和客户端仍保留了按「通知必须及时送达、授权必须实时收回」设计的机制：服务端定期复核授权、按种类串行追赶目标版本、四级帧优先级与流帧溢出重订阅。本轮按「通知可丢、Query 权威、快照可重建」继续收敛，并推迟不在成员消息主线上的范围。

| 调整对象 | 处理 | 依据 |
| --- | --- | --- |
| 服务端定期复核授权与群失权控制通知（PR25、PR38） | 删除；撤销控制只覆盖登出与停用。连接最长存活 1 小时，到期关闭后客户端重连并重新认证；`SubscribeRun` 每次重新校验登录会话、账号状态与会话阅读资格。群失权由 PR19 的 `conversation_removed` 通知驱动 Gateway 移除该会话的 AI 流订阅，客户端发现失权后也取消订阅 | 会话投影由 Query 与兜底探针恢复；受限的临时内容只有 AI 流，由逐次订阅校验、失权通知与连接寿命约束。`conversation_removed` 丢失且客户端未取消时，被移出者最多继续收到已订阅的当前一轮输出 |
| 客户端 `target`／`applied` 版本比较与串行追赶（PR28） | 删除；客户端不保存实体版本，收到通知即失效对应 resourceKeys，同一 key 的失效按短窗口防抖合并 | 失效后发起新的读取，最终以失效后的重读为准；本人写入回显的通知多触发一次重读，不影响正确性；Query 返回值无需增加实体版本字段 |
| 每连接四级帧优先级与流帧溢出重订阅（PR25、PR38） | 改为单写协程加一条有界发送队列，队列内同一 Run 的待发增量合并；队列溢出即以 `slow_consumer` 关闭连接 | 重连后由探针恢复通知、由订阅首帧快照恢复流，关闭连接覆盖全部溢出场景 |
| 独立 JSON 帧契约 PR 与平台探针记录（PR24） | 并入 PR25；首版只定义当前使用的帧。已完成的 Web、macOS 原生端与 Cloudflare Tunnel 探针结论写入 PR25 与 `chat-roadmap.md` 第 10.12 节，iOS、Android 在 PR33 实际接入时记录 | 帧契约脱离 Gateway 无法端到端验收；探针发现的升级挂载与写入器约束直接决定 Gateway 实现方式 |
| Gateway 部署与生命周期（PR35） | 原生端子路径、心跳与优雅关闭并入 PR25；指标与就绪顺序推迟 | 前者决定连接能否在反向代理和隧道后工作；Web 端的 Wails 运行时本身不支持子路径部署，服务端不增加路径前缀配置；出现真实部署前以 `INFO`／`WARN` 日志观测 |
| 名称、头像与渠道资料失效（PR22） | 推迟 | 资料修改低频，推迟期间他人资料在会话因其他变化重读时更新；第二轮保留的设计理由仍成立，重新开启时按原设计实施 |
| 网站挂件拆分为 TS 模块（PR31） | 推迟；PR32 在现有 `chat.js` 内接入连接，收到通知或重连时调用现有拉取逻辑 | 挂件实时只需把轮询触发换成通知触发，不依赖整份脚本重构 |
| 网站公开 AI 流（PR39） | 推迟；访客经会话通知读取最终回复，生成期间沿用挂件现有的正在输入提示 | 公开投影与接管后候选作废的复杂度高于首版价值 |
| 挂件跨标签页身份发现（PR32）、本地通知跨标签页前台协调（PR34）、NATS 恢复时主动要求校验探针（PR25）、前端收发队列上限与 `bufferedAmount` 观察（PR27） | 推迟 | 不影响单页面内的最终正确性，NATS 恢复场景由 30 秒兜底探针覆盖 |

编号 PR24 空缺，帧契约由 PR25 交付；PR22、PR31、PR35、PR39 保留条目并标注触发条件。PR19、PR37 已合并，剩余 20 个可交付 PR，其中 PR43–PR46 按需求或证据开启。

## 已交付基线

PR01–PR19 与 PR37 已交付以下能力，后续 PR 直接依赖，不重复定义。

- **锁序：** `internal/actions/chatstate` 提供会话授权与锁定入口，覆盖真人单聊、独立 AI 聊天、群聊和客户会话；可读、可发、可管理三种资格在锁后判断。
- **`message_seq`：** 全部会话类型统一，会话锁内分配，`last_message_seq` 保存已提交位置；HTTP 用字符串、TS 用 bigint。`read_seq` 为阅读基线，服务端单调推进。
- **`lastActivityAt`：** 独立活动排序时间，会话锁内取 `GREATEST(last_activity_at, clock_timestamp())`；幂等重放、摘要重算和纯状态变化不推进。预览时间仍是 `lastMessageAt`。
- **`chatstate.AppendMessage`：** 唯一消息追加入口，同事务维护会话／周期摘要。
- **`clientMessageId`：** 发送方作用域的发送关联，只向本人返回，与业务幂等键分开保存。
- **收件箱查询：** `LoadInbox`（keyset 双向分页，`nextCursor`／`beforeCursor`／`startCursor`／`endCursor`／`hasMore`／`hasBefore`）、`ReadInboxWindow`（按首尾游标重读包含边界的闭区间）、`GetInboxContext`（锚点邻域，锚点状态 matching／outside_query／unavailable）、按 ID 独立摘要与批量摘要。游标绑定企业、用户、规范化筛选与排序规则版本。
- **列表控制器：** `frontend/src/features/inbox/inbox-list-controller.ts` 串行调度刷新与补页，保存已加载 ID、双向边界和锚点；Web、桌面与移动端已按服务端顺序自动重排并保留原邻域。
- **收件箱筛选：** `LoadInput` 的渠道、服务状态与会话类型参与游标校验和按 ID 资格；前端范围规则集中在 `inbox-query.ts`，三端共用。
- **发送状态：** `outgoing-message-store.ts` 由工作台级 Provider 持有，按会话分组，文本与附件共用；按本人 `clientMessageId` 与消息窗口对齐收尾，离开页签后仍写入结果。
- **禁用账号会话：** 单聊与 AI 会话的列表资格只按会话归属与会话状态判断，摘要返回 `peerStatus`／`agentStatus`。
- **变更版本：** `conversations.version` 在分配 `message_seq` 的同一语句推进，覆盖成员消息、附件消息、客户入站、AI 回复和群系统事件，群资料修改同样推进；`conversation_user_states.version` 随已读、提及确认、静音、手动未读和入群阅读基线推进；`users.profile_version` 经 `identity.UpdateUserIdentity` 和用户账号更新按实际变化推进，修改密码与团队不推进。幂等重放、回滚和实际无变化的写入均不推进。
- **同步探针：** `GetSyncHeads`（`GET /sync/heads`）在一条语句内返回可见会话数量、`(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希和与身份资料版本，64 位值以字符串传输；客户会话覆盖全部视图与服务状态。
- **提交后发布：** 写事务经 `realtime.RunInTx` 执行，`realtime.Notify` 在事务内登记通知，同一受众、种类和会话合并为最高版本；提交成功后交给进程级 `Publisher` 异步发布 Core NATS，回滚丢弃，队列已满或发布失败记录 `WARN` 并丢弃。Subject 为 `cervi.<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，载荷为 `kind`、`conversationId` 与字符串 `version`。已接入内部会话消息（含群系统事件、AI 结果与附件消息）通知当前真人成员，本人会话状态与身份资料只通知本人，目前只登记 `user` 受众。附件在上传完成后逐个发送，服务端不保存上传中的占位消息。
- **成员会话变化：** 群创建、资料、增员、移除、退出、转让、解散，以及成员会话 AI 运行开始与失效取消接入会话版本和通知；`chatstate.TouchConversation` 在已持有会话锁的事务内推进版本并通知真人成员。失去阅读资格的真人收到仅含会话 ID 的 `conversation_removed`，该种类不与会话变更合并；客户会话只推进版本，受众由 PR20 接入。
- **运行流：** `ExecuteAction.SubscribeRunStream(runID, onDelta, onEnd)` 在同一把锁内返回当前尝试的快照并登记回调，之后按序回调增量；增量携带 `runId`、`attempt`、`streamId` 与起止序号，`MergeStreamDeltas` 合并首尾相接的增量，执行尝试退出或增量无法应用时回调结束。token 不写 Message、不推进会话版本。

## 变更版本与通知契约

| 名称 | 归属与含义 | 不能拿来做什么 |
| --- | --- | --- |
| `message_seq` | 单会话内最终消息顺序，事务内分配；游标和阅读位置据此比较 | 不表示资料变更 |
| `conversation.version` | 单会话可见变化的版本，消息、群关系与群资料、资料引用和 Run 终态推进 | 不等于未读数，不与 `message_seq` 混用，不跨会话比较大小 |
| `identityProfileVersion` | 本人身份资料与账户级偏好的版本 | 不表示某个会话变化，不包含设备本地通知许可 |
| `conversation_user_states.version` | 本人对单会话个人状态的版本，已读、提及确认、静音和手动未读推进 | 不表示会话内容变化，不跨用户比较，不代替 `read_seq` |
| `read_seq` | 用户实际阅读基线，保留提及确认的独立语义 | 收到帧、打开连接或补拉完成都不能直接推进 |
| `generation` / `queryGeneration` | 当前登录会话／当前列表查询的本地代次 | 不跨设备同步，也不代替服务端授权 |
| `pinOrderVersion` | 本人全局置顶顺序版本，写入时用于冲突检测 | 不因新消息、已读或静音变化而推进 |

写入链路为：Action 校验与加锁 → 业务事实与 `conversations.version` 或本人会话状态版本同事务提交，事务内登记待发通知 → 提交成功后异步发布 Core NATS，请求响应不等待 Flush，回滚则丢弃 → 客户端经 HTTP／Wails 重读。**通知只说明「哪个会话或本人会话状态变到了版本 N」或「身份资料、置顶顺序变了」，不携带业务内容；通知可重复、可合并、可丢失，业务正确性完全由 Query 恢复。**

客户端不保存实体版本：收到会话、本人会话状态、身份资料或置顶顺序通知即失效对应 resourceKeys 并重读，同一 key 的失效按短窗口防抖合并。失效后发起新的读取，已发出的 Wails 绑定调用照常完成、返回后按 signal 丢弃，最终以失效后的重读为准；未挂载的 Query 不处理，打开时直接读最新；读取失败时查询保持错误状态并由重试恢复。通知携带的版本用于 Gateway 发送队列合并与日志，客户端不据此比较。

独立详情、列表、消息窗口和总数都从对应 Query 获取权威结果。同步协调器保存失效状态与探针上次返回值，视图控制器保存 ID 顺序、窗口边界、锚点和交互状态；它们不复制一份手工维护的业务 DTO。列表边界暂时过期属于可恢复状态，不能据此判定会话已删除或失权。

### 定期兜底校验

客户端每 30 秒无条件调用一次 `GetSyncHeads`，返回本人当前可见会话的数量、版本校验和、身份资料版本，以及 PR40 起的置顶顺序版本；任一项与上次返回值不符就走已加载窗口重读（`ReadInboxWindow` ＋ 按 ID 资格核对 ＋ 当前会话窗口）。前台恢复、网络恢复和重连各额外触发一次。

版本校验和是可见会话 `(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希之和（没有个人状态行的可见会话按状态版本 0 计入），只用于判断「有没有变」，不比较新旧，也不能推断变的是哪一条。会话版本或本人会话状态推进、会话加入和移出都会改变它，一增一减同版本会话也能区分；64 位哈希碰撞概率可忽略。客户端把探针各项当作不透明值保存上次返回结果，不用已加载数据重算。

会话、本人会话状态、身份资料和置顶顺序的通知丢失后，这条兜底是最终恢复的唯一保证；提交后发布和 Gateway 订阅顺序只让恢复更快，不是正确性前提。提交后、发布前进程崩溃或发布失败丢失的通知，由这条兜底在一个校验周期内恢复。登出与停用的撤销控制丢失时，连接最迟在最长存活时间到期时关闭，重连时重新认证。**若实测该聚合在真实数据量下过重，那就是引入持久用户水位行的证据；在此之前不建。**

### 受众与 Subject

通知受众是**临时的 NATS 订阅目标，不是持久流**：`cervi.<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，`audienceKind` 固定为 `user`、`customer_inbox`、`visitor_directory`。用户用 userId，客服共享一条企业流并不逐客服扇出，访客用渠道身份记录 ID；这些值来自服务端授权结果，使用无点的内部规范值，禁止原始凭据或用户输入直接拼接。

登出与停用的撤销控制和变更通知共用提交后发布链路，以明确的通知种类区分，不参与变更通知的版本合并；登出通知携带 tokenSessionId，停用按用户受众发送。控制通知丢失时，连接最迟在最长存活时间到期时关闭。群失权不发送控制通知：失权者按 PR19 收到仅含会话 ID 的 `conversation_removed` 通知，该种类不与会话变更合并，Gateway 据此移除该连接在该会话上的 AI 流订阅并转发给客户端。

### 锁序

完成各入口守卫后，多会话业务锁按 conversationId 排序，并在每个会话内遵守已定义的业务对象顺序。**业务锁之后不取受众锁**；事务内登记通知只写内存集合，不参与锁序。`pinOrderVersion` 与身份资料版本同存于本人用户账号行，该行由写事务开头的 LockActiveUser 锁定，不新增独立顺序锁。资料变更涉及多个会话时先确定并锁业务集合，再统一登记通知。

## 列表行为基线

查询身份包含企业、用户、scope、customerView、assigneeIdentityId、channelId、serviceStatus、kinds，启用后再加入分区和规范化搜索词；数据 key 共享，页面实例的选择与滚动位置各自保存。普通区按 `lastActivityAt DESC NULLS LAST, conversationId DESC` 排序；时间使用数据库精度，游标携带原值。置顶区按本人顺序升序，不采用活动时间。

| 输入／位置 | 内容与顺序如何处理 | 选择与滚动如何处理 |
| --- | --- | --- |
| 顶部且没有点击／拖动等交互 | 应用新项和最新活动序 | 保持顶部，不自动换当前详情 |
| 深处收到变化 | 更新内容并自动应用新增和活动排序；触摸、惯性、菜单或键盘操作期间暂缓重排，资格移除立即生效 | 保存原可见邻居；当前行上浮时锚定原后继、再前驱，不跟随回顶 |
| 用户向上浏览 | 自动双向补页，接近顶部主动核对最新窗口；不要求点击刷新或查看最新 | 按原邻域补偿，滚到全局顶部即可看到最新会话 |
| 交互结束 | 一次性应用最新权威顺序，不展示待更新提示 | 以停止操作时的可见邻域保位，不打断惯性或重复补偿 |
| 切换筛选 | 读取新查询；旧结果不可操作 | 恢复该查询历史，否则保持未选中 |
| 会话不再匹配筛选 | 从列表移除 | 有阅读权限则保留详情；失权立即清理敏感内容 |
| 断网／请求失败 | 保留已展示数据和独立错误状态 | 重试同一窗口，不换主体 DOM、不跳第一行 |

“保锚点”记录稳定可见 ID、相对滚动视口顶部的像素偏移、原后继／前驱及原窗口边界。读取前捕获，DOM 更新后测量一次并补偿；锚点因排序远移时优先留在原邻域。原生 scroll anchoring、控制器或虚拟列表只能有一个补偿所有者。用户正在滚动时延后非必要重排；失权清理不等待用户停止操作。

列表刷新和补页由同一控制器串行调度：刷新遇到在途补页，先等待其收尾，再捕获已经扩展后的窗口边界；刷新中触发补页则排队，重复刷新合并。切换查询使整个队列的 queryGeneration 失效，但不取消已经执行的 Wails 业务调用。刷新重读已加载双向边界之间的连续范围，并按 ID 重读已加载项的当前资格；不能只以首页或可见视口替换全部已加载结果。交互中的可读行仅暂缓布局移动，停止操作后按权威顺序应用；真正失权才按失权规则清理；请求失败保留已确认的窗口和重试边界。

## 个人置顶写入与读取契约

置顶、取消和移动均提交 expectedPinOrderVersion，成功返回新版本。移动输入为 `{ conversationId, position, neighborId?, expectedPinOrderVersion }`：position 是 `before`／`after` 时 neighborId 必填，表示插入该邻居在**全局个人顺序**中的紧前／紧后；`start`／`end` 不带邻居，表示整个置顶区的首／尾。目标和邻居必须是本人当前有权读取的置顶会话；版本或邻居失效时整次操作失败，客户端重新读取后让用户重试，不自动用旧意图覆盖。

“隐藏项”包括被当前筛选排除及尚未加载的置顶项。移动只改变目标相对位置，其余项之间相对顺序保持；如全局 `A,X,B`、当前仅看见 A/B，将 B 移到 A 前，结果必须是 `B,A,X`。前端只提交位置命令，不能把可见 ID 数组当成全量个人顺序保存。rank 重编号只是存储实现变化，不改变逻辑顺序。

任意改变置顶集合／顺序的事务（含失权清理及随操作发生的重编号）推进 pinOrderVersion 并写本人受众通知，客户端据此重读最新顺序版本。发现版本变化时让整个置顶分区及其游标失效，重新取得同一版本的权威窗口；不得把单条新 pinRank 与缓存旧 rank 混排。多页重读途中版本再次变化，舍弃该轮候选并从新版本恢复锚点；已展示内容可保留为待更新视图，但不能提交使用旧版本的排序。新消息只推进会话版本，不推进置顶顺序版本。

## 实施 PR 的完成证据

后端项在相关 Action／Query 集成测试中构造数据并断言数据库及 appservice 返回值；并发用可控屏障组织，不靠 sleep 碰概率。运行 `wails3 task test:server`；契约或平台接入同时运行相关绑定生成、前端／服务端构建与平台 Task。前端项以本条具体交互场景验收，状态机复杂处增加可控响应顺序测试；界面操作遵守 AGENTS.md 授权规则。PR 描述记录实际执行命令、平台和结果，未执行的场景如实标记，不把本清单中的预期写成通过记录。

## 一、变更版本与发布

### PR20：客服共享收件箱通知

- **依赖：** PR18。
- **范围：** 客户入站、回复（含 AI 最终消息）、隐式领取、转交、关闭、显式重开及新周期接入会话版本与企业客服受众通知。共享受众不逐客服扇出；本人客服已读只写本人受众。
- **落点：** `inbox/load_inbox.go`、客服收发与服务周期 Action。
- **实施：** 所有有权客服共享一条企业受众，通知只表示该客户会话需要重读；公共／本人／同事／关闭资格仍由现有 Query 判断。负责人变化不为所有客服创建 Participant，个人已读继续走本人受众。
- **验收：** 公共、本人、同事和关闭视图同时收敛；筛选迁移不等于授权移除；客服参与者不被误当全部有权阅读者。
- **验证步骤：** A 领取公共会话后，B 的公共列表移除、A 的本人列表出现，双方有权详情仍可读；关闭／重开改变队列但不改活动时间；AI 最终回复也能触发共享受众通知。

### PR21：访客授权与线程目录通知

- **依赖：** PR20。
- **范围：** 提取网站 HTTP 共同授权，建立渠道身份级受众通知与目录／会话公开投影查询，补齐未知新线程发现。只返回访客公开投影。
- **落点：** `website_visitor_direct_backend.go`、`api/website_visitor.go`、`conversation/list_website_conversations.go`、`list_website_messages.go`。
- **实施：** 把 Cookie／Header 恢复出的渠道身份作为唯一受众，目录按该身份列出线程；成员 Query 的内部字段不复用到访客 DTO。首次消息以外的读操作不建立联系人。
- **验收：** 同一访客两标签页新建不同线程可互相发现；跨渠道／身份请求被拒绝；初始化和草稿不创建联系人，客服内部资料不外泄。
- **验证步骤：** 同渠道同访客在两个标签页分别首发新线程，互相能发现；换另一个渠道或伪造线程 ID 读不到消息；空白页反复初始化后数据库业务记录数不变。

### PR22：名称、头像和渠道资料失效（推迟）

- **依赖：** PR21（PR19 已合并）。
- **触发条件：** 他人资料更新不及时出现实际使用反馈，或 PR43 会话名称搜索需要改名即时改变匹配资格。
- **范围：** 接通成员／Agent、联系人／渠道身份及渠道资料写入口；按真实 JOIN 依赖标记受影响会话，推进这些会话的版本并按固定锁序写对应受众通知；头像异步导入成功也覆盖。
- **落点：** `internal/actions/identity`、`agent`、`channel` 及联系人资料入口；inbox 与消息查询的资料 JOIN。
- **实施：** 先以实际 JOIN 列出资料字段影响清单，再在各真实写事务中推进对应会话版本；同步只要求重读当前资料，不复制姓名头像到每条历史消息。异步头像完成属于独立写入口，也要发布；批量会话锁按稳定 ID 排序。
- **验收：** 无新消息时列表、发送者和引用资料可更新；访客仅刷新公开字段；失败和相同值不产生伪变化；不建立通用实体事件平台。
- **验证步骤：** 会话两天没有新消息时修改对方姓名和头像，列表、消息署名与引用展示均更新；异步头像落库后再验一次；活动序、未读和访客内部字段不变。

## 二、连接与各端实时闭环

### PR25：成员 Gateway、帧契约与连接认证

- **依赖：** PR18。合并原清单 PR28、PR29、PR30，以及本清单 PR24 与 PR35 的原生端子路径、心跳和关闭部分；客服共享受众的订阅在 PR20 完成后生效。
- **范围：** 按 `chat-roadmap.md` 第 10.11 节定义首版 WebSocket JSON 帧：Authenticate、ClientHello／ServerHello、Ping／Pong、ConversationChanged、ConversationRemoved、ConversationStateChanged、IdentityProfileChanged、SessionRevoked、ServerGoingAway、RealtimeError；访客帧由 PR26 增加，AI 流订阅与流帧由 PR38 增加，PinOrderChanged 由 PR40 增加，均遵守同一版本与未知帧规则，64 位整数一律用字符串。Server 内嵌 Gateway，首帧限时 Authenticate；Web 使用现有 Bearer，原生端由 Go 侧 Proxy 持有连接并把帧经 Wails 事件交给 TS。按本节点在线受众订阅，禁止 Queue Group 与企业通配订阅。登出按 token session、停用按用户发送控制通知，连接设最长存活时间。原生端支持子路径部署，配置心跳，停止时发送下线提示后有界关闭。不建票据表。
- **落点：** 新增 `internal/realtime/protocol`（Go 帧定义、编解码与共用夹具 `testdata/frames.json`）、`internal/realtime/gateway`（连接、发送队列与受众订阅）与 `frontend/src/api/realtime/protocol.ts`；`internal/realtime` 通知新增 `session_logged_out`、`user_disabled` 撤销种类与 tokenSessionId；`actions/auth/logout.go` 与 `actions/user/update_status.go` 登记撤销；`appservice.DirectBackend` 提供 `AuthenticateMember`／`MemberSyncHeads`；Service 新增原生端 `ConnectRealtime`／`DisconnectRealtime`，由 `internal/apiproxy/realtime.go` 实现；服务端 `Assets.Middleware` 装配与 `internal/config/server` 的 `realtime.maxFrameBytes`。
- **实施：**
  - **帧契约：** Go 结构体与 TS 类型各自手写，线上 `type` 使用 snake_case 并与 NATS `kind` 一致；以共用夹具锁定线上格式、协议主版本和字符串整数边界，Go 覆盖两个方向的编解码，TS 覆盖客户端帧编码与服务端帧解码。
  - **挂载与地址：** 连接路径为 `/api/realtime`；Gateway 在服务端 `Assets.Middleware` 中位于租户上下文中间件之内处理该路径的升级请求，升级前沿 `Unwrap` 解包 Wails 写入器，允许来源保持默认同源。原生端按服务器地址路径拼接，与 API Proxy 一致。
  - **下线：** 服务端退出时 Gateway 先拒绝新的升级请求（503），再向现有连接发送 `server_going_away` 并以同名原因关闭，默认 5 秒内未完成的连接强制断开，之后停止通知发布器。
  - **认证与寿命：** 连接依次处于 awaiting_auth、authenticated、closing；认证前只允许 Authenticate，五秒内未认证即以 `authentication_timeout` 关闭；Authenticate 复用业务调用的身份解析，令牌须存在、未过期且账号活跃，身份解析同时返回令牌编号（即 tokenSessionId）与到期时间。连接最长存活 1 小时且不晚于登录会话到期，到期以 `session_expired` 关闭后客户端重连并重新认证。认证后收到 `client_hello` 时登记 tokenSessionId、userId 和企业并安装受众订阅，Flush 之后重新校验登录会话，再读取 Hello 探针值；变更通知可能先于 `server_hello` 送达。
  - **撤销：** 登出在删除令牌的事务内登记携带 tokenSessionId 的 `session_logged_out`，停用在账号状态事务内按用户受众登记 `user_disabled`；Gateway 收到后清除该连接未发送的帧，发送 `session_revoked` 并以 `session_revoked` 原因关闭。
  - **发送队列与心跳：** 每条连接一个写协程与一条有界发送队列，单帧上限由 `realtime.maxFrameBytes`（环境变量 `REALTIME_MAX_FRAME_BYTES`，默认 65536，范围 64–256KB）配置，同时约束客户端帧读取；变更通知按同一会话同一种类保留最高版本，`conversation_removed` 与撤销控制不合并；队列溢出时按 `slow_consumer` 关闭连接，单帧写入超过 10 秒说明对端停止读取，直接断开；客户端重连后经探针恢复。客户端每 25 秒发送 Ping，服务端 60 秒内未收到任何帧即以 `idle_timeout` 关闭；心跳间隔与代理空闲时间一起记录。协议错误发送 `realtime_error` 后以错误码作为关闭原因，认证读取失败使用 `unavailable`。
  - **原生端：** Service 的 `ConnectRealtime`／`DisconnectRealtime` 由 PR27 的传输内核驱动启停与退避重连；Go 侧 `apiproxy` 用当前登录凭据拨号 `<服务器地址路径>/api/realtime`，拨号期间登录会话或企业服务器变化时丢弃新连接，发送 Authenticate、ClientHello 并每 25 秒发送 Ping，把服务端帧原文经 `cervi:realtime:frame` 事件、关闭码与原因经 `cervi:realtime:closed` 事件交给前端，事件携带本地连接编号区分新旧连接；登录、登出与切换企业服务器时关闭当前连接。
  - **日志与凭据：** 认证失败、慢连接关闭和发布失败记录 `WARN`，不记凭据或正文；原生端不把长期 Token 交给 TS。
- **验收：** 共用夹具下两端编解码结果一致，未知可忽略帧与不支持的协议主版本分别有明确结果，超过 JS 安全整数的值正确；一用户多设备均收到通知；先注册订阅再给探针值；慢消费者断开后可补拉；已连接后登出或停用不再收到通知；撤销事务回滚不产生错误断连；连接到达最长存活时间后关闭，凭据有效时重连成功，登出或停用后重连认证失败；原生端经剥离子路径前缀的反向代理可连，空闲超过代理默认时间仍正常。
- **验证步骤：** 客户端帧由 TS 编码、Go 解码，服务端帧由 Go 编码、TS 解码，均与共用夹具一致；在安装订阅与读取探针之间提交消息，客户端通过 Hello 或后续通知至少发现一次；同账号建立两条连接两端均收到；堵塞一端读流触发有界关闭，另一端继续正常工作。连接后登出本次 token 只关闭对应登录会话，停用用户关闭其全部会话；分别丢弃登出与停用的控制通知后，连接在最长存活时间到期时关闭，重连认证失败。SIGTERM 后连接收到下线提示，重启后客户端经探针补齐停机期间的变化，退出不关闭共享 NATS。
- **平台探针结论（原 PR24，2026-09-14）：** macOS、Go 1.27.1、Wails v3.0.0-beta.21 服务端模式、`coder/websocket` v1.8.15；临时端点用首版帧应答认证、Hello 与 Ping，每个场景保持 75 秒，验证后删除。
  - **挂载与写入器：** Wails AssetServer 在匹配服务 Route 前对 WebSocket 升级请求直接返回 501，Gin `/api` 路由与 Wails 服务 Route 都无法升级；中间件收到的 `assetserver.contentTypeSniffer` 延迟下发响应头，直接 `websocket.Accept` 时 101 响应随连接劫持丢失、客户端握手超时，沿 `Unwrap` 解包到 `*http.response` 后握手成功。
  - **截止时间：** `net/http` 劫持连接时清除读写截止时间，Wails 默认 30 秒读写超时不影响升级后的连接。
  - **Origin 与 Host：** `coder/websocket` 默认只接受与 Host 相同的 Origin；本地复刻 `ingress` 重写规则的反向代理与 Cloudflare Tunnel 都保留原始 Host（隧道带 `X-Forwarded-Proto: https`），跨源连接被拒绝；原生端 Go 连接不带 Origin。
  - **Web（Chrome 153）：** 同源 `ws://` 与 Cloudflare Tunnel `wss://` 均保持连接，双向 ping／pong 正常；Chrome 请求 `permessage-deflate`，服务端默认不协商。Wails 运行时以 `window.location.origin + "/wails/runtime"` 调用绑定，Web 端不支持子路径部署。
  - **macOS 原生端：** 探针程序使用与 `apiproxy` 相同的 Go 网络栈，未在桌面应用进程内运行；带 30 秒 `Timeout` 的 `http.Client` 可直接拨号，本地、Cloudflare Tunnel 与剥离 `/cervi` 前缀的反向代理均通过。
  - **未验证：** iOS、Android（PR33 记录）与 `ingress` 自动证书模式。

### PR26：访客 Gateway 受众

- **依赖：** PR21、PR25。
- **范围：** 访客首帧用 Cookie／Header 恢复的渠道身份认证，独立访客 Subject 与公开帧白名单；渠道停用撤销连接。Origin 按发起 Socket 的 iframe 来源校验，宿主页白名单沿用嵌入规则。
- **落点：** `api/website_visitor.go`、`website_visitor_direct_backend.go`、渠道停用入口与 Gateway 访客认证 adapter。
- **实施：** 受众使用数据库渠道身份 ID，不使用 Cookie 原值或可逆外部标识构造 Subject。先验证渠道与访客归属，再允许其目录与会话公开投影；尚无业务身份时不建立连接，也不为连接创建联系人。
- **验收：** 随意传 conversationId 不能订阅；独立链接、嵌入、Header 恢复与预览互相隔离；成员认证不能用于访客连接；停用渠道后已有连接和重新连接都失效。
- **验证步骤：** 抓取访客连接帧和 Subject，不能出现原始 Cookie 值；跨身份指定线程失败；停用渠道后重连被拒。

### PR27：共享客户端连接与会话代次

- **依赖：** PR25。
- **范围：** `src/api/realtime` 实现无 feature 依赖的 TS 传输内核；一个应用实例一连接、业务页签共用，浏览器各标签页可独立连接。原生端调用 `ConnectRealtime`／`DisconnectRealtime` 驱动 Go 侧连接并消费 `cervi:realtime:frame`／`cervi:realtime:closed` 事件，Web 端自建 Socket 并发送 Ping。抖动退避、重新认证，网络／认证／版本错误分流；`slow_consumer` 关闭与 `server_going_away` 按网络错误退避重连。
- **落点：** 新增 `frontend/src/api/realtime`；`frontend/src/api` 的认证边界与各端登录外壳。
- **实施：** 传输内核只接收认证输入、URL／Origin 适配、frame handler 和生命周期输入，不导入 inbox 或 workspace。维护 disconnected、connecting、authenticating、ready、backoff、stopped；退出登录或切企业先使 generation 失效再释放旧连接。
- **验收：** 企业／账号切换先提升 generation 再清缓存；旧 Promise、Socket 和通知失效，同源凭据改变可恢复；验证 WebView 地址；临时断网保留草稿。
- **验证步骤：** A 企业的认证慢响应在切到 B 之后返回，不得创建 A 的连接；重连与旧 onclose 交错时只保留一条当前连接；服务端版本不支持时不无限重试刷日志。Web 与桌面在应用内分别记录实际 Origin、服务端地址拼接与 TLS 结果并与 PR25 探针结论核对，不以 Web 通过代替原生通过。

### PR28：登录会话级同步协调器与兜底校验

- **依赖：** PR17、PR27。
- **范围：** 登录后启动，收到变更通知即集中失效对应 resourceKeys，同一 key 按短窗口防抖合并；通知不影响阅读水位。实现每 30 秒的 `GetSyncHeads` 兜底校验及前台、网络恢复、重连触发。
- **落点：** 新增登录会话级同步模块，`frontend/src/hooks/resource-keys.ts`、`frontend/src/lib/resource-client.ts`。
- **实施：** 通知种类映射到会话摘要、消息窗口、本人会话状态与身份资料对应的 resourceKeys，`conversation_removed` 同时触发对应会话的资格重读；防抖合并即将发生的失效，失效后发起新的读取，已发出的 Wails 绑定调用照常完成、返回后按 signal 丢弃。协调器不保存实体版本，未挂载的 Query 不处理，打开时直接读最新；读取失败时查询保持错误状态并由重试恢复。兜底校验发现探针不符时走已加载窗口重读，不做全量重建。
- **验收：** 离开消息页仍同步；空页、持续热点、读取失败和乱序通知正确收敛；本人写入回显的通知至多再触发一次重读；不持久化游标冒充离线库，不逐条通知手工修补 Query DTO；断开 Socket 后仅靠兜底校验也能收敛。
- **验证步骤：** 防抖窗口内同一会话连续送来三条通知只触发一次重读；在途读取发出后提交新消息并送达通知，最终展示新消息；窗口 Query 失败后保持错误状态，重试可恢复。整段断开 Socket，只靠 30 秒兜底仍能看到新消息、失权和资料变化。离开消息页仍处理身份与未读变化，账号切换清空原用户全部同步状态。

### PR29：消息窗口接入实时同步

- **依赖：** PR28（PR15 已合并）。
- **范围：** 用版本驱动权威窗口重读，消除初始 Query 更新而 page 副本不变的问题；最新窗口补连续页，锚点窗口不拼远端尾部但处理资料、删除与失权。收到推送不自动已读；移除对应的成员消息和提及高频轮询。
- **落点：** `use-conversation-timeline.ts`、`conversation-window.ts`、`use-conversation-viewport.ts`、提及与阅读 hooks。
- **实施：** 以窗口模式 latest／anchor 和 queryGeneration 组织读取，权威 Query 更新必须能替换对应窗口内容；合并消息页不等于保留已删除实体。最新模式连续补到目标，锚点模式只重读当前窗口与引用依赖，远端尾部更新留提示。阅读仍由可见消息判定。
- **验收：** 上翻、引用跳转、窗口外原文删除、并发发送、过期页响应和连续提及查看稳定；Wails 调用仅忽略过期结果，不取消业务执行。
- **验证步骤：** 停在引用跳转窗口，同时发生新消息和原文删除：不跳尾部、引用失效、最新提示出现；连续提及导航后返回最新不丢消息；旧窗口响应晚到不能覆盖新会话。

### PR30：列表实时更新与可变排序补全

- **依赖：** PR28（PR14 已合并）。
- **范围：** 延续已交付的三端自动排序：顶部跟随最新，深处应用新项与重排并保留原邻域，仅交互期间延后布局。向上浏览自动取得最新会话，无刷新、查看最新或待更新提示。keyset 继续原边界，通知发现未加载却已上浮的会话，按会话 ID 去重；移除列表与群资料高频轮询。
- **落点：** 已交付的列表控制器、PR28 同步协调器、按 ID 批量摘要 Query；替换 inbox 页面轮询消费者。
- **实施：** 通知提供「哪些会话变化」，按 ID 取得当前资格和排序键，失效并重读权威连续窗口；普通 keyset 仍按原边界继续补历史，不改成最新边界。交互结束自动应用窗口顺序，当前锚点上浮时恢复原后继、前驱，回到顶部无需额外点击。
- **验收：** C90 在读取旧页期间上浮仍可发现；资格移除立即修复、真实失权立即清除；旧 generation 页面不接新窗口；`hasMore=false` 后仍可发现新项；刷新失败不换整棵 DOM。
- **验证步骤：** 初始显示 C1–C50，未加载的 C90 收到消息并排到首位；读取 C51 之后的页不会再返回 C90，但通知必须发现它。深处保持原邻域，向上滚动到顶部后 C90 只出现一次；`hasMore=false` 后新增会话也可发现。

### PR31：拆分网站挂件网络与窗口状态（推迟）

- **依赖：** 无（PR07 已合并）。
- **触发条件：** 挂件需要与成员端共用传输内核，或 `chat.js` 状态逻辑的修改出现可复现的回归。
- **范围：** 通过现有 messenger 构建入口，将请求、身份、连续窗口与发送状态迁入 `frontend/src/publicweb` 可测试模块；保留 Go 模板、主题及现有 DOM 外观，访客 DTO 与成员隔离。
- **落点：** `internal/publicweb/chat.js`、`page.html`、`frontend/src/publicweb` 与现有 messenger 构建入口。
- **实施：** 按网络身份、会话目录、消息窗口、待发送状态拆出可测试模块，DOM 事件转成显式操作，不把整段脚本包进一个 TS 类。先保留三秒轮询验证等价，再删除迁出的原函数与状态变量；构建产物继续由现有 Go 嵌入链路加载。
- **验收：** 原轮询下文本、引用定位、分线程草稿和幂等重试不变；删除被替代的旧状态逻辑；不打包内部身份或工具详情。
- **验证步骤：** 在真实挂件构建产物上发送、重试、切线程、引用跳转和恢复 Cookie；切回原线程草稿仍在。检查 bundle 依赖没有成员登录模块或内部 Agent 详情。

### PR32：网站挂件实时与多线程恢复

- **依赖：** PR26。
- **范围：** 在现有 `chat.js` 内实现访客连接，收到目录或会话通知、重连成功时调用现有目录与消息拉取逻辑；已有身份时初始化即连接，无身份不连接，首条有效消息建立身份后再连接；替换业务消息轮询。
- **落点：** `internal/publicweb/chat.js`、网站目录与窗口 HTTP 接口。
- **实施：** 状态分为无身份、已连接、退避重连；重连成功与页面回到前台时重新拉取目录与当前线程窗口。访客同步使用挂件自身状态，不依赖成员登录协调器；其他标签页建立的身份在本页刷新后生效。
- **验收：** 新线程发现、离线补拉、发送响应乱序、引用窗口和渠道停用均收敛；初始化不创建业务记录，预览不连真实服务。
- **验证步骤：** 同一访客打开 A、B 两个已有身份的挂件，断线期间 A 新建第二线程，B 恢复后列出两条且不制造新联系人；空白页反复初始化不建立连接，也不创建业务记录。

### PR33：移动端实时接入

- **依赖：** PR29、PR30。
- **范围：** 移动登录外壳接同一连接与同步内核，列表、单聊及已有群聊使用统一实时输入；恢复前台先做兜底校验，再按独立阅读规则工作。保留当前移动功能边界，不顺带开启建群或已读交互。
- **落点：** `frontend/src/apps/mobile` 登录外壳、mobile-inbox-page、mobile-individual-conversation-page、mobile-group-conversation-page。
- **实施：** 应用前台事件只触发同一协调器的兜底校验，不由每个页面各建连接；系统挂起后旧连接一律按可能失活处理。移动页面订阅共用版本输入，保持自身详情导航、阅读能力和草稿行为。
- **验收：** iOS／Android 前后台、旋转、详情返回、连接重建与旧响应隔离；完成对应回归后移除移动三秒轮询。
- **验证步骤：** 系统挂起期间另一端发送、已读、移除群成员，恢复后列表与详情收敛；快速返回再进另一会话，无重复连接、旧内容闪入或错误已读。iOS 与 Android 分别记录实际 Origin、服务端地址拼接与 TLS 结果。

### PR34：实时未读与本地通知闭环

- **依赖：** PR29、PR30。
- **范围：** 同步确认的新 Message 接现有通知策略；静音、@、工作状态、全局／设备开关及可见上下文独立判断。通知去重；总数绑定权威版本，客户端不做 +1/-1 推算。
- **落点：** `features/notifications/new-message-notifications.ts`、`platform/notifications.ts`、当前通知偏好与 PR28 同步入口。
- **实施：** 以 messageId 去重，并区分 live 与 catchup／bootstrap 来源；一次通知覆盖多条消息时读取实际新增范围后逐条执行策略，不用最后预览代表全部。通知内容来自已授权消息 Query，旧账号 generation 立即失效。
- **验收：** 一次通知合并多条消息时不只看最后预览；冷启动与重连历史不逐条补弹，他端已读只更新状态；旧总数不覆盖新版本；客户未读不进入桌面总提醒。不承诺跨设备恰好一次或退出后推送。
- **验证步骤：** 一次通知包含普通消息和 @ 消息，静音场景仍按既有 @ 策略判断；重连追赶十条历史只更新未读，不连弹十次。

### PR35：Gateway 指标与就绪顺序（推迟）

- **依赖：** PR26、PR32。原生端子路径、心跳与优雅关闭由 PR25 交付。
- **触发条件：** 出现真实部署并需要监控实时链路，或已观察到启动就绪顺序导致的连接失败。
- **范围：** 记录认证、慢连接、通知发布失败和同步落后指标，不记凭据或正文；明确启动就绪顺序。多节点 Drain 与随机重连分散在拆分部署时另开。
- **落点：** 服务端应用装配、`internal/ingress`、Gateway 与通知发布，部署说明。
- **实施：** 启动顺序明确为依赖就绪 → 路由／订阅可服务 → 对外 ready；指标按结构化原因码聚合。
- **验收：** 未就绪时不对外接受连接；指标覆盖认证失败、慢连接关闭、发布失败与探针不一致次数。
- **验证步骤：** NATS 未就绪时启动服务端，连接请求在就绪前不被接受，就绪后可连；分别触发认证失败、慢连接和发布失败，对应指标增加。

## 三、AI 实时过程

### PR36：拆分运行摘要与过程详情

- **依赖：** PR29（PR19 已合并）。
- **范围：** Run 摘要按 runId／agentIdentityId 返回集合，当前 UI 可选最相关项；消息只留过程引用，展开时取详情，移除每次消息查询携带完整 blocks 的实现。
- **落点：** `direct_backend_agent_process.go`、`types_agent_process.go`、`conversation/agent_process.go`、`features/inbox/agent-process.tsx`。
- **实施：** 列表与消息窗口只带轻量 Run 摘要和过程引用；独立过程 Query 按 runId 获取持久 blocks、工具参数结果和用量。当前 Run 集合与历史消息关联分开，失败通过结果消息定位，取消无消息也可选中查看；展开状态 key 用 runId。
- **验收：** 多 Run、无消息失败和历史过程均可读取；窗口更新不丢展开状态；运行详情按会话授权，保留工具完整参数、结果及用量。
- **验证步骤：** 同会话两个 Agent 各有 Run，刷新不会只剩「最近一个」；历史过程首次展开才请求完整 blocks，关闭再展开可用缓存；移出群后该 Query 不再返回详情。

### PR38：成员 AI 流订阅与终态收敛

- **依赖：** PR28（PR37 已合并）。流帧走 PR25／PR27 建立的同一条 WebSocket 连接，不新增 SSE 或其他连接；取代原清单 PR44、PR45 的焦点注册与跨节点快照方案。
- **范围：** 在 JSON 帧契约中增加按 runId 的 SubscribeRun／UnsubscribeRun 及 RunStreamSnapshot、RunStreamDelta、RunStreamEnded 帧；展开运行过程时订阅、收起时取消。服务端先按会话授权再挂接该 Run 的内存流，首帧发当前快照，之后发增量。仅数据库提交后发 RunStreamEnded，持久终态覆盖临时候选。
- **落点：** `internal/realtime` Gateway 的连接订阅集合与流帧发送、Run 流订阅、PR25 帧契约与共用夹具、`frontend/src/api/realtime`、成员 `agent-process.tsx` 与运行展示控制器。
- **实施：** `SubscribeRun` 每次重新校验登录会话有效、账号活跃与会话阅读资格；连接登记已订阅的 runId 集合并限制数量；撤销控制或最长存活时间关闭连接时订阅随之释放，Gateway 收到本人某会话的 `conversation_removed` 通知时移除该会话的全部 Run 订阅。客户端经 `conversation_removed` 或资格读取发现失去会话阅读资格时，对该会话的 Run 发送 UnsubscribeRun 并丢弃临时候选；失权通知丢失且客户端未取消时，服务端最多推送到当前 Run 结束。流帧与变更通知共用连接的有界发送队列，队列内同一 Run 的待发增量合并；队列溢出时以 `slow_consumer` 关闭连接，客户端重连后重新订阅取新快照。快照按块分帧，单帧受大小上限约束，工具完整参数与结果经 PR36 过程详情 Query 读取。客户端按 streamId 与增量的起止序号判断重复与缺口，发送队列合并后的增量可跨越多个序号；gap 或重连后按退避重新订阅取新快照，不做单独的快照修复协议。终态由持久 Query 确认，RunStreamEnded 只是加速提示；运行过程未展开时不订阅。
- **边界：** 服务端当前为单进程内嵌 Worker Pool，Gateway 与执行在同一进程内定位 Run。拆分部署时再评估跨节点快照，本条不预建。
- **验收：** 发送队列未溢出时，长输出期间的变更通知与流帧一同送达，慢客户端溢出后关闭连接并经重连与探针收敛；断线重连、失败、取消和人工接管不复活旧候选；RunStreamEnded 丢失可由会话版本恢复；未展开的运行不产生订阅；无权订阅被拒绝，移出群的失权通知送达后订阅结束，通知丢失时客户端经兜底探针发现失权后取消订阅。
- **验证步骤：** 展开过程中持续输出，同时向该会话发送新消息，客户端收到变更通知；堵塞客户端读流触发队列溢出后连接关闭，重连并重新订阅后不重复拼接已有 token；丢弃 RunStreamEnded 后仅靠会话版本仍显示最终消息；取消后晚到的 delta 不能重新打开运行中状态；无权用户及已登出但连接仍在的登录会话订阅该 runId 均被拒绝，运行中移出群后不再收到流帧；丢弃失权通知时，客户端经兜底探针发现失权后取消订阅，服务端随之停止推送。

### PR39：网站公开 AI 流（推迟）

- **依赖：** PR32、PR38。
- **触发条件：** 访客侧需要逐字展示 AI 回复。推迟期间访客经会话通知读取最终回复，生成期间沿用挂件现有的正在输入提示。
- **范围：** 访客只接公开候选正文、基础状态和最终回复；内部思考与工具原文不外发。客户负责人变化停止旧流，取消只是加速，最终写回仍受数据库与租约门禁约束。
- **落点：** PR32 访客展示、PR26 访客连接上的 Run 流订阅 adapter、客服 Agent 输出映射。
- **实施：** 在服务器构造访客专属公开帧，不先发全量工具与思考再靠前端隐藏；流授权始终绑定渠道身份可读线程。人工接管、转交、关闭取消旧公开候选资格，最终回复仍由原有事务门禁决定是否保存。
- **验收：** 客服接管、转交、关闭、重连换流及渠道失效均正确；抓取访客 WebSocket 帧与 HTTP 响应都无内部过程数据。
- **验证步骤：** 模型同时输出思考、工具结果和正文，抓包只含公开正文和基础状态；客服接管后迟到的旧 delta 不再展示，重连读取的最终消息也不包含被取消的候选。

## 四、个人会话置顶

### PR40：个人置顶事实、顺序与分页

- **依赖：** 无（PR19 已合并）。
- **范围：** 个人状态增加 pinRank，本人用户账号行增加 pinOrderVersion；置顶、取消、按邻居移动同事务推进版本并写本人受众通知；`GetSyncHeads` 同时返回 pinOrderVersion，帧契约增加 PinOrderChanged。提供显式分区查询，未启用置顶的调用方继续完整活动序查询；扩展锚点上下文的分区、顺序版本及跨区邻居。新增默认追加末尾，取消回普通活动顺序；过滤只是全局个人顺序的投影。
- **落点：** `models/conversation_user_state.go`、成员身份模型、新增置顶写 Action、已交付的分页与锚点 Query。
- **实施：** 命令使用共用置顶契约的 position／neighborId 和 expectedPinOrderVersion，不提交全列表覆盖隐藏项。锁定所涉会话并校验当前资格后，在已锁定的本人用户账号行上校验并推进顺序版本；冲突返回当前版本且不部分写入。按 rank 留间隔，耗尽时同事务无唯一冲突地重编号；取消清 rank，再次置顶追加末尾。
- **验收：** expectedPinOrderVersion 防两端覆盖；隐藏项相对顺序保留，重编号无唯一冲突；置顶页游标绑定版本、与普通页分开，活动序调用方不漏项；跨区移动使旧分区资格失效；失权清除置顶，重入不自动恢复，解散可读则保留。
- **验证步骤：** 全局 A,X,B、当前隐藏 X，将 B 移到 A 前，结果为 B,A,X；两端持同版本同时排序只允许一个成功。触发 rank 重编号后，两端整区重读所得顺序与服务端一致。失权清理也推进顺序版本，旧分区游标要求重读而非静默漏项。

### PR41：Web／桌面置顶交互

- **依赖：** PR30、PR40。
- **范围：** 客户端切换分区查询，单滚动容器先置顶再普通，同会话只显示一次；支持菜单置顶、拖动及等效键盘排序。新消息不动置顶顺序；本人移动保留操作锚点，远端重排按浏览意图暂存，跨分区原子更新并补偿位置。
- **落点：** Web／桌面列表控制器、行菜单及新增排序交互，PR40 分区 Query。
- **实施：** 控制器维护 pinned 与 regular 各自的游标、内容版本及置顶顺序版本；顺序版本变化整区重读，渲染仍是一条纵向列表；置顶区未加载完时继续取置顶，不把尚未到达的置顶错误接到普通区之后。操作期间可显示临时拖动外观，成功后权威重读，冲突保留页面上下文并提示重试，不手写新业务 DTO。
- **验收：** 两区分别分页、只有一个分区、置顶较多、取消后重新置顶、锚点跨区和拖动期间版本冲突均正确；不自动跳顶，不清草稿／静音／阅读水位，不增加嵌套滚动或任意数量上限。
- **验证步骤：** 从普通区置顶一条当前可见会话，确认两区没有重复，详情与草稿仍在；他端取消当前锚点置顶时保持原邻域。拖动尚未完成时收到远端排序，提交冲突后恢复权威顺序且不跳顶。

### PR42：移动端置顶与跨端顺序

- **依赖：** PR33、PR41。
- **范围：** 移动端增加触屏置顶、排序与可访问的替代操作；复用个人顺序、版本冲突及分区分页，不同步各设备滚动位置。
- **落点：** `apps/mobile/mobile-inbox-page.tsx`、移动列表导航，复用置顶命令与分区状态。
- **实施：** 长按菜单提供置顶、取消及明确的移动操作，触屏拖动有独立手势启动条件；无拖动能力时仍可通过菜单完成同样排序。跨端只接收顺序版本，各端选择、焦点、展开和滚动位置不上传。
- **验收：** 长按与惯性滚动互不误触；其他端置顶、取消或重排后，详情返回保持原邻近锚点，新消息只更新置顶内容。
- **验证步骤：** A 桌面调整置顶后 B 手机在详情内收到变化，B 返回仍在原列表邻域；B 长按后转为滚动不得误发排序。新消息连续到达，置顶相对顺序始终不变。

## 五、按证据跟进

本节各项不阻塞实时主线：搜索按实际产品需要开启，虚拟化和容量验证按实测证据开启。

### PR43：会话名称搜索接口

- **依赖：** PR40。
- **范围：** 搜索当前有权会话的名称与标题，完整查询身份包含规范化搜索词；分页、上下文、变化资格判断与置顶分区共用筛选。不是只搜索已加载页，不加入正文全文搜索。
- **落点：** inbox 候选 Query、锚点上下文、分区请求和 `types_inbox.go`。
- **实施：** 服务端统一搜索词空白规范化与匹配规则，复用当前会话标题与对象名投影；先在完整有权候选集合筛选，再分页。查询身份和游标均包含规范化词，变化重读时重新计算匹配资格。
- **验收：** 首页之外可命中；改名导致匹配进入／退出、隐藏置顶、失效同事和跨企业隔离正确。
- **验证步骤：** 第 100 条标题匹配时首页可找到；将当前结果改名为不匹配则移除，另一未加载会话改名为匹配则可发现；同词跨企业请求不能扩大范围。

### PR44：会话搜索输入与浏览恢复

- **依赖：** PR42、PR43。
- **范围：** 启用各端搜索入口，组合输入结束后查询，旧词响应受 queryGeneration 隔离；清空搜索恢复原筛选锚点，未展示查询仅标旧，结果详情仍独立。
- **落点：** Web／桌面与移动列表搜索入口、列表控制器、`resource-keys.ts`、双语 inbox 文案。
- **实施：** 输入框只维护本地编辑词，compositionend 后形成规范化查询；请求状态与结果查询绑定，防止新词标题下出现旧词可点击行。保存搜索前筛选与锚点，清空只恢复浏览上下文，不清个人置顶或会话详情。
- **验收：** 中文输入、快速改词、重连中搜索和搜索期间新消息不串结果；真实无匹配才显示空态，搜索不重置个人置顶。
- **验证步骤：** 中文输入法尚在组词时不逐键发最终查询；A 词慢响应晚于 B 词返回不覆盖。搜索中断网再清空，恢复原列表后迟到的搜索结果仍被忽略。

### PR45：列表虚拟化与窗口裁剪（测量后决定）

- **依赖：** PR42。
- **触发条件：** 真实列表规模出现可复现的渲染或滚动问题，并记录实际节点数与滚动耗时。
- **范围：** key 固定为会话 ID，裁剪保留前后边界与语义锚点。固定头像与摘要行布局，缩放、语言、未读位数与时间更新不制造跳动；选择单一滚动补偿所有者。
- **落点：** 共用列表控制器与各端 ScrollArea 渲染层。
- **实施：** 先量实际节点数和滚动耗时，记录为何需要虚拟化；只替换可见行渲染，数据分页、选择和同步契约沿用。裁剪窗口保留可恢复边界，行高度变化统一触发测量，禁用另一套滚动补偿。
- **验收：** 快速滚动、重复补页触发、长时间停留、隐藏后恢复及行高重测稳定；交互使用稳定 ID，尊重减少动态效果。不用固定截断条数冒充分页或容量治理。
- **验证步骤：** 长时间补页并裁剪后反向滚动仍能补回，锚点会话不因 DOM 回收失去选择；隐藏页签恢复、切语言、字体缩放和大量未读位数变化后位置稳定。

### PR46：实时链路容量与故障验证（测量后决定）

- **依赖：** PR25、PR33。原清单 PR41。
- **触发条件：** 出现真实部署规模，或已观察到连接、队列、锁争用的具体问题。
- **范围：** 增加可重复 Task 场景：同用户多会话、小群扇出、共享客服受众、资料批量失效、突发重连和持续热点；分别测 LockActiveUser 守卫、内存、队列、积压与 HTTP 放大，报告真实规模和结果。
- **落点：** 现有 Task 与服务端集成测试；新增只用于测试的连接、发布和 Query 故障注入点。
- **实施：** 每个场景记录数据量、在线连接数、受众大小、持续时间和机器配置，再测端到端收敛时间、HTTP 次数及队列峰值；用结果定位热点，不把合成测试数字写成产品承诺。注入点不得成为生产接口。
- **验收：** 杀进程、NATS 短断、慢客户端及连接过期均恢复。锁优化、普通性能索引和大群 Shared Fanout 只按证据另开 PR。
- **验证步骤：** 消息已提交后杀进程、NATS 单独断连而 Socket 存活、慢客户端分别重跑；最终数据库版本与客户端投影一致，并证明队列与协程不随重复重连持续增长。

### 已推迟的原范围

| 原范围 | 重新开启的条件 |
| --- | --- |
| 持久用户／客服／访客水位与压缩唤醒行（原 PR17／19／20） | `GetSyncHeads` 聚合实测过重，或确定要做持久离线库 |
| 完整同步索引、重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 客户端需要跨会话持久游标，即离线库落地时 |
| 会话变更事件日志与 `sync_seq`（原 PR16） | 按会话版本重读被实测证明过重，需要细粒度失效时 |
| Protobuf 协议与工具链（原 PR27） | 帧载荷显著变大变热，或协议要提供给第三方 |
| 一次性连接票据（原 PR28） | 原生端不再由 Go 侧持有连接，或出现新的凭据边界要求 |
| 跨执行节点运行快照（原 PR44） | Gateway 与 Worker 拆分部署 |
| 事务内 `realtime_outbox` 与租约发布器（原 PR18／PR23 方案） | 实测提交后发布丢失导致的感知延迟不可接受，或兜底探针间隔需要显著拉长 |
| 多节点 Drain、随机重连分散与多节点验收（原 PR25／PR35 部分） | 服务端拆分为多实例部署 |
| 服务端定期复核授权与群失权控制通知（PR25、PR38 原方案） | 实测 `conversation_removed` 通知丢失导致失权后仍收到 AI 流，最长存活时间限定的撤销窗口不可接受，或出现需要即时收回的其他受限帧 |
| 客户端 `target`／`applied` 版本比较与串行追赶（PR28 原方案） | 通知触发的重复重读被实测证明过重 |
| 每连接帧优先级与流帧溢出重订阅（PR25、PR38 原方案） | 实测长输出期间慢连接频繁被关闭，或变更通知被流帧明显延迟 |
| 名称、头像与渠道资料失效（PR22） | 见 PR22 触发条件 |
| 网站挂件拆分为 TS 模块（PR31） | 见 PR31 触发条件 |
| Gateway 指标与就绪顺序（PR35） | 见 PR35 触发条件 |
| 网站公开 AI 流（PR39） | 见 PR39 触发条件 |
| 挂件跨标签页身份发现、本地通知跨标签页前台协调、NATS 恢复时主动校验探针、前端收发队列上限与 `bufferedAmount` 观察 | 出现实际重复通知、身份发现遗漏或发送堆积反馈 |

## 里程碑与范围外事项

- **变更版本可独立验证：** PR17–PR21 通过 HTTP 与兜底探针验收（PR22 开启后同样验收），实时通知丢失不影响最终数据；PR18 验证发布失败不影响业务写入。
- **端到端最薄一刀：** PR17、PR18、PR25、PR27、PR28、PR29 完成后，成员消息窗口即可跑通「提交 → 通知 → 重读」并删除该端轮询；其余受众和端按各自 PR 依次接入，尚未交付的端继续原轮询。
- **增强独立交付：** AI 流 PR36–PR38 与个人置顶 PR40–PR42 互不依赖；搜索与虚拟化不阻塞实时主线。
- **不在本清单实现：** 图片／文件消息、Telegram 外发 Delivery、群聊 @Agent 策略、移动建群、输入／在线状态、后台系统推送、离线发送队列、设备执行、WebRTC、联邦、大群 Shared Fanout。消息置顶、团队共享置顶、普通区手排、会话分组与未读筛选也不随个人置顶加入。
