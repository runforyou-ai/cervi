# 聊天重构与实时功能 PR 清单

## 执行约定

现状检查基于 `ce8bb6f`，并核对至 `35b8a9a`。本文是可交接的实施任务书，包含聊天重构、列表滚动刷新和个人置顶的设计约束；各 PR 条目定义交付范围和验收要求，不表示已经创建远程 PR 或完成验证。PR03 本轮基于 `7c64c07` 开发，提交前已同步 `b93abee` 并再次通过服务端全量测试；2026-09-08 数据库恢复后已通过服务端、桌面端、前端测试和服务端生产构建（含前端构建），界面回归及 Pi CLI 审核结果见 PR03 验证记录。依赖列只写直接前置；具备前置条件即可单独开发、验证和合并，不要求先完成整阶段。

- 一个 PR 交付一个明确行为；后端能力可用真实数据库与应用服务测试独立验收。正确性耦合的迁移、写入、读取及绑定一起提交，不拆成无法运行的中间版本。
- 保留 Conversation、ChatSubject、Participant、ServiceSession、Agent Run 和 appservice 边界。持久命令走 HTTP／Wails，WebSocket 推水位与临时过程；客户端通过业务 Query 读取权威数据。
- 按下列范围实施；PR01 将涉及产品行为的具体选择及路线图调整集中写明。已确认的置顶语义不再重复确认：**个人置顶、本人跨端同步、手动排序、新消息不改变顺序**。
- 每个 PR 使用相关 Task 测试和构建，契约变更同 PR 运行 appservice 生成器及 Wails 绑定生成；不手改生成文件。提交前完成 Pi CLI 审核并处理反馈。界面验收遵守仓库授权约定，每轮清理本次启动的进程和端口。
- 已合入 main 的迁移不改；新增迁移由 Task 取实际时间生成并提供 Down。无历史回填、双协议或临时兼容分支；开发库切换见 PR06，不用默认零值伪造已有消息序号。
- 有当前用户的写 Action 先执行 LockActiveUser；渠道／Agent 任务保留各自守卫。随后按共用契约锁会话、业务状态和各类受众水位，置顶版本与本人用户水位共用一行锁。
- 各端尚未切换实时前保留原轮询；只在对应端通过补拉、失权和阅读回归后移除。每个 PR 删除本次替代的代码，不保留两个状态所有者。

## 阅读入口与共用契约

实施者先读本节，再读领取的 PR 及其直接前置。每项的「落点」是开始查代码的位置；标为“新增”的模块／接口尚不存在。接口名称可以按仓库命名统一，但下列字段语义、事务边界与用户行为不能留给各端自行解释。若实际实现需要改变产品选择，应先说明差异并确认。

### 当前缺口与交付边界

| 当前入口 | 已核对的问题 | 由哪些 PR 解决 |
| --- | --- | --- |
| [收件箱 Query](internal/actions/inbox/load_inbox.go) | 每种类型最多 50 条再混排，不能继续浏览完整列表 | PR08–14 |
| [收件箱页面](frontend/src/features/inbox/inbox-page.tsx) | 用来源时间在前端排序；选择依赖当前结果，并混入本地新建／退群集合 | PR08–09、PR12–14 |
| [消息窗口 hook](frontend/src/features/inbox/use-conversation-timeline.ts) | 首次 Query 与本地 page 双份状态，后续失效不一定更新已有窗口 | PR15、PR34 |
| [成员轮询](frontend/src/features/inbox/use-member-chat-polling.ts)、[网站挂件](internal/publicweb/chat.js) | 三秒轮询承担更新，没有登录会话级补拉协调 | PR16–41 |
| [会话模型](internal/storage/server/models/conversation.go) | 已统一消息序号，缺少持久变更水位与受众同步流 | PR16–25 |
| [AI 进度](internal/integration/agentruntime/progress.go) | 过程在执行进程内，需区分持久终态、临时流与跨节点快照 | PR23、PR42–46 |

后端新增能力先经 appservice／真实数据库测试交付，再由指定前端 PR 启用；不得提前删除仍有消费者的入口。表结构与现有调用链发生破坏性变化时，在同一个 PR 改完所有调用方，不能以“后面的 PR 会修”作为通过条件。

### 水位、版本和接口语义

| 名称 | 归属与含义 | 不能拿来做什么 |
| --- | --- | --- |
| `message_seq` | 单会话内最终消息顺序，事务内分配；游标和阅读位置据此比较 | 不表示资料变更或用户同步进度 |
| `sync_seq` | 单会话可恢复变化的版本，消息／资料／Run 终态等推进 | 不直接等于未读数 |
| 用户／客服 Inbox／访客目录 `head` | 各自受众同步流已提交的最高序号 | 不同流之间不可比较大小 |
| `target` / `applied` | 客户端已知目标／已处理位置；目标可以先于数据到达 | 缓存丢失后不能仅凭旧 applied 跳过重建 |
| `read_seq` | 用户实际阅读基线，继续保留提及确认的独立语义 | 收到帧、打开连接或补拉完成都不能直接推进 |
| `generation` / `queryGeneration` | 当前登录会话／当前列表查询的本地代次 | 不跨设备同步，也不代替服务端授权 |
| `pinOrderVersion` | 本人全局置顶顺序版本，写入时用于冲突检测 | 不因新消息、已读或静音变化而推进 |

持久写入链路为：Action 校验与加锁 → 业务事实／变更索引／realtime_outbox 同事务提交 → 发布器发水位 → 客户端经 HTTP／Wails 拉取。Socket 可重复、合并或丢失；业务正确性由 Query 恢复。

增量请求携带 `after`、可续用的 `head`、`limit` 和 `epoch`；首响应固定本轮 head，返回 `nextAfter`、`hasMore` 与当前公开投影／移除标记。续页只扫描 `(after, head]`，`nextAfter` 取已扫描位置而非最后一条可见实体；本轮结束后再追赶更新的 head。压缩 Wakeup 若被更新到本轮 head 之外，可在下一轮读取，不能因此宣称永远追平。响应里的实体版本可能比触发它的事件新，客户端不得把旧事件版本当成实体当前版本。

独立详情、列表、消息窗口、总数都从对应 Query 获取权威结果。同步协调器保存版本与失效状态，视图控制器保存 ID 顺序、窗口边界、锚点和交互状态；它们不复制一份手工维护的业务 DTO。列表边界暂时过期属于可恢复状态，不能据此判定会话已删除或失权。

### 受众标识与跨域锁序

同步受众统一以 `(namespace, organizationId, audienceKind, audienceId)` 标识；`audienceKind` 固定为 `user`、`customer_inbox`、`visitor_directory`。用户 ID 使用 userId，客服 Inbox 使用 organizationId，访客使用渠道身份记录 ID；这些值来自服务端授权结果。会话变更日志另按 conversationId 定位，不把所有用户当前打开的会话变成第四种持久受众。

Outbox、Hello heads、水位帧和客户端追赶 key 都携带同一受众标识，epoch 独立表示该流的恢复代次。PR17 定义此契约，PR26 实现唯一 Subject 编解码：`<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`；ID 使用无点的内部规范值，禁止原始凭据或用户输入直接拼接。撤销／下线控制与水位共用 Outbox，以明确的通知种类区分；控制载荷按需要附 tokenSessionId／conversationId，不能被当作普通水位合并掉。

完成各入口守卫后，多会话业务锁按 conversationId 排序，并在每个会话内遵守已定义的业务对象顺序；随后锁客服 Inbox 行（按 organizationId）、访客目录行（按渠道身份 ID）、用户水位行（按 userId）。只锁本次实际需要的行，拿到受众锁后不再反向获取业务锁。`pinOrderVersion` 存在 `user_sync_states` 本人行上，不新增独立顺序锁。资料变更涉及多个会话时先确定并锁业务集合，最后锁受众集合；PR01 必须把守卫和业务前置关系也纳入锁序检查。

### 列表行为基线

查询身份包含企业、用户、scope、customerView、assigneeIdentityId，启用后再加入分区和规范化搜索词；数据 key 共享，页面实例的选择与滚动位置各自保存。普通区按 `lastActivityAt DESC NULLS LAST, conversationId DESC` 排序；时间使用数据库精度，游标携带原值。置顶区按本人顺序升序，不采用活动时间。

| 输入／位置 | 内容与顺序如何处理 | 选择与滚动如何处理 |
| --- | --- | --- |
| 顶部且没有点击／拖动等交互 | 应用新项和最新活动序 | 保持顶部，不自动换当前详情 |
| 深处收到变化 | 更新已展示内容，暂存新增和活动序移动；资格移除立即生效 | 保存原可见邻居，不跟随活跃行上浮 |
| 点击“刷新” | 重读窗口并合并待更新结果 | 保留原浏览范围；原锚点已远离则用原后继／前驱定位 |
| 点击“查看最新” | 重建顶部窗口、清除已应用提示 | 明确回顶，仍保留有权阅读的详情 |
| 切换筛选 | 读取新查询；旧结果不可操作 | 恢复该查询历史，否则保持未选中 |
| 会话不再匹配筛选 | 从列表移除 | 有阅读权限则保留详情；失权立即清理敏感内容 |
| 断网／请求失败 | 保留已展示数据和独立错误状态 | 重试同一窗口，不换主体 DOM、不跳第一行 |

“保锚点”记录稳定可见 ID、相对滚动视口顶部的像素偏移、原后继／前驱及原窗口边界。读取前捕获，DOM 更新后测量一次并补偿；锚点因排序远移时优先留在原邻域。原生 scroll anchoring、控制器或虚拟列表只能有一个补偿所有者。用户正在滚动时延后非必要重排；失权清理不等待用户停止操作。

列表刷新和补页由同一控制器串行调度：刷新遇到在途补页，先等待其收尾，再捕获已经扩展后的窗口边界；刷新中触发补页则排队，重复刷新合并。切换查询使整个队列的 queryGeneration 失效，但不取消已经执行的 Wails 业务调用。刷新重读已加载双向边界之间的连续范围，并按 ID 重读已加载项的当前资格；不能只以首页或可见视口替换全部已加载结果。已上浮的可读行进入待重排集合，真正失权才按失权规则清理；请求失败保留已确认的窗口和重试边界。

### 个人置顶写入与读取契约

置顶、取消和移动均提交 expectedPinOrderVersion，成功返回新版本。移动输入为 `{ conversationId, position, neighborId?, expectedPinOrderVersion }`：position 是 `before`／`after` 时 neighborId 必填，表示插入该邻居在**全局个人顺序**中的紧前／紧后；`start`／`end` 不带邻居，表示整个置顶区的首／尾。目标和邻居必须是本人当前有权读取的置顶会话；版本或邻居失效时整次操作失败，客户端重新读取后让用户重试，不自动用旧意图覆盖。

“隐藏项”包括被当前筛选排除及尚未加载的置顶项。移动只改变目标相对位置，其余项之间相对顺序保持；如全局 `A,X,B`、当前仅看见 A/B，将 B 移到 A 前，结果必须是 `B,A,X`。前端只提交位置命令，不能把可见 ID 数组当成全量个人顺序保存。rank 重编号只是存储实现变化，不改变逻辑顺序。

任意改变置顶集合／顺序的事务（含失权清理及随操作发生的重编号）推进用户水位与 pinOrderVersion，并经本人流带出最新顺序版本。客户端发现版本变化时让整个置顶分区及其游标失效，重新取得同一版本的权威窗口；不得把单条新 pinRank 与缓存旧 rank 混排。多页重读途中版本再次变化，舍弃该轮候选并从新版本恢复锚点；已展示内容可保留为待更新视图，但不能提交使用旧版本的排序。新消息只推进内容版本，不推进置顶顺序版本。

### 实施 PR 的完成证据

后端项在相关 Action／Query 集成测试中构造数据并断言数据库及 appservice 返回值；并发用可控屏障组织，不靠 sleep 碰概率。运行 `wails3 task test:server`；契约或平台接入同时运行相关绑定生成、前端／服务端构建与平台 Task。前端项以本条具体交互场景验收，状态机复杂处增加可控响应顺序测试；界面操作遵守 AGENTS.md 授权规则。PR 描述记录实际执行命令、平台和结果，未执行的场景如实标记，不把本清单中的预期写成通过记录。

## 一、事实写入与列表基础

### PR00：禁用 AI 员工后保留会话并统一确认交互

- **依赖：** 无实时基础设施依赖；以已合并的独立 AI 会话实现为基础。
- **范围：** 禁用 AI 员工后仍在成员消息列表保留其已有 AI 会话；聊天页显示「该 AI 员工已禁用」并禁止发送新消息。账号禁用不撤销会话阅读资格，已提交输入继续执行，有在途 Run 时保留独立停止回复入口。
- **落点：** `actions/inbox/load_inbox.go`、appservice 会话摘要契约、`features/inbox`、`apps/mobile`、`features/contacts/agents/agents-panel.tsx` 和 `agent-detail.tsx`。
- **实施：** 会话摘要返回 Agent 账号状态，列表按会话归属和阅读资格查询，不再因 Agent 被禁用而隐藏历史；新消息发送继续校验账号状态。列表三点菜单和详情账号状态修改共用禁用确认交互，确认前不保存状态。文案明确禁止新消息、已提交任务继续执行、相关渠道分配重置为公共队列，团队关系保留；恢复后仍为下班，渠道配置不自动恢复。
- **边界：** 本条不新增会话归档／恢复、批量取消、群聊 Agent 或客服接管语义；停止回复由独立 Agent PR 实现，本条只保持入口可达。
- **验收：** 两个禁用入口取消确认均不改变账号状态，确认只提交一次；禁用前后历史、当前会话选择和滚动位置保持，不能新发消息但能查看在途结果并主动停止；恢复后允许发送，跨端状态一致，企业及成员隔离不变。
- **验证步骤：** 同一 Agent 创建两个会话，在其中一个运行时禁用；验证两个会话仍可见、结果写回原会话、停止仅影响指定 Run。分别从列表和详情禁用并取消／确认，检查渠道目标、下班状态和恢复后的行为。覆盖 Web／桌面端及移动端，运行契约生成、前端与服务端测试和构建。

### PR01：对齐路线图和实施边界

- **依赖：** 无。
- **范围：** 更新聊天／Agent 路线图：本地消息顺序、内存投影断线追赶、首次访客连接时机、最小协议、列表刷新／选择规则和通知边界；纠正当前单聊唯一关系、Telegram 入站及群聊 Agent 状态的过时描述。列出真人、渠道、Agent、资料、权限和个人置顶写入口及锁序。
- **落点：** `chat-roadmap.md`、`agent-roadmap.md`，逐一追踪 conversation／agentrun／channel 的写 Action。
- **实施：** 产出“入口 → 守卫 → 锁定对象与顺序 → 事实写入 → 受众”表；分别列真人发送、Telegram／网站入站、AI 回调和系统资料任务，不能把没有当前用户的入口强行套入 LockActiveUser。把本清单的消息本地顺序、列表行为表及访客时机同步回路线图。
- **验收：** 路线图与本清单无冲突；置顶、消息顺序、冷启动等产品选择有明确结论；不包含业务代码或新表。
- **验证步骤：** 抽查群移除、客服接管、AI 写回三条链路能沿表找到实际函数；已确认置顶规则直接落文，其他产品分歧在实施前解决。

### PR02：提取群聊访问与锁定能力

- **依赖：** PR01。
- **范围：** 在 `actions/chatstate` 提取真实共用的会话授权／锁定能力，群文本、群管理、已读、提及与静音共用锁后资格检查；不引入通用 Repository、反射事件或模型 Hook。
- **落点：** `internal/actions/conversation/group_conversation.go`、`group_management.go`、`mark_conversation_read.go`；新增 `internal/actions/chatstate`。
- **实施：** 区分可读、可发、可管理三种资格；写路径在会话／成员关系锁定后判断，Query 复用语义但不额外加写锁。先迁移群路径，不在本条改消息顺序或通知投递；删除被替代的群访问辅助函数。
- **验收：** 群移除与个人设置并发时不会写入过期关系；重入群、群主转让、解散只读和阅读基线保持现有语义。
- **验证步骤：** A 先读取群资料，B 移除 A 并提交，A 再静音／发送应失败；把 B 换成解散操作时，历史查询仍成功、发送失败。

### PR03：统一真人单聊与独立 AI 聊天的锁序

- **依赖：** PR02。
- **范围：** `direct` 真人单聊与 `agent` 独立 AI 聊天的首发、后续发送，以及 AI 聊天 Agent 的开始、认领、成功、失败和最终失败收尾。已有会话统一 Conversation → Participant → 个人状态（发送者才需要）→ AgentState → Run → 任务执行记录；新建共享主体按身份 ID 升序处理。
- **落点：** `conversation/direct_conversation.go`、`agent_conversation.go`、`internal_text_message.go`、`chat_subject.go`；`chatstate/access.go`；`agentrun/schedule.go`、`agent_chat_execute.go`、`execute.go`、`input_feed.go`。群创建、增员同步调整共享主体创建顺序，保留原展示顺序。
- **真人首发：** 当前用户守卫 → 按规范身份对读取；已有会话锁定后复核资格，显式首发可恢复归档；不存在时按身份 ID 确保双方主体，创建 Conversation、唯一关系和 Participant，再进入统一发送路径。身份对唯一冲突整笔回滚，在新事务中重读并锁定胜出的会话；普通 Query 不加写锁。
- **AI 首发：** 按草稿稳定 conversationId 定位；已存在时锁后核对企业、用户、Agent、类型和活跃状态；不存在时按身份 ID 确保主体，原子创建会话、扩展、参与者及首条消息。相同草稿与消息编号幂等确认，不同草稿保持独立，不按身份对复用或取消旧 Run。
- **发送与执行：** 等待会话锁后复核当前发送资格，再检查消息幂等；新消息、摘要和个人已读在 Agent 输入调度前保存，幂等命中不重复副作用。Agent 先读 Run 定位信息，再通过运行策略锁会话和参与关系，随后锁 State、Run、Task 并校验执行尝试和租约。成功 `text`、失败 `agent_error` 及终态、消费水位同事务提交，每个 Run 最多一条结果消息。
- **已确认行为：** 停用目标拒绝之后资格校验的新发送，已通过事务内资格校验的发送允许完成，不新增目标账号锁。停用 Agent 或归档 AI 会话不取消已提交的排队、运行中或尚待消费输入；这些输入继续处理并写入结果。真人归档只在显式首发时恢复；不新增 AI 归档／恢复操作。幂等重放仍须通过当前发送授权。
- **边界：** 客服通过原策略保持业务边界，完整会话锁序归 PR04；共用 begin 也按策略上下文 → State → Run → Task 进入。消息统一追加与序号仍归 PR05–06，不新增迁移、前端或 appservice 契约。
- **验收：** 真人双向首发只建一份长期会话，AI 同草稿重试只确认一份结果、不同草稿各自独立；发送与 AI 成功／失败无反向锁序；不同 Conversation 的输入、Run 和结果不混合；旧租约不能写回，重复终态回调不重复消息。
- **验证步骤：** 用可控查询屏障和数据库阻塞关系组织首发及共享主体竞争；交错真人发送与 Agent 开始、认领、成功、失败、最终失败；覆盖等待锁期间失权／归档／目标停用、停用前已接受发送、AI 停用／归档前已提交输入、多个 AI 会话并行。使用实际任务执行上下文验证旧尝试、旧 Worker、过期租约及最终失败回调。2026-09-08 已通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（27 项）和 `wails3 task build:server`（含 TypeScript 检查及前端生产构建）。Pi CLI 只读审核未发现实质缺陷，已修正文档中的旧函数名、过时验证说明及一处主体帮助函数注释；可选的查询精简与错误码拆分未改变本轮行为。Web 已于同日通过 `http://127.0.0.1:8081` 完成回归：真人双账号收发（主账号浏览器操作、第二账号 HTTP API）、相同 clientMessageId 幂等确认、AI 首发及后续回复、同一 AI 的两个独立 Conversation、失败 agent_error 刷新后保留和会话间结果隔离。AI 链路使用仅监听本机的临时 OpenAI 兼容模型桩覆盖成功／失败，经实际 NATS Worker 执行，不代表外部模型验证；临时供应商、Agent、会话、消息和任务记录均已清理。本轮入口检查纠正：企业按 access_host 绑定，localhost:8081 显示初始化页不能证明 127.0.0.1:8081 对应企业未初始化。

### PR04：统一客户会话与客服 Agent 的锁序

- **依赖：** PR03。
- **范围：** 网站／Telegram 入站、成员回复、领取／转交／关闭／重开和 AI 取消／写回，统一 Conversation → CustomerConversation → ServiceSession → AgentState → Run。补齐渠道凭据、身份、文件和任务回调的锁序交叉检查。
- **落点：** `receive_customer_text_message.go`、`receive_website_customer_text_message.go`、`manage_service_session.go`（均在 conversation）；以及 `agentrun/customer_execute.go`。
- **实施：** 首次入站尚无 Conversation 时，先稳定定位渠道身份／客户关系，再创建或锁现有会话；已有会话统一锁序。最终写回必须在锁后重新检查 serviceSessionId、负责人、Run 尝试和租约，前置 HTTP 检查不作为写回凭证。
- **验收：** 首次入站、续开周期、人工接管与迟到 AI 回复并发收敛；不因阅读创建参与者或改变负责人。
- **验证步骤：** 阻塞 AI 写回，让人工领取／转交／关闭先提交，再释放 AI；旧结果不得成为当前客服回复。反向顺序则只允许已提交回复一次，且所有摘要对应同一周期。

- **PR04 实现记录：** 从已合并 PR03 的 `6dc3bdc` 开始，提交前快进同步 `8f96135`；`chatstate.LockCustomerServiceSession` 统一已有会话、客户扩展和当前周期锁，入站与成员回复按需要使用拆分的会话／周期入口。转交目标身份先于会话锁；Agent 开始、认领、成功、失败及最终失败经相同策略入口再锁 State、Run、Task。网站／Telegram 入站在渠道身份稳定定位后锁会话，联系人 Participant 在周期确定后创建，消息落库后调度 Agent；Agent 回复 Participant 在 Task 门禁之后创建，抑制结果不建参与关系。同一 Conversation 锁覆盖这两类参与者写入，不存在持有参与者锁再首次获取会话锁的入口。共享企业身份主体统一使用冲突后重读能力；Telegram 头像新旧文件按 ID 锁定。
- **交叉检查：** Telegram 真人回复包含 `customerdelivery.Prepare → Enqueue`，前置渠道共享锁、渠道身份锁，之后锁会话与周期并原子保存消息和投递；配置使用渠道／Bot 外层锁后按 Channel → Setting 更新。Webhook 先锁 Setting 再锁渠道身份与会话，不反向更新 Channel；投递认领与人工处理按渠道身份 → Delivery，网络调用在事务外。联系人恢复使用渠道身份 → Contact，不反向锁会话；目标停用及路由重置不进入会话业务集合。本条不改变 Telegram 的投递重试规则或网站 Agent 的渠道能力。
- **验证记录：** 已添加数据库屏障覆盖网站首次入站幂等竞争、客服管理与回复的会话前置锁、显式重开与客户续开竞争、Agent 各阶段与入站交错、人工接管／转交／关闭续开与迟到结果的双向提交顺序，以及客服旧尝试／Worker／过期租约。2026-09-08 已通过 `wails3 task test:server`（含客服各阶段、真实任务上下文和唯一索引未提交等待）、`wails3 task test:desktop`、`wails3 task test:frontend`（27 项）以及 `wails3 task build:server`（含绑定生成、TypeScript 检查和前端生产构建）；审核修正及同步 main 后再次通过服务端全量测试、前端测试和服务端生产构建。Web／公开 Messenger 在 `http://127.0.0.1:8081` 验证访客首发、成员首次回复自动领取、真人转交／接管、关闭、显式重开和客户消息续开第二周期；临时 AI 客服经真实 NATS Worker 和仅监听本机的 OpenAI 兼容模型桩完成回复，两端均能读取持久结果，不代表外部模型或 Telegram 真平台验证。同步 main 和审核修正后，已再次完成访客首发、客服回复、访客刷新恢复与同周期关闭／重开的冒烟验证。临时渠道、联系人、用户、Agent、供应商、消息、周期与运行任务已清理，本轮进程均已关闭，8081、9246、18404 端口已释放。Pi CLI 完整只读审核未发现生产锁序缺陷；已按反馈补强唯一冲突等待屏障、收窄会话类型条件、修正注释并合并重复锁定入口，修正复核已确认无阻塞问题。

- **Telegram 真平台补测（2026-09-08）：** 使用用户配置的公网渠道与本机 Telegram 客户端验证文本入站、中文／emoji 双向传输、3 条真人回复实际送达、领取、关闭后连续入站只新增第 2 个周期，以及显式重开保留第 2 个周期。3 条测试回复的 Delivery 均为 sent、attempt=1，平台消息 ID 按投递顺序递增；官方 getWebhookInfo 返回 pending_update_count=0，未返回错误。对真实入站消息的业务字段重建回调，在进行中与关闭后分别并发重放 4 次，均返回 204，消息／参与者／周期数量及最后消息不变，关闭状态不被复活；错误 Secret 返回 401。公网脚本请求被 Cloudflare 1010 拦截，重放和错误 Secret 检查通过同一服务的本地入口完成，不计为公网脚本验证。当前无可转交的客服同事，真平台未补测转交，沿用前述自动化与网站验证。测试消息以 PR04 TG 标记保留，渠道配置、原有记录和用户启动的服务保持可用；本轮未启动服务进程。
- **补测发现：** 关闭后时间线中的周期标题可能仍显示“进行中”，页头及数据库已为关闭，重新加载页面后标题正确。`use-conversation-timeline.ts` 的增量合并仅在有新消息或 Agent 状态变化时更新，周期状态变化未使现有窗口收敛；记录为前端查询收敛回归项，本 PR 未修改前端实现。

### PR05：集中消息追加与摘要维护

- **依赖：** PR04。
- **范围：** 各类文本、群系统消息、附件消息（含批量与说明）、AI 成功／失败及主动停止结果消息调用 chatstate 的事务内追加能力；集中维护会话／周期摘要及附件取消后的摘要重算。业务 Action 继续负责路由、领取、引用、提及、附件状态与文件激活、客服首响和 Agent Trigger。共享模块不反向依赖 Action 或实时传输。
- **落点：** conversation 各发送／入站文件、群系统消息、`attachment_message.go`、`attachment_batch.go`、`attachment_upload.go` 和 agentrun 最终回复／停止路径；新增 chatstate 的事务内追加与摘要重算函数。
- **实施：** 追加函数接收调用方事务、已锁会话、发送主体及规范化消息；返回已保存 Message 和本次是否新建。幂等命中先返回既有事实，不再次改摘要、触发 Agent 或入 Outbox；领域 Action 继续决定客服路由和群系统文案。此项是保行为提取，PR06 才切换序号契约。
- **验收：** 所有真实写入口覆盖；消息、摘要、Trigger 与 task_outbox 一起提交或回滚；幂等重放不产生额外副作用。
- **验证步骤：** 分别从真人、群系统、访客、Telegram、附件及 AI 成功／失败／停止入口写入消息，断言摘要指向正确消息；在写摘要／Trigger／task_outbox 后主动回滚，消息及任务记录均不存在，群分配器恢复原位置。覆盖批量附件重放、说明、取消末条／唯一附件后的摘要，以及 Telegram 旧来源时间不回退摘要。
- **范围确认（2026-09-08）：** 用户确认纳入 PR04 之后合并的附件和停止回复能力，保持现有业务行为；统一消息序号与排序切换仍归 PR06。
- **实现记录：** 基于 `5f63ef0`，`chatstate.AppendMessage` 接收调用方事务、已锁会话和规范化消息（含发送 Participant），返回已保存消息与新建标记；群序号在确认未重放后分配，消息与会话／周期摘要一起保存。Action 保留副作用前的完整幂等校验，Agent 以同事务 Run 终态门禁排除重复回调；客服首响由业务策略决定。`RecomputeConversationSummary` 负责附件取消后的末条摘要重算，删除被替代的消息 INSERT、摘要更新和群序号辅助函数。本条不新增迁移、业务接口或实时 Outbox。
- **验证记录（2026-09-08）：** 已通过 `wails3 task test:server`（含新增的共享追加重放／群序号回滚、Telegram 旧来源时间，群改名系统消息及取消末条附件摘要，以及加强后的 Agent／网站访客／Telegram 消息、摘要、Trigger 与真实 task_outbox 回滚断言）、`wails3 task test:desktop`、`wails3 task test:frontend`（39 项）和 `wails3 task build:server`（含绑定生成、TypeScript 检查与前端生产构建）。原有附件生命周期、引用、取消、AI 成功／失败／停止及客服锁序回归均通过。本条未改变界面结构或交互流程，未启动界面或外部平台手动验证；测试仅重建本 worktree 的测试库，未重建开发库或启动持久服务。审核修正后再次通过服务端全量测试与服务端生产构建。Pi CLI 完整只读审核未发现阻塞缺陷；已按建议删除附件重复重放分支、统一锁定会话赋值位置、调整注释并补齐上述回滚与摘要断言，针对性复核确认未引入实质问题。

### PR06：统一消息序号、分页与阅读水位

- **依赖：** PR05。
- **范围：** 真人单聊、AI 聊天、群聊和客户会话统一 `message_seq`／`last_message_seq`，会话锁内分配并设唯一约束；同步修改各端分页、引用定位、已读和提及比较，删除群专属排序分支。来源时间／顺序保留，晚到外部消息按本地接收位置展示；HTTP 序号用字符串、TS 用 bigint。
- **落点：** `models/message.go`、`models/conversation.go`、`chatstate/message.go`、消息／引用 Query 与 `types_conversation.go`。
- **实施：** 新增统一分配器和会话内序号唯一约束，把 before／after／上下文／阅读与提及的排序全部切换；展示时间仍来自来源字段。事务回滚允许未提交编号重用，已提交分配器不随摘要重算回退。迁移、全部读取方、绑定及旧排序删除同 PR 完成，避免双顺序并存。
- **验收：** 屏障测试覆盖先开始后提交、回滚、Telegram 旧时间消息及大整数精度；重入群基线、乱序提及确认正确。摘要可重算，分配器不回退；用空库／可丢弃测试库验收结构切换，已有开发库停服后按仓库流程重建，不提供保留原数据的回填或运行时回退。
- **验证步骤：** A 已取会话锁但未提交时 B 发送，断言 B 等待并排在 A 后；A 回滚后无可见空消息。造超过 JS 安全整数的序号，经 HTTP、Wails、前端比较后分页和已读仍精确。

- **范围确认（2026-09-08）：** 用户确认开始 PR06，并允许按需停服重建本 worktree 的开发库，不保留旧数据。附件、停止回复沿用 PR05 已纳入的业务行为，全部消息读取方同时切换；列表活动时间仍归 PR08。
- **实现记录：** 基于 `3821a12`，同步 `b58480e`。共用追加入口在会话锁内为所有消息分配 `message_seq`，以 `last_message_seq` 保存已提交分配位置；新增迁移将消息序号设为必填并建立会话内唯一约束，删除群专属排序及摘要中的来源顺序副本。个人状态持久化 `read_seq`，已读提交仍使用消息 ID，服务端按序号单调推进；重入群以本次加入系统消息初始化普通已读与提及基线。成员、访客、引用上下文、未读计数、提及和 Agent 历史边界均按统一序号读取；成员与访客共用绑定会话的序号游标，HTTP／Wails 返回字符串，前端比较使用 bigint。待发气泡保留本地队列顺序，不伪造持久序号。
- **验证记录（2026-09-08）：** 已通过服务端全量测试、桌面端测试、前端测试（同步 main 前 39 项，知识库收缩合入后 34 项）、服务端生产构建及 macOS arm64 原生构建，完成 appservice 与 Wails 绑定生成。新增真实数据库测试覆盖四种会话的提交屏障与回滚重用、大整数双向分页／引用上下文／访客读取／HTTP 传输、删除已读目标后的水位、摘要重算不回退分配器及唯一约束；已有附件、停止回复、重入群和乱序提及回归通过。`task1` 已停服重建，新增迁移 Down／Up 已实测，随后按同步后的完整迁移再次重建。
- **界面记录：** Web、macOS 桌面端及 `mobile-preview` 用 65 条超过 JS 安全整数的群消息验证历史窗口、跨页引用与返回最新，Web 验证提及导航。实际 Wails 调用返回末条及阅读水位 `9007199254741056`，50 条窗口首尾差精确为 49。公开 Messenger 验证首发、客服回复、刷新恢复、历史补页和跨页引用，来源时间改早的末条仍保持本地末尾位置。同步 main 后再次完成企业初始化、单聊展示、65 条大整数群消息跨页引用及返回最新的 Web 冒烟验证。未执行 Telegram 真平台、外部模型或 iOS／Android 真机验证。界面验证结束后已关闭本轮 Server、Vite、桌面端、移动预览及 MCP，8081、9246、9100 端口已释放。
- **审核记录：** Pi CLI 完整只读审核未发现阻塞问题；已按反馈修正序号及分页注释、移除测试中的来源三元排序，并明确列表仍使用来源时间，独立活动时间由 PR08 交付。同步 main 后的针对性复核确认无生产阻塞，测试已适配当前 Backend 构造签名。

### PR07：保存发送方客户端消息关联

- **依赖：** PR06。
- **范围：** 明确保存 clientMessageId 与发送者关系，成员／访客查询仅向本人返回关联；保留正文、引用、提及的幂等冲突检查，不解析或公开 idempotency_key 充当关联契约。
- **落点：** `models/message.go`、发送 Input／消息 DTO、成员及网站消息 Query。
- **实施：** 为 clientMessageId 定义明确的发送方作用域，与业务幂等键分开保存；首次发送、重放响应、历史查询均返回同一关联。返回关联的条件是请求身份等于发送身份，内部 Agent 或来源渠道键不能借此暴露。
- **验收：** 超时重试只保存一条消息；其他用户和访客读不到他人的发送关联或内部幂等键；各端绑定同步生成。
- **验证步骤：** 同 ID 同内容重放得到同一 messageId；同 ID 改正文／引用／提及返回既有冲突；另一个成员读取同一可见消息时关联字段为空；另一个访客读取该会话被拒绝，访客读取客服回复时关联字段为空。

- **范围确认（2026-09-08）：** 成员发送编号按企业内成员身份隔离，跨设备、跨会话属于同一作用域；网站发送编号按渠道访客身份隔离，跨线程属于同一作用域。不同发送者可使用相同编号，同一发送者不能复用编号改变会话或发送意图。覆盖真人单聊、独立 AI 聊天的成员输入、群聊、客服人工回复、网站访客文本及现有附件／批量附件入口；发送状态收敛仍归 PR15／PR37。
- **编辑与限时撤回边界：** clientMessageId 只标识首次发送，与 messageId、message_seq 一起保持稳定；编辑不创建新消息，也不重新计算撤回时限，撤回后旧发送重试不能恢复消息。后续开放编辑／限时撤回时，发送幂等校验须改用不可变的原始发送意图（可保存规范化指纹），不能继续与可编辑正文比较；编辑、撤回使用独立操作幂等标识，变化通过 sync_seq 同步。本条不新增编辑、撤回、意图指纹或历史版本功能；撤回时限、提示和 AI／外部渠道处理规则在对应 PR 确认。
- **实现记录：** 基于 `da7853a`，新增可空 `messages.client_message_id`，成员发送者复用 Participant → ChatSubject → 企业身份关系，访客同时核对消息所属 ServiceSession 的渠道身份；业务幂等键独立保存，不解析内部键生成公开关联。网站幂等键改为 `chmsg:<contact_channel_identity_id>:<client_message_id>`，发送编号统一规范化为小写 UUID。首次发送、重放及成员／访客历史返回同一关联，非本人返回 null；Agent、系统与外部渠道消息不填充关联。迁移不回填历史数据，不增加兼容分支。

- **验证记录（2026-09-08）：** 已运行 `go generate ./internal/appservice` 和 `wails3 generate bindings -clean=true -ts -i`；通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`、`wails3 task build:server`（含前端生产构建）及 `wails3 task darwin:build ARCH=arm64`。真实数据库覆盖成员并发重放、正文／引用／提及冲突、成员及访客编号作用域、跨线程冲突、首发和历史关联；附件及 AI 聊天既有集成测试补充关联断言，HTTP 验证同一成员两个登录会话可读本人关联、其他成员及访客关联不泄露。服务端全量回归在补充 AI 输出为空的断言后再次通过。仅重建本 worktree 测试库，未修改开发库、未启动业务服务或界面运行时；本条未改变布局或交互，未做手动界面、Telegram 真平台或移动真机验证。

- **审核记录：** Pi CLI 完成完整只读审核，未发现生产阻塞项；采纳建议精简 DTO 注释，并明确重复登录用例验证令牌无关的本人关联，移除未独立断言的测试注释。网站历史保留周期关联查询，符合现有客户消息关系；最终绑定差异仅为自动生成的 clientMessageId 字段，无 dock／color 绑定删除。

### PR08：统一列表活动时间和读取快照

- **依赖：** PR06。
- **范围：** 使用独立、同会话单调的 lastActivityAt 排序，数据库在会话锁内生成活动时间；收件箱行与总数在一致只读快照中读取。新消息／系统消息更新活动，token、已读、静音和客服状态变化不改排序；DTO 区分活动排序时间与预览来源时间，前端不再用 Date 精度重算业务顺序。
- **落点：** `models/conversation.go`、chatstate 追加函数、`inbox/load_inbox.go`、`types_inbox.go` 和前端列表比较函数。
- **实施：** 新增 lastActivityAt，消息追加时取数据库当前时间与该会话已有值的较大值；相同时间由 ID 打破并列。客服状态、用户偏好只改投影。Query 在一个只读一致快照内读取行及权威总数，前端按服务端顺序展示。
- **验收：** 多实例时钟差异、相同时间、空会话及混合类型排序稳定；客户“全部”范围沿用原规则；客服未读不自动并入桌面总提醒。
- **验证步骤：** 一条带昨天来源时间的 Telegram 消息今天入站：预览仍显示昨天，列表活动排到今天；在行查询与总数查询之间提交另一消息，当前响应不得混用两个快照。

- **范围确认（2026-09-08）：** 用户确认开始 PR08。统一活动写入、读取快照及前端顺序，保持新建会话可达；独立详情查询、选择状态和临时集合的完整清理仍归 PR09。
- **实现记录：** 基于 `7ced7eb`，新增可空 `conversations.last_activity_at`，不回填历史数据。共用追加入口在会话锁内取 `GREATEST(last_activity_at, clock_timestamp())`，幂等重放、摘要重算及纯状态变化不推进活动。统一收件箱各类型查询及混排使用数据库精度的活动时间与 ID，行数据和完整未读／提醒总数在同一只读 Repeatable Read 事务中读取；客户范围与提醒口径保持原规则。列表 DTO 和单聊首发／查找摘要携带 `lastActivityAt`，预览仍用 `lastMessageAt`。Web／桌面删除 Date 二次排序，临时会话保持可达，列表响应接管后按服务端顺序展示；移动端沿用服务端顺序。
- **验证记录（2026-09-08）：** 完成 appservice 与 Wails 绑定生成；通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（34 项）、`wails3 task build:server`（含前端生产构建）及 `wails3 task darwin:build ARCH=arm64`。真实数据库新增用例覆盖四种会话的数据库活动时钟、未来／过去来源时间、活动单调、重放、回滚与摘要重算；混合列表微秒精度、同时间 ID 顺序及空会话；appservice 读取列表后并发提交消息，当前响应与下次响应分别保持行和总数一致。Telegram 用真实接收 Action 验证昨日来源消息今日入站、重复投递、公共客户范围及关闭／重开不移动活动，个人已读、静音和纯资料／系统消息区别均有回归。补充用例初次因夹具未注入 Telegram 调度器失败，修正夹具后服务端全量再次通过。
- **界面记录：** Web 创建群聊、首发后刷新及空群创建验证通过，列表刷新后仍选中目标；将本轮测试消息来源时间设为昨天，Web、macOS 桌面端及 `mobile-preview` 均保持活动会话在前且预览显示昨天。实际 Wails 调用返回活动时间 `2026-09-08T14:35:23.593944Z`，完整保留微秒。开发库仅应用新增迁移并添加本轮验证数据，未重建或回填旧数据；未验证 Telegram 真平台、外部模型或移动真机。验证结束后关闭本轮服务端、桌面端、移动预览、Vite 与 MCP，确认 8081、9246、9100 端口释放。
- **审核记录：** Pi CLI 配置恢复后完成完整只读审核，未发现阻塞项。采纳建议统一 AI 会话查询错误上下文，并让静音／已读测试直接使用该轮设定的活动时间基准；处理后服务端全量测试再次通过。活动时间与通用更新时间保持各自语义，不增加性能索引；不回填历史数据的影响在 PR 描述中明确说明，沿用已确认的 MVP 边界。

### PR09：详情选择与列表结果解耦

- **依赖：** PR08。
- **范围：** 增加独立会话摘要／详情读取及按 ID 批量查询，逐项返回失权结果；Web／桌面移除“选中项消失就打开第一条”。客服本人操作默认保留详情，明确操作才切目的队列；筛选移除仍可读的详情保持，真实失权清除正文、引用、草稿和缓存，主动新建仍打开目标。
- **落点：** `direct_backend_inbox.go`、`types_inbox.go`、`inbox-page.tsx` 的 selectedConversation／startedConversations／leftGroupConversationIDs。
- **实施：** 新增按 ID 独立详情和批量摘要 Query，输入不依赖当前列表页；批量逐项区分可读、不匹配当前筛选、不可用且不泄露不可见实体。选择只由导航／明确用户操作改变，移除用本地集合长期覆盖服务端结果的做法。
- **验收：** 第 50 条外深链、转交、关闭、筛选变空不切换到另一会话或误标已读；解散但可读的群保留只读详情。
- **验证步骤：** 直接打开第 80 个会话，刷新只有前 50 条时详情仍在；把本人客服会话转给他人后保留可读详情；移出群后详情正文、引用及草稿被清理。

- **范围确认（2026-09-09）：** 用户确认按本轮建议实施。移动端真人单聊与 AI 聊天一并接入独立摘要，移动列表分页和返回锚点仍归 PR13。领取、转交、关闭、重开及回复隐式领取后保持当前筛选和详情，不新增目的队列跳转入口；点击已有队列导航才切换。主动退群清空选择并关闭窄屏详情，被动失权或不可用深链显示简短提示，不自动打开下一条。主动切换筛选恢复该查询已记录的选择，无记录则未选中；会话自身变化导致不匹配筛选时仍保留可读详情。
- **实现边界：** 独立摘要只按阅读资格授权，批量摘要在同一只读快照内逐项返回匹配、筛选外可读或不可用；不可用项不区分不存在、跨企业及失权。保留轮询，确认失权后清理正文、引用、草稿、附件队列与共享缓存，并忽略迟到结果；网络错误保留已展示详情。客服转交、关闭和群解散造成只读时保持输入组件与草稿，文本只读且可选择复制，禁用发送；真实失权才卸载清理。新建会话先读取权威摘要再由草稿切换，不依赖首页接管临时集合；关闭创建群聊表单或卸载页面后忽略迟到回调，已完成的创建只刷新列表。

- **验证记录（2026-09-09）：** 已通过服务端全量测试、桌面端测试、前端测试（37 项）、前端与服务端生产构建、macOS arm64 原生构建；已重新生成 appservice 与 Wails 绑定。新增回归覆盖首页外独立读取、四类摘要、批量结果顺序与重复 ID、筛选外可读、跨企业与失权、领取／转交期间快照一致性、解散只读、移出重入，以及共享缓存与附件准备请求迟到后的清理。Web 实测 80 个群的首页外深链与刷新、筛选选择恢复、被动失权、主动退群、客服领取与转交；桌面 MCP 实测新建群、转交／关闭／重开保持路由、引用及草稿；Web 实测解散群保留只读历史与草稿；mobile-preview 验证无路由摘要的真人单聊深链。移动 AI 摘要通过服务端回归和前端构建，未调用真实模型做界面验证。本轮创建的 82 个会话及访客关联数据已清理，本轮启动的 Web／桌面／Vite／MCP 已关闭，8081、9246、9100 端口已释放。

- **审核记录：** Pi CLI 已分别完成后端、前端和收尾局部审核，均无阻断缺陷。按建议补齐独立摘要失败日志的企业编号、筛选恢复说明，并将只读草稿保持为可选择复制的文本。复核附件遗留建议时确认既有两分钟上传活跃期限会向接收方返回失败，失权后不追加需要成员权限的取消请求；补充真实数据库回归验证超时失败且消息位置不变。不可用摘要继续轮询以支持重新获权自动恢复；按 ID 批量摘要和缓存 key 保留供后续列表控制器使用。

- **提交前同步：** 同步 `main` 的 `ea6bf26c`，保留 Telegram 引用回复和移动端头部更新，补齐失权时的引用状态缓存清理，并改用通用重试文案。同步后再次通过服务端全量测试、桌面端测试、前端测试（42 项）、服务端生产构建与 macOS arm64 原生构建；Pi CLI 对冲突整合完整复核通过，未发现实质问题。本轮未启动持久验证服务。

- **冲突更新：** 再次同步 `main` 的 `24fa04f0`（前端共享组件重构），创建群聊采用共享图片上传与成员选择组件，同时保留关闭、卸载和重开时的过期请求保护；会话列表采用共享时间与客服状态文案函数，独立详情逻辑不变。51 项前端测试、前端生产构建及 Pi CLI 冲突解决复核均通过。

### PR10：服务端真实收件箱分页

- **依赖：** PR09。
- **范围：** 全部／内部／客户统一 keyset 分页，替代每类型截取 50 条后混排；游标绑定完整查询、精确排序值、ID、排序版本，定义空时间边界。现有调用方同 PR 适配首屏，暂不增加空置顶区或搜索接口。
- **落点：** `inbox/load_inbox.go` 的各类型查询和混排、`LoadInboxInput`／`Inbox`、`frontend/src/api/inbox.ts` 及各端首屏调用。
- **实施：** 先组合真人单聊、AI 聊天、群聊和客户会话的可见候选再按共用顺序取 limit+1，返回 items、nextCursor、hasMore；游标绑定查询身份、版本和原始排序边界，删除游标行仍能按边界续读。总数不按当前页累加，空时间区单独表达 null 边界。
- **验收：** 不同类型与客户筛选、并列时间、游标行移除、重复页／空页均可继续；列表未返回不等于不存在；不使用 offset。
- **验证步骤：** 各类型至少造 60 条、夹入同时间和空时间，顺序翻页结果与一次完整 SQL 排序一致；读第一页后删除边界行，下一页可继续，换筛选使用旧游标得到明确无效结果。


- **范围确认（2026-09-09）：** 用户确认开始 PR10，默认每页 50 条，Web／桌面／移动端本轮只适配首屏；接受“全部／内部”从原每类最多 50 条混排改为统一排序的前 50 条，后续浏览仍由 PR12–13 接通。首页外已打开详情继续独立读取，完整未读与提醒总数不受分页影响。
- **实现记录：** 四类有权候选先在数据库中 `UNION ALL`，统一按 `last_activity_at DESC NULLS LAST, id DESC` 读取 `limit + 1`，再在同一只读 Repeatable Read 快照中按 ID 读取摘要及完整未读总数。删除各类型固定截断和 Go 混排，按 ID 资格判断共用候选筛选；返回现有 `conversations` 字段及 `nextCursor`／`hasMore`。不透明游标绑定企业、用户、规范化筛选与排序规则版本，保存微秒活动时间或明确空时间边界及 ID，不依赖边界行存活；无效游标返回本地化 `inbox_cursor_invalid`，终页返回空游标。页大小可在续页调整，不属于筛选身份。独立 `InboxQuery` 供按 ID 资格读取与前端筛选使用，分页字段只属于 `LoadInboxInput`；API Proxy 和 Wails 绑定按契约重新生成。
- **一致性边界：** 每次请求的列表和总数共享快照，跨请求不保留数据库事务，也不保证活动变化期间的一次遍历等同历史全量快照；新消息导致的上浮与筛选变化由后续列表刷新和锚点能力收敛。本轮不新增迁移、普通性能索引、置顶、搜索或滚动加载交互。

- **验证记录（2026-09-09）：** 已完成 appservice 与 Wails 绑定生成，通过服务端全量测试、桌面端全量测试、前端测试（53 项）、前端生产构建、`wails3 task build:server` 和 `wails3 task darwin:build ARCH=arm64`。真实数据库以四类各 60 条会话覆盖全量 SQL 对照、微秒并列、空时间区、客户各队列及负责人筛选、默认页大小、重复请求、页外未读总数；另覆盖时间／空时间边界行删除、失权空尾页、跨账号和跨企业游标，以及列表与总数的并发快照一致性。HTTP 与原生代理测试覆盖分页参数和响应元数据，游标单测覆盖损坏载荷、排序版本及查询身份。原生代理新增用例初次因夹具未初始化客户端凭据失败，修正后桌面端全量通过。本轮仅调整接口和首屏取数，没有新增布局或交互，未启动 Web、桌面端或移动预览做可视化验证；未调用业务对话模型或真实渠道。本轮测试服务在测试结束时关闭，未启动持久服务或调整开发库；构建有宿主 macOS 链接目标版本警告，构建成功。

- **审核处理：** Pi CLI 首轮完整审核未发现阻断问题。采纳候选查询精简建议：新增 `candidates.go` 统一阅读资格及有效关联，分页和按 ID 资格核对只投影 ID／活动时间，摘要按相同阅读基线追加；同步减少内部总数查询中不需要的 AI 运行摘要读取。临时只读 `EXPLAIN (ANALYZE, BUFFERS, VERBOSE)` 对照确认候选阶段不再执行未读、群成员数量或 AI 运行摘要计算，按 ID 条件可下推到各类型分支；诊断代码已移除，不把此小规模执行时间作为性能承诺。补充同快照下候选与摘要一一对应的直述注释。未采纳任意页大小上限，沿用 MVP 边界；保留 `math.MaxInt` 校验以防 `limit + 1` 溢出，该正整数本身能通过 HTTP 解析，并非不可达分支。跨页活动变化语义已在上文明确。
- **收尾复核：** Pi CLI 对查询抽取再次只读复核通过，确认阅读资格、四类摘要列、全局未读、AI 首发摘要与原有筛选均保留，无实质问题，也无需再删减查询层次。临时执行计划诊断代码移除后，服务端全量测试与服务端生产构建再次通过；保留任务开始时已有的五个 dock/color 生成文件删除。本轮未启动持久服务，既有 8081 服务保持运行，9246、9100 无本轮遗留进程。


### PR11：列表锚点上下文查询

- **依赖：** PR10。
- **范围：** 按 queryIdentity 和 conversationId／原排序边界读取邻近列表窗口，返回前后边界和资格；用于缓存淘汰、深处返回及定位，不从第一页逐页重建到目标。
- **落点：** 新增 inbox 锚点窗口 Query、appservice 方法和 DTO；复用 PR10 的资格谓词与排序。
- **实施：** 输入包含 queryIdentity、anchorId、原排序边界及前后窗口大小；返回锚点状态、邻域 items、双向游标和是否还有前后页。提供原排序边界时始终按原边界查邻域；未提供时才按仍匹配筛选的锚点当前位置定位，不设置远移阈值，不靠不断加载首屏找回，也不扩大当前筛选。
- **验收：** 锚点不存在、不再匹配、无消息、邻近结果为空及权限撤销有明确结果；上下文仍遵守当前筛选。
- **验证步骤：** 定位第 200 条后使其上浮、被移除或改为不匹配，分别断言返回当前资格与原邻域；前后都无结果时返回真正空窗口，并由客户端选择空态。

- **范围确认（2026-09-09）：** 用户确认开始 PR11。有原位置时恢复原邻域，无原位置时按会话当前位置定位；同轮补齐双向续页与完整已加载区间重读。仅交付查询、跨端契约和测试，列表控制器与界面交互仍归 PR12–13；不新增迁移、置顶、搜索或实时订阅。
- **接口契约：** 列表行新增 `positionCursor`，绑定企业、用户、规范化筛选及排序版本，保留数据库精度；独立摘要不携带查询位置。`GetInboxContext` 接收 `query`、`anchorId`、可选 `anchorCursor` 和前后条数（零值各取 25），原游标必须属于该锚点及当前查询。锚点结果复用 matching／outside_query／unavailable，匹配锚点携带当前位置游标；列表只渲染窗口行，独立锚点可能与窗口内同一 ID 重叠，不再额外插入列表；窗口始终按原位置构造，锚点是否仍在其中由当前排序及资格决定。不存在、跨企业和失权统一不可用，不返回摘要；无原游标且锚点不匹配时返回未定位的空窗口，不使用首页替代。
- **窗口与续页：** `LoadInbox` 增加互斥的 `beforeCursor`，返回 `startCursor`／`endCursor`／`hasBefore`，现有 `hasMore` 仍表示窗口之后还有会话，`nextCursor` 在无后页时为空。各方向最终均按活动降序返回。`ReadInboxWindow` 按同一查询的首尾游标重读包含边界的完整闭区间，不按页大小截断，边界行移动、删除或失权不改变区间；响应保留请求边界，区间为空也可重试或向两侧续读。客户端另用按 ID Query 核对已加载项的当前资格，不能把区间外上浮当成失权。窗口读取及前后存在性探测、锚点资格和摘要在各自一次只读 Repeatable Read 快照内完成；跨请求不保持历史快照。
- **查询实现：** 分页、邻域和闭区间共用候选资格与双向排序谓词，以独立存在性查询确定前后范围；因此普通分页不再使用 `limit + 1`，也不再需要对应的整数加一溢出分支。时间为空时仍以 ID 定位，前页查询反向取最近邻后恢复活动降序。邻域为空只表示该窗口无结果，不能单独据此断言整个筛选为空。

- **验证记录（2026-09-09）：** 已完成 appservice 与 Wails 绑定生成，通过 `wails3 task test:server`、`wails3 task test:desktop`、`wails3 task test:frontend`（53 项）、`wails3 task build:server`（含前端生产构建）和 `wails3 task darwin:build ARCH=arm64`。真实数据库用 220 条会话验证第 200 条定位、微秒并列及空时间区、前向遍历、160 条闭区间重读、锚点上浮／失权／删除、首尾边界删除后继续双向读取；另覆盖四类会话、所有客户队列及指定负责人、领取后筛选外可读、缺失或跨企业锚点、跨查询／用户／企业游标、倒置区间，以及全空邻域和非空列表中的空区间。通过可控查询屏障验证锚点和完整区间的候选、摘要、资格来自同一快照。HTTP 与原生代理覆盖前向游标、嵌套请求、双向边界元数据及锚点／窗口头像归一化。新增接口与边界测试补齐后，服务端和桌面端全量再次通过。未新增界面交互，未启动 Web／桌面／移动预览做可视化验证；未调用真实渠道或业务模型，未调整开发库。生产构建有宿主 macOS 链接目标版本及重复系统库警告，构建成功。

- **审核处理：** Pi CLI 已完成完整只读审核，未发现阻塞性缺陷，确认排序、空时间、边界删除、资格、筛选、快照、双向续读及原生头像归一化符合契约。采纳建议：明确独立锚点可能与窗口行重叠、列表只渲染窗口；补充原位置未变时并入中心的直述注释；统一列表、独立摘要、批量摘要及窗口的错误转换，异常 WARN 携带企业与查询入口；增加大写 UUID 游标归一化测试；反馈处理后服务端全量测试与服务端生产构建再次通过。锚点与窗口内同一会话的摘要读取仍在同一快照中分别完成，沿用审核建议不为当前规模增加复用分支。五个 dock/color 生成文件删除为任务开始前已有变更，不纳入本次提交；本轮测试进程已结束，未启动持久服务，原有 8081 服务保持运行，9246、9100 无本轮遗留进程。

### PR12：Web／桌面列表分页与滚动锚点

- **依赖：** PR11。
- **范围：** 提取列表控制器，接通补页；保存首个稳定可见 ID、像素偏移、相邻候选和窗口边界。轮询刷新重读已加载边界窗口，深处只更新内容／资格、暂存活动序重排；顶部无交互或显式查看最新才跟随。刷新保留 ScrollArea，工具区／头像／摘要行尺寸稳定；锚点消失选原后继再前驱，窗口全空才重读邻近范围。原生锚定与手动补偿只保留一个所有者。
- **落点：** `frontend/src/features/inbox/inbox-page.tsx`；新增 feature 私有列表控制器，Query key 放 `resource-keys.ts`。
- **实施：** 状态至少区分 initial、ready、loadingMore、refreshing 与局部失败；维护已加载 ID／双向边界、pendingChanges 和锚点，不另存业务 DTO。刷新／补页按共用串行调度规则执行，刷新从已加载窗口边界与按 ID Query 重读；响应成功后一次布局测量恢复偏移，失败保留内容。
- **验收：** 上方插删、边界行变化、刷新／下一页失败不跳位；每视图只发一个补页请求；快照刷新可独立运行，不依赖 WebSocket。
- **验证步骤：** 已加载第 51–150 条、锚在 80，补页 151+ 未返回时点击刷新，待补页收尾后重读完整扩展范围，不丢已加载页；再使第 80 条上浮，刷新仍看原第 81 条附近。连续触底只发一次补页。注入补页／刷新失败，重试不重置顶部，也不把空响应当列表全空。

- **范围确认（2026-09-09）：** 用户确认开始 PR12。Web／桌面端按每页 50 条自动补页，固定高度工具区提供刷新、查看最新及待更新提示，失败原位重试；移动端返回恢复与完整页签历史分别留 PR13／14，不新增实时订阅、置顶、搜索或业务迁移。
- **实现记录：** 新增 feature 私有的列表控制器、Query 接入 hook 与视口适配器。控制器只保存 ID 顺序、服务端排序位置、双向边界、失权 ID 和浏览状态，摘要由批量资格 Query 缓存持有；查询 key 携带企业、用户和规范化筛选。首页、双向补页、轮询、刷新与查看最新共用串行队列，重复请求合并，返回的 Promise 等待本轮队列完成；查询切换和失权使旧结果失效，不取消 Wails 业务调用。刷新等待在途补页结束后重读完整扩展区间，并按 ID 核对已加载项；顶部新增跨过一页时重读连续扩展范围，深处只更新内容和资格并暂存重排，显式刷新恢复原邻域，查看最新重建首页。完整窗口为空时调用原位置邻域查询，空窗口不解释为整个筛选为空。
- **布局与恢复：** 列表工具区固定 40px、行高固定 68px，刷新和失败保留原 ScrollArea；保存首个可见 ID、像素偏移、原后继／前驱及排序游标，React 布局提交后补偿一次。禁用该视口的原生滚动锚定，滚动、指针和菜单交互抑制自动重排；隐藏容器保留恢复意图。独立详情先确认失权时立即移除对应行并作废在途旧窗口；其余行继续读取当前缓存，新的权威批次接替后清除含失权摘要的旧批次，避免整列闪空与误触发补页。保留全局未读快照与独立详情读取。
- **验证记录（2026-09-09）：** 本轮基于 PR11 合并提交开发，随后同步 `ad5f3cb3`。已通过前端测试（审核修复后 69 项）、服务端与桌面端全量测试、前端生产构建、服务端生产构建和 `wails3 task darwin:build ARCH=arm64`。新增可控响应顺序测试覆盖补页在途时刷新完整扩展范围、重复触底、刷新中补页排队、上浮、空邻域、资格读取失败不接纳半页、独立详情先失权、旧代次响应和顶部跨页扩展；React 布局测试覆盖上方插入、锚点移走、后继消失、单次像素补偿及隐藏容器恢复。
- **界面验证：** 使用隔离的临时数据库、企业访问地址与 NATS 命名空间构造 220 条测试会话。Web 实际滚动加载到 150 条，第 85 条活动时间上浮后深处轮询保留原位，显式刷新后第 86 条仍保持原来的 52px 偏移；查看最新回顶到最新 50 条并清除提示。macOS 通过 Wails MCP 验证重复触底只发一个补页请求，补页收尾后刷新读取完整 150 条窗口；注入刷新／补页失败时，100 条内容、滚动位置与原 ScrollArea 保持，重试继续原窗口。独立详情先失权的复测记录为 100 → 99 条，无中间空列表，详情不可用且旧敏感批次已清除。审核修复后另用 120 条临时会话复测轮询失败，点击工具区“重试”后保留原 100 条窗口、4000px 滚动位置及同一 ScrollArea；后续轮询恢复由可控响应测试覆盖。未发送真实业务消息、调用渠道或业务模型；移动端交互留 PR13。
- **审核处理：** Pi CLI 已完成完整只读审核。修复轮询失败后重试被同一错误门禁吞掉的问题，主动重试改为重读原窗口，并补充恢复后继续轮询的测试；排队／合并请求统一返回队列完成 Promise 并增加等待顺序测试。简化待更新状态的三元分支，补充初次加载 INFO、带筛选上下文的失败 WARN 和局部顶边的直述注释；初始失败不展示“还没有会话”，真正空查询只在主区展示空态。其余分页、资格、缓存归属和锚点语义经审核确认符合本轮范围。
- **收尾：** 审核修复后前端测试（69 项）、服务端生产构建和 macOS arm64 原生构建再次通过；构建有宿主 macOS 链接目标版本及重复系统库警告，构建成功。两轮界面验证启动的服务端、桌面、Vite 与 MCP 均已关闭，18081、9246、9100 端口已释放，临时数据库已删除，桌面连接地址恢复为原 8081，原有 8081 服务及共享 PostgreSQL／NATS 保持运行。五个 dock/color 生成文件删除为开始任务前已有变更，不纳入本次提交。

### PR13：移动端列表分页与返回恢复

- **依赖：** PR12。
- **范围：** 移动聊天列表复用控制器，用 ID＋偏移替代仅保存 scrollTop；详情返回、旋转、字体／宽度改变后重测，缓存失效调用锚点查询。桌面窄屏 Sheet 同样保持底层列表。
- **落点：** `frontend/src/apps/mobile/mobile-inbox-page.tsx`、`mobile-inbox-navigation.tsx`，共用 PR12 控制器。
- **实施：** 离开列表时保存 ID、偏移、原邻居及尺寸信息；回到可见容器后重测，宽度变化不得照搬旧 scrollTop。触摸操作期间先记录待恢复位置，避免惯性每帧被程序重写；缓存缺页时用锚点 Query 恢复。
- **验收：** 触屏惯性、弹性回弹和滚动条拖动不与程序补偿争夺控制；原锚点移位后恢复原邻居，不跳到顶部。
- **验证步骤：** 滚至深处进详情，他端新发消息，旋转后返回列表，仍在原邻域；在惯性滚动中收到刷新，手指操作不中断，停止后无二次跳位。

### PR14：筛选、历史和交互保护

- **依赖：** PR12。
- **范围：** 按页面实例＋查询保存选择／锚点，支持工作台页签及前后退；旧响应不能覆盖新查询，失效同事筛选不扩大范围。菜单、键盘、长按与 pointerdown/up 绑定 ID；暂缓交互中的重排，失权立即清理。
- **落点：** `inbox-route.tsx`、`inbox-page.tsx`、工作台页签路由与 `resource-keys.ts`。
- **实施：** 将共享查询缓存与每页签浏览状态分开；请求发出时捕获 queryGeneration，筛选改变立即让旧行不可操作。菜单／pointer／键盘保存 conversationId，提交动作前校验它仍有资格，不能读取变动后的数组下标。
- **验收：** 相同查询两个页签共享数据但位置独立；新筛选无历史选择则未选中；点击途中行移除不误开下一行；隐藏容器不按零高度恢复；新筛选标题不展示可点击的旧筛选结果。
- **验证步骤：** 让 A 筛选慢响应晚于 B 返回，画面只能显示 B；同查询开两个页签各滚到不同位置，往返不互抢。按下某行后移除该行，再松开不能打开它的后继。

### PR15：成员发送气泡与查询结果收敛

- **依赖：** PR07、PR09。
- **范围：** 本地待发送项通过本人 clientMessageId／messageId 与查询消息对齐；成功响应、查询补拉和离开页签后的结果共用发送状态收尾。保持手动失败重试，不新增离线自动发送队列。
- **落点：** `use-outgoing-conversation-messages.ts`、`use-conversation-timeline.ts`、`frontend/src/api/inbox.ts`。
- **实施：** 本地发送项依次经历 pending、confirmed 或 failed；查询含本人关联时可确认发送，即使命令响应尚未到达。临时 ID 只用于展示，messageId 到达后统一去重；发送完成通知归属原会话，路由卸载不取消已经执行的命令。
- **验收：** 查询先到、响应先到、超时已提交、切换会话后响应迟到均只显示一个气泡；改正文／引用／提及生成新逻辑编号。
- **验证步骤：** 可控地交换发送响应与消息查询顺序，两种顺序都只剩一个气泡；服务端提交后客户端超时，再手动重试仍只出现一条，切到另一会话不得插入原会话回复。

## 二、持久同步与 HTTP 恢复

### PR16：会话变更日志和窗口水位

- **依赖：** PR05、PR06。
- **范围：** 建立 conversation_sync_events／sync_seq，消息追加同事务记录最小类型化变化；提供会话增量及带水位窗口查询。message_seq 与 sync_seq 分离，未实现的编辑／删除入口不提前创建。
- **落点：** 新增 conversation_sync_events 模型／Query，chatstate 追加路径和成员消息窗口 DTO。
- **实施：** 事件至少记录企业、会话、syncSeq、类型和目标 ID；会话锁内推进 syncSeq，并让事件与 Message 原子提交。变化 API 遵守共用分页契约；窗口返回同快照 syncSeq，资料失效／删除只携带重读需要的标识，不存旧正文副本。
- **验收：** 固定 head／nextAfter／hasMore，过滤空页仍推进扫描位置；实体当前版本与事件版本区分；权限重新校验，夹具覆盖删除引用，不暴露旧正文。
- **验证步骤：** 固定 head 后并发追加，当前轮只扫描该 head，下一轮发现新变化；夹入不可见事件使页为空，nextAfter 仍前进。窗口读取与写入交错时不能把较旧消息标成已应用较新版本。

### PR17：个人 Mailbox 和事务实时 Outbox

- **依赖：** PR16。
- **范围：** 建立 user_sync_states、压缩 user_conversation_wakeups、个人状态 version 和 realtime_outbox，受众种类显式区分用户、客服 Inbox 和访客目录；内部新消息及本人已读／提及／静音／未读标记同事务写入。受众按稳定顺序锁独立水位行，不锁接收方 users，不在网络中持事务。
- **落点：** 新增 user_sync_states、user_conversation_wakeups、realtime_outbox 模型；接入消息追加及个人设置 Action。
- **实施：** 每用户一条水位行、每用户每会话一条压缩唤醒行；唤醒保留 conversationVersion 与 personalVersion，覆盖为当前状态。Outbox 保存受众类型／ID、通知种类和目标水位，业务事务不发 NATS。个人设置事务写本人水位，内部消息写真实真人受众。
- **验收：** 用户固定 Head 分页及并发 UPSERT 不永久跳过；保存会话与个人两维版本；实际无变化不发事件，个人设置不广播给其他人。
- **验证步骤：** 分页固定 head=10 后把尚未读到的行从 8 更新到 11：本轮可以跳过，下一轮必须读到 11；同一用户并发两个会话写入不丢序号，失败事务不留下唤醒。

### PR18：群关系与资料变化同步

- **依赖：** PR17。
- **范围：** 群创建、资料、增员、移除、退出、转让与解散接入变化清单；变更前后真人受众写 Wakeup，失权者只收会话 ID 和 removed；Agent 不消费用户 Mailbox。
- **落点：** `conversation/group_management.go`、`group_conversation.go`，PR17 的受众写入能力。
- **实施：** 事务锁后分别计算变更前后成员集合；仍有权者获得新版本，失权者获得仅含 ID 的 removed。重入是新的有效关系，不能被旧 removed 覆盖；解散保留只读历史，和移除成员使用不同资格结果。
- **验收：** 移除后重入的压缩行恢复当前关系；新成员阅读基线正确；解散只读与失权区分；事务失败无半条系统消息或孤立唤醒。
- **验证步骤：** A 被移除后立刻重入，延迟消费 Mailbox 仍恢复最新成员关系；A 仅被移除时批量摘要不可读，通知中无群名／正文；事务回滚前后受众都不变化。

### PR19：客服共享收件箱同步

- **依赖：** PR17。
- **范围：** 建立企业客服 Inbox 水位与压缩会话索引；客户入站、回复（含 AI 最终消息）、隐式领取、转交、关闭、显式重开及新周期接入。共享受众不逐客服扇出；本人客服已读只更新本人 Mailbox。
- **落点：** `inbox/load_inbox.go`、客服收发／服务周期 Action；新增企业客服同步水位及压缩索引。
- **实施：** 所有有权客服共享一条企业 Inbox 流，变化只表示该客户会话需要重读；具体公共／本人／同事／关闭资格由现有 Query 判断。负责人变化不为所有客服创建 Participant，个人已读继续走本人流。
- **验收：** 公共／本人／同事／关闭视图同时收敛；筛选迁移不等于授权移除；客服参与者不被误当全部有权阅读者。
- **验证步骤：** A 领取公共会话后，B 的公共列表移除、A 的本人列表出现，双方有权详情仍可读；关闭／重开改变队列但不改活动时间；AI 最终回复也能推进共享 head。

### PR20：访客授权和线程目录同步

- **依赖：** PR19。
- **范围：** 提取网站 HTTP 共同授权，建立渠道身份级目录水位、压缩 Wakeup、目录／会话增量与快照；只返回访客公开投影，补齐未知新线程发现。
- **落点：** `website_visitor_direct_backend.go`、`api/website_visitor.go`、`conversation/list_website_conversations.go`、`list_website_messages.go`。
- **实施：** 把 Cookie／Header 恢复出的渠道身份作为唯一受众，目录按该身份列出线程；成员 Query 的内部字段不复用到访客 DTO。持久变化提供目录索引、head 与窗口快照，首次消息以外的读操作不建立联系人。
- **验收：** 同一访客两标签页新建不同线程可互相发现；跨渠道／身份请求被拒绝；初始化／草稿不创建联系人，客服内部资料不外泄。
- **验证步骤：** 同渠道同访客在两个标签页分别首发新线程，互相增量后能发现；换另一个渠道或伪造线程 ID 不能读到消息；空白页反复初始化数据库业务记录数不变。

### PR21：本人工作状态与通知偏好同步

- **依赖：** PR17。
- **范围：** 当前身份资料版本随用户水位同事务推进，客户端可据版本重新读身份；不制造虚拟会话、不逐会话写偏好变化。
- **落点：** 当前身份 Query、成员工作状态／通知偏好 Action、PR17 用户水位。
- **实施：** 用户流增加身份资料版本，变更后重读已有身份契约；账户级开关进入服务端版本，设备通知许可仍留本地。合并同步时独立比较身份资料、会话个人状态和会话内容版本。
- **验收：** 空会话列表也能同步开关与工作状态；偏好变化和消息同时发生不互相覆盖，静音、已读、设备开关仍独立。
- **验证步骤：** 用户没有任何会话，在 A 端改工作状态，B 端同步后身份更新；同时发消息并切通知开关，最终两项均保留，设备权限不会被另一端覆盖。

### PR22：名称、头像和渠道资料失效

- **依赖：** PR18、PR20。
- **范围：** 接通成员／Agent、联系人／渠道身份及渠道资料写入口；按真实 JOIN 依赖标记受影响会话投影，按固定锁序批量写对应受众变化；头像异步导入成功也覆盖。
- **落点：** `internal/actions/identity`、`agent`、`channel` 及联系人资料入口；inbox／消息查询的资料 JOIN。
- **实施：** 先以实际 JOIN 列出资料字段影响清单，再在各真实写事务标记对应会话；同步只要求重读当前资料，不复制姓名头像到每条历史消息。异步头像完成属于独立写入口，也要发布；批量会话锁按稳定 ID 排序。
- **验收：** 无新消息时列表／发送者／引用资料可更新；访客仅刷新公开字段；失败和相同值不产生伪变化，不建立通用实体事件平台。
- **验证步骤：** 会话两天没有新消息时修改对方姓名和头像，列表、消息署名与引用展示均更新；异步头像落库后再验一次，活动序／未读／访客内部字段不变。

### PR23：AI 持久状态变化同步

- **依赖：** PR18、PR20。
- **范围：** queued、running、成功、失败、取消及人工接管接入会话变化；成功消息与终态同事务提交，客服运行状态同时推进共享 Inbox 水位。成员与访客运行状态按不同投影读取，不接 token 流。
- **落点：** `internal/actions/agentrun/schedule.go`、`execute.go`、`customer_schedule.go`、`customer_execute.go`、`cancellation.go`。
- **实施：** 对每次持久状态转换写 syncSeq，成功与最终消息共用事务；仅内存进度不得推进持久水位。客户 Run 同时更新客服共享投影，访客只暴露公开状态；重复回调以当前 attempt／终态判定无变化。
- **验收：** 失败结果消息与 Run 终态、无结果消息的取消都能通过 HTTP 恢复；重复终态回调不重复发布；租约和当前负责人继续约束迟到结果。
- **验证步骤：** 让模型失败并生成 agent_error 结果消息，另构造不生成消息的取消，断开 Socket 后仅靠 HTTP 能恢复两种终态；旧租约成功回调晚于取消到达时不得复活 Run，也不追加最终回复。

### PR24：完整同步索引与重建基线

- **依赖：** PR20、PR21、PR22、PR23。
- **范围：** 为各受众提供完整可见会话索引和 Heads，不受展示列表限制；重建从 H0 开始按不可变 ID 扫描，再追赶 H0 后变化。单响应投影与水位同快照，跨 HTTP 不持长事务或伪装历史快照。
- **落点：** 新增各受众完整可见索引／Heads Query，复用 PR17–23 的受众与授权。
- **实施：** 重建先记录 H0，再按不可变 conversationId 扫描全量可见索引，最后增量追赶 H0 后变化；扫描页返回当前版本，不声称跨页历史快照。客户共享 head 与用户个人 head 分别保存，不拼成一个可比较数字。
- **验收：** 分页重建期间新建／移除／资料修改最终收敛；未缓存会话只记录目标版本，打开再取快照；读取失败不确认未应用水位。
- **验证步骤：** 扫描中新增一个 ID 小于已扫描边界的会话，必须由 H0 后增量补入；同期间移除的会话最终消失。模拟中途查询失败，不能记录“基线完成”。

### PR25：同步保留窗口和恢复 epoch

- **依赖：** PR24。
- **范围：** 清理事件／tombstone 与推进最老保留边界原子执行；过旧、超前游标和 epoch 不符返回明确恢复结果；数据库重建／恢复备份更换同步 epoch 并写入运维流程。
- **落点：** 新增同步清理任务与 epoch 存储，增量 Query 校验入口、部署恢复说明。
- **实施：** 每条流记录最老可恢复边界；清除事件与推进边界同事务，分页同时检查 epoch／保留边界。响应区分 need_snapshot 与认证失败，客户端只重建受影响流。epoch 更换必须是数据库恢复操作的一步，不能依赖普通进程重启碰巧换值。
- **验收：** 清理与分页竞争不会误称追平；恢复旧备份后即使序号数值重合也重建；保留有效关系的压缩 Wakeup。
- **验证步骤：** 客户端持旧 after，清理后请求明确要求重建；恢复旧备份并更换 epoch，即使 after 数值合法仍重建。单纯重启服务端不造成全量重建风暴。

### PR26：实时 Outbox 发布器

- **依赖：** PR17。
- **范围：** 独立发布器短租约认领，在事务外发布 Core NATS 并 Flush，再凭同 leaseToken 删除；按 namespace／企业／行中受众种类定向发布，不限定用户受众。失败释放并退避，可整条重发；不改 task_outbox 或建立每用户 JetStream Consumer。
- **落点：** 新增实时 Outbox 发布模块，参考 `internal/task/server/runtime.go` 的启动／关闭组织，但不复用可靠任务投递语义。
- **实施：** 短事务认领一批待发记录并写 leaseToken／到期时间，释放事务后逐受众发布和 Flush；完成时按记录 ID＋leaseToken 条件删除。重复发布水位可合并，多个节点不能凭旧租约确认新认领任务。
- **验收：** 提交后崩溃、发布后崩溃、部分受众失败及过期租约均恢复；旧发布者不能删除新租约记录；离线恢复不依赖 NATS 留存。
- **验证步骤：** 在发布前和 Flush 后分别杀发布进程，重启后目标水位最终可见；故意延迟旧发布者到新租约生效之后，旧确认影响行数为零。

## 三、连接与各端实时闭环

### PR27：Protobuf 工具链与平台探针

- **依赖：** PR01。
- **范围：** 锁定 Edition 2024、Buf、Go／TS 生成器与运行时；仅定义认证、Hello、心跳、水位、撤销、错误和下线帧，补齐访客目录／会话帧。增加 Task／CI 生成、lint、breaking 检查；探测 Wails 各端 Origin、Upgrade、TLS 与子路径。
- **落点：** 新增协议目录和生成 Task；`go.mod`、`frontend/package.json`、`build`、CI。
- **实施：** 先以双向编解码夹具锁定 Envelope、版本、oneof 与 uint64 字符串边界；认证帧承载一次性票据，Hello 返回连接信息与受众 heads。水位帧只描述哪条流已变化。平台探针单独记录实际 Origin、服务端地址拼接和 TLS 结果，再据此确定允许来源配置。
- **验收：** 两端编码互通，oneof、presence、未知帧与 64 位整数正确；记录真实平台结果，不把 Web 通过视为原生通过；探针不成为另一套产品协议。
- **验证步骤：** 同一份帧分别由 Go／TS 编码并对端解码；超过安全整数、未知可忽略帧、协议主版本不支持分别有明确结果。Web、桌面、iOS、Android 分别记录通过或未验证，不能以模拟结果替代真平台。

### PR28：成员一次性连接票据

- **依赖：** PR27。
- **范围：** appservice 换票复用 Bearer，绑定已有 tokens.id、企业／用户、设备、客户端及允许 Origin；PostgreSQL 原子消费摘要并清理过期票据，签发和消费都复核登录会话。原生凭据仍由 Go Proxy 管理，设备标识不充当认证凭据。
- **落点：** `appservice/backend.go`、`direct_backend_auth.go`、`models/token.go`、`internal/clientsession/session.go`；新增连接票据模型。
- **实施：** 换票方法返回短期随机票据及有效期，数据库只保存摘要与原登录 token ID；消费用原子条件更新／删除并复核 token、企业、用户、Origin。Web 沿用现有 Bearer 存储；原生端由 Go Proxy 换票后只把短票据交给 TS，不额外暴露其长期凭据。
- **验收：** 并发消费只有一次成功，过期／撤销／跨企业票据拒绝；原生长期 Token 不因实时接入进入 TS，Web 沿用既有认证边界，票据不进入 URL 或日志。
- **验证步骤：** 同票据并发两次消费仅一次成功；签发后登出再消费失败；原生绑定返回体及日志不含长期 Token，票据不出现在 WebSocket URL。

### PR29：成员 Gateway 与有界发送队列

- **依赖：** PR19、PR26、PR27、PR28。
- **范围：** Server 内嵌 Gateway，首帧限时 Authenticate；按本节点在线受众订阅，禁止 Queue Group／企业通配订阅。单写协程、有界队列及帧限制、控制优先、水位取最大；到期不晚于登录会话，低频授权复核随首版提供。
- **落点：** 新增 `internal/realtime` Gateway；服务端 HTTP 路由与 PR26 NATS 发布协议。
- **实施：** 连接依次处于 awaiting_auth、authenticated、closing；认证前只允许 Authenticate，超时关闭。订阅安装完成并 Flush 后读取 Hello heads，堵住“先读 head 再订阅”的丢通知窗口。每连接单写协程，控制帧优先，水位同流取最大；身份流与企业客服流按资格订阅。
- **验收：** 一用户多设备／多节点均收到；先注册订阅再给 Head；慢消费者断开可补拉；NATS 恢复但 Socket 未断时主动要求校验 Head。
- **验证步骤：** 在安装订阅与读取 head 之间提交消息，客户端通过 Hello 或后续水位至少发现一次；同账号连两个节点两端均收到。堵塞一端读流导致有界关闭，另一端继续正常工作。

### PR30：登录与权限撤销控制

- **依赖：** PR18、PR29。
- **范围：** 登出按 token session、停用按用户发送事务控制；群失权清理焦点及发送队列。控制丢失由服务器复核修复，不能只依赖客户端自觉；明确授权检查与在途字节的边界。
- **落点：** `actions/auth/logout.go`、用户停用入口、群关系 Action 与 Gateway 连接注册表。
- **实施：** 连接登记 tokenSessionId、userId、企业和已授权焦点；登出／停用与控制 Outbox 同事务提交。收到控制后撤销订阅、清理未发送受限帧并关闭或缩减连接权限；定期复核是丢控制帧后的修复路径，记录复核间隔和最坏生效窗口。
- **验收：** 已连接后登出／停用／移出群不继续得到受限内容；撤销事务回滚无错误断连；旧票据不能重新建立有效连接。
- **验证步骤：** 连接后登出本次 token，只关闭对应登录会话；用户停用关闭其全部会话。丢弃一次 NATS 撤销帧，服务器复核仍生效；已写入网络的字节不承诺可撤回。

### PR31：网站票据与 Gateway 受众

- **依赖：** PR20、PR30。
- **范围：** Cookie／Header 换票绑定真实渠道身份，独立访客 Subject 与公开帧白名单；频道停用撤销连接。Origin 按发起 Socket 的 iframe 来源校验，宿主页白名单沿用嵌入规则。
- **落点：** `api/website_visitor.go`、`website_visitor_direct_backend.go`、渠道停用入口与 Gateway 访客认证 adapter。
- **实施：** 票据受众使用数据库身份 ID，不使用 Cookie／原始 visitor token 或可逆外部标识构造 NATS Subject。先验证渠道与访客归属，再允许其目录与会话公开投影；iframe 自身来源用于 Socket Origin，嵌入宿主来源沿用网站渠道校验。
- **验收：** 随意传 conversationId 不能订阅；独立链接、嵌入、Header 恢复与预览隔离；尚无业务身份时不为换票创建联系人。
- **验证步骤：** 抓取访客连接帧和 Subject，不能出现原始 token；把成员票据用于访客连接、跨身份指定线程均失败；停用渠道后已有连接和再次换票都失效。

### PR32：共享客户端连接与会话代次

- **依赖：** PR29、PR30。
- **范围：** `src/api/realtime` 实现无 feature 依赖的 TS 传输内核；一个应用实例一连接、业务页签共用，浏览器各标签页可独立连接。抖动退避、重新换票，网络／认证／版本错误分流；前端发送／接收队列有界。
- **落点：** 新增 `frontend/src/api/realtime`；`frontend/src/api` 的认证边界与各端登录外壳。
- **实施：** 传输内核只接收 getTicket、URL／Origin 适配、frame handler 和生命周期输入，不导入 inbox／workspace。维护 disconnected、connecting、authenticating、ready、backoff、stopped；每次重连用新票据，退出登录或切企业先使 generation 失效再释放旧连接。
- **验收：** 企业／账号切换先提升 generation 再清缓存；旧 Promise／Socket／通知无效，同源凭据改变可恢复。验证 WebView 地址与 bufferedAmount；临时断网保留草稿。
- **验证步骤：** A 企业换票慢响应在切到 B 后返回，不得创建 A 的 Socket；重连和旧 onclose 交错时只保留一条当前连接；服务端版本不支持不能无限重试刷日志。

### PR33：登录会话级同步协调器

- **依赖：** PR24、PR25、PR32。
- **范围：** 登录后启动，每条同步流串行追赶并合并最高目标；区分目标、已应用和阅读水位，集中失效 resourceKeys。首版内存投影断线追赶，重载重建必要基线；前台恢复及低频 Head 校验保留。
- **落点：** 新增登录会话级同步模块，`frontend/src/hooks/resource-keys.ts`、`frontend/src/lib/resource-client.ts`。
- **实施：** 每流一个追赶任务：合并 target → 拉增量 → 登记目标实体版本／资格移除并失效 Query → 推进已处理索引位置 → 检查更高目标。索引 applied 和实体已读到的版本分开；未加载实体可以只留待读目标，已加载窗口重读失败须保留 dirty，不能因索引追平而清除。
- **验收：** 离开消息页仍同步；空页、持续热点、读取失败和更高目标正确收敛；不单独持久化游标冒充离线库，不逐条通知手工修补 Query DTO。
- **验证步骤：** 连续送来 head 10、8、12，仅串行追到最高目标；其中窗口 Query 失败后提示仍待同步，重试可恢复。离开消息页仍处理身份／未读变化，账号切换清空原用户全部同步状态。

### PR34：消息窗口接入实时同步

- **依赖：** PR15、PR16、PR33。
- **范围：** 用版本驱动权威窗口重读，消除初始 Query 更新而 page 副本不变的问题；最新窗口补连续页，锚点窗口不拼远端尾部但处理资料／删除／失权。收到推送不自动已读；移除对应成员消息和提及高频轮询。
- **落点：** `use-conversation-timeline.ts`、`conversation-window.ts`、`use-conversation-viewport.ts`、提及／阅读 hooks。
- **实施：** 以窗口模式 latest／anchor 和 queryGeneration 组织读取，权威 Query 更新必须能替换对应窗口内容；合并消息页不等于保留已删除实体。最新模式连续补到目标，锚点模式只重读当前窗口与引用依赖，远端尾部更新留提示。阅读仍由可见消息判定。
- **验收：** 上翻、引用跳转、窗口外原文删除、并发发送、过期页响应和连续提及查看稳定；Wails 调用仅忽略过期结果，不取消业务执行。
- **验证步骤：** 停在引用跳转窗口，同时发生新消息和原文删除：不跳尾部、引用失效、最新提示出现；连续提及导航后返回最新不丢消息；旧窗口响应晚到不能覆盖新会话。

### PR35：列表实时更新与可变排序补全

- **依赖：** PR12、PR14、PR33。
- **范围：** 顶部无交互时跟随最新，深处内容及时更新但暂存新项／重排；“刷新”保锚点，“查看最新”回顶，固定位置提示不同会话数。keyset 继续原边界，同步发现未加载却已上浮的会话，按 ID／版本去重；移除列表／群资料高频轮询。
- **落点：** PR12 列表控制器、PR33 同步协调器、PR09 按 ID Query；替换 inbox 页面轮询消费者。
- **实施：** 同步索引提供“哪些会话变化”，按 ID 取得当前资格和排序键，合并进 pendingChanges；普通 keyset 仍按原边界继续补历史，不能改成最新边界。提示数按去重会话 ID 计算并固定占位，显式刷新应用当前窗口，查看最新才整体回顶。
- **验收：** C90 在读取旧页期间上浮仍可发现；资格移除立即修复、真实失权立即清除；旧 generation 页面不接新窗口；hasMore=false 后仍可发现新项，刷新失败不换整棵 DOM。
- **验证步骤：** 初始显示 C1–C50，未加载 C90 收到消息并排到首位；读取 C51 后的页不会再返回 C90，但同步索引必须发现它。深处先显示更新提示、点击查看最新后 C90 出现一次；hasMore=false 后新增会话也可发现。

### PR36：移动端实时接入

- **依赖：** PR13、PR34、PR35。
- **范围：** 移动登录外壳接同一连接／同步内核，列表、单聊及已有群聊使用统一实时输入；恢复前台先校验 Head，再按独立阅读规则工作。保留当前移动功能边界，不顺带开启建群或已读交互。
- **落点：** `frontend/src/apps/mobile` 登录外壳、mobile-inbox-page、mobile-individual-conversation-page、mobile-group-conversation-page。
- **实施：** 应用前台事件只触发同一协调器校验 heads，不由每个页面各建 Socket；系统挂起后旧连接一律按可能失活处理。移动页面订阅共用版本输入，保持自身详情导航、阅读能力和草稿行为。
- **验收：** iOS／Android 前后台、旋转、详情返回、连接重建与旧响应隔离；完成对应回归后移除移动三秒轮询。
- **验证步骤：** 系统挂起期间另一端发送／已读／移除群成员，恢复后列表与详情收敛；同时快速返回再进另一会话，无重复连接、旧内容闪入或错误已读。

### PR37：拆分网站挂件网络与窗口状态

- **依赖：** PR07。
- **范围：** 通过现有 messenger 构建入口，将请求、身份、连续窗口与发送状态迁入 `frontend/src/publicweb` 可测试模块；保留 Go 模板／主题及现有 DOM 外观，访客 DTO 与成员隔离。
- **落点：** `internal/publicweb/chat.js`、`page.html`、`frontend/src/publicweb` 与现有 messenger 构建入口。
- **实施：** 按网络身份、会话目录、消息窗口、待发送状态拆出可测试模块，DOM 事件转成显式操作；不要把整段脚本包进一个 TS 类。先保留三秒轮询验证等价，再删除迁出的原函数与状态变量；构建产物继续由现有 Go 嵌入链路加载。
- **验收：** 原轮询下文本、引用定位、分线程草稿和幂等重试不变；删除替代的旧状态逻辑，不打包内部身份／工具详情。
- **验证步骤：** 在真实挂件构建产物上发送、重试、切线程、引用跳转和恢复 Cookie；切回原线程草稿仍在。检查 bundle 依赖没有成员登录模块或内部 Agent 详情。

### PR38：网站挂件实时与多线程恢复

- **依赖：** PR25、PR31、PR32、PR37。
- **范围：** 共用传输内核，以访客 API adapter 同步目录和窗口；已有身份初始化时先换票再连接；无身份不换票、不连接，首条有效消息建立身份后再执行。空白页以前台恢复及低频无副作用检查发现另一页建立的身份；替换业务消息轮询。
- **落点：** PR37 访客状态模块、PR32 传输内核、网站目录／窗口 HTTP adapter。
- **实施：** 状态分为无身份、可换票、已连接、恢复中；无身份页面只在前台恢复和低频身份检查时发现其他页建立的 Cookie，发现后先取目录基线再正常追赶。访客同步拥有独立 keys／内存，不挂到成员登录协调器。
- **验收：** 新线程发现、离线补拉、发送响应乱序、引用窗口和渠道停用收敛；初始化不创建业务记录，预览不连真实服务。
- **验证步骤：** A、B 两个空白挂件，A 首发建立身份，B 不发消息也能发现身份和线程；断线期间 A 新建第二线程，B 恢复后列出两条且不制造新联系人。

### PR39：实时未读与本地通知闭环

- **依赖：** PR21、PR34、PR35。
- **范围：** 在线同步确认的新 Message 接现有通知策略；静音、@、工作状态、全局／设备开关及可见上下文独立判断。通知去重和跨标签页前台协调；总数绑定权威版本，客户端不做 +1/-1 推算。
- **落点：** `features/notifications/new-message-notifications.ts`、`platform/notifications.ts`、当前通知偏好与 PR33 同步入口。
- **实施：** 以 messageId 去重，并区分 live 与 catchup／bootstrap 来源；一个压缩唤醒覆盖多消息时读取实际新增范围后逐条执行策略，不用最后预览代表全部。跨浏览器标签协调前台处理者；通知内容来自已授权消息 Query，旧账号 generation 立即失效。
- **验收：** 压缩唤醒合并多条消息不只看最后预览；冷启动／重连历史不逐条补弹，他端已读只更新状态；旧总数不覆盖新版本，客户未读不进入桌面总提醒。不承诺跨设备恰好一次或退出后推送。
- **验证步骤：** 一次唤醒包含普通消息和 @ 消息，静音场景仍按既有 @ 策略判断；重连追赶十条历史只更新未读，不连弹十次；两标签前台切换时同一设备不重复通知。

### PR40：Gateway 生命周期与部署

- **依赖：** PR31、PR38。
- **范围：** 配置 Upgrade、TLS、子路径、代理空闲超时、默认关闭压缩及帧大小；启动就绪顺序、停止接收、随机重连下线、发送／订阅 Drain 和停止认领。记录认证、慢连接、Outbox 和同步落后指标，不记凭据或正文。
- **落点：** 服务端应用装配、`internal/config/server`、`internal/ingress`、新增 Gateway／发布器，部署说明。
- **实施：** 启动顺序明确为依赖就绪 → 路由／订阅可服务 → 对外 ready；退出先停止接入和认领，向连接发下线提示，再有界 Drain 并释放资源。将路径前缀、代理空闲时间和心跳配置一起记录，认证／关闭用结构化原因码。
- **验收：** 原生 Wails HTTP、反向代理／Cloudflare 路径可连；重启后未发布 Outbox 可继续；只释放本实例连接和资源。
- **验证步骤：** 通过带子路径的反向代理连 Web 与原生端，空闲超过代理默认时间仍正常；SIGTERM 后连接提示重连，未确认 Outbox 留给下一实例，退出不关闭共享 NATS。

### PR41：实时链路容量与故障验证

- **依赖：** PR22、PR36、PR38、PR39、PR40。
- **范围：** 增加可重复 Task 场景：同用户多会话、小群扇出、共享客服水位、资料批量失效、突发重连和持续热点；分别测 users 守卫、Mailbox 争用、内存、队列、积压与 HTTP 放大，报告真实规模和结果。
- **落点：** 现有 Task／服务端集成测试；新增只用于测试的连接、发布和 Query 故障注入点。
- **实施：** 每个场景记录数据量、在线连接数、受众大小、持续时间和机器配置，再测端到端收敛时间、HTTP 次数及队列峰值；用结果定位热点，不把合成测试数字写成产品承诺。注入点不得成为生产接口。
- **验收：** 杀进程、NATS 短断、慢客户端及日志过期均恢复。先保留当前 LockActiveUser；锁优化、普通性能索引、Shared Fanout 只按证据另开 PR，不加任意配额或把规模承诺写成事实。
- **验证步骤：** 消息已提交后杀进程、NATS 单独断连而 Socket 存活、慢客户端、日志过期分别重跑；最终数据库版本与客户端投影一致，并证明队列／协程不会随重复重连持续增长。

## 四、AI 实时过程

### PR42：拆分运行摘要与过程详情

- **依赖：** PR23、PR34。
- **范围：** Run 摘要按 runId／agentIdentityId 返回集合，当前 UI 可选最相关项；消息只留过程引用，展开时取详情，移除每次消息查询携带完整 blocks 的实现。
- **落点：** `direct_backend_agent_process.go`、`types_agent_process.go`、`conversation/agent_process.go`、`features/inbox/agent-process.tsx`。
- **实施：** 列表／消息窗口仅带轻量 Run 摘要和过程引用；独立过程 Query 按 runId 获取持久 blocks、工具参数结果和用量。当前 Run 集合与历史消息关联分开，失败通过结果消息定位，取消无消息也可选中查看；展开状态 key 用 runId。
- **验收：** 多 Run、无消息失败和历史过程均可读取；窗口更新不丢展开状态；运行详情按会话授权，保留工具完整参数／结果及用量。
- **验证步骤：** 同会话两个 Agent 各有 Run，刷新不会只剩“最近一个”；历史过程首次展开才请求完整 blocks，关闭再展开可用缓存；移出群后该 Query 不再返回详情。

### PR43：模型增量消费与流基线

- **依赖：** PR42。
- **范围：** Runtime 真正消费 token／工具进度，使用 runId、attempt、streamId、sequence 与稳定块编号，短周期合并；暴露当前内存快照和临时发布接口，不直接依赖 Socket／Subject。
- **落点：** `internal/integration/agentruntime/eino.go`、`openai.go`、`progress.go`、`types.go`。
- **实施：** 将整段输出收集点替换为运行时增量消费；每个 attempt 创建独立 streamId，块有稳定 blockId，同流 sequence 单调。内存快照与最高 sequence 同步捕获，发布端按短周期合并可重建增量，模型执行不等待浏览器消费。
- **验收：** 同流重复／乱序、gap、尝试切换、工具并行与输入抢占正确；token 不写 Message／Changelog／Outbox；限速发送不阻塞模型执行。
- **验证步骤：** 分片产生文本、工具参数、工具结果并模拟并行工具，块顺序稳定；故意漏一片后快照可恢复全文；重试产生新 streamId，旧尝试尾部不能拼入新正文。

### PR44：跨执行节点的运行快照

- **依赖：** PR26、PR43。
- **范围：** 当前尝试记录执行节点及 streamId；appservice 快照先授权，再通过内部适配器定位 Worker，允许 Core NATS request/reply。节点缺失返回 snapshot_unavailable 和持久状态。
- **落点：** PR43 流快照存储、Agent 当前尝试元数据、appservice 过程快照入口和内部节点通信适配器。
- **实施：** 快照请求含 runId／attempt／streamId，先按会话授权再定位执行节点；Worker 返回同一尝试的 blocks＋sequence。读到节点后发生换代时返回当前尝试或 unavailable，由调用方重读持久 Run，不能返回旧快照冒充当前。
- **验收：** Gateway／Worker 分离、节点退出和重试换节点不读旧快照；不轮询 token、不要求重放全部历史流，失权请求不转发内容。
- **验证步骤：** Gateway 在 A、执行在 B 时展开流能读取 B 快照；B 退出或任务转至 C 后，延迟的 B 响应不能覆盖 C；不可见用户的请求不进入 Worker 内容读取。

### PR45：成员 AI 流展示与终态收敛

- **依赖：** PR27、PR33、PR42、PR44。
- **范围：** 扩展同一 Protobuf 的 AI 帧和授权焦点控制；按尝试替换过程，gap 经 HTTP 快照修复。仅数据库提交后发 completed，持久终态覆盖临时候选；控制／水位不能被 AI 帧饿死。
- **落点：** 协议 AI 扩展、Gateway 焦点注册、成员 `agent-process.tsx`／运行展示控制器。
- **实施：** 焦点输入是当前可见会话／Run，服务端按受众授权后订阅临时流；客户端按 attempt、streamId、sequence 去重，gap 时取 PR44 快照并只接快照之后的增量。终态由持久 Query 确认，临时 completed 只是加速提示。
- **验收：** 长输出期间仍及时收到消息；断线、快照竞态、失败、取消和人工接管不复活旧候选；completed 丢失可由水位恢复。
- **验证步骤：** 快照请求途中继续输出，快照返回后不重复拼接已有 token；丢 completed 后仅靠会话水位仍显示最终消息；取消后晚到 delta 不能重新打开运行中状态。

### PR46：网站公开 AI 流

- **依赖：** PR38、PR45。
- **范围：** 访客只接公开候选正文、基础状态和最终回复；内部思考／工具原文不广播。客户负责人变化停止旧流，跨节点取消只是加速，最终写回仍受数据库与租约门禁约束。
- **落点：** PR38 访客展示、Gateway 访客 AI adapter、客服 Agent 输出映射。
- **实施：** 在服务器构造访客专属公开帧，不能先发全量工具／思考再靠前端隐藏；流焦点始终绑定渠道身份可读线程。人工接管／转交／关闭取消旧公开候选资格，最终回复仍由原有事务门禁决定是否保存。
- **验收：** 客户接管、转交、关闭、重连换流及渠道失效正确；抓取访客帧和 HTTP 响应均无内部过程数据。
- **验证步骤：** 模型同时输出思考、工具结果和正文，抓包只含公开正文和基础状态；客服接管后迟到旧 delta 不再展示，重连读取的最终消息也不包含被取消候选。

## 五、个人会话置顶

### PR47：个人置顶事实、顺序与分页

- **依赖：** PR11、PR18。
- **范围：** 个人状态增加 pinRank，user_sync_states 增加 pinOrderVersion；置顶／取消／按邻居移动同事务发布本人水位。提供显式分区查询，未启用置顶的调用方继续完整活动序查询；扩展锚点上下文的分区、顺序版本及跨区邻居。新增默认追加末尾、取消回普通活动顺序；过滤只是全局个人顺序的投影，置顶不越权或越过筛选。
- **落点：** `models/conversation_user_state.go`；扩展本人用户水位行、新增置顶写 Action；PR10 分页和 PR11 锚点 Query。
- **实施：** 命令使用共用置顶契约的 position／neighborId 和 expectedPinOrderVersion，不提交全列表覆盖隐藏项。锁定所涉会话并校验当前资格后，再锁本人顺序；冲突返回当前版本且不部分写入。按 rank 留间隔，耗尽时同事务无唯一冲突地重编号；取消清 rank、再次置顶追加末尾。
- **验收：** expectedPinOrderVersion 防两端覆盖；隐藏项相对顺序保留，重编号无唯一冲突。置顶页游标绑定版本、与普通页分开，活动序调用方不漏项；跨区移动使旧分区资格失效；失权清除置顶，重入不自动恢复，解散可读则保留。
- **验证步骤：** 全局 A,X,B、当前隐藏 X，将 B 移到 A 前，结果为 B,A,X；两端持同版本同时排序只允许一个成功。触发 rank 重编号后，两端整区重读所得顺序必须与服务端一致。失权清理也推进顺序版本，旧分区游标应要求重读而非静默漏项。

### PR48：Web／桌面置顶交互

- **依赖：** PR14、PR35、PR47。
- **范围：** 客户端切换分区查询，单滚动容器先置顶再普通，同会话只显示一次；支持菜单置顶、拖动及等效键盘排序。新消息不动置顶顺序；本人移动保留操作锚点，远端重排按浏览意图暂存，跨分区原子更新并补偿位置。
- **落点：** Web／桌面列表控制器、行菜单及新增排序交互，PR47 分区 Query。
- **实施：** 控制器维护 pinned／regular 各自游标、内容水位及置顶顺序版本；顺序版本变化整区重读，渲染仍是一条纵向列表；置顶区未加载完时继续取置顶，不能把尚未到达的置顶错误接到普通区后。操作期间可显示临时拖动外观，成功后权威重读，冲突保留页面上下文并提示重试，不手写新业务 DTO。
- **验收：** 两区分别分页、只有一个分区、置顶较多、取消后重新置顶、锚点跨区和拖动期间版本冲突；不自动跳顶、不清草稿／静音／阅读水位、不增加嵌套滚动或任意数量上限。
- **验证步骤：** 从普通区置顶一条当前可见会话，确认两区没有重复，详情／草稿仍在；他端取消当前锚点置顶时保持原邻域。拖动尚未完成收到远端排序，提交冲突后恢复权威顺序且不跳顶。

### PR49：移动端置顶与跨端顺序

- **依赖：** PR36、PR48。
- **范围：** 移动端增加触屏置顶／排序与可访问的替代操作；复用个人顺序、版本冲突及分区分页，不同步各设备滚动位置。
- **落点：** `apps/mobile/mobile-inbox-page.tsx`、移动列表导航，复用置顶命令与分区状态。
- **实施：** 长按菜单提供置顶／取消及明确移动操作，触屏拖动有独立手势启动条件；无拖动能力时仍可通过菜单完成同样排序。跨端仅接收顺序版本，各端选择、焦点、展开和滚动位置不上传。
- **验收：** 长按与惯性滚动互不误触；其他端置顶／取消／重排后，详情返回保持原邻近锚点，新消息只更新置顶内容。
- **验证步骤：** A 桌面调整置顶后 B 手机在详情内收到变化，B 返回仍在原列表邻域；B 长按后转为滚动不得误发排序。新消息连续到达，置顶相对顺序始终不变。

## 六、按实际需要跟进

### PR50：会话名称搜索接口

- **依赖：** PR10、PR11、PR47。
- **范围：** 搜索当前有权会话名称／标题，完整查询身份包含规范化搜索词；分页、上下文、变化资格判断与置顶分区共用筛选。不是只搜索已加载页，不加入正文全文搜索。
- **落点：** inbox 候选 Query、PR11 锚点上下文、PR47 分区请求和 `types_inbox.go`。
- **实施：** 服务端统一搜索词空白规范化与匹配规则，复用当前会话标题／对象名投影；先在完整有权候选集合筛选，再分页。查询身份和游标均包含规范化词，变化重读时也要重新计算匹配资格。
- **验收：** 首页之外可命中；改名导致匹配进入／退出、隐藏置顶、失效同事和跨企业隔离正确。
- **验证步骤：** 第 100 条标题匹配时首页可找到；将当前结果改名为不匹配则移除，另一未加载会话改名为匹配则可发现；同词跨企业请求不能扩大范围。

### PR51：会话搜索输入与浏览恢复

- **依赖：** PR14、PR49、PR50。
- **范围：** 启用各端搜索入口，组合输入结束后查询，旧词响应受 queryGeneration 隔离；清空搜索恢复原筛选锚点，未展示查询仅标旧，结果详情仍独立。
- **落点：** Web／桌面与移动列表搜索入口、列表控制器、`resource-keys.ts`、双语 inbox 文案。
- **实施：** 输入框只维护本地编辑词，compositionend 后形成规范化查询；请求状态与结果查询绑定，防止新词标题下出现旧词可点击行。保存搜索前筛选与锚点，清空只恢复浏览上下文，不清个人置顶或会话详情。
- **验收：** 中文输入、快速改词、重连中搜索和搜索期间新消息不串结果；真实无匹配才显示空态，搜索不重置个人置顶。
- **验证步骤：** 中文输入法尚在组词时不逐键发最终查询；A 词慢响应晚于 B 词返回不覆盖。搜索中断网再清空，恢复原列表后迟到搜索结果仍被忽略。

### PR52：列表虚拟化与窗口裁剪（测量后决定）

- **依赖：** PR41、PR49。
- **范围：** 仅当真实列表规模出现渲染问题时引入虚拟化，key 固定为会话 ID，裁剪保留前后边界与语义锚点。固定头像／摘要行布局，缩放、语言、未读位数与时间更新不制造跳动；选择单一滚动补偿所有者。
- **落点：** PR41 测量结果、共用列表控制器与各端 ScrollArea 渲染层。
- **实施：** 先量实际节点数和滚动耗时，记录为何需要虚拟化；只替换可见行渲染，数据分页、选择和同步契约沿用。裁剪窗口保留可恢复边界，行高度变化统一触发测量，禁用另一套滚动补偿。
- **验收：** 快速滚动、重复补页触发、长时间停留、隐藏后恢复及行高重测稳定；交互使用稳定 ID，尊重减少动态效果。不用固定截断条数冒充分页或容量治理。
- **验证步骤：** 长时间补页并裁剪后反向滚动仍能补回，锚点会话不因 DOM 回收失去选择；隐藏页签恢复、切语言、字体缩放和大量未读位数变化后位置稳定。

## 里程碑与范围外事项

- **列表可独立使用：** PR02–15 完成后，用原查询／轮询验证分页、选择与滚动，不等待 WebSocket。
- **可靠恢复可独立验证：** PR16–25 通过 HTTP 验收，实时通知丢失不影响最终数据；PR26 可单独验证发布故障。
- **基础实时验收：** PR02–41 中各前置完成后，内外聊天、列表、未读和各端恢复闭环；尚未交付的端继续原轮询，不宣称全端完成。
- **增强独立交付：** AI 流 PR42–46 与个人置顶 PR47–49 互不依赖；搜索／虚拟化不阻塞实时主线。后续持久离线库需将数据、目标和 applied 游标原子落盘，不能只保存数字。
- **不在本清单实现：** 图片／文件消息、Telegram 外发 Delivery、群聊 @Agent 策略、移动建群、输入／在线状态、后台系统推送、离线发送队列、设备执行、WebRTC、联邦、大群 Shared Fanout。消息置顶、团队共享置顶、普通区手排、会话分组与未读筛选也不随个人置顶加入。
