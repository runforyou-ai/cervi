# 聊天重构与实时功能 PR 清单

## 执行约定

PR01–PR13 已合并（最新 `#256`），原始范围与验证记录见 `chat-realtime-completed.md`。本文只保留共用契约和尚未交付的范围。2026-09-10 对原路线图做过一次范围削减评审，结论见「本轮范围削减」；原清单的 PR00 未实现，已改编为本文 PR16。

- 一个 PR 交付一个明确行为；后端能力可用真实数据库与应用服务测试独立验收。正确性耦合的迁移、写入、读取及绑定一起提交，不拆成无法运行的中间版本。
- 保留 Conversation、ChatSubject、Participant、ServiceSession、Agent Run 和 appservice 边界。持久命令走 HTTP／Wails；WebSocket 只推「哪个会话变到了哪个版本」，客户端通过业务 Query 读权威数据。
- 已确认的置顶语义不再重复确认：**个人置顶、本人跨端同步、手动排序、新消息不改变顺序**。
- 每个 PR 使用相关 Task 测试和构建，契约变更同 PR 运行 appservice 生成器及 Wails 绑定生成；不手改生成文件。提交前完成 Pi CLI 审核并处理反馈。界面验收遵守仓库授权约定，每轮清理本次启动的进程和端口。
- 已合入 main 的迁移不改；新增迁移由 Task 取实际时间生成并提供 Down。无历史回填、双协议或临时兼容分支。
- 有当前用户的写 Action 先执行 LockActiveUser；渠道／Agent 任务保留各自守卫。随后按共用契约锁会话和业务状态。
- 各端尚未切换实时前保留原轮询；只在对应端通过补拉、失权和阅读回归后移除。每个 PR 删除本次替代的代码，不保留两个状态所有者。

## 本轮范围削减

原清单把「实时」建成了一套可离线重放的持久同步引擎：三类受众各有持久水位表、压缩唤醒行、完整重建基线、保留窗口和恢复 epoch，外加 Protobuf 工具链、一次性票据表和跨节点快照。而清单自己把持久离线库列在范围外，客户端是刷新即重建的内存投影，服务端当前是单进程内嵌 Worker Pool。以下按 CLAUDE.md「MVP 阶段不为尚未出现的生产规模问题预先建设」削减。

| 原范围 | 处理 | 依据 |
| --- | --- | --- |
| `user_sync_states`、`user_conversation_wakeups`、客服／访客持久水位与压缩索引（原 PR17／19／20） | 删除，只保留 `realtime_outbox` | 持久行的唯一价值是离线重放，而离线库在范围外；客户端重连等价于重读已加载窗口。持久行也不减少扇出 |
| 完整同步索引与重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 推迟 | 服务对象是持久游标；内存投影刷新即重建，旧游标随页面消失 |
| `conversation_sync_events` 与 `sync_seq`（原 PR16） | 降级为 `conversations.version` | 事件载荷没有消费者，全部靠 Query 重读；细粒度失效属于优化 |
| Protobuf、Buf、Edition 2024 与 breaking 检查（原 PR27） | 降级为手写 JSON 帧契约，保留平台探针 | 帧载荷是几个整数且允许丢失；仓库已有 appservice 生成器和 Wails 绑定两条强制生成链路 |
| 一次性连接票据表（原 PR28 及 PR31 票据部分） | 删除 | 原生端由 Go 侧持有连接，凭据边界不变；Web 首帧沿用既有 Bearer |
| 跨执行节点运行快照（原 PR44） | 推迟 | 服务端当前单进程内嵌 Worker Pool，没有消费者 |
| AI 流走 Gateway 焦点注册与帧优先级（原 PR45／PR46 传输部分） | 改为按 runId 授权的 SSE | AI 流是高频、可丢、临时的，与变更通知语义不同；省掉焦点注册表、帧饿死处理和快照修复 |
| 实时链路容量与故障验证（原 PR41） | 推迟为证据触发 | 尚无生产部署，合成压测数字不构成产品承诺 |

剩余 33 个可交付 PR（原为 40 个，含未做的 PR00）。条目数减少有限，真正的削减在内容：删掉了四个子系统，事件日志和协议工具链各降一级。

## 已交付基线

PR01–PR13 已交付以下能力，后续 PR 直接依赖，不重复定义。

- **锁序：** `internal/actions/chatstate` 提供会话授权与锁定入口，覆盖真人单聊、独立 AI 聊天、群聊和客户会话；可读、可发、可管理三种资格在锁后判断。
- **`message_seq`：** 全部会话类型统一，会话锁内分配，`last_message_seq` 保存已提交位置；HTTP 用字符串、TS 用 bigint。`read_seq` 为阅读基线，服务端单调推进。
- **`lastActivityAt`：** 独立活动排序时间，会话锁内取 `GREATEST(last_activity_at, clock_timestamp())`；幂等重放、摘要重算和纯状态变化不推进。预览时间仍是 `lastMessageAt`。
- **`chatstate.AppendMessage`：** 唯一消息追加入口，同事务维护会话／周期摘要；`RecomputeConversationSummary` 负责附件取消后的重算。
- **`clientMessageId`：** 发送方作用域的发送关联，只向本人返回，与业务幂等键分开保存。
- **收件箱查询：** `LoadInbox`（keyset 双向分页，`nextCursor`／`beforeCursor`／`startCursor`／`endCursor`／`hasMore`／`hasBefore`）、`ReadInboxWindow`（按首尾游标重读包含边界的闭区间）、`GetInboxContext`（锚点邻域，锚点状态 matching／outside_query／unavailable）、按 ID 独立摘要与批量摘要。游标绑定企业、用户、规范化筛选与排序规则版本。
- **列表控制器：** `frontend/src/features/inbox/inbox-list-controller.ts` 串行调度刷新与补页，保存已加载 ID、双向边界和锚点；Web、桌面与移动端已按服务端顺序自动重排并保留原邻域。

## 变更版本与通知契约

| 名称 | 归属与含义 | 不能拿来做什么 |
| --- | --- | --- |
| `message_seq` | 单会话内最终消息顺序，事务内分配；游标和阅读位置据此比较 | 不表示资料变更 |
| `conversation.version` | 单会话可见变化的版本，消息、群关系、资料引用和 Run 终态推进 | 不等于未读数，不与 `message_seq` 混用，不跨会话比较大小 |
| `identityProfileVersion` | 本人身份资料与账户级偏好的版本 | 不表示某个会话变化，不包含设备本地通知许可 |
| `target` / `applied` | 客户端已知目标版本／已实际读到的版本 | 缓存丢失后不能仅凭旧 applied 跳过重读 |
| `read_seq` | 用户实际阅读基线，保留提及确认的独立语义 | 收到帧、打开连接或补拉完成都不能直接推进 |
| `generation` / `queryGeneration` | 当前登录会话／当前列表查询的本地代次 | 不跨设备同步，也不代替服务端授权 |
| `pinOrderVersion` | 本人全局置顶顺序版本，写入时用于冲突检测 | 不因新消息、已读或静音变化而推进 |

写入链路为：Action 校验与加锁 → 业务事实、`conversations.version`、`realtime_outbox` 同事务提交 → 发布器发通知 → 客户端经 HTTP／Wails 重读。**通知只说明「哪个会话变到了版本 N」或「哪条身份资料变了」，不携带业务内容；通知可重复、可合并、可丢失，业务正确性完全由 Query 恢复。**

客户端为已加载会话保存 `conversationId → appliedVersion`，另保存身份资料版本。通知版本高于已应用版本就失效对应 resourceKeys 并重读；未加载的会话只记目标版本，打开时自然读到最新。读到的实体版本可能比触发它的通知更新，客户端按实际读到的值记录 applied，不回写通知携带的版本号。

独立详情、列表、消息窗口和总数都从对应 Query 获取权威结果。同步协调器保存版本与失效状态，视图控制器保存 ID 顺序、窗口边界、锚点和交互状态；它们不复制一份手工维护的业务 DTO。列表边界暂时过期属于可恢复状态，不能据此判定会话已删除或失权。

### 定期兜底校验

客户端每 30 秒无条件调用一次 `GetSyncHeads`，返回本人当前可见会话的数量、版本校验和以及身份资料版本；三项任一与本地不符就走已加载窗口重读（`ReadInboxWindow` ＋ 按 ID 资格核对 ＋ 当前会话窗口）。前台恢复、网络恢复和重连各额外触发一次。

版本校验和是可见会话 `version` 的合计值，只用于判断「有没有变」，不比较新旧，也不能推断变的是哪一条。它对提交顺序不敏感，任何一次提交都会改变它。

这条兜底是「丢通知最终一定恢复」的唯一保证；Outbox 重发、Gateway 订阅顺序和撤销复核都是让恢复更快，不是正确性前提。**若实测该聚合在真实数据量下过重，那就是引入持久用户水位行的证据；在此之前不建。**

### 受众与 Subject

通知受众是**临时的 NATS 订阅目标，不是持久流**：`<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，`audienceKind` 固定为 `user`、`customer_inbox`、`visitor_directory`。用户用 userId，客服共享一条企业流并不逐客服扇出，访客用渠道身份记录 ID；这些值来自服务端授权结果，使用无点的内部规范值，禁止原始凭据或用户输入直接拼接。

撤销与下线控制和变更通知共用 Outbox，以明确的通知种类区分，不能被当作普通通知合并掉；控制载荷按需要附 tokenSessionId／conversationId。

### 锁序

完成各入口守卫后，多会话业务锁按 conversationId 排序，并在每个会话内遵守已定义的业务对象顺序。**不再存在受众水位行，业务锁之后不需要再取受众锁**；`realtime_outbox` 只做插入，不参与锁序。`pinOrderVersion` 存在本人成员身份行上，置顶事务在会话业务锁之后锁该行，不新增独立顺序锁。资料变更涉及多个会话时先确定并锁业务集合，再统一写通知记录。

## 列表行为基线

查询身份包含企业、用户、scope、customerView、assigneeIdentityId、channelId，启用后再加入分区和规范化搜索词；数据 key 共享，页面实例的选择与滚动位置各自保存。普通区按 `lastActivityAt DESC NULLS LAST, conversationId DESC` 排序；时间使用数据库精度，游标携带原值。置顶区按本人顺序升序，不采用活动时间。

| 输入／位置 | 内容与顺序如何处理 | 选择与滚动如何处理 |
| --- | --- | --- |
| 顶部且没有点击／拖动等交互 | 应用新项和最新活动序 | 保持顶部，不自动换当前详情 |
| 深处收到变化 | 更新内容并自动应用新增和活动排序；触摸、惯性、菜单或键盘操作期间暂缓重排，资格移除立即生效 | 保存原可见邻居；当前行上浮时锚定原后继、再前驱，不跟随回顶 |
| 用户向上浏览 | 自动双向补页，接近顶部主动核对最新窗口；不要求点击刷新或查看最新 | 按原邻域补偿，滚到全局顶部即可看到最新会话 |
| 交互结束 | 一次性应用最新权威顺序，不展示待更新提示 | 以停止操作时的可见邻域保位，不打断惯性或重复补偿 |
| 切换筛选 | 读取新查询；旧结果不可操作 | 恢复该查询历史，否则保持未选中 |
| 会话不再匹配筛选 | 从列表移除 | 有阅读权限则保留详情；失权立即清理敏感内容 |
| 断网／请求失败 | 保留已展示数据和独立错误状态 | 重试同一窗口，不换主体 DOM、不跳第一行 |

“保锚点”记录稳定可见 ID、相对滚动视口顶部的像素偏移、原后继／前驱及原窗口边界。读取前捕获，DOM 更新后测量一次并补偿；锚点因排序远移时优先留在原邻域。原生 scroll anchoring、控制器或虚拟列表只能有一个补偿所有者。用户正在滚动时延后非必要重排；失权清理不等待用户停止操作。

列表刷新和补页由同一控制器串行调度：刷新遇到在途补页，先等待其收尾，再捕获已经扩展后的窗口边界；刷新中触发补页则排队，重复刷新合并。切换查询使整个队列的 queryGeneration 失效，但不取消已经执行的 Wails 业务调用。刷新重读已加载双向边界之间的连续范围，并按 ID 重读已加载项的当前资格；不能只以首页或可见视口替换全部已加载结果。交互中的可读行仅暂缓布局移动，停止操作后按权威顺序应用；真正失权才按失权规则清理；请求失败保留已确认的窗口和重试边界。

## 个人置顶写入与读取契约

置顶、取消和移动均提交 expectedPinOrderVersion，成功返回新版本。移动输入为 `{ conversationId, position, neighborId?, expectedPinOrderVersion }`：position 是 `before`／`after` 时 neighborId 必填，表示插入该邻居在**全局个人顺序**中的紧前／紧后；`start`／`end` 不带邻居，表示整个置顶区的首／尾。目标和邻居必须是本人当前有权读取的置顶会话；版本或邻居失效时整次操作失败，客户端重新读取后让用户重试，不自动用旧意图覆盖。

“隐藏项”包括被当前筛选排除及尚未加载的置顶项。移动只改变目标相对位置，其余项之间相对顺序保持；如全局 `A,X,B`、当前仅看见 A/B，将 B 移到 A 前，结果必须是 `B,A,X`。前端只提交位置命令，不能把可见 ID 数组当成全量个人顺序保存。rank 重编号只是存储实现变化，不改变逻辑顺序。

任意改变置顶集合／顺序的事务（含失权清理及随操作发生的重编号）推进 pinOrderVersion 并写本人受众通知，客户端据此重读最新顺序版本。发现版本变化时让整个置顶分区及其游标失效，重新取得同一版本的权威窗口；不得把单条新 pinRank 与缓存旧 rank 混排。多页重读途中版本再次变化，舍弃该轮候选并从新版本恢复锚点；已展示内容可保留为待更新视图，但不能提交使用旧版本的排序。新消息只推进会话版本，不推进置顶顺序版本。

## 实施 PR 的完成证据

后端项在相关 Action／Query 集成测试中构造数据并断言数据库及 appservice 返回值；并发用可控屏障组织，不靠 sleep 碰概率。运行 `wails3 task test:server`；契约或平台接入同时运行相关绑定生成、前端／服务端构建与平台 Task。前端项以本条具体交互场景验收，状态机复杂处增加可控响应顺序测试；界面操作遵守 AGENTS.md 授权规则。PR 描述记录实际执行命令、平台和结果，未执行的场景如实标记，不把本清单中的预期写成通过记录。

## 一、列表与发送收敛

### PR14：选中会话定位与列表边界

- **依赖：** 无（PR12、PR13 已合并）。
- **已由 PR12／PR13 交付：** 旧响应不覆盖新查询（控制器按 generation 丢弃过期结果）、交互期间暂缓重排与失权立即清理、菜单和指针按会话 ID 绑定、隐藏容器不按零高度恢复。本条只交付余下部分。
- **范围：** 进入列表时按已选中会话定位，不持久化滚动位置；选择历史保存键按规范化查询生成。修正切到空筛选时的列表报错、末页底部空白和轮询期间的页脚占位。
- **落点：** `inbox-list-controller.ts`、`use-inbox-list.ts`、`inbox-list-panel.tsx`、`inbox-route.tsx`。
- **实施：** 进入时有选中会话则以它为初始锚点读 `GetInboxContext`，锚点无原位置，按邻居补偿对齐到窗口顶部；`ReadContext` 在原游标为空时按锚点自身位置推导邻域，因此不需要保存滚动状态。记录了原位置的锚点才判定为已移动。空区间兜底只接受属于本窗口的锚点。页脚在可向前翻页、分页出错和空列表加载时占位。
- **边界：** 不保存滚动锚点，不新增浏览状态存储；工作台页签模型不变，`/inbox` 仍只有一个页签。
- **验收：** 带选中会话进入列表时该会话停在窗口顶部，无选中时停在顶部；选中会话不属于当前筛选时读取首页并回到顶部；从已滚动的非空筛选切到空筛选不报错；末页最后一条会话贴到底部，有数据时的轮询不改变列表高度。
- **验证步骤：** 深处选中会话后切换筛选再切回，核对该会话距容器顶为零；带会话参数刷新地址，核对同样定位。滚到末页测量最后一行到容器底的距离。从已滚动的非空筛选切到空筛选，核对无报错且服务端无游标失效日志。

### PR14A：客服会话按渠道筛选

- **依赖：** PR14。
- **范围：** 客户范围新增与队列视图正交的渠道筛选，公共、我的、同事和已关闭四个视图共用同一个渠道条件；单选一个渠道实例，`all` 与 `internal` 范围不提供该筛选。新增收件箱渠道候选查询，包含已停用渠道。
- **落点：** `actions/inbox/load_inbox.go` 的 `LoadInput` 与 `filterCustomerInbox`、`actions/inbox/cursor.go`、新增 `actions/inbox/list_channels.go`、appservice `types_inbox.go` 与 `Backend`、`inbox-route.tsx`、`inbox-page.tsx`、`inbox-list-controller.ts`、`resource-keys.ts`、`mobile-inbox-navigation.tsx`。
- **实施：** `filterCustomerInbox` 在既有 `cci` 关联上追加渠道条件，不新增 JOIN；`inboxCursor` 携带渠道并在解码时校验，排序版本不变。渠道候选返回本企业全部消息渠道并按类型和名称排序，不按是否产生过会话收敛。前端把筛选维度整体参与查询 key、游标和请求代次，`scope` 离开 `customer` 时清空渠道参数。
- **边界：** 本条不新增渠道多选、按渠道类型聚合的筛选，也不改变各视图的资格判断规则。
- **验收：** 四个客户视图下渠道筛选都生效；停用渠道的历史会话仍可筛出且候选列表保留该渠道；切换渠道使旧游标失效且不串页；`ReadByIDs` 的资格判断随渠道条件收敛；跨企业读不到其他企业渠道。
- **验证步骤：** 同一联系人在两个渠道各有会话，逐个视图切换渠道核对结果集；翻到第二页后切换渠道，断言不出现上一渠道的尾页；停用一个渠道后其历史会话仍在筛选结果中；切到「全部」和「内部」时 URL 与请求都不带渠道参数。覆盖 Web／桌面端及移动端，运行契约生成、前端与服务端测试和构建。

### PR15：成员发送气泡与查询结果收敛

- **依赖：** 无（PR07、PR09 已合并）。
- **范围：** 本地待发送项通过本人 clientMessageId／messageId 与查询消息对齐；成功响应、查询补拉和离开页签后的结果共用发送状态收尾。保持手动失败重试，不新增离线自动发送队列。
- **落点：** `use-outgoing-conversation-messages.ts`、`use-conversation-timeline.ts`、`frontend/src/api/inbox.ts`。
- **实施：** 本地发送项依次经历 pending、confirmed 或 failed；查询含本人关联时可确认发送，即使命令响应尚未到达。临时 ID 只用于展示，messageId 到达后统一去重；发送完成通知归属原会话，路由卸载不取消已经执行的命令。
- **验收：** 查询先到、响应先到、超时已提交、切换会话后响应迟到均只显示一个气泡；改正文／引用／提及生成新逻辑编号。
- **验证步骤：** 可控地交换发送响应与消息查询顺序，两种顺序都只剩一个气泡；服务端提交后客户端超时，再手动重试仍只出现一条，切到另一会话不得插入原会话回复。

### PR16：禁用 AI 员工后保留会话并统一确认交互

- **依赖：** 无实时基础设施依赖；以已合并的独立 AI 会话实现为基础。原清单编号为 PR00。
- **范围：** 禁用 AI 员工后仍在成员消息列表保留其已有 AI 会话；聊天页显示「该 AI 员工已禁用」并禁止发送新消息。账号禁用不撤销会话阅读资格，已提交输入继续执行，有在途 Run 时保留独立停止回复入口。
- **落点：** `internal/actions/inbox/candidates.go` 的 `agentConversationsQuery`（当前以 `agent.status = active` 过滤）、appservice 会话摘要契约、`features/inbox`、`apps/mobile`、`features/contacts/agents/agents-panel.tsx` 和 `agent-detail.tsx`。
- **实施：** 会话摘要返回 Agent 账号状态，列表按会话归属和阅读资格查询，不再因 Agent 被禁用而隐藏历史；新消息发送继续校验账号状态。列表三点菜单和详情账号状态修改共用禁用确认交互，确认前不保存状态。文案明确禁止新消息、已提交任务继续执行、相关渠道分配重置为公共队列，团队关系保留；恢复后仍为下班，渠道配置不自动恢复。
- **边界：** 本条不新增会话归档／恢复、批量取消、群聊 Agent 或客服接管语义；停止回复由独立 Agent PR 实现，本条只保持入口可达。
- **验收：** 两个禁用入口取消确认均不改变账号状态，确认只提交一次；禁用前后历史、当前会话选择和滚动位置保持，不能新发消息但能查看在途结果并主动停止；恢复后允许发送，跨端状态一致，企业及成员隔离不变。
- **验证步骤：** 同一 Agent 创建两个会话，在其中一个运行时禁用；验证两个会话仍可见、结果写回原会话、停止仅影响指定 Run。分别从列表和详情禁用并取消／确认，检查渠道目标、下班状态和恢复后的行为。覆盖 Web／桌面端及移动端，运行契约生成、前端与服务端测试和构建。

## 二、变更版本与发布

### PR17：会话变更版本、身份资料版本与同步探针

- **依赖：** 无（PR06、PR08 已合并）。合并原清单 PR16 与 PR21。
- **范围：** 新增 `conversations.version`，会话锁内随消息追加和会话状态变化推进；新增本人身份资料版本，随成员资料、工作状态和账户级通知偏好推进；新增 `GetSyncHeads` 探针。不建事件日志表，不建持久受众水位表。
- **落点：** `models/conversation.go`、`chatstate/message.go`、成员身份模型与工作状态／通知偏好 Action、`actions/inbox/candidates.go`、appservice 新增 `GetSyncHeads`。
- **实施：** 版本与消息、摘要、活动时间在同一事务内推进；幂等重放、摘要重算和纯读取不推进。身份资料版本存在本人成员身份行上，设备通知许可仍留本地。探针复用已交付的候选资格查询，在一次只读一致快照内返回可见会话数量、可见会话 `version` 合计值和身份资料版本，只投影聚合值。
- **验收：** 四种会话的版本单调、事务回滚不留版本；幂等重放不推进；探针在会话新增、移除、失权、消息和资料变更后都发生变化；只改本人偏好时会话版本不变而身份版本变；跨企业与失权不扩大探针范围。
- **验证步骤：** 并发向同一会话追加消息，断言版本逐次推进且不丢；把成员移出群后探针的数量和合计值都变化；对一个低版本会话追加消息，断言合计值变化（而最高版本可能不变）；用户没有任何会话时改工作状态，探针仍变化。

### PR18：实时 Outbox 与受众通知契约

- **依赖：** PR17。
- **范围：** 新增 `realtime_outbox`，保存受众种类、受众 ID、通知种类和最小载荷；内部新消息及本人已读／提及／静音／未读标记同事务写入通知记录。受众种类显式区分 `user`、`customer_inbox`、`visitor_directory`。业务事务不发 NATS，不在网络中持事务。
- **落点：** 新增 `realtime_outbox` 模型与写入能力；接入 `chatstate` 追加路径与个人设置 Action。
- **实施：** 一次事务可写多条受众记录，载荷只含会话 ID、版本和通知种类，不含正文、姓名或未读数。个人设置只写本人受众，内部消息写真实真人受众。同一事务内针对同一受众的多次变化合并为一条记录。
- **验收：** 业务事实与通知记录同提交或同回滚；实际无变化不写记录；个人设置不广播给其他人；载荷不含业务内容。
- **验证步骤：** 在写入通知记录之后主动回滚，消息和记录均不存在；同一事务写两个会话产生两条记录；本人静音只产生本人受众记录，且不含会话正文。

### PR19：成员会话变化接入

- **依赖：** PR18。合并原清单 PR18 与 PR23。
- **范围：** 群创建、资料、增员、移除、退出、转让、解散，以及 AI 运行的 queued、running、成功、失败、取消和人工接管接入版本推进与通知。变更前后的真人受众都写通知，失权者只收会话 ID 和 removed 标记；Agent 不消费成员受众。
- **落点：** `conversation/group_management.go`、`group_conversation.go`、`agentrun/schedule.go`、`execute.go`、`cancellation.go`。
- **实施：** 事务锁后分别计算变更前后的成员集合；仍有权者获得新版本，失权者获得仅含 ID 的 removed。重入是新的有效关系，不被旧 removed 覆盖；解散保留只读历史，与移除成员使用不同资格结果。AI 每次持久状态转换推进版本，成功消息与终态同事务提交；仅内存进度不推进版本，重复终态回调按当前 attempt 判定无变化。
- **验收：** 移除后重入恢复当前成员关系；新成员阅读基线正确；解散只读与失权区分；失败结果消息与 Run 终态、无结果消息的取消都能通过 HTTP 恢复；事务失败不留半条系统消息或孤立通知记录。
- **验证步骤：** A 被移除后立刻重入，延迟消费通知仍恢复最新成员关系；A 仅被移除时批量摘要不可读，通知载荷无群名或正文。让模型失败并生成 agent_error，另构造不产生消息的取消，断开 Socket 后仅靠 HTTP 恢复两种终态；旧租约成功回调晚于取消到达时不得复活 Run。

### PR20：客服共享收件箱通知

- **依赖：** PR18。
- **范围：** 客户入站、回复（含 AI 最终消息）、隐式领取、转交、关闭、显式重开及新周期接入会话版本与企业客服受众通知。共享受众不逐客服扇出；本人客服已读只写本人受众。
- **落点：** `inbox/load_inbox.go`、客服收发与服务周期 Action。
- **实施：** 所有有权客服共享一条企业受众，通知只表示该客户会话需要重读；公共／本人／同事／关闭资格仍由现有 Query 判断。负责人变化不为所有客服创建 Participant，个人已读继续走本人受众。
- **验收：** 公共、本人、同事和关闭视图同时收敛；筛选迁移不等于授权移除；客服参与者不被误当全部有权阅读者。
- **验证步骤：** A 领取公共会话后，B 的公共列表移除、A 的本人列表出现，双方有权详情仍可读；关闭／重开改变队列但不改活动时间；AI 最终回复也能触发共享受众通知。

### PR21：访客授权与线程目录通知

- **依赖：** PR20。
- **范围：** 提取网站 HTTP 共同授权，建立渠道身份级受众通知与目录／会话公开投影查询，补齐未知新线程发现。只返回访客公开投影。
- **落点：** `website_visitor_direct_backend.go`、`api/website_visitor.go`、`conversation/list_website_conversations.go`、`list_website_messages.go`。
- **实施：** 把 Cookie／Header 恢复出的渠道身份作为唯一受众，目录按该身份列出线程；成员 Query 的内部字段不复用到访客 DTO。首次消息以外的读操作不建立联系人。
- **验收：** 同一访客两标签页新建不同线程可互相发现；跨渠道／身份请求被拒绝；初始化和草稿不创建联系人，客服内部资料不外泄。
- **验证步骤：** 同渠道同访客在两个标签页分别首发新线程，互相能发现；换另一个渠道或伪造线程 ID 读不到消息；空白页反复初始化后数据库业务记录数不变。

### PR22：名称、头像和渠道资料失效

- **依赖：** PR19、PR21。
- **范围：** 接通成员／Agent、联系人／渠道身份及渠道资料写入口；按真实 JOIN 依赖标记受影响会话，推进这些会话的版本并按固定锁序写对应受众通知；头像异步导入成功也覆盖。
- **落点：** `internal/actions/identity`、`agent`、`channel` 及联系人资料入口；inbox 与消息查询的资料 JOIN。
- **实施：** 先以实际 JOIN 列出资料字段影响清单，再在各真实写事务中推进对应会话版本；同步只要求重读当前资料，不复制姓名头像到每条历史消息。异步头像完成属于独立写入口，也要发布；批量会话锁按稳定 ID 排序。
- **验收：** 无新消息时列表、发送者和引用资料可更新；访客仅刷新公开字段；失败和相同值不产生伪变化；不建立通用实体事件平台。
- **验证步骤：** 会话两天没有新消息时修改对方姓名和头像，列表、消息署名与引用展示均更新；异步头像落库后再验一次；活动序、未读和访客内部字段不变。

### PR23：实时 Outbox 发布器

- **依赖：** PR18。
- **范围：** 独立发布器短租约认领，在事务外发布 Core NATS 并 Flush，再凭同一 leaseToken 删除；按 namespace、企业和行中受众种类定向发布。失败释放并退避，可整条重发；不改 `task_outbox`，不建立每用户 JetStream Consumer。
- **落点：** 新增实时 Outbox 发布模块，参考 `internal/task/server/runtime.go` 的启动／关闭组织，不复用可靠任务投递语义。
- **实施：** 短事务认领一批待发记录并写 leaseToken 与到期时间，释放事务后逐受众发布和 Flush；完成时按记录 ID ＋ leaseToken 条件删除。重复发布的通知可合并，多个节点不能凭旧租约确认新认领的记录。
- **验收：** 提交后崩溃、发布后崩溃、部分受众失败及过期租约均恢复；旧发布者不能删除新租约记录；恢复不依赖 NATS 留存。
- **验证步骤：** 在发布前和 Flush 后分别杀发布进程，重启后目标通知最终送达；故意延迟旧发布者到新租约生效之后，旧确认影响行数为零。

## 三、连接与各端实时闭环

### PR24：JSON 帧契约与平台探针

- **依赖：** 无（可与第二阶段并行）。取代原清单 PR27 的 Protobuf 工具链。
- **范围：** 定义 WebSocket JSON 帧：Authenticate、Hello、Ping／Pong、ChangeNotice、Control、Error、Bye，并覆盖访客目录与会话通知。定义版本字段、未知帧忽略规则，64 位整数一律用字符串。探测 Wails 各端 Origin、Upgrade、TLS 与子路径。
- **落点：** 新增协议定义（Go 结构体与 TS 类型各自手写，以双向夹具锁定）；`build`、平台探针记录。
- **实施：** 以双向编解码夹具锁定帧结构、版本协商和字符串整数边界。不引入 Protobuf、Buf 或第三方代码生成器，避免在 appservice 生成器和 Wails 绑定之外叠加第三条强制生成链路。Hello 返回连接信息与当前身份的探针值。平台探针单独记录实际 Origin、服务端地址拼接和 TLS 结果，再据此确定允许来源配置。
- **验收：** Go 与 TS 双向解析一致；未知可忽略帧、协议主版本不支持分别有明确结果；超过 JS 安全整数的值正确；记录真实平台结果，不把 Web 通过视为原生通过；探针不成为另一套产品协议。
- **验证步骤：** 同一份帧分别由 Go／TS 编码并对端解码；Web、桌面、iOS、Android 分别记录通过或未验证，不以模拟结果替代真平台。

### PR25：成员 Gateway、连接认证与撤销控制

- **依赖：** PR23、PR24。合并原清单 PR28、PR29 与 PR30；客服共享受众的订阅在 PR20 完成后生效。
- **范围：** Server 内嵌 Gateway，首帧限时 Authenticate；Web 使用现有 Bearer，原生端由 Go 侧 Proxy 持有连接并把帧经 Wails 事件交给 TS。按本节点在线受众订阅，禁止 Queue Group 与企业通配订阅。单写协程、有界队列与帧大小限制，控制帧优先。登出按 token session、停用按用户、群失权按会话发送控制通知，服务端低频复核授权。不建票据表。
- **落点：** 新增 `internal/realtime` Gateway 与连接注册表；`internal/apiproxy` 的原生连接持有；服务端 HTTP 路由；`actions/auth/logout.go`、用户停用入口与群关系 Action。
- **实施：** 连接依次处于 awaiting_auth、authenticated、closing；认证前只允许 Authenticate，超时关闭；连接到期不晚于登录会话。订阅安装完成并 Flush 之后才读取 Hello 探针值，堵住「先读探针再订阅」的丢通知窗口。连接登记 tokenSessionId、userId、企业和已授权会话；登出／停用与控制记录同事务提交，收到控制后撤销订阅、清理未发送的受限帧并关闭或缩减连接权限。定期复核是丢控制帧后的修复路径，记录复核间隔和最坏生效窗口。原生端不把长期 Token 交给 TS。
- **验收：** 一用户多设备、多节点均收到通知；先注册订阅再给探针值；慢消费者断开后可补拉；NATS 恢复而 Socket 未断时主动要求重新校验探针；已连接后登出、停用或移出群不再获得受限内容；撤销事务回滚不产生错误断连。
- **验证步骤：** 在安装订阅与读取探针之间提交消息，客户端通过 Hello 或后续通知至少发现一次；同账号连两个节点两端均收到；堵塞一端读流触发有界关闭，另一端继续正常工作。连接后登出本次 token 只关闭对应登录会话，停用用户关闭其全部会话；丢弃一次控制通知后服务端复核仍生效；已写入网络的字节不承诺可撤回。

### PR26：访客 Gateway 受众

- **依赖：** PR21、PR25。
- **范围：** 访客首帧用 Cookie／Header 恢复的渠道身份认证，独立访客 Subject 与公开帧白名单；渠道停用撤销连接。Origin 按发起 Socket 的 iframe 来源校验，宿主页白名单沿用嵌入规则。
- **落点：** `api/website_visitor.go`、`website_visitor_direct_backend.go`、渠道停用入口与 Gateway 访客认证 adapter。
- **实施：** 受众使用数据库渠道身份 ID，不使用 Cookie 原值或可逆外部标识构造 Subject。先验证渠道与访客归属，再允许其目录与会话公开投影；尚无业务身份时不建立连接，也不为连接创建联系人。
- **验收：** 随意传 conversationId 不能订阅；独立链接、嵌入、Header 恢复与预览互相隔离；成员认证不能用于访客连接；停用渠道后已有连接和重新连接都失效。
- **验证步骤：** 抓取访客连接帧和 Subject，不能出现原始 Cookie 值；跨身份指定线程失败；停用渠道后重连被拒。

### PR27：共享客户端连接与会话代次

- **依赖：** PR25。
- **范围：** `src/api/realtime` 实现无 feature 依赖的 TS 传输内核；一个应用实例一连接、业务页签共用，浏览器各标签页可独立连接。原生端消费 Go 侧连接投递的帧，Web 端自建 Socket。抖动退避、重新认证，网络／认证／版本错误分流；前端发送与接收队列有界。
- **落点：** 新增 `frontend/src/api/realtime`；`frontend/src/api` 的认证边界与各端登录外壳。
- **实施：** 传输内核只接收认证输入、URL／Origin 适配、frame handler 和生命周期输入，不导入 inbox 或 workspace。维护 disconnected、connecting、authenticating、ready、backoff、stopped；退出登录或切企业先使 generation 失效再释放旧连接。
- **验收：** 企业／账号切换先提升 generation 再清缓存；旧 Promise、Socket 和通知失效，同源凭据改变可恢复；验证 WebView 地址与 bufferedAmount；临时断网保留草稿。
- **验证步骤：** A 企业的认证慢响应在切到 B 之后返回，不得创建 A 的连接；重连与旧 onclose 交错时只保留一条当前连接；服务端版本不支持时不无限重试刷日志。

### PR28：登录会话级同步协调器与兜底校验

- **依赖：** PR17、PR27。
- **范围：** 登录后启动，接收变更通知并串行合并同一会话的最高目标版本；区分目标、已应用和阅读水位，集中失效 resourceKeys。实现每 30 秒的 `GetSyncHeads` 兜底校验及前台、网络恢复、重连触发。
- **落点：** 新增登录会话级同步模块，`frontend/src/hooks/resource-keys.ts`、`frontend/src/lib/resource-client.ts`。
- **实施：** 收到通知则合并目标版本、失效对应 Query 并在读取完成后按实际读到的版本推进 applied；未加载实体只留待读目标。已加载窗口重读失败须保留 dirty 状态，不能因为收到过通知就当作已应用。兜底校验发现探针不符时走已加载窗口重读，不做全量重建。
- **验收：** 离开消息页仍同步；空页、持续热点、读取失败和更高目标正确收敛；不持久化游标冒充离线库，不逐条通知手工修补 Query DTO；断开 Socket 后仅靠兜底校验也能收敛。
- **验证步骤：** 同一会话连续送来版本 10、8、12，只串行追到 12；窗口 Query 失败后仍标记待同步，重试可恢复。整段断开 Socket，只靠 30 秒兜底仍能看到新消息、失权和资料变化。离开消息页仍处理身份与未读变化，账号切换清空原用户全部同步状态。

### PR29：消息窗口接入实时同步

- **依赖：** PR15、PR28。
- **范围：** 用版本驱动权威窗口重读，消除初始 Query 更新而 page 副本不变的问题；最新窗口补连续页，锚点窗口不拼远端尾部但处理资料、删除与失权。收到推送不自动已读；移除对应的成员消息和提及高频轮询。
- **落点：** `use-conversation-timeline.ts`、`conversation-window.ts`、`use-conversation-viewport.ts`、提及与阅读 hooks。
- **实施：** 以窗口模式 latest／anchor 和 queryGeneration 组织读取，权威 Query 更新必须能替换对应窗口内容；合并消息页不等于保留已删除实体。最新模式连续补到目标，锚点模式只重读当前窗口与引用依赖，远端尾部更新留提示。阅读仍由可见消息判定。
- **验收：** 上翻、引用跳转、窗口外原文删除、并发发送、过期页响应和连续提及查看稳定；Wails 调用仅忽略过期结果，不取消业务执行。
- **验证步骤：** 停在引用跳转窗口，同时发生新消息和原文删除：不跳尾部、引用失效、最新提示出现；连续提及导航后返回最新不丢消息；旧窗口响应晚到不能覆盖新会话。

### PR30：列表实时更新与可变排序补全

- **依赖：** PR14、PR28。
- **范围：** 延续已交付的三端自动排序：顶部跟随最新，深处应用新项与重排并保留原邻域，仅交互期间延后布局。向上浏览自动取得最新会话，无刷新、查看最新或待更新提示。keyset 继续原边界，通知发现未加载却已上浮的会话，按 ID 与版本去重；移除列表与群资料高频轮询。
- **落点：** 已交付的列表控制器、PR28 同步协调器、按 ID 批量摘要 Query；替换 inbox 页面轮询消费者。
- **实施：** 通知提供「哪些会话变化」，按 ID 取得当前资格和排序键，失效并重读权威连续窗口；普通 keyset 仍按原边界继续补历史，不改成最新边界。交互结束自动应用窗口顺序，当前锚点上浮时恢复原后继、前驱，回到顶部无需额外点击。
- **验收：** C90 在读取旧页期间上浮仍可发现；资格移除立即修复、真实失权立即清除；旧 generation 页面不接新窗口；`hasMore=false` 后仍可发现新项；刷新失败不换整棵 DOM。
- **验证步骤：** 初始显示 C1–C50，未加载的 C90 收到消息并排到首位；读取 C51 之后的页不会再返回 C90，但通知必须发现它。深处保持原邻域，向上滚动到顶部后 C90 只出现一次；`hasMore=false` 后新增会话也可发现。

### PR31：拆分网站挂件网络与窗口状态

- **依赖：** 无（PR07 已合并）。
- **范围：** 通过现有 messenger 构建入口，将请求、身份、连续窗口与发送状态迁入 `frontend/src/publicweb` 可测试模块；保留 Go 模板、主题及现有 DOM 外观，访客 DTO 与成员隔离。
- **落点：** `internal/publicweb/chat.js`、`page.html`、`frontend/src/publicweb` 与现有 messenger 构建入口。
- **实施：** 按网络身份、会话目录、消息窗口、待发送状态拆出可测试模块，DOM 事件转成显式操作，不把整段脚本包进一个 TS 类。先保留三秒轮询验证等价，再删除迁出的原函数与状态变量；构建产物继续由现有 Go 嵌入链路加载。
- **验收：** 原轮询下文本、引用定位、分线程草稿和幂等重试不变；删除被替代的旧状态逻辑；不打包内部身份或工具详情。
- **验证步骤：** 在真实挂件构建产物上发送、重试、切线程、引用跳转和恢复 Cookie；切回原线程草稿仍在。检查 bundle 依赖没有成员登录模块或内部 Agent 详情。

### PR32：网站挂件实时与多线程恢复

- **依赖：** PR26、PR27、PR31。
- **范围：** 共用传输内核，以访客 API adapter 同步目录和窗口；已有身份时初始化即连接，无身份不连接，首条有效消息建立身份后再执行。空白页以前台恢复及低频无副作用检查发现另一页建立的身份；替换业务消息轮询。
- **落点：** PR31 访客状态模块、PR27 传输内核、网站目录与窗口 HTTP adapter。
- **实施：** 状态分为无身份、可连接、已连接、恢复中；无身份页面只在前台恢复和低频身份检查时发现其他页建立的 Cookie，发现后先取目录基线再正常追赶。访客同步拥有独立 keys 与内存，不挂到成员登录协调器。
- **验收：** 新线程发现、离线补拉、发送响应乱序、引用窗口和渠道停用均收敛；初始化不创建业务记录，预览不连真实服务。
- **验证步骤：** A、B 两个空白挂件，A 首发建立身份，B 不发消息也能发现身份和线程；断线期间 A 新建第二线程，B 恢复后列出两条且不制造新联系人。

### PR33：移动端实时接入

- **依赖：** PR29、PR30。
- **范围：** 移动登录外壳接同一连接与同步内核，列表、单聊及已有群聊使用统一实时输入；恢复前台先做兜底校验，再按独立阅读规则工作。保留当前移动功能边界，不顺带开启建群或已读交互。
- **落点：** `frontend/src/apps/mobile` 登录外壳、mobile-inbox-page、mobile-individual-conversation-page、mobile-group-conversation-page。
- **实施：** 应用前台事件只触发同一协调器的兜底校验，不由每个页面各建连接；系统挂起后旧连接一律按可能失活处理。移动页面订阅共用版本输入，保持自身详情导航、阅读能力和草稿行为。
- **验收：** iOS／Android 前后台、旋转、详情返回、连接重建与旧响应隔离；完成对应回归后移除移动三秒轮询。
- **验证步骤：** 系统挂起期间另一端发送、已读、移除群成员，恢复后列表与详情收敛；快速返回再进另一会话，无重复连接、旧内容闪入或错误已读。

### PR34：实时未读与本地通知闭环

- **依赖：** PR29、PR30。
- **范围：** 同步确认的新 Message 接现有通知策略；静音、@、工作状态、全局／设备开关及可见上下文独立判断。通知去重和跨标签页前台协调；总数绑定权威版本，客户端不做 +1/-1 推算。
- **落点：** `features/notifications/new-message-notifications.ts`、`platform/notifications.ts`、当前通知偏好与 PR28 同步入口。
- **实施：** 以 messageId 去重，并区分 live 与 catchup／bootstrap 来源；一次通知覆盖多条消息时读取实际新增范围后逐条执行策略，不用最后预览代表全部。跨浏览器标签协调前台处理者；通知内容来自已授权消息 Query，旧账号 generation 立即失效。
- **验收：** 一次通知合并多条消息时不只看最后预览；冷启动与重连历史不逐条补弹，他端已读只更新状态；旧总数不覆盖新版本；客户未读不进入桌面总提醒。不承诺跨设备恰好一次或退出后推送。
- **验证步骤：** 一次通知包含普通消息和 @ 消息，静音场景仍按既有 @ 策略判断；重连追赶十条历史只更新未读，不连弹十次；两标签前台切换时同一设备不重复通知。

### PR35：Gateway 生命周期与部署

- **依赖：** PR26、PR32。
- **范围：** 配置 Upgrade、TLS、子路径、代理空闲超时、默认关闭压缩及帧大小；启动就绪顺序、停止接收、随机重连下线、发送与订阅 Drain、停止认领。记录认证、慢连接、Outbox 和同步落后指标，不记凭据或正文。
- **落点：** 服务端应用装配、`internal/config/server`、`internal/ingress`、Gateway 与发布器，部署说明。
- **实施：** 启动顺序明确为依赖就绪 → 路由／订阅可服务 → 对外 ready；退出先停止接入和认领，向连接发下线提示，再有界 Drain 并释放资源。将路径前缀、代理空闲时间和心跳配置一起记录，认证与关闭使用结构化原因码。
- **验收：** 原生 Wails HTTP、反向代理与 Cloudflare 路径可连；重启后未发布的 Outbox 可继续；只释放本实例的连接和资源。
- **验证步骤：** 通过带子路径的反向代理连 Web 与原生端，空闲超过代理默认时间仍正常；SIGTERM 后连接提示重连，未确认 Outbox 留给下一实例，退出不关闭共享 NATS。

## 四、AI 实时过程

### PR36：拆分运行摘要与过程详情

- **依赖：** PR19、PR29。
- **范围：** Run 摘要按 runId／agentIdentityId 返回集合，当前 UI 可选最相关项；消息只留过程引用，展开时取详情，移除每次消息查询携带完整 blocks 的实现。
- **落点：** `direct_backend_agent_process.go`、`types_agent_process.go`、`conversation/agent_process.go`、`features/inbox/agent-process.tsx`。
- **实施：** 列表与消息窗口只带轻量 Run 摘要和过程引用；独立过程 Query 按 runId 获取持久 blocks、工具参数结果和用量。当前 Run 集合与历史消息关联分开，失败通过结果消息定位，取消无消息也可选中查看；展开状态 key 用 runId。
- **验收：** 多 Run、无消息失败和历史过程均可读取；窗口更新不丢展开状态；运行详情按会话授权，保留工具完整参数、结果及用量。
- **验证步骤：** 同会话两个 Agent 各有 Run，刷新不会只剩「最近一个」；历史过程首次展开才请求完整 blocks，关闭再展开可用缓存；移出群后该 Query 不再返回详情。

### PR37：模型增量消费与流基线

- **依赖：** PR36。
- **范围：** Runtime 真正消费 token 与工具进度，使用 runId、attempt、streamId、sequence 与稳定块编号，短周期合并；暴露当前内存快照和临时发布接口，不直接依赖传输实现。
- **落点：** `internal/integration/agentruntime/eino.go`、`openai.go`、`progress.go`、`types.go`。
- **实施：** 将整段输出收集点替换为运行时增量消费；每个 attempt 创建独立 streamId，块有稳定 blockId，同流 sequence 单调。内存快照与最高 sequence 同步捕获，发布端按短周期合并可重建增量，模型执行不等待浏览器消费。
- **验收：** 同流重复／乱序、gap、尝试切换、工具并行与输入抢占正确；token 不写 Message、不推进会话版本、不入 `realtime_outbox`；限速发送不阻塞模型执行。
- **验证步骤：** 分片产生文本、工具参数、工具结果并模拟并行工具，块顺序稳定；故意漏一片后快照可恢复全文；重试产生新 streamId，旧尝试尾部不能拼入新正文。

### PR38：成员 AI 流 SSE 与终态收敛

- **依赖：** PR28、PR37。取代原清单 PR44、PR45 的 Gateway 焦点与跨节点快照方案。
- **范围：** 新增按 runId 授权的 SSE 端点，展开运行过程时连接、收起时断开；服务端先按会话授权再订阅该 Run 的内存流，首帧发当前快照，之后发增量。仅数据库提交后发 completed，持久终态覆盖临时候选。
- **落点：** 服务端 SSE 路由与 Run 流订阅、成员 `agent-process.tsx` 与运行展示控制器。
- **实施：** 连接即取快照＋后续增量，客户端按 attempt、streamId、sequence 去重；gap 或断线直接重连取新快照，不做单独的快照修复协议。终态由持久 Query 确认，SSE 的 completed 只是加速提示；运行过程未展开时不建立连接。Gateway 与变更通知不承载 AI 流。
- **边界：** 服务端当前为单进程内嵌 Worker Pool，SSE 端点与执行在同一进程内定位 Run。拆分部署时再评估跨节点快照，本条不预建。
- **验收：** 长输出期间仍及时收到消息；断线重连、失败、取消和人工接管不复活旧候选；completed 丢失可由会话版本恢复；未展开的运行不产生连接。
- **验证步骤：** 展开过程中持续输出，断开重连后不重复拼接已有 token；丢弃 completed 后仅靠会话版本仍显示最终消息；取消后晚到的 delta 不能重新打开运行中状态；无权用户请求该 runId 被拒绝。

### PR39：网站公开 AI 流

- **依赖：** PR32、PR38。
- **范围：** 访客只接公开候选正文、基础状态和最终回复；内部思考与工具原文不外发。客户负责人变化停止旧流，取消只是加速，最终写回仍受数据库与租约门禁约束。
- **落点：** PR32 访客展示、访客 SSE adapter、客服 Agent 输出映射。
- **实施：** 在服务器构造访客专属公开帧，不先发全量工具与思考再靠前端隐藏；流授权始终绑定渠道身份可读线程。人工接管、转交、关闭取消旧公开候选资格，最终回复仍由原有事务门禁决定是否保存。
- **验收：** 客服接管、转交、关闭、重连换流及渠道失效均正确；抓取访客 SSE 与 HTTP 响应都无内部过程数据。
- **验证步骤：** 模型同时输出思考、工具结果和正文，抓包只含公开正文和基础状态；客服接管后迟到的旧 delta 不再展示，重连读取的最终消息也不包含被取消的候选。

## 五、个人会话置顶

### PR40：个人置顶事实、顺序与分页

- **依赖：** PR19。
- **范围：** 个人状态增加 pinRank，本人成员身份行增加 pinOrderVersion；置顶、取消、按邻居移动同事务推进版本并写本人受众通知。提供显式分区查询，未启用置顶的调用方继续完整活动序查询；扩展锚点上下文的分区、顺序版本及跨区邻居。新增默认追加末尾，取消回普通活动顺序；过滤只是全局个人顺序的投影。
- **落点：** `models/conversation_user_state.go`、成员身份模型、新增置顶写 Action、已交付的分页与锚点 Query。
- **实施：** 命令使用共用置顶契约的 position／neighborId 和 expectedPinOrderVersion，不提交全列表覆盖隐藏项。锁定所涉会话并校验当前资格后，再锁本人身份行；冲突返回当前版本且不部分写入。按 rank 留间隔，耗尽时同事务无唯一冲突地重编号；取消清 rank，再次置顶追加末尾。
- **验收：** expectedPinOrderVersion 防两端覆盖；隐藏项相对顺序保留，重编号无唯一冲突；置顶页游标绑定版本、与普通页分开，活动序调用方不漏项；跨区移动使旧分区资格失效；失权清除置顶，重入不自动恢复，解散可读则保留。
- **验证步骤：** 全局 A,X,B、当前隐藏 X，将 B 移到 A 前，结果为 B,A,X；两端持同版本同时排序只允许一个成功。触发 rank 重编号后，两端整区重读所得顺序与服务端一致。失权清理也推进顺序版本，旧分区游标要求重读而非静默漏项。

### PR41：Web／桌面置顶交互

- **依赖：** PR30、PR40。
- **范围：** 客户端切换分区查询，单滚动容器先置顶再普通，同会话只显示一次；支持菜单置顶、拖动及等效键盘排序。新消息不动置顶顺序；本人移动保留操作锚点，远端重排按浏览意图暂存，跨分区原子更新并补偿位置。
- **落点：** Web／桌面列表控制器、行菜单及新增排序交互，PR40 分区 Query。
- **实施：** 控制器维护 pinned 与 regular 各自的游标、内容版本及置顶顺序版本；顺序版本变化整区重读，渲染仍是一条纵向列表；置顶区未加载完时继续取置顶，不把尚未到达的置顶错误接到普通区之后。操作期间可显示临时拖动外观，成功后权威重读，冲突保留页面上下文并提示重试，不手写新业务 DTO。
- **验收：** 两区分别分页、只有一个分区、置顶较多、取消后重新置顶、锚点跨区和拖动期间版本冲突均正确；不自动跳顶，不清草稿／静音／阅读水位，不增加嵌套滚动或任意数量上限。
- **验证步骤：** 从普通区置顶一条当前可见会话，确认两区没有重复，详情与草稿仍在；他端取消当前锚点置顶时保持原邻域。拖动尚未完成时收到远端排序，提交冲突后恢复权威顺序且不跳顶。

### PR42：移动端置顶与跨端顺序

- **依赖：** PR33、PR41。
- **范围：** 移动端增加触屏置顶、排序与可访问的替代操作；复用个人顺序、版本冲突及分区分页，不同步各设备滚动位置。
- **落点：** `apps/mobile/mobile-inbox-page.tsx`、移动列表导航，复用置顶命令与分区状态。
- **实施：** 长按菜单提供置顶、取消及明确的移动操作，触屏拖动有独立手势启动条件；无拖动能力时仍可通过菜单完成同样排序。跨端只接收顺序版本，各端选择、焦点、展开和滚动位置不上传。
- **验收：** 长按与惯性滚动互不误触；其他端置顶、取消或重排后，详情返回保持原邻近锚点，新消息只更新置顶内容。
- **验证步骤：** A 桌面调整置顶后 B 手机在详情内收到变化，B 返回仍在原列表邻域；B 长按后转为滚动不得误发排序。新消息连续到达，置顶相对顺序始终不变。

## 六、按证据跟进

本节各项不阻塞实时主线：搜索按实际产品需要开启，虚拟化和容量验证按实测证据开启。

### PR43：会话名称搜索接口

- **依赖：** PR40。
- **范围：** 搜索当前有权会话的名称与标题，完整查询身份包含规范化搜索词；分页、上下文、变化资格判断与置顶分区共用筛选。不是只搜索已加载页，不加入正文全文搜索。
- **落点：** inbox 候选 Query、锚点上下文、分区请求和 `types_inbox.go`。
- **实施：** 服务端统一搜索词空白规范化与匹配规则，复用当前会话标题与对象名投影；先在完整有权候选集合筛选，再分页。查询身份和游标均包含规范化词，变化重读时重新计算匹配资格。
- **验收：** 首页之外可命中；改名导致匹配进入／退出、隐藏置顶、失效同事和跨企业隔离正确。
- **验证步骤：** 第 100 条标题匹配时首页可找到；将当前结果改名为不匹配则移除，另一未加载会话改名为匹配则可发现；同词跨企业请求不能扩大范围。

### PR44：会话搜索输入与浏览恢复

- **依赖：** PR42、PR43。
- **范围：** 启用各端搜索入口，组合输入结束后查询，旧词响应受 queryGeneration 隔离；清空搜索恢复原筛选锚点，未展示查询仅标旧，结果详情仍独立。
- **落点：** Web／桌面与移动列表搜索入口、列表控制器、`resource-keys.ts`、双语 inbox 文案。
- **实施：** 输入框只维护本地编辑词，compositionend 后形成规范化查询；请求状态与结果查询绑定，防止新词标题下出现旧词可点击行。保存搜索前筛选与锚点，清空只恢复浏览上下文，不清个人置顶或会话详情。
- **验收：** 中文输入、快速改词、重连中搜索和搜索期间新消息不串结果；真实无匹配才显示空态，搜索不重置个人置顶。
- **验证步骤：** 中文输入法尚在组词时不逐键发最终查询；A 词慢响应晚于 B 词返回不覆盖。搜索中断网再清空，恢复原列表后迟到的搜索结果仍被忽略。

### PR45：列表虚拟化与窗口裁剪（测量后决定）

- **依赖：** PR42。
- **触发条件：** 真实列表规模出现可复现的渲染或滚动问题，并记录实际节点数与滚动耗时。
- **范围：** key 固定为会话 ID，裁剪保留前后边界与语义锚点。固定头像与摘要行布局，缩放、语言、未读位数与时间更新不制造跳动；选择单一滚动补偿所有者。
- **落点：** 共用列表控制器与各端 ScrollArea 渲染层。
- **实施：** 先量实际节点数和滚动耗时，记录为何需要虚拟化；只替换可见行渲染，数据分页、选择和同步契约沿用。裁剪窗口保留可恢复边界，行高度变化统一触发测量，禁用另一套滚动补偿。
- **验收：** 快速滚动、重复补页触发、长时间停留、隐藏后恢复及行高重测稳定；交互使用稳定 ID，尊重减少动态效果。不用固定截断条数冒充分页或容量治理。
- **验证步骤：** 长时间补页并裁剪后反向滚动仍能补回，锚点会话不因 DOM 回收失去选择；隐藏页签恢复、切语言、字体缩放和大量未读位数变化后位置稳定。

### PR46：实时链路容量与故障验证（测量后决定）

- **依赖：** PR33、PR35。原清单 PR41。
- **触发条件：** 出现真实部署规模，或已观察到连接、队列、锁争用的具体问题。
- **范围：** 增加可重复 Task 场景：同用户多会话、小群扇出、共享客服受众、资料批量失效、突发重连和持续热点；分别测 LockActiveUser 守卫、内存、队列、积压与 HTTP 放大，报告真实规模和结果。
- **落点：** 现有 Task 与服务端集成测试；新增只用于测试的连接、发布和 Query 故障注入点。
- **实施：** 每个场景记录数据量、在线连接数、受众大小、持续时间和机器配置，再测端到端收敛时间、HTTP 次数及队列峰值；用结果定位热点，不把合成测试数字写成产品承诺。注入点不得成为生产接口。
- **验收：** 杀进程、NATS 短断、慢客户端及连接过期均恢复。锁优化、普通性能索引和大群 Shared Fanout 只按证据另开 PR。
- **验证步骤：** 消息已提交后杀进程、NATS 单独断连而 Socket 存活、慢客户端分别重跑；最终数据库版本与客户端投影一致，并证明队列与协程不随重复重连持续增长。

### 已推迟的原范围

| 原范围 | 重新开启的条件 |
| --- | --- |
| 持久用户／客服／访客水位与压缩唤醒行（原 PR17／19／20） | `GetSyncHeads` 聚合实测过重，或确定要做持久离线库 |
| 完整同步索引、重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 客户端需要跨会话持久游标，即离线库落地时 |
| 会话变更事件日志与 `sync_seq`（原 PR16） | 按会话版本重读被实测证明过重，需要细粒度失效时 |
| Protobuf 协议与工具链（原 PR27） | 帧载荷显著变大变热，或协议要提供给第三方 |
| 一次性连接票据（原 PR28） | 原生端不再由 Go 侧持有连接，或出现新的凭据边界要求 |
| 跨执行节点运行快照（原 PR44） | Gateway 与 Worker 拆分部署 |

## 里程碑与范围外事项

- **列表可独立使用：** PR14、PR14A–PR16 完成后，用原查询与轮询验证分页、选择与滚动，不等待 WebSocket。
- **变更版本可独立验证：** PR17–PR22 通过 HTTP 与兜底探针验收，实时通知丢失不影响最终数据；PR23 可单独验证发布故障。
- **端到端最薄一刀：** PR15、PR17、PR18、PR23、PR24、PR25、PR27、PR28、PR29 完成后，成员消息窗口即可跑通「提交 → 通知 → 重读」并删除该端轮询；其余受众和端按各自 PR 依次接入，尚未交付的端继续原轮询。
- **增强独立交付：** AI 流 PR36–PR39 与个人置顶 PR40–PR42 互不依赖；搜索与虚拟化不阻塞实时主线。
- **不在本清单实现：** 图片／文件消息、Telegram 外发 Delivery、群聊 @Agent 策略、移动建群、输入／在线状态、后台系统推送、离线发送队列、设备执行、WebRTC、联邦、大群 Shared Fanout。消息置顶、团队共享置顶、普通区手排、会话分组与未读筛选也不随个人置顶加入。
