# 聊天重构与实时功能 PR 清单

## 执行约定

PR01–PR21、PR25、PR25A、PR26、PR27、PR28、PR29、PR30、PR32、PR33、PR34、PR36、PR37、PR38、PR40、PR41 与 PR42 已交付，原始范围与验证记录见 `chat-realtime-completed.md`。本文只保留共用契约和尚未交付的范围。2026-09-10 对原路线图做过一次范围削减评审，结论见「本轮范围削减」；原清单的 PR00 已改编为 PR16 并交付。2026-09-13 对通知发布、兜底探针和 AI 流传输做了第二轮调整，结论见「第二轮调整」，与第一轮结论冲突处以第二轮为准。2026-09-15 对授权复核、客户端版本追赶、帧优先级和非主线范围做了第三轮调整，结论见「第三轮调整」，与前两轮冲突处以第三轮为准。同日 PR25 合入后把实时传输从 WebSocket 改为 SSE，结论见「第四轮调整」，与前三轮冲突处以第四轮为准。

- 一个 PR 交付一个明确行为；后端能力可用真实数据库与应用服务测试独立验收。正确性耦合的迁移、写入、读取及绑定一起提交，不拆成无法运行的中间版本。
- 保留 Conversation、ChatSubject、Participant、ServiceSession、Agent Run 和 appservice 边界。持久命令与临时上报走 HTTP／Wails；SSE 事件流只下行推送版本通知和 AI 流等临时事件，撤销与到期直接结束事件流，客户端通过业务 Query 读权威数据。
- 已确认的置顶语义不再重复确认：**个人置顶、本人跨端同步、手动排序、新消息不改变顺序**。
- 每个 PR 使用相关 Task 测试和构建，契约变更同 PR 运行 appservice 生成器及 Wails 绑定生成；不手改生成文件。界面验收遵守仓库授权约定，每轮清理本次启动的进程和端口。
- 已合入 main 的迁移不改；新增迁移由 Task 取实际时间生成并提供 Down。无历史回填、双协议或临时兼容分支。
- 有当前用户的写 Action 先执行 LockActiveUser；渠道／Agent 任务保留各自守卫。随后按共用契约锁会话和业务状态。
- 各端尚未切换实时前保留原轮询；只在对应端通过补拉、失权和阅读回归后移除。每个 PR 删除本次替代的代码，不保留两个状态所有者。

## 本轮范围削减

原清单把「实时」建成了一套可离线重放的持久同步引擎：三类受众各有持久水位表、压缩唤醒行、完整重建基线、保留窗口和恢复 epoch，外加 Protobuf 工具链、一次性票据表和跨节点快照。而清单自己把持久离线库列在范围外，客户端是刷新即重建的内存投影，服务端当前是单进程内嵌 Worker Pool。以下按 CLAUDE.md「MVP 阶段不为尚未出现的生产规模问题预先建设」削减。

| 原范围 | 处理 | 依据 |
| --- | --- | --- |
| `user_sync_states`、`user_conversation_wakeups`、客服／访客持久水位与压缩索引（原 PR17／19／20） | 删除；保留的 `realtime_outbox` 已由第二轮删除 | 持久行的唯一价值是离线重放，而离线库在范围外；客户端重连等价于重读已加载窗口。持久行也不减少扇出 |
| 完整同步索引与重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 推迟 | 服务对象是持久游标；内存投影刷新即重建，旧游标随页面消失 |
| `conversation_sync_events` 与 `sync_seq`（原 PR16） | 降级为 `conversations.version` | 事件载荷没有消费者，全部靠 Query 重读；细粒度失效属于优化 |
| Protobuf、Buf、Edition 2024 与 breaking 检查（原 PR27） | 降级为手写 JSON 帧契约，保留平台探针 | 帧载荷是几个整数且允许丢失；仓库已有 appservice 生成器和 Wails 绑定两条强制生成链路 |
| 一次性连接票据表（原 PR28 及 PR31 票据部分） | 删除 | 原生端由 Go 侧持有连接，凭据边界不变；Web 首帧沿用既有 Bearer |
| 跨执行节点运行快照（原 PR44） | 推迟 | 服务端当前单进程内嵌 Worker Pool，没有消费者 |
| AI 流走 Gateway 焦点注册与帧优先级（原 PR45／PR46 传输部分） | 改为独立 SSE；已由第二轮改为同一 WebSocket 按 runId 订阅 | AI 流是高频、可丢、临时的，与变更通知语义不同；省掉焦点注册表、帧饿死处理和快照修复 |
| 实时链路容量与故障验证（原 PR41） | 推迟为证据触发 | 尚无生产部署，合成压测数字不构成产品承诺 |

第一轮后剩余 33 个可交付 PR（原为 40 个，含未做的 PR00），当前数量见「第三轮调整」。条目数减少有限，真正的削减在内容：删掉了四个子系统，事件日志和协议工具链各降一级。

## 第二轮调整

第一轮削减后，兜底探针已经是「丢通知最终一定恢复」的唯一保证，但清单仍保留了为「通知不丢」服务的 Outbox 与租约发布器；探针的版本合计值存在碰撞；AI 流为避开帧优先级另开 SSE，使原生端要维护两条长连接。本轮按同一原则继续收敛。

| 调整对象 | 处理 | 依据 |
| --- | --- | --- |
| `realtime_outbox` 表与独立租约发布器（PR18、PR23） | 删除；事务内登记通知，提交成功后直接发布 Core NATS，PR23 并入 PR18 | Outbox 只关闭「提交后、发布前进程崩溃」这一窗口，该窗口丢失的通知由 30 秒探针恢复，符合通知可丢的契约；少一张表、一个后台组件和整套租约测试 |
| 探针的版本合计值（PR17） | 改为对可见会话 `(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希求和 | 数量加 `version` 求和在「失去一个会话、同时获得另一个同版本会话」时数量与合计值都不变，探针漏检 |
| 探针覆盖范围（PR17、PR40） | `conversation_user_states` 新增本人会话状态版本并纳入校验和；PR40 起探针同时返回 `pinOrderVersion` | 已读、提及确认、静音、手动未读和置顶顺序不推进会话版本；通知丢失后只能靠探针发现，探针必须覆盖所有写本人受众通知的状态 |
| AI 流独立 SSE（PR38、PR39） | 改走同一条 WebSocket，按 runId 显式订阅与取消订阅 | 原生端连接由 Go 侧 Proxy 持有，SSE 需要另一套认证、重连与事件转发；按 runId 订阅、首帧快照、流帧低于控制帧和变更通知、溢出时丢弃并要求重订阅，即可覆盖焦点注册与帧饿死问题；流帧优先级与溢出重订阅已由第三轮删除 |
| 多节点验收与 Drain、随机重连下线（PR25、PR35） | 降级为单节点正确；订阅继续按受众 Subject 定向，保持多节点可扩展的边界 | 服务端当前是单进程内嵌 Worker Pool，多节点行为没有真实部署可验收 |
| 资料变更逐会话推进版本（PR22） | 保留 | 探针因此自然覆盖资料变化，客户端只有一种失效路径；改名等低频操作的写放大在 MVP 规模可接受 |

编号保持不变以维持与 `chat-roadmap.md`、`agent-roadmap.md` 的引用一致；编号 PR23 空缺，发布职责由 PR18 交付。PR18 起剩余 28 个可交付 PR。

## 第三轮调整

第二轮之后，通知只携带版本，兜底探针保证最终恢复，但连接层和客户端仍保留了按「通知必须及时送达、授权必须实时收回」设计的机制：服务端定期复核授权、按种类串行追赶目标版本、四级帧优先级与流帧溢出重订阅。本轮按「通知可丢、Query 权威、快照可重建」继续收敛，并推迟不在成员消息主线上的范围。

| 调整对象 | 处理 | 依据 |
| --- | --- | --- |
| 服务端定期复核授权与群失权控制通知（PR25、PR38） | 删除；撤销控制只覆盖登出与停用。连接最长存活 1 小时，到期关闭后客户端重连并重新认证；`SubscribeRun` 每次重新校验登录会话、账号状态与会话阅读资格。群失权由 PR19 的 `conversation_removed` 通知驱动 Gateway 移除该会话的 AI 流订阅，客户端发现失权后也取消订阅 | 会话投影由 Query 与兜底探针恢复；受限的临时内容只有 AI 流，由逐次订阅校验、失权通知与连接寿命约束。`conversation_removed` 丢失且客户端未取消时，被移出者最多继续收到已订阅的当前一轮输出 |
| 客户端 `target`／`applied` 版本比较与串行追赶（PR28） | 删除；客户端不保存实体版本，收到通知即失效对应 resourceKeys，同一 key 的失效按短窗口防抖合并 | 失效后发起新的读取，最终以失效后的重读为准；本人写入回显的通知多触发一次重读，不影响正确性；Query 返回值无需增加实体版本字段 |
| 每连接四级帧优先级与流帧溢出重订阅（PR25、PR38） | 改为单写协程加一条有界发送队列，队列内同一 Run 的待发增量合并；队列溢出即以 `slow_consumer` 关闭连接 | 重连后由探针恢复通知、由订阅首帧快照恢复流，关闭连接覆盖全部溢出场景 |
| 独立 JSON 帧契约 PR 与平台探针记录（PR24） | 并入 PR25；首版只定义当前使用的帧。已完成的 Web、macOS 原生端与 Cloudflare Tunnel 探针结论写入 PR25 与 `chat-roadmap.md` 第 10.12 节，iOS、Android 在各自真机接入时记录 | 帧契约脱离 Gateway 无法端到端验收；探针发现的升级挂载与写入器约束直接决定 Gateway 实现方式 |
| Gateway 部署与生命周期（PR35） | 原生端子路径、心跳与优雅关闭并入 PR25；指标与就绪顺序推迟 | 前者决定连接能否在反向代理和隧道后工作；Web 端的 Wails 运行时本身不支持子路径部署，服务端不增加路径前缀配置；出现真实部署前以 `INFO`／`WARN` 日志观测 |
| 名称、头像与渠道资料失效（PR22） | 推迟 | 资料修改低频，推迟期间他人资料在会话因其他变化重读时更新；第二轮保留的设计理由仍成立，重新开启时按原设计实施 |
| 网站挂件拆分为 TS 模块（PR31） | 推迟；PR32 在现有 `chat.js` 内接入连接，收到通知或重连时调用现有拉取逻辑 | 挂件实时只需把轮询触发换成通知触发，不依赖整份脚本重构 |
| 网站公开 AI 流（PR39） | 推迟；访客经会话通知读取最终回复，生成期间沿用挂件现有的正在输入提示 | 公开投影与接管后候选作废的复杂度高于首版价值 |
| 挂件跨标签页身份发现（PR32）、本地通知跨标签页前台协调（PR34）、NATS 恢复时主动要求校验探针（PR25）、前端收发队列上限与 `bufferedAmount` 观察（PR27） | 推迟 | 不影响单页面内的最终正确性，NATS 恢复场景由 30 秒兜底探针覆盖 |

编号 PR24 空缺，帧契约由 PR25 交付；PR22、PR31、PR35、PR39 保留条目并标注触发条件。PR19、PR37 已合并，剩余 20 个可交付 PR，其中 PR43–PR46 按需求或证据开启。

## 第四轮调整

第三轮之后，实时通道只下行版本通知和 AI 流，客户端没有必需的上行帧，但 WebSocket 仍带来首帧认证状态机、双向心跳、协议升级劫持与 Origin 校验、双向帧契约，以及在一条连接内复用 AI 流所需的订阅集合与共用队列。2026-09-15 PR25 合入后评估，实时传输改用 SSE（Server-Sent Events），PR25 已交付的 Gateway 由 PR25A 原地改造。本轮与前三轮冲突处以本轮为准。

| 调整对象 | 处理 | 依据 |
| --- | --- | --- |
| 实时传输（PR25、PR26、PR27、PR32、PR33） | WebSocket 改为 SSE：成员事件流 `GET /api/realtime`，响应 `text/event-stream`，每个事件一行 `data: <JSON>`，不使用 `event`、`id`、`retry` 与 `Last-Event-ID`；原生端由 Go 侧发起流式 GET，Web 端用 fetch 流式读取，访客挂件用 `EventSource` 或携带 Header 的 fetch | 下行只有版本通知和流数据；SSE 复用 HTTP 认证、日志和代理链路，私有化部署中比协议升级更易穿透；以后需要高频双向时新增 WebSocket 是增量工作 |
| 连接认证（PR25、PR26） | 删除首帧 `Authenticate` 与五秒认证时限，改为请求头 Bearer 复用业务身份解析，失败返回与业务接口相同的错误体与状态码；访客沿用渠道 Cookie／Header | 认证在写出事件前完成，不需要 awaiting_auth 状态机；凭据仍不进入 URL，成员端不引入 Cookie |
| 帧集合与关闭原因（PR25） | 删除 Authenticate、ClientHello、客户端 Ping／Pong、Authenticated、SessionRevoked、ServerGoingAway、RealtimeError、全部关闭原因码与 Hello 能力协商；服务端每 25 秒发送 `ping`，客户端 60 秒未收到事件即重连；共用夹具只覆盖服务端事件 | 认证失败由 HTTP 状态码表达；其他流结束客户端一律按网络错误退避重连，重连遇到 401 再进入登录；能力需要协商时用请求头表达 |
| AI 流（PR38、PR39） | 删除同一连接上的 SubscribeRun／UnsubscribeRun、连接内订阅集合和 Gateway 收到失权通知时移除订阅；每个展开的运行过程发起 `GET /api/realtime/runs/{runId}`，请求时授权，快照、增量、结束事件发完后结束响应，每条流使用独立有界队列；客户端收到 `conversation_removed` 时关闭该会话的运行过程流 | 授权与生命周期跟随请求；AI 流溢出只结束该流，不连带断开变更通知；HTTP/2 下多条流复用同一 TCP 连接；失权通知丢失的最坏情况与第三轮相同 |
| `realtime.maxFrameBytes` 配置（PR25） | 删除 | 当前事件都是小通知，单事件大小约束随 PR38 快照分块定义 |
| 上行临时状态（输入状态、在线状态、焦点会话、WebRTC 信令） | 随真实功能走普通 HTTP 并经过 `appservice.Service`，每次一次认证，不为其设计节流；在线状态可由事件流是否存在推导 | 这些功能尚未排期；出现事件流加 HTTP 无法满足的高频双向需求时再评估 WebSocket，下行事件一并迁移，不形成双主协议 |
| 部署约束（PR25、PR35） | Web 端部署要求 HTTPS／HTTP2，接受 HTTP/1.1 下同源 6 连接上限对多标签页的限制；反向代理对 `/api/realtime` 关闭响应缓冲并允许长响应；删除 Upgrade 配置与 `permessage-deflate` 评估 | SSE 在 HTTP/2 下与业务请求共用连接；服务端 25 秒心跳维持代理空闲超时 |
| 优雅下线（PR25） | 删除 `server_going_away`；收到 SIGINT／SIGTERM 即拒绝新的事件流请求（503）并结束全部事件流，5 秒内未结束的强制断开，之后停止通知发布器 | Wails 服务端先执行 `http.Server.Shutdown` 并等待进行中的长响应，之后才停止各服务；被劫持的 WebSocket 不在等待范围内，SSE 长响应在 |

PR25 原文与 WebSocket 平台探针结论保留在 `chat-realtime-completed.md` 作为交付记录；Cloudflare Tunnel 下的 SSE 实时下发在 PR27 实际接入时记录；PR33 已在桌面开发的移动端预览窗验证原生端 SSE 长响应，iOS 与 Android 真机结论待各自接入时补记。PR20、PR25、PR25A、PR27 已合并，PR21、PR26、PR28、PR29、PR30、PR32、PR33、PR34、PR36、PR38、PR40、PR41、PR42 已交付；剩余 5 个可交付 PR（含 2026-09-18 开启的 PR22，不含标注推迟的 PR31、PR35、PR39），其中 PR43–PR46 按需求或证据开启。

## 已交付基线

PR01–PR21、PR25、PR25A、PR26、PR27、PR28、PR29、PR30、PR32、PR33、PR34、PR36、PR37、PR38、PR40、PR41 与 PR42 已交付以下能力，后续 PR 直接依赖，不重复定义。

- **锁序：** `internal/actions/chatstate` 提供会话授权与锁定入口，覆盖真人单聊、独立 AI 聊天、群聊和客户会话；可读、可发、可管理三种资格在锁后判断。
- **`message_seq`：** 全部会话类型统一，会话锁内分配，`last_message_seq` 保存已提交位置；HTTP 用字符串、TS 用 bigint。`read_seq` 为阅读基线，服务端单调推进。
- **`lastActivityAt`：** 独立活动排序时间，会话锁内取 `GREATEST(last_activity_at, clock_timestamp())`；幂等重放、摘要重算和纯状态变化不推进。预览时间仍是 `lastMessageAt`。
- **`chatstate.AppendMessage`：** 唯一消息追加入口，同事务维护会话／周期摘要。
- **`clientMessageId`：** 发送方作用域的发送关联，只向本人返回，与业务幂等键分开保存。
- **收件箱查询：** `LoadInbox`（keyset 双向分页，`nextCursor`／`beforeCursor`／`startCursor`／`endCursor`／`hasMore`／`hasBefore`）、`ReadInboxWindow`（按首尾游标重读包含边界的闭区间）、`GetInboxContext`（锚点邻域，锚点状态 matching／outside_query／unavailable）、按 ID 独立摘要与批量摘要。游标绑定企业、用户、规范化筛选与排序规则版本。
- **列表控制器：** `frontend/src/features/inbox/inbox-list-controller.ts` 串行调度刷新与补页，保存已加载 ID、双向边界和锚点；Web、桌面与移动端已按服务端顺序自动重排并保留原邻域。
- **收件箱筛选：** `LoadInput` 的渠道、服务状态与会话类型参与游标校验和按 ID 资格；前端范围规则集中在 `inbox-query.ts`，三端共用。
- **发送状态：** `outgoing-message-store.ts` 由工作台级 Provider 持有，按会话分组，文本与附件共用；按本人 `clientMessageId` 与消息窗口对齐收尾，离开页签后仍写入结果。
- **禁用账号会话：** 单聊与 AI 会话的列表资格只按会话归属与会话状态判断，摘要返回 `peerStatus`／`agentStatus`。
- **变更版本：** `conversations.version` 在分配 `message_seq` 的同一语句推进，覆盖成员消息、附件消息、客户入站、AI 回复和群系统事件，群资料修改同样推进；`conversation_user_states.version` 随已读、提及确认、静音、手动未读和入群阅读基线推进；`users.profile_version` 经 `identity.UpdateUserIdentity` 和用户账号更新按实际变化推进，修改密码与团队不推进。幂等重放、回滚和实际无变化的写入均不推进。
- **同步探针：** `GetSyncHeads`（`GET /sync/heads`）在一条语句内返回可见会话数量、`(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希和与身份资料版本，64 位值以字符串传输；客户会话覆盖全部视图与服务状态。
- **提交后发布：** 写事务经 `realtime.RunInTx` 执行，`realtime.Notify` 在事务内登记通知，同一受众、种类和会话合并为最高版本；提交成功后交给进程级 `Publisher` 异步发布 Core NATS，回滚丢弃，队列已满或发布失败记录 `WARN` 并丢弃。Subject 为 `cervi.<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，载荷为 `kind`、`conversationId` 与字符串 `version`。已接入内部会话消息（含群系统事件、AI 结果与附件消息）通知当前真人成员，本人会话状态与身份资料只通知本人；客户会话由 PR20 通知企业客服共享受众，登出与账号停用登记撤销控制，载荷相应携带 `tokenSessionId`。附件在上传完成后逐个发送，服务端不保存上传中的占位消息。
- **成员会话变化：** 群创建、资料、增员、移除、退出、转让、解散，以及成员会话 AI 运行开始与失效取消接入会话版本和通知；`chatstate.TouchConversation` 在已持有会话锁的事务内推进版本并通知真人成员。失去阅读资格的真人收到仅含会话 ID 的 `conversation_removed`，该种类不与会话变更合并；客户会话受众见下一条。
- **客服共享受众：** 客户入站、成员与 AI 回复、隐式领取、领取与人工接管、转交、关闭、显式重开与新周期，以及 Telegram 投递状态、渠道启停与更换机器人推进受影响客户会话的版本，并通知企业客服共享受众 `customer_inbox`（受众 ID 为企业 ID），不逐客服扇出；本人客服已读只写本人受众；`realtime.RunInTx` 接受 `bun.DB` 或 `bun.Conn`。
- **访客目录受众：** 网站渠道的客户会话在通知企业客服共享受众的同时，按 `customer_conversations` 关联的渠道身份通知访客目录受众 `visitor_directory`（受众 ID 为渠道身份记录 ID）；`channels.type = website` 之外的客户会话和客服 Copilot 线程不产生该通知。公开路由 `GET /public/website-channels/:channelID/conversations` 按当前渠道身份返回线程目录的访客公开投影，与 Messenger 初始化共用同一查询；发送、目录与历史三条公开路由共用 `authorizeWebsiteVisitor` 中间件读取访客 Token 并写入渠道外部编号，渠道身份在 Action 内按该编号加载，尚未建立身份的读操作不创建联系人。
- **成员实时事件流：** `GET /api/realtime` 由服务端 `Assets.Middleware` 中租户上下文之内的 `internal/realtime/gateway` 处理：请求头 Bearer 复用业务调用的身份解析，失败返回业务错误体与状态码；登记连接并订阅本人用户受众与本企业客服共享受众，NATS Flush 后重新校验登录会话、读取探针，再以 `server_hello` 开始事件流。每个事件一行 `data: <JSON>`，信封为 `{v, type, data}`，当前事件为 `server_hello`、`ping`（每 25 秒）与 `conversation_changed`、`conversation_removed`、`conversation_state_changed`、`identity_profile_changed`；Go 定义在 `internal/realtime/protocol`，TS 在 `frontend/src/api/realtime/protocol.ts`，共用夹具只覆盖服务端事件。每条事件流单写协程加有界发送队列，按会话与种类合并最高版本，溢出即结束；登出在删除令牌的事务内登记携带 tokenSessionId 的 `session_logged_out`，停用按用户登记 `user_disabled`，Gateway 收到后结束对应事件流；事件流最长存活 1 小时且不晚于令牌到期；收到退出信号即拒绝新请求并有界结束全部事件流。原生端 `ConnectRealtime`／`DisconnectRealtime` 由 Go 侧 `apiproxy` 发起流式 GET（等待响应头最长 30 秒），事件原文经 `cervi:realtime:frame`、流结束经 `cervi:realtime:closed` 交给前端，60 秒未读到任何事件时主动断开，并记录实际 HTTP 协议版本。
- **访客事件流：** `GET /public/website-channels/:channelID/realtime` 与发送、目录、历史共用 `authorizeWebsiteVisitor` 中间件，由 `internal/realtime/gateway` 的 `ServeVisitor` 处理：按启用的网站渠道和渠道外部编号解析渠道身份，渠道停用或不存在返回渠道未找到，尚无业务身份返回会话未找到，错误体与状态码同访客 HTTP 接口，读操作不创建联系人。成员与访客事件流共用同一条实现，受众 Subject、可下发事件白名单、撤销标识与最长存活时间由各自路由给出。访客受众为 `visitor_directory.<渠道身份记录 ID>` 与 `website_channel.<渠道 ID>`，Subject 只用数据库内部 ID；访客只下发 `visitor_hello`（仅含连接编号，访客没有同步探针）与 `conversation_changed`，成员专用事件在发送队列直接丢弃，请求中的额外参数不扩大接收范围。渠道停用在 `UpdateMessageChannelStatusAction` 的事务内登记 `channel_disabled` 撤销控制，Gateway 据此结束该渠道全部访客事件流，重新请求被拒。挂件接入见「访客挂件实时」。
- **客户端事件流与会话代次：** `frontend/src/api/session-scope.ts` 维护登录会话代次，`beginSessionBoundary()` 先提升代次并同步通知订阅方，再清空查询缓存；登录与初始化成功后、登出与切换企业服务器之前、`recoverSession` 处理 login／connect／setup 状态时，以及 Web 端其他标签页改动令牌（storage 事件或请求时比对令牌归属）时进入新代次，Web／桌面工作台按代次重新挂载。`call()` 只交付发起时代次仍为当前代次的结果，过期结果既不 resolve 也不 reject；会话边界操作与原生连接使用不受代次约束的 `invoke()`。登出先清除本地令牌并进入新代次，再用原令牌通知企业服务器。`frontend/src/api/realtime` 提供应用级单例 `realtimeClient`（`start`／`stop`／`resume`／`subscribe`），状态为 disconnected、connecting、ready、backoff、stopped，收到 `server_hello` 进入 ready；流结束、网络错误或 60 秒无数据时按上限 1s×2ⁿ（最高 30s）的一半到上限之间抖动退避重连，会话错误交给订阅方恢复入口，协议主版本不支持时停止，代次变化立即关闭事件流并丢弃在途回调。Web 端 fetch 携带 Bearer 流式读取 `data:` 行；原生端串行执行 `ConnectRealtime`／`DisconnectRealtime`，连接编号返回前到达的事件缓存后按编号回放。原生端 Go 侧按前端窗口持有实时通道，一个窗口一条成员事件流及其运行过程流：`DisconnectRealtime` 按连接编号结束它所属窗口的通道，同一窗口重新建立成员事件流即通道重建并关闭原有事件流，登录、登出与切换企业服务器结束全部通道，桌面开发模式的主窗口与移动端预览窗因此各自持有一条。Web、桌面与移动端外壳在身份就绪后连接，网络恢复与回到前台时跳过剩余退避；移动端外壳额外声明回到前台重建事件流，`realtimeClient.restart()` 只替换已就绪的连接，连接中、退避中与已停止的状态不受影响。通知由下一条的同步协调器消费。
- **同步协调器与兜底校验：** `frontend/src/features/session/sync-coordinator.ts` 的 `SyncCoordinator` 由 `useRealtimeConnection` 随 Web、桌面与移动端登录外壳创建，外壳按登录会话代次重新挂载时销毁，事件流事件经它转成 resourceKeys 失效。`conversation_changed` 失效收件箱列表、列表行、提醒总数、最近会话，以及该会话的摘要、消息首页与增量页、提及进度与队列、群资料、投递与引用状态和已有单聊查找；`conversation_state_changed` 失效收件箱类查询与该会话的摘要、提及进度与队列和群资料（群资料携带本人免打扰状态）；`conversation_removed` 失效收件箱类查询、检索结果与该会话的摘要和群资料，由摘要或群资料重读确认失权并清理资源；`identity_profile_changed` 失效身份。待失效 key 在 300ms 固定窗口内合并，窗口不因后续通知顺延，同批已被较短前缀覆盖的 key 不重复失效；`resource-refresher.ts` 的 `ResourceRefresher` 只重读挂载中的查询，同一查询串行读取：在途读取不取消且结果照常写入缓存，读取期间的失效合并为结束后的一次补读，断网暂停的读取在网络恢复并完成后才补读；读取失败的查询在下一次合并窗口结束时重试，探针与连接问候成功后即使探针值一致也开启合并窗口。协调器启动时立即、之后每 30 秒无条件调用 `GetSyncHeads`，回到前台与网络恢复时额外调用，`server_hello` 携带的探针值同样参与比较；会话数量或校验和变化时按前缀失效全部会话类查询与检索结果，列表控制器据此重读已加载窗口，身份资料版本变化时失效身份，首个探针值按不一致处理。探针串行执行，在途时的再次请求于结束后补读一次，早于已应用结果发起的读取直接丢弃；读取失败交给会话恢复入口，其他错误记录 `WARN`。Web／桌面端的成员消息、提及、列表、会话摘要与群资料轮询已分别由 PR29 和 PR30 移除，移动端的消息、列表、会话摘要、群资料与页签提醒轮询由 PR33 移除。
- **消息窗口重读：** `ReadConversationMessageWindow`（`GET /conversations/:conversationID/message-window`）按首尾游标读取包含两端的连续范围，范围内消息全部删除时保留请求的两端。`frontend/src/features/inbox/conversation-window.ts` 的 `ConversationWindowController` 串行调度重读与补页：最新模式重读后连续补页到尾端，锚点模式只重读当前范围，内容不变时保留原窗口，打开新窗口后丢弃早于它的结果。窗口重读登记为挂载中的查询，由同步协调器失效触发；Web、桌面与移动端的成员消息、提及进度与队列、投递与引用状态轮询已移除；`contexts/realtime-sync-context.ts` 判定当前外壳是否接入实时同步，未接入的外壳保留轮询。
- **列表实时更新：** `useInboxList` 把列表窗口重读登记为挂载中的查询，首次读取、同步失效、失败重试与前台轮询共用该入口，重读由 `ResourceRefresher` 串行调度，页面不在前台也接收失效。`InboxListController` 的变更重读只与尚未开始的窗口重读合并，在途读取结束后必定补读一次，读取失败不阻止后续变更通知的重读；重读按已加载双向边界读取连续窗口，原先覆盖顶部的窗口扩展到最新首页，按会话 ID 去重后再按 ID 核对资格。会话摘要与群资料同样由会话变更通知失效后重读。Web、桌面与移动端的列表、会话摘要与群资料轮询已移除。
- **访客挂件实时：** `internal/publicweb/chat.js` 以携带访客 Token 请求头的流式 fetch 建立访客事件流，连接状态为 idle、connecting、ready、backoff、stopped：目录中存在线程即视为已建立渠道身份并连接，空白页只读取不连接，首条消息写入成功后再连接；响应状态码小于 500 的拒绝与协议主版本不符停止重连，其余按抖动退避重连。`visitor_hello` 与 `conversation_changed` 复用既有拉取逻辑，分别重新拉取线程目录与按 `after` 游标补拉当前打开线程；补拉取满一页时沿游标继续读取到线程尾端。目录与会话补拉各自在历史加载或在途时登记，结束后补读一次；拉取失败按抖动退避重试到一次成功，问候、回到前台与结束事件流取消待重试的拉取。线程目录先于发送响应建立占位对象时，由发送中的会话接管该线程，保留其消息节点与草稿。页面回到前台、挂件重新显示与网络恢复跳过剩余退避并重新拉取，后台标签页不触发前台补拉，页面卸载结束事件流。挂件三秒消息轮询已移除。
- **运行流：** `ExecuteAction.SubscribeRunStream(runID, onDelta, onEnd)` 在同一把锁内返回当前尝试的快照并登记回调，之后按序回调增量；增量携带 `runId`、`attempt`、`streamId` 与起止序号，`MergeStreamDeltas` 合并首尾相接的增量，执行尝试退出或增量无法应用时回调结束。token 不写 Message、不推进会话版本。
- **运行过程流：** `GET /api/realtime/runs/{runId}` 按运行所属会话的阅读资格授权，运行不存在与不可读对外是同一结果；服务端挂接本进程中该运行当前执行尝试的过程流，先按文本预算把快照拆成多个 `run_stream_snapshot` 分片写出，再写按序号相接合并的 `run_stream_delta`，提交后写 `run_stream_ended` 并结束响应。运行过程流只加入本人用户受众，登出、账号停用与所属会话失权都结束该流；运行不在本进程执行时直接写出结束事件。原生端按运行编号并发持有多条过程流并随所属窗口的成员事件流一起关闭；前端按 `streamId` 与增量起止序号判重判缺口，缺口或断开后退避重新取快照，`run_stream_ended` 只作加速提示，终态由持久 Query 确认。
- **运行摘要与过程详情：** `ConversationMessageList.agentRuns` 按创建顺序返回仍在执行的运行，以及结束后会话再无新消息的取消运行，各带 `agentIdentityId`；取代判断用最后一条消息的 `created_at`，不用记录外部时钟的 `conversations.last_message_at`。消息与底部状态行只带轻量过程引用，`GetAgentRunProcessQuery` 按 runId 读取已开始且已完成运行的持久内容块、工具完整参数结果与用量；成功、失败、停止与取消的过程一律可读，只有确实产生过内容的运行给出过程引用。`ExecuteAction.persistPartialProcess` 在运行进入终态后写入中断前的内容块与用量并推进会话版本，已有内容或运行仍可继续时不写入。气泡对齐方向由 `incoming` 决定，文字配色由 `onPrimary` 决定。
- **个人置顶：** `conversation_user_states.pin_rank`（空即未置顶）保存本人全局置顶顺序，`users.pin_order_version` 为顺序版本。`UpdateConversationPin` 覆盖置顶、取消与移动并返回新版本：`position` 为空时新置顶追加末尾、已置顶保持原位，`before`／`after` 带邻居，`start`／`end` 指整个置顶区首尾，取消置顶不接受位置指令；`expectedPinOrderVersion` 不匹配返回 `pin_order_version_stale` 冲突且不部分写入，重复置顶、重复取消与落点等于原位置不推进版本。本人置顶写入由 `LockActiveUser` 锁定的用户账号行串行，不逐会话加锁；顺序值按 2^20 留间隔，取不到中点时同事务整区重编号。`LoadInput.partition` 未指定时按完整活动序返回全部会话，`pinned` 按顺序值排序且游标绑定 `pinOrderVersion`，顺序变化后旧游标以 `inbox_cursor_invalid` 要求整区重读，`regular` 只含未置顶会话；按 ID 核对列表资格与锚点上下文同样应用分区。`GetSyncHeads` 与列表、窗口、锚点查询返回当前 `pinOrderVersion`；`pin_order_changed` 只通知本人受众，同步协调器收到该事件或探针发现版本变化时整区失效收件箱类查询。失权清理只清除该会话的 `pinRank` 并推进本人会话状态版本，重入不恢复置顶。Web／桌面端置顶交互由 PR41 交付，移动端由 PR42 交付，见下两条。
- **Web／桌面置顶交互：** `usePartitionedInboxList` 为置顶区与普通区各建一个 `InboxListController`，经纯函数 `inbox-partitions.ts` 合并为先置顶后普通、同一会话只出现一次的单滚动列表；普通区停在深处窗口时只展示普通区，置顶区首次读取结束后才接上普通区。置顶区按同一 `pinOrderVersion` 整区读取，续页游标失效或带回新版本时从首页重读（最多三轮），以前驱变化识别被移动的行，滚动补页只作用于普通区。行菜单提供置顶与取消置顶（客户会话同样可置顶）；置顶区用 `@dnd-kit` 排序，指针移动 6px 后开始拖动，键盘空格拿起、方向键移动、空格或回车放下、Esc 取消。放下后提交相对可见邻居的 `before`／`after` 命令，`ConversationPinCommand` 在 `src/api` 内收敛位置枚举零值。拖动或菜单打开期间到达的远端重排连同顺序版本一起暂缓，放下仍携带旧版本并由服务端以冲突拒绝，前端提示后重读；写入成功后先重读目标分区再重读另一分区。两个分区共用一个视口，定位目标属于置顶区时由置顶区完成定位。移动端同样经 `usePartitionedInboxList` 读取，见下一条。
- **移动端置顶交互：** 移动端收件箱经 `usePartitionedInboxList` 读取，与 Web／桌面端合并规则一致。长按菜单提供置顶、取消置顶，置顶会话另有「上移」「下移」与「调整置顶顺序」：上移、下移提交相对可见邻居的 `before`／`after` 命令，首项上移与末项下移保留显示并停用；排序模式下只有按住拖动手柄才开始拖动，点击、长按、左右滑动与安卓系统返回照常生效并同时结束排序。两端的拖动排序共用 `pinned-sort.tsx` 的 `PinnedSortArea` 与 `pinMoveCommand`，落点计算 `pinMoveTarget` 在 `inbox-partitions.ts`。本人置顶写入成功后经 `InboxListController.refreshOwnWrite` 另排一次写入之后发起的重读，这一次的结果不受列表操作暂缓、立即提交顺序与版本；远端通知、探针与补页触发的重读仍按原规则暂缓。

## 变更版本与通知契约

| 名称 | 归属与含义 | 不能拿来做什么 |
| --- | --- | --- |
| `message_seq` | 单会话内最终消息顺序，事务内分配；游标和阅读位置据此比较 | 不表示资料变更 |
| `conversation.version` | 单会话可见变化的版本，消息、群关系与群资料、资料引用和 Run 终态推进 | 不等于未读数，不与 `message_seq` 混用，不跨会话比较大小 |
| `identityProfileVersion` | 本人身份资料与账户级偏好的版本 | 不表示某个会话变化，不包含设备本地通知许可 |
| `conversation_user_states.version` | 本人对单会话个人状态的版本，已读、提及确认、静音和手动未读推进 | 不表示会话内容变化，不跨用户比较，不代替 `read_seq` |
| `read_seq` | 用户实际阅读基线，保留提及确认的独立语义 | 收到事件、打开事件流或补拉完成都不能直接推进 |
| `generation` / `queryGeneration` | 当前登录会话／当前列表查询的本地代次 | 不跨设备同步，也不代替服务端授权 |
| `pinOrderVersion` | 本人全局置顶顺序版本，写入时用于冲突检测 | 不因新消息、已读或静音变化而推进 |

写入链路为：Action 校验与加锁 → 业务事实与 `conversations.version` 或本人会话状态版本同事务提交，事务内登记待发通知 → 提交成功后异步发布 Core NATS，请求响应不等待 Flush，回滚则丢弃 → 客户端经 HTTP／Wails 重读。**通知只说明「哪个会话或本人会话状态变到了版本 N」或「身份资料、置顶顺序变了」，不携带业务内容；通知可重复、可合并、可丢失，业务正确性完全由 Query 恢复。**

客户端不保存实体版本：收到会话、本人会话状态、身份资料或置顶顺序通知即失效对应 resourceKeys 并重读，同一 key 的失效按短窗口防抖合并。同一查询的重读串行执行：失效时已有读取在途则不取消，其结果照常写入缓存，结束后再补读一次，最终以失效后的重读为准；未挂载的 Query 不处理，打开时直接读最新；读取失败时查询保持错误状态，下一次探针、连接问候或通知的合并窗口结束时重试。通知携带的版本用于 Gateway 发送队列合并与日志，客户端不据此比较。

独立详情、列表、消息窗口和总数都从对应 Query 获取权威结果。同步协调器保存失效状态与探针上次返回值，视图控制器保存 ID 顺序、窗口边界、锚点和交互状态；它们不复制一份手工维护的业务 DTO。列表边界暂时过期属于可恢复状态，不能据此判定会话已删除或失权。

### 定期兜底校验

客户端每 30 秒无条件调用一次 `GetSyncHeads`，返回本人当前可见会话的数量、版本校验和、身份资料版本，以及 PR40 起的置顶顺序版本；任一项与上次返回值不符就走已加载窗口重读（`ReadInboxWindow` ＋ 按 ID 资格核对 ＋ 当前会话窗口）。前台恢复、网络恢复和重连各额外触发一次。

版本校验和是可见会话 `(conversationId, conversation.version, 本人会话状态 version)` 的 64 位哈希之和（没有个人状态行的可见会话按状态版本 0 计入），只用于判断「有没有变」，不比较新旧，也不能推断变的是哪一条。会话版本或本人会话状态推进、会话加入和移出都会改变它，一增一减同版本会话也能区分；64 位哈希碰撞概率可忽略。客户端把探针各项当作不透明值保存上次返回结果，不用已加载数据重算。

会话、本人会话状态、身份资料和置顶顺序的通知丢失后，这条兜底是最终恢复的唯一保证；提交后发布和 Gateway 订阅顺序只让恢复更快，不是正确性前提。提交后、发布前进程崩溃或发布失败丢失的通知，由这条兜底在一个校验周期内恢复。登出与停用的撤销控制丢失时，事件流最迟在最长存活时间到期时结束，重连时重新认证。**若实测该聚合在真实数据量下过重，那就是引入持久用户水位行的证据；在此之前不建。**

### 受众与 Subject

通知受众是**临时的 NATS 订阅目标，不是持久流**：`cervi.<namespace>.realtime.<organizationId>.<audienceKind>.<audienceId>`，`audienceKind` 固定为 `user`、`customer_inbox`、`visitor_directory`、`website_channel`。用户用 userId，客服共享一条企业流并不逐客服扇出，访客用渠道身份记录 ID，网站渠道停用的撤销控制用渠道 ID；这些值来自服务端授权结果，使用无点的内部规范值，禁止原始凭据或用户输入直接拼接。

登出与停用的撤销控制和变更通知共用提交后发布链路，以明确的通知种类区分，不参与变更通知的版本合并；登出通知携带 tokenSessionId，停用按用户受众发送。控制通知丢失时，事件流最迟在最长存活时间到期时结束。群失权不发送控制通知：失权者按 PR19 收到仅含会话 ID 的 `conversation_removed` 通知，该种类不与会话变更合并，Gateway 把它转发给客户端，客户端据此关闭该会话的运行过程流。

### 锁序

完成各入口守卫后，多会话业务锁按 conversationId 排序，并在每个会话内遵守已定义的业务对象顺序。**业务锁之后不取受众锁**；事务内登记通知只写内存集合，不参与锁序。`pinOrderVersion` 与身份资料版本同存于本人用户账号行，该行由写事务开头的 LockActiveUser 锁定，不新增独立顺序锁。资料变更涉及多个会话时先确定并锁业务集合，再统一登记通知。

## 列表行为基线

查询身份包含企业、用户、scope、customerView、assigneeIdentityId、channelId、serviceStatus、kinds，启用后再加入分区和规范化搜索词；数据 key 共享，页面实例的选择与滚动位置各自保存。普通区按 `lastActivityAt DESC NULLS LAST, conversationId DESC` 排序；时间使用数据库精度，游标携带原值。置顶区按本人顺序升序，不采用活动时间。

| 输入／位置 | 内容与顺序如何处理 | 选择与滚动如何处理 |
| --- | --- | --- |
| 顶部且没有点击／拖动等交互 | 应用新项和最新活动序 | 保持顶部，不自动换当前详情 |
| 深处收到变化 | 更新内容并自动应用新增和活动排序；触摸、惯性、菜单或键盘操作期间暂缓重排，资格移除立即生效 | 保存原可见邻居；当前行上浮时锚定原后继、再前驱，不跟随回顶 |
| 用户向上浏览 | 自动双向补页，接近顶部主动核对最新窗口；不要求点击刷新或查看最新 | 按原邻域补偿，滚到全局顶部即可看到最新会话 |
| 交互结束 | 一次性应用最新权威顺序，不展示待更新提示 | 以停止操作时的可见邻域保位，不打断惯性或重复补偿 |
| 切换筛选 | 读取新查询；旧结果不可操作 | 恢复该查询历史，否则保持未选中 |
| 会话不再匹配筛选 | 从列表移除 | 有阅读权限则保留详情；失权立即清理敏感内容 |
| 断网／请求失败 | 保留已展示数据和独立错误状态 | 重试同一窗口，不换主体 DOM、不跳第一行 |

“保锚点”记录稳定可见 ID、相对滚动视口顶部的像素偏移、原后继／前驱及原窗口边界。读取前捕获，DOM 更新后测量一次并补偿；锚点因排序远移时优先留在原邻域。原生 scroll anchoring、控制器或虚拟列表只能有一个补偿所有者。用户正在滚动时延后非必要重排；失权清理不等待用户停止操作。

列表刷新和补页由同一控制器串行调度：刷新遇到在途补页，先等待其收尾，再捕获已经扩展后的窗口边界；刷新中触发补页则排队，重复刷新合并。切换查询使整个队列的 queryGeneration 失效，但不取消已经执行的 Wails 业务调用。刷新重读已加载双向边界之间的连续范围，并按 ID 重读已加载项的当前资格；不能只以首页或可见视口替换全部已加载结果。交互中的可读行仅暂缓布局移动，停止操作后按权威顺序应用；真正失权才按失权规则清理；请求失败保留已确认的窗口和重试边界。

## 个人置顶写入与读取契约

置顶、取消和移动均提交 expectedPinOrderVersion，成功返回新版本。移动输入为 `{ conversationId, position, neighborId?, expectedPinOrderVersion }`：position 是 `before`／`after` 时 neighborId 必填，表示插入该邻居在**全局个人顺序**中的紧前／紧后；`start`／`end` 不带邻居，表示整个置顶区的首／尾。目标和邻居必须是本人当前有权读取的置顶会话；版本或邻居失效时整次操作失败，客户端重新读取后让用户重试，不自动用旧意图覆盖。

“隐藏项”包括被当前筛选排除及尚未加载的置顶项。移动只改变目标相对位置，其余项之间相对顺序保持；如全局 `A,X,B`、当前仅看见 A/B，将 B 移到 A 前，结果必须是 `B,A,X`。前端只提交位置命令，不能把可见 ID 数组当成全量个人顺序保存。rank 重编号只是存储实现变化，不改变逻辑顺序。

本人提交的置顶命令（含随操作发生的重编号）推进 pinOrderVersion 并写本人受众通知，客户端据此重读最新顺序版本。失权清理由他人的事务执行，只清除该会话的 pinRank 并推进本人会话状态版本，不推进 pinOrderVersion：推进它需要在他人的事务里写本人用户账号行，与「先锁本人账号行再锁会话」的置顶写入构成循环等待，也与成员资料等管理操作的锁序冲突；失权由同一受众的 `conversation_removed` 通知触发整区重读，效果与版本变化一致。发现版本变化时让整个置顶分区及其游标失效，重新取得同一版本的权威窗口；不得把单条新 pinRank 与缓存旧 rank 混排。多页重读途中版本再次变化，舍弃该轮候选并从新版本恢复锚点；已展示内容可保留为待更新视图，但不能提交使用旧版本的排序。新消息只推进会话版本，不推进置顶顺序版本。

## 实施 PR 的完成证据

后端项在相关 Action／Query 集成测试中构造数据并断言数据库及 appservice 返回值；并发用可控屏障组织，不靠 sleep 碰概率。运行 `wails3 task test:server`；契约或平台接入同时运行相关绑定生成、前端／服务端构建与平台 Task。前端项以本条具体交互场景验收，状态机复杂处增加可控响应顺序测试；界面操作遵守 AGENTS.md 授权规则。PR 描述记录实际执行命令、平台和结果，未执行的场景如实标记，不把本清单中的预期写成通过记录。

## 一、变更版本与发布

### PR22：名称、头像和渠道资料失效

- **依赖：** 无（PR19 已合并，PR21 已交付）。
- **开启：** 2026-09-18 开启，先于 PR43 交付，使改名即时改变会话名称搜索的匹配资格。
- **范围：** 接通成员／Agent、联系人／渠道身份及渠道资料写入口；按真实 JOIN 依赖标记受影响会话，推进这些会话的版本并按固定锁序写对应受众通知；头像异步导入成功也覆盖。成员与 Agent 的停用和恢复改变列表展示的账号状态，按同一方式推进；Copilot 线程列表展示 Agent 与创建人名称，随客户会话变化失效重读。
- **落点：** `internal/actions/identity`、`agent`、`channel` 及联系人资料入口；inbox 与消息查询的资料 JOIN。
- **实施：** 先以实际 JOIN 列出资料字段影响清单，再在各真实写事务中推进对应会话版本；同步只要求重读当前资料，不复制姓名头像到每条历史消息。异步头像完成属于独立写入口，也要发布；批量会话锁按稳定 ID 排序。
- **验收：** 无新消息时列表、发送者和引用资料可更新；访客仅刷新公开字段；失败和相同值不产生伪变化；不建立通用实体事件平台。
- **验证步骤：** 会话两天没有新消息时修改对方姓名和头像，列表、消息署名与引用展示均更新；异步头像落库后再验一次；活动序、未读和访客内部字段不变。

## 二、连接与各端实时闭环

### PR31：拆分网站挂件网络与窗口状态（推迟）

- **依赖：** 无（PR07 已合并）。
- **触发条件：** 挂件需要与成员端共用传输内核，或 `chat.js` 状态逻辑的修改出现可复现的回归。
- **范围：** 通过现有 messenger 构建入口，将请求、身份、连续窗口与发送状态迁入 `frontend/src/publicweb` 可测试模块；保留 Go 模板、主题及现有 DOM 外观，访客 DTO 与成员隔离。
- **落点：** `internal/publicweb/chat.js`、`page.html`、`frontend/src/publicweb` 与现有 messenger 构建入口。
- **实施：** 按网络身份、会话目录、消息窗口、待发送状态拆出可测试模块，DOM 事件转成显式操作，不把整段脚本包进一个 TS 类。先保留 PR32 的事件流与拉取逻辑验证等价，再删除迁出的原函数与状态变量；构建产物继续由现有 Go 嵌入链路加载。
- **验收：** 现有实时拉取下文本、引用定位、分线程草稿和幂等重试不变；删除被替代的旧状态逻辑；不打包内部身份或工具详情。
- **验证步骤：** 在真实挂件构建产物上发送、重试、切线程、引用跳转和恢复 Cookie；切回原线程草稿仍在。检查 bundle 依赖没有成员登录模块或内部 Agent 详情。

### PR35：Gateway 指标与就绪顺序（推迟）

- **依赖：** 无（PR26、PR32 已交付）。原生端子路径、心跳与优雅关闭由 PR25、PR25A 交付。
- **触发条件：** 出现真实部署并需要监控实时链路，或已观察到启动就绪顺序导致的连接失败。
- **范围：** 记录认证、慢连接、通知发布失败和同步落后指标，不记凭据或正文；明确启动就绪顺序。多节点 Drain 与随机重连分散在拆分部署时另开。
- **落点：** 服务端应用装配、`internal/ingress`、Gateway 与通知发布，部署说明。
- **实施：** 启动顺序明确为依赖就绪 → 路由／订阅可服务 → 对外 ready；指标按结构化原因码聚合。
- **验收：** 未就绪时不对外接受连接；指标覆盖认证失败、慢连接关闭、发布失败与探针不一致次数。
- **验证步骤：** NATS 未就绪时启动服务端，事件流请求在就绪前不被接受，就绪后可连；分别触发认证失败、慢连接和发布失败，对应指标增加。

## 三、AI 实时过程

### PR39：网站公开 AI 流（推迟）

- **依赖：** 无（PR38、PR32 已交付）。
- **触发条件：** 访客侧需要逐字展示 AI 回复。推迟期间访客经会话通知读取最终回复，生成期间沿用挂件现有的正在输入提示。
- **范围：** 访客只接公开候选正文、基础状态和最终回复；内部思考与工具原文不外发。客户负责人变化停止旧流，取消只是加速，最终写回仍受数据库与租约门禁约束。
- **落点：** PR32 访客展示、按 runId 的访客运行过程流 adapter、客服 Agent 输出映射。
- **实施：** 在服务器构造访客专属公开事件，不先发全量工具与思考再靠前端隐藏；流授权始终绑定渠道身份可读线程。人工接管、转交、关闭取消旧公开候选资格，最终回复仍由原有事务门禁决定是否保存。
- **验收：** 客服接管、转交、关闭、重连换流及渠道失效均正确；抓取访客运行过程流与 HTTP 响应都无内部过程数据。
- **验证步骤：** 模型同时输出思考、工具结果和正文，抓包只含公开正文和基础状态；客服接管后迟到的旧 delta 不再展示，重连读取的最终消息也不包含被取消的候选。

## 四、按证据跟进

本节各项不阻塞实时主线：搜索按实际产品需要开启，虚拟化和容量验证按实测证据开启。

### PR43：会话名称搜索接口

- **依赖：** 无（PR40 已交付）。
- **范围：** 搜索当前有权会话的名称与标题，完整查询身份包含规范化搜索词；分页、上下文、变化资格判断与置顶分区共用筛选。不是只搜索已加载页，不加入正文全文搜索。
- **落点：** inbox 候选 Query、锚点上下文、分区请求和 `types_inbox.go`。
- **实施：** 服务端统一搜索词空白规范化与匹配规则，复用当前会话标题与对象名投影；先在完整有权候选集合筛选，再分页。查询身份和游标均包含规范化词，变化重读时重新计算匹配资格。
- **验收：** 首页之外可命中；改名导致匹配进入／退出、隐藏置顶、失效同事和跨企业隔离正确。
- **验证步骤：** 第 100 条标题匹配时首页可找到；将当前结果改名为不匹配则移除，另一未加载会话改名为匹配则可发现；同词跨企业请求不能扩大范围。

### PR44：会话搜索输入与浏览恢复

- **依赖：** PR43（PR42 已交付）。
- **范围：** 启用各端搜索入口，组合输入结束后查询，旧词响应受 queryGeneration 隔离；清空搜索恢复原筛选锚点，未展示查询仅标旧，结果详情仍独立。
- **落点：** Web／桌面与移动列表搜索入口、列表控制器、`resource-keys.ts`、双语 inbox 文案。
- **实施：** 输入框只维护本地编辑词，compositionend 后形成规范化查询；请求状态与结果查询绑定，防止新词标题下出现旧词可点击行。保存搜索前筛选与锚点，清空只恢复浏览上下文，不清个人置顶或会话详情。
- **验收：** 中文输入、快速改词、重连中搜索和搜索期间新消息不串结果；真实无匹配才显示空态，搜索不重置个人置顶。
- **验证步骤：** 中文输入法尚在组词时不逐键发最终查询；A 词慢响应晚于 B 词返回不覆盖。搜索中断网再清空，恢复原列表后迟到的搜索结果仍被忽略。

### PR45：列表虚拟化与窗口裁剪（测量后决定）

- **依赖：** 无（PR42 已交付）。
- **触发条件：** 真实列表规模出现可复现的渲染或滚动问题，并记录实际节点数与滚动耗时。
- **范围：** key 固定为会话 ID，裁剪保留前后边界与语义锚点。固定头像与摘要行布局，缩放、语言、未读位数与时间更新不制造跳动；选择单一滚动补偿所有者。
- **落点：** 共用列表控制器与各端 ScrollArea 渲染层。
- **实施：** 先量实际节点数和滚动耗时，记录为何需要虚拟化；只替换可见行渲染，数据分页、选择和同步契约沿用。裁剪窗口保留可恢复边界，行高度变化统一触发测量，禁用另一套滚动补偿。
- **验收：** 快速滚动、重复补页触发、长时间停留、隐藏后恢复及行高重测稳定；交互使用稳定 ID，尊重减少动态效果。不用固定截断条数冒充分页或容量治理。
- **验证步骤：** 长时间补页并裁剪后反向滚动仍能补回，锚点会话不因 DOM 回收失去选择；隐藏页签恢复、切语言、字体缩放和大量未读位数变化后位置稳定。

### PR46：实时链路容量与故障验证（测量后决定）

- **依赖：** 无（PR25、PR33 已交付）。原清单 PR41。
- **触发条件：** 出现真实部署规模，或已观察到连接、队列、锁争用的具体问题。
- **范围：** 增加可重复 Task 场景：同用户多会话、小群扇出、共享客服受众、资料批量失效、突发重连和持续热点；分别测 LockActiveUser 守卫、内存、队列、积压与 HTTP 放大，报告真实规模和结果。
- **落点：** 现有 Task 与服务端集成测试；新增只用于测试的连接、发布和 Query 故障注入点。
- **实施：** 每个场景记录数据量、在线连接数、受众大小、持续时间和机器配置，再测端到端收敛时间、HTTP 次数及队列峰值；用结果定位热点，不把合成测试数字写成产品承诺。注入点不得成为生产接口。
- **验收：** 杀进程、NATS 短断、慢客户端及连接过期均恢复。锁优化、普通性能索引和大群 Shared Fanout 只按证据另开 PR。
- **验证步骤：** 消息已提交后杀进程、NATS 单独断连而事件流存活、慢客户端分别重跑；最终数据库版本与客户端投影一致，并证明队列与协程不随重复重连持续增长。

### 已推迟的原范围

| 原范围 | 重新开启的条件 |
| --- | --- |
| 持久用户／客服／访客水位与压缩唤醒行（原 PR17／19／20） | `GetSyncHeads` 聚合实测过重，或确定要做持久离线库 |
| 完整同步索引、重建基线、保留窗口与恢复 epoch（原 PR24／PR25） | 客户端需要跨会话持久游标，即离线库落地时 |
| 会话变更事件日志与 `sync_seq`（原 PR16） | 按会话版本重读被实测证明过重，需要细粒度失效时 |
| Protobuf 协议与工具链（原 PR27） | 事件载荷显著变大变热，或协议要提供给第三方 |
| 一次性连接票据（原 PR28） | 原生端不再由 Go 侧持有连接，或出现新的凭据边界要求 |
| 跨执行节点运行快照（原 PR44） | Gateway 与 Worker 拆分部署 |
| 事务内 `realtime_outbox` 与租约发布器（原 PR18／PR23 方案） | 实测提交后发布丢失导致的感知延迟不可接受，或兜底探针间隔需要显著拉长 |
| 多节点 Drain、随机重连分散与多节点验收（原 PR25／PR35 部分） | 服务端拆分为多实例部署 |
| 服务端定期复核授权与群失权控制通知（PR25、PR38 原方案） | 实测 `conversation_removed` 通知丢失导致失权后仍收到 AI 流，最长存活时间限定的撤销窗口不可接受，或出现需要即时收回的其他受限事件 |
| 客户端 `target`／`applied` 版本比较与串行追赶（PR28 原方案） | 通知触发的重复重读被实测证明过重 |
| WebSocket 双向实时连接（PR25 原方案） | 出现事件流与 HTTP 无法满足的高频双向需求，届时下行事件一并迁移 |
| 网站挂件拆分为 TS 模块（PR31） | 见 PR31 触发条件 |
| Gateway 指标与就绪顺序（PR35） | 见 PR35 触发条件 |
| 网站公开 AI 流（PR39） | 见 PR39 触发条件 |
| 挂件跨标签页身份发现、本地通知跨标签页前台协调、NATS 恢复时主动校验探针、前端事件处理队列上限 | 出现实际重复通知、身份发现遗漏或发送堆积反馈 |

## 里程碑与范围外事项

- **变更版本可独立验证：** PR17–PR21 通过 HTTP 与兜底探针验收（PR22 开启后同样验收），实时通知丢失不影响最终数据；PR18 验证发布失败不影响业务写入。
- **端到端最薄一刀：** PR17、PR18、PR25、PR27、PR28、PR29、PR30 已完成，Web／桌面端的成员消息窗口与会话列表跑通「提交 → 通知 → 重读」并删除该端轮询；移动端由 PR33、网站访客挂件由 PR32 接入并各自删除轮询，业务消息轮询已全部移除。
- **增强独立交付：** 个人置顶由 PR40–PR42 交付，与已交付的 AI 流互不依赖；搜索与虚拟化不阻塞实时主线。
- **不在本清单实现：** 图片／文件消息、Telegram 外发 Delivery、群聊 @Agent 策略、移动建群、输入／在线状态、后台系统推送、离线发送队列、设备执行、WebRTC、联邦、大群 Shared Fanout。消息置顶、团队共享置顶、普通区手排、会话分组与未读筛选也不随个人置顶加入。
